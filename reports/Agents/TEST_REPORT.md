# TEST_REPORT — отчёты тестировщика «Сборщик game-dev team» → game-lead

> Канал: **«Сборщик» (QA-пилот, Computer Use) пишет сюда** баг-репорты по тестированию фаз; **game-lead читает** и чинит.
> game-lead ставит задания на тест в `STATUS.md` с меткой «🟢 ГОТОВО К ТЕСТУ: Фаза N + чеклист».
> Последняя запись — сверху. Формат записи:
> ```
> ## [дата-время] ТЕСТ Фазы N (раунд K) — ветка/коммит
> ИТОГ: PASS / FAIL (X из Y пунктов)
> По пунктам: 1.[PASS/FAIL] <пункт> — ожидалось/получилось [скрин]
> БАГИ (по приоритету): - [критич/средний/мелкий] <описание+воспроизведение+скрин>
> ВОПРОСЫ ДИЗАЙНА (Ринату): …
> НЕ СМОГ ПРОВЕРИТЬ (честно): …
> ```
> game-lead отвечает/реагирует в `STATUS.md`. Дизайн-решения и финальная приёмка фазы — за Ринатом.

---

## [2026-06-15 ~23:24 MSK] 💤 ПУЛЬС (лёгкий режим): free, жду CU

Лёгкий режим по STATUS 22:08. Тест НЕ запускал, редактор не трогал, lock не ставил, CU не запрашивал. Триггера нет: верх STATUS — та же ПАУЗА 22:08, свежего 🟢 «CU есть — жми» нет, живого CU-апрува Рината нет. Стою на 🟢 Этап 1 (`13181eb`).

⚠️ Протухший `QA_TESTING.lock` (mtime 20:30 MSK, ~2ч54м > 90 мин) всё ещё лежит — снять не могу (read-only из песочницы; Проводник = CU, а CU в паузе). Не блокер: старше 90 мин, следующий полноценный прогон сочтёт его протухшим. Сними вручную, если мешает.

🔵 РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~23:17 MSK] 💤 ПУЛЬС (лёгкий режим): free, жду CU

Лёгкий режим по STATUS 22:08. Тест НЕ запускал, редактор не трогал, lock не ставил, CU не запрашивал. Триггера нет: верх STATUS — та же ПАУЗА 22:08, свежего 🟢 «CU есть — жми» нет, живого CU-апрува Рината нет. Стою на 🟢 Этап 1 (`13181eb`).

⚠️ Протухший `QA_TESTING.lock` (mtime 20:30 MSK, ~2ч47м > 90 мин) всё ещё лежит — снять не могу (read-only из песочницы; Проводник = CU, а CU в паузе). Не блокер: старше 90 мин, следующий полноценный прогон сочтёт его протухшим. Сними вручную, если мешает.

🔵 РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~23:15 MSK] 💤 ПУЛЬС (лёгкий режим): free, жду CU + нашёл протухший lock (снять не смог)

Лёгкий режим по STATUS 22:08. Тест НЕ запускал, редактор не трогал, CU не запрашивал. Триггера нет: свежего 🟢 «CU есть — жми» в STATUS не появилось, верх — та же ПАУЗА 22:08. Стою на 🟢 Этап 1 (`13181eb`).

⚠️ ПО ОЧЕРЕДИ: в директории лежит **протухший пустой** `QA_TESTING.lock` (mtime 20:30 MSK, ~2ч45м > 90 мин). Активного теста за ним нет — осиротевший маркер. **Снять НЕ смог:** из песочницы `rm` → «Operation not permitted» (файл read-only на маунте), а удаление через Проводник требует CU, который в паузе — не запрашивал. Маркер ОСТАЁТСЯ. Это не блокер: он старше 90 мин, следующий прогон по протоколу сам сочтёт его протухшим. Если мешает — сними вручную (`E:/game-dev-team/reports/Agents/QA_TESTING.lock`).

🔵 РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~23:10 MSK] 💤 ПУЛЬС (лёгкий режим): free, жду CU

Лёгкий режим по STATUS 22:08. Тест НЕ запускал, редактор не трогал, lock не ставил, CU не запрашивал. Триггера нет: свежего 🟢 «CU есть — жми» в STATUS не появилось, последнее от тебя — та же ПАУЗА 22:08. Lock в очереди нет (директория чиста). Стою на 🟢 Этап 1 (`13181eb`), жду: (а) Ринат подтвердит CU вживую, ИЛИ (б) твой 🟢 с «CU есть — жми».

🔵 РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~22:57 MSK] 💤 ПУЛЬС (лёгкий режим): free, жду CU

Принял твою ПАУЗУ авто-ретраев (STATUS 22:08). Тест НЕ запускал, редактор не трогал, lock не ставил — НЕ плодлю «НЕ СМОГ».
Стою на 🟢 Этап 1 (`13181eb`). Жду триггер: (а) Ринат подтвердит CU вживую, ИЛИ (б) твой свежий 🟢 с пометкой «CU есть — жми». До этого — только лёгкий пульс.

🔵 РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~22:05 MSK] НЕ СМОГ (повтор) — нет CU (плановый прогон по 🟢 Этап 1)

ИТОГ: **ТЕСТ НЕ ВЫПОЛНЕН — апрув Computer Use не пришёл (таймаут 180с).** Этап 1 (WASD+камера, HEAD `13181eb`) по-прежнему НЕ протестирован.

Повторный авто-прогон по таймеру. Увидел тот же `🟢 ГОТОВО К ТЕСТУ: ЭТАП 1` (21:45) + ноту game-lead 21:50 («бери, как Ринат даст CU»). Lock в очереди нет, редактор по STATUS свободен — не трогал. Запросил управление (Проводник для открытия `.uproject`) — апрув не подтверждён (Ринат не у машины). Lock НЕ ставил. Редактор не открывал.

**ВОПРОСЫ РИНАТУ:** так и стоим на CU-гейте. Подтверди апрув у машины (или скажи «жми» в чате) — следующий прогон возьмёт Этап 1: WASD+спринт, камера LDoE, словлю строку `QA: MOVE ... mode=.. vel2D=.. onGround=.. nPlayers=..` дословно.

🔵 ТЕСТ ЗАВЕРШЁН — РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~21:45 MSK] НЕ СМОГ — нет CU (плановый прогон по 🟢 Этап 1)

ИТОГ: **ТЕСТ НЕ ВЫПОЛНЕН — нет доступа Computer Use.** Этап 1 (WASD+камера, HEAD `13181eb`) остаётся НЕ протестирован.

**Что было:** автоматический прогон по таймеру увидел свежий `🟢 ГОТОВО К ТЕСТУ: ЭТАП 1` (21:45) в STATUS, которого ещё нет в TEST_REPORT → начал процедуру теста. Lock в очереди отсутствовал (директория чиста), редактор по STATUS «СВОБОДЕН». Запросил управление (`request_access` Проводник, чтобы открыть `.uproject`) — **апрув не пришёл, таймаут 180с** (Ринат не у машины в момент прогона). Без CU открыть редактор и поднять PIE нельзя.

**Lock НЕ ставил** (намеренно: запрашивал доступ ДО постановки lock, чтобы при отказе CU не оставить осиротевший маркер). Очередь чиста, редактор не трогал.

**ВОПРОСЫ РИНАТУ:** автономный тест по таймеру невозможен, пока CU-грант не подтверждён вживую. Когда будешь у машины — подтверди апрув, и следующий прогон возьмёт Этап 1 (или скажи «жми сейчас» в чате).

🔵 ТЕСТ ЗАВЕРШЁН — РЕДАКТОР СВОБОДЕН (не открывался).

---

## [2026-06-15 ~21:07 MSK] 🤝 СВЯЗЬ ЕСТЬ + принял НОВЫЙ протокол ПО ЭТАПАМ — стою на 🟢 Этап 1

[21:07] Сборщик: free | редактор ЗАКРЫТ, lock снят, жду 🟢 Этап 1.

**Связь подтверждена** (Ринат передал твой онбординг-промпт в чат). Принял новый порядок: MVP по ЭТАПАМ 1→5 (1 управление / 2 бой+погоня / 3 деревня-вид / 4 два квеста / 5 смерть+выживание); протокол волны (🟡 ВЗЯЛ → lock → PIE → 🔵 PASS/FAIL по пунктам с QA:-строками); пульс каждые 5–7 мин. Известные открытые баги принял **НА СВЕРКУ, не как «починено»**: WASD не едет (Этап 1); костёр/декали/дома/дорога (Этап 3).

**Что было ДО твоего промпта (честно):** этот плановый CU-прогон стартовал по старому 🟢 «ВОЛНА 1+2» (`ab5f0d5`) из STATUS — успел поставить lock, открыть редактор РОВНО 1 раз через .uproject, поднять PIE. Как пришёл новый протокол — **свернулся**, чтобы не тестить устаревшую волну и не держать редактор (он тебе нужен под сборку Этапа 1): остановил PIE, закрыл редактор БЕЗ сохранения, снял lock.

**Пруфы из Output Log за короткую PIE-сессию (только реально увиденное):**
- `PIE: Play in editor total start time 0.565 seconds` — старт чистый, фаталов нет.
- `LogQA: EQUIP armor ... total 0.50`; HUD: HP 100/100, Ammo 12/48, Money 50.
- `LogQA: lock target -> BP_EnemyBandit_C_0 (auto)` → `lock target cleared`.
- `TraderSpawnSubsystem: spawned TraderNPC_0 ... navmesh=yes`; `ElderSpawnSubsystem: spawned ElderNPC_0 ... navmesh=yes`.
- Голод тикал на HUD (60→54) — выживание-таймер живой.

**НЕ проверял (честно):** WASD-движение — нажатие `W` НЕ прошло (фокус перехватила всплывашка таскбара; CU+PIE-ввод капризен), вывода по движению НЕ даю. Это и так Этап 1 — перепроверю на 🟢 Этап 1 пристыкованным (F8 Possess). В логе шум: много `LogTemp: Warning: ARangedWeapon::Fire() — no target` (авто-фаер без цели) — на сверку.

**Визуал (в редакторе edit-mode, НЕ фронтальный PIE-скрин — формально не сужу):** на экране ровно то, что в Этапе 3 — кровь-декали = непрозрачные РОЗОВЫЕ прямоугольники, кости красноватые. Совпадает с твоим списком и скринами Рината (папка `13. BugReport`).

Жду 🟢 Этап 1 с чеклистом и что ловить в QA:. 🤝

---

## [2026-06-15 днём] 🤝 СБОРЩИК НА СВЯЗИ — готов к волнам (Ринат вернулся)
game-lead, на связи, готов тестировать по твоему плану.
- Помню мой прошлый результат: **погоня — FAIL, корень = НАВМЕШ** (по прямому логу: и враг, и игрок ВНЕ навмеша на боевых телепорт-точках → `MoveTo` падает даже в direct-fallback). Жду твой фикс покрытия навмеша на этих точках → перепроверю первым делом (ловлю `QA: ... chase mode=.. dist=.. selfNav=.. targetNav=..` с убывающей dist + урон игроку).
- **CU теперь проходит** (в 00:15 грант применился молча) — авто-прогоны по таймеру должны тестировать сами; Ринат сейчас у машины — подстрахует.
- Протокол прежний: ты «🟢 волна N» (укажи РЕДАКТОР ОТКР/ЗАКР + чеклист + что ловить в `QA:`) → я «🟡 ВЗЯЛ» → тест → «🔵» + отчёт с QA-строками → закрываю редактор под твою сборку.
Жду 🟢 (фикс погони или следующая волна). 🤝

---

## [2026-06-15 ~00:15 MSK] ТЕСТ ПОГОНИ — live PIE, свежая сборка (новый лог-формат chase mode/selfNav/targetNav/moveResult) — feature/phase5-quests
ИТОГ: **FAIL по погоне (главное).** CU-окно сработало (грант молча), редактор открыл РОВНО 1 раз через .uproject (был закрыт), PIE поднял. **Сборка ТОЧНО новая** (не a548272): формат лога погони сменился на `chase mode=direct dist=.. (selfNav=.. targetNav=.. moveResult=..)` — это новый диагностический лог (которого я просил). По нему **корень подтверждён прямым логом, не гипотезой:** на боевых телепорт-точках И враг, И игрок ВНЕ навмеша → `MoveTo` падает даже в direct-fallback.

### 🔴 #1 Погоня — Логово (V) — FAIL (вход в стейт есть, движения/контакта нет)
- `QA: teleported`→ `WolfDen activated, spawned 5 wolves` (все `navmesh=yes` по сабсистеме, НО через `Wolf spawn fallback via floor-trace`).
- Ровно 2 строки погони, обе на спавне (21:11:51), дальше — ТИШИНА ~30с:
  `QA: WolfCharacter_1 chase mode=direct dist=1439 (selfNav=no targetNav=no moveResult=Failed)`
  `QA: WolfCharacter_2 chase mode=direct dist=1482 (selfNav=no targetNav=no moveResult=Failed)`
- **Нет повторов, dist НЕ убывает, контакта нет.** Скрин (×3 за ~20с): волки стоят на одних местах, не сближаются. HP игрока 100/100 весь заход. `lock target -> WolfCharacter_1 (auto)` → через 1.4с `lock cleared` (волк не приблизился).
- Диагноз из самого лога: `selfNav=no` (волк вне навмеша) + `targetNav=no` (игрок вне навмеша) + `moveResult=Failed`. Direct-fallback существует (`mode=direct`), но тоже Failed.

### 🔴 #2 Погоня — База (Z) — FAIL (бандиты floor-trace, погони НЕТ вообще)
- `QA: teleported to BanditBase` → `BanditBase player in range, spawning in 1.2s (waiting for navmesh build)` (задержка-фикс ПРИСУТСТВУЕТ) → но 3 бандита сели `BP_EnemyBandit_C_1/2/3 ... (navmesh=floor-trace)` — **НЕ `navmesh=yes`**, как и раньше.
- **НИ ОДНОЙ строки `chase mode=` для бандитов** за весь заход. Урона нет, HP 100/100. Камера утоплена в сарай базы (игрок просел в геометрию = вне навмеша). Квест-ноутбук заспавнился норм (`quest notebook 'Ноутбук' spawned`), `DROPLOOT … QuestItem … spawned`.

### Что РАБОТАЕТ (PASS, попутно)
- PIE-старт чистый, деревня грузится, авто-лок, спавн торговца/старосты `navmesh=yes`, броня total 0.50, ammo 12/48, money 50.
- **Новый лог погони (mode/selfNav/targetNav/moveResult) — реализован** (моя просьба из прошлого отчёта закрыта game-lead).
- Задержка спавна бандитов 1.2с «waiting for navmesh build» — в логе есть.

### ❓ game-lead (корень теперь виден прямым логом)
1. **И враг, И игрок садятся floor-trace (вне навмеша) на обеих боевых точках** — `selfNav=no targetNav=no`. Фикс «непрерывного MoveTo» работать не может, пока обе стороны вне навмеша. Нужно: динамический навмеш РЕАЛЬНО построен в зоне телепорта к моменту спавна (не только Invoker-флаг), и спавн врага/игрока строго НА навмеш (`ProjectPointToNavigation`), а не floor-trace-фоллбэк.
2. База до сих пор `navmesh=floor-trace` несмотря на задержку 1.2с — задержки мало / Invoker не покрывает юг. Возможно увеличить задержку ИЛИ строить навмеш заранее по зоне.
3. Раз `mode=direct moveResult=Failed` — даже прямое ведение (SetActorLocation/velocity к игроку, минуя навмеш) сейчас не срабатывает; стоит проверить почему direct-fallback не двигает пешку.

### НЕ ПРОВЕРЕНО (честно)
- **Движение WASD через CU — НЕ подтвердил:** клавиши-телепорты `V`/`Z` доходят (после клика в вьюпорт), но удержание `W` сцену не сдвинуло (вход CU частичный — известное ограничение PIE/раскладки). Погоню это не меняет (она про ИИ, не про мой ввод).
- **Огонь костра (#19)** — отдельно не смотрел: фокус был на погоне, камера на боевых точках утоплена в геометрию. Перенесу на след. окно.
- Сустейн-контакт/урон врага — лог damage пуст (враги не дошли), так что подтверждать нечего.

🔵 РЕДАКТОР закрываю после отчёта. **Вердикт: фикс погони НЕ закрыт — новый лог прямо показывает общий корень (обе стороны вне навмеша + direct-fallback Failed). Бандиты базы по-прежнему floor-trace.**

---

## [2026-06-15 ~00:10 MSK] CU-FREE РАБОТА по GL_SIGNAL [00:07] — лог-верификация + автотест-драфт (БЕЗ PIE)
ИТОГ: **частично выполнено CU-free.** Редактор НЕ трогал (другой инстанс в `QA_SIGNAL [00:07]` уже открывает редактор под live-тест 5426944 → во избежание двойного запуска не лезу). Code-review C++ из задания GL_SIGNAL #1 **выполнить не смог** — на плановом прогоне примонтирована только `…/Saved/Logs`, папка `Source/` НЕ доступна (нет `MasterHumanoidCharacter.h`/`EnemyAIController.cpp`). Чтобы не фабриковать — вместо чтения исходников верифицировал по СВЕЖЕМУ live-логу + дал драфт автотестов (#2).

### ✅ Что подтверждает свежий live-лог (CU-free, артефакт: `ContrarySurvivor-backup-2026.06.14-20.54.38.log`, 3 PIE-старта 20:46/20:50/20:53 UTC)
- **Сборка грузится, старты чистые:** `PIE: Play in editor total start time 0.49–0.71s` ×3; фатальных ошибок нет (только бенигн `LogStreaming` иконки).
- **Старт-эконом/броня/лок живы:** `QA: starting money initialised -> 50` ×3; `EQUIP armor … total 0.50`; `QA: lock target -> BP_EnemyBandit_C_0 (auto)` → `lock target cleared`.
- **Спринт-лог НЕ спамит** (фикс a548272 жив в этой сборке): `Sprint state changed to: true`(1)/`false`(1) на 20:53:06–07, без покадрового спама.
- **NPC на навмеше:** Trader/Elder `navmesh=yes`.

### ⚠️ Чего лог НЕ подтверждает (честно — границы артефакта)
- **5426944 (ходьба WASD)** — НЕ доказана по логу: в сессии виден только тоггл спринта, позиционного проўfa движения игрока нет; V/Z/бой не гонялись. Нужен live-PIE ИЛИ позиционный QA-лог дельты игрока.
- **224fbc9 (погоня-fallback)** — НЕ проверяема: `chasing` = 0, `MoveTo` = 0, V/Z-телепортов в сессии не было. Direct-steering в логе не фигурирует (некому было сработать).
- **Огонь костра** — только маунт плагина Niagara движком; спавна `P_Fire`/партикла в сессии нет.
- (поправка к самому себе: первичный `grep -ci` дал «9 loot-строк» — это ЛОЖНОЕ срабатывание на `LogCollectionManager/Collections`. Реальных `DROPLOOT/COLLECT/ADDITEM` в сессии НЕТ. Не выдаю за лут.)

### 🔬 Сохраняется корневой след (для game-lead)
- **Игрок на старте СНОВА садится floor-trace fallback, не на навмеш:** `QA:  hit[26] … actor='BP_Campfire_C_1' impZ=162.7` → `floortrace[Player-start] FLOOR 'BP_Campfire_C_1' -> spawnZ=260.7` → `Warning: player start was under map -> relocated to floor Z=260.7`. Тот же класс «игрок вне навмеша», что я флагал по a548272/3b85e42. Если корень погони — игрок off-nav, это его прямой пруф на player-start.

### 🧪 Драфт автотестов (GL_SIGNAL #2) — для headless Functional-тестов (CLI, без CU)
**Погоня (закрывает «враги входят в стейт, но не доходят»):**
- Сетап: игрок в заведомо `navmesh=yes` точке деревни; 1 враг на навмеше на dist0≈1500.
- Ассерты: (a) вход в стейт ≤2с (`chasing` залогано); (b) **dist убывает монотонно** — сэмпл каждые 0.5с, ≥80% сэмплов `dist[t+1] < dist[t]−ε`; (c) контакт ≤15с (dist ≤ attackRange); (d) на контакте урон игроку (HP падает).
- Fallback-ветка 224fbc9: если `MoveTo` вернул Failed/OffMesh → ассерт «direct-steering включился» (нужна явная лог-строка, см. просьбу) **И dist всё равно убывает**.
**Лут:** force-kill(N)+force-drop(U) → ассерт-цепочка `DROPLOOT`→overlap/E→`COLLECT`→`ADDITEM` + инвентарь +1; квест-дроп волка = «Шкура волка» (имя совпадает).
- ⚠️ **Эскалирую Ринату (не выдумываю):** числовые пороги — MaxWalkSpeed/спринт-множитель, attackRange, радиус агро, органик-дроп % — это баланс = GDD (источник истины). Пока их нет в GDD — порог «контакт ≤15с / dist убывает» как behavioral-прокси.

### ❓ ПРОСЬБЫ game-lead
1. Для CU-free code-review нужен доступ к `Source/` на стороне Сборщика (сейчас примонтирована только `Saved/Logs`). Либо клади ключевые файлы/диффы в `reports/Agents/` — тогда ревью сделаю по ним.
2. Добавь явную лог-строку direct-steering: `QA: <enemy> MoveTo FAILED -> direct steering (dist=..)` — тогда 224fbc9 проверю headless-логом без CU.
3. Сталый `QA_TESTING.lock` помечен RELEASED (мной, 00:03), но файл на месте — сними при случае.

🔵 ПРОГОН CU-FREE ЗАВЕРШЁН — РЕДАКТОР НЕ ТРОГАЛ/СВОБОДЕН. 5426944 и 224fbc9 по логу НЕ закрыты (нужен live-PIE/CU или direct-steering-лог); отдал лог-верификацию свежей сборки + драфт автотестов.

---

## [2026-06-15 ~00:03 MSK] ПРОПУЩЕН ПРОГОН (CU-таймаут) — 🟢 5426944 «ФИКС ДВИЖЕНИЯ» НЕ покрыт
ИТОГ: ⏸️ НЕ ТЕСТИРОВАЛ — **блокер Computer Use** (плановое пробуждение по таймеру).

**Что нашёл свежего (новее моего последнего теста a548272 ~22:34):**
- `GL_SIGNAL.txt [23:57]`: **🟢 ФИКС ДВИЖЕНИЯ — HEAD 5426944, DLL 23:56** — «персонаж СНОВА ХОДИТ (был баг: MaxWalkSpeed=0 → поворачивался но стоял). РЕДАКТОР ЗАКРЫТ/СВ[ОБОДЕН]». Этот коммит в TEST_REPORT ещё НЕ покрыт.
- `QA_SIGNAL.txt [23:51]`: резерв-инстанс видел живой PIE (224fbc9), но V/Z не гонялись, строк chasing нет — судя по всему как раз из-за бага MaxWalkSpeed=0 (персонаж не ходил). Сейчас редактор закрыт.

**Почему не протестировал:** редактор ЗАКРЫТ (`GL_SIGNAL` прямо пишет «ЗАКРЫТ/СВ»), надо открыть самому. `request_access`:
1. `["Проводник","unrealeditor.exe"]` → `unrealeditor.exe` = `notInstalled` (редактор закрыт, процесса нет) → весь запрос short-circuit.
2. Повтор только `["Проводник"]` → **истёк по таймауту 180с** — апрув CU не получен (Ринат не подтвердил доступ на плановом прогоне; в `QA_SIGNAL [23:46]` была пометка «Ринат проснулся», но к 00:01 апрува нет).

Без CU редактор не открыть, PIE не погонять. Редактор НЕ трогал, git/STATUS/INBOX не трогал.

**Lock:** сталый `QA_TESTING.lock` (mtime 10:02 UTC, ~11ч, протух) удалить не смог — mounted-файл из песочницы не удаляется (`rm: Operation not permitted`), Проводник требует CU (тот же таймаут). Перезаписал содержимое маркером **«RELEASED — тест не шёл»**, чтобы не блокировать живой инстанс / следующие прогоны. **Прошу game-lead снять файл `QA_TESTING.lock`.**

### ❓ ПРОСЬБЫ game-lead
1. **Открыть редактор и оставить ОТКРЫТЫМ** (как в рабочем окне 20:02) под сборку 5426944 — тогда на следующем прогоне беру управление без открытия и проверяю: (а) персонаж ХОДИТ (WASD двигает, не только поворот); (б) ре-проверка погони 224fbc9 (V→волки, Z→бандиты, `QA: chasing` с убывающим dist + контакт/урон).
2. **Снять сталый lock** `QA_TESTING.lock` (я не могу из песочницы).
3. Системный затык повторяется: авто-прогоны по таймеру глохнут без присутствия Рината для апрува CU. Лечится присутствием Рината на запуске ИЛИ постоянным грантом CU приложению Unreal.

🔵 ТЕСТ НЕ СОСТОЯЛСЯ — РЕДАКТОР НЕ ТРОГАЛ. **5426944 (фикс движения) и ре-проверка погони 224fbc9 — НЕ покрыты, ждут CU-окна.**

---

## [2026-06-15 00:04] ПРОПУЩЕН ПРОГОН (нет Computer Use) — 🟢 5426944 НЕ покрыт
ИТОГ: ⏸️ НЕ ТЕСТИРОВАЛ — **блокер CU**. Овернайт-будильник (00:00 МСК). В `GL_SIGNAL.txt` свежий **🟢 5426944** ([00:00] game-lead: «ФИКС ДВИЖЕНИЯ+погоня-fallback+огонь», редактор свободен) — НОВЕЕ всех записей в TEST_REPORT (последняя — a548272 ~22:34) и ещё не покрыт. Хотел протестить WASD-ходьбу + V/Z/B погоню + огонь. **`request_access('Проводник')` истёк по таймауту 180с** — апрува Computer Use нет (Ринат не подтвердил у машины на этом окне; `unrealeditor.exe` → notInstalled = редактор закрыт → запрос short-circuit). Без CU редактор не открыть, PIE не погонять. Редактор НЕ трогал, git/STATUS/INBOX не трогал. Пульс QA_SIGNAL → `waiting CU`. Подхвачу на следующем CU-окне (нужен апрув Рината у машины).

---

## [2026-06-14 ~22:34] ТЕСТ ФИКСА ПОГОНИ (a548272) — feature/phase5-quests
ИТОГ: смешанный. **Спринт-лог без спама — PASS** (и это подтверждает: загружен ИМЕННО новый DLL a548272). **Погоня — НЕ подтвердилась**: волки Логова входят в стейт погони (лог `chasing player` есть), но за ~90с не дошли и не атаковали (HP 100/100); бандиты БАЗЫ по-прежнему `navmesh=floor-trace` (НЕ `navmesh=yes`) и в погоню вообще не входят. Сильный диагностический след: **сам игрок на телепортах V и Z садится через floor-trace (камера утоплена в пропы) — вероятно ВНЕ навмеша**, поэтому ИИ не может построить путь к игроку. Редактор открыл сам (1 запуск через .uproject, грант CU активен с 20:02). Пруфы — живые `QA:`-строки.

### Контекст / какой DLL
PIE старт чистый. **Подтверждение нового DLL:** спринт 2с → лог ровно 2 строки `Sprint state changed to: true` / `... false` (раньше спамил каждый кадр десятками строк). Значит фикс-сборка a548272 реально загружена, и результаты ниже — по ней.

### 🟠 #1 Погоня — Логово (V) — частично (вход в стейт есть, контакта нет)
1. **[PASS вход] Волки `navmesh=yes`, погоня логнулась.** `QA: teleported to WolfDen at V(Y=2200, Z=98)`; 5 волков `WolfSpawnSubsystem: spawned WolfCharacter_0..4 ... (navmesh=yes)`; `QA: WolfCharacter_1 chasing player dist=1439`, `QA: WolfCharacter_2 chasing player dist=1482` + `QA: lock target -> WolfCharacter_1 (auto)`.
2. **[FAIL контакт] Не дошли, не атаковали.** Спавн в 19:29:21; за ~90с (до 19:30:50) — НИ повтора `chasing` с убывающим dist, НИ урона: HP остался 100/100, ноль строк damage. Строка `chasing` появилась один раз на спавне (как и в прошлой сборке). Сближение волков визуально не подтвердил (камера утоплена в геометрию Логова). → По наблюдению поведение НЕ изменилось: погоня не доводится до игрока.

### 🔴 #2 Погоня — База (Z) — НЕ работает
3. **[FAIL] Бандиты по-прежнему `navmesh=floor-trace`, погони нет.** `QA: teleported to BanditBase at V(Y=-1500, Z=98)`; `BanditSpawnSubsystem: spawned BP_EnemyBandit_C_1/2/3 at base ... (navmesh=floor-trace)` — ожидалось `navmesh=yes`. За весь заход (стоял + двигал W) — НИ ОДНОЙ строки `chasing` для бандитов, урона нет. Т.е. на базе фикс не виден совсем: бандиты садятся вне навмеша → в погоню не входят.

### 🔬 Диагностика (вероятный корень — игрок ВНЕ навмеша на боевых точках)
- На ОБОИХ телепортах сам игрок садится через floor-trace, не на навмеш: `QA: floortrace[WolfDen-teleport] ... -> spawnZ=98.0` + `Warning: WolfDen-teleport spawn fallback via floor-trace`; аналогично `BanditBase-teleport`. Камера при этом утоплена в пропы (логово/база) на обоих скринах.
- Гипотеза (озвучиваю как гипотезу): враги входят в стейт погони (видно у волков), но `MoveTo` к игроку проваливается, потому что **цель (игрок) вне навмеша** — путь не строится, ИИ стоит. У базы хуже: бандиты и сами floor-trace. Фикс «непрерывного MoveTo» проверить по делу нельзя, пока игрок и враги не окажутся ОБА на навмеше в одной точке.

### ✅ #3 Спринт-лог — PASS (спам убран)
4. **[PASS]** Спринт 2с → `Sprint state changed to: true` (1) … `false` (1). Раньше — десятки строк/сек. Чисто.

### 🟠 БАГИ / ЗАМЕЧАНИЯ
- **[средний] База: бандиты всё ещё `navmesh=floor-trace`** (не yes) — погони на базе нет. Тот же корень, что тянется: навмеш у базы к моменту спавна не построен.
- **[средний] Телепорты V/Z садят ИГРОКА через floor-trace** (вероятно вне навмеша) → даже у Логова (волки navmesh=yes) погоня не доходит до игрока. Подозрение: путь к игроку не строится, т.к. игрок вне навмеша.
- **[мелкий→исправлено] Спам лога спринта — УБРАН** (был в 3b85e42).

### ❓ ВОПРОСЫ/ПРОСЬБЫ game-lead
1. Проверить навмеш у Логова и базы как ЕДИНЫЙ корень: пусть и игрок (телепорт), и враги (спавн) садятся `navmesh=yes` в одной зоне. Сейчас игрок везде floor-trace, база и по бандитам floor-trace.
2. Если можешь — добавь явный лог провала пути: `QA: <enemy> MoveTo FAILED (target off-nav?)` или `path partial`. Тогда я отличу «ИИ не видит игрока» от «путь к игроку не строится (игрок вне навмеша)» по логу, без ненадёжной камеры.
3. Для чистой проверки именно фикса MoveTo: дай спавн тест-врага ВПЛОТНУЮ к игроку на заведомо хорошем навмеше деревни (где старт `navmesh=yes`) — тогда видно, преследует ли он непрерывно, без фактора телепортных floor-trace зон.

### НЕ ПРОВЕРЕНО (честно)
- Сустейн/контакт погони и урон врага игроку — лог не показал, камера на боевых точках утоплена в пропы.
- Органический бой/лут в этом заходе не гонял (фокус — фикс погони + спринт-лог).

🔵 ТЕСТ ЗАВЕРШЁН — РЕДАКТОР ЗАКРЫТ/СВОБОДЕН. **DLL a548272 точно загружен (спринт-спам убран). Погоня по наблюдению не подтвердилась: волки входят в стейт но не доходят; база — бандиты floor-trace, погони нет. Вероятный общий корень — игрок/враги ВНЕ навмеша на телепортных боевых точках.**

---

## [2026-06-14 ~20:33] ПРОПУЩЕН ПРОГОН (нет Computer Use) — 🟢 a548272 НЕ покрыт
> ↻ Повтор плановых пробуждений по таймеру **~20:42, ~20:45, ~20:50, ~20:55 … ~21:00 … 18:05 UTC … 18:17 UTC … 18:20 UTC … 18:25 UTC … 18:30 UTC … 18:35 UTC … 18:40 UTC … 18:45 UTC … 18:50 UTC … 18:55 UTC … 19:00 UTC … 19:05 UTC … 19:10 UTC … 19:20 UTC (22:20 МСК)** и далее — тот же блокер CU: `request_access` снова отвалился по таймауту 180с (на 19:20 UTC: `list_granted_applications` пуст; `unrealeditor.exe` → `notInstalled` = редактор ЗАКРЫТ → запрос short-circuit; повторный запрос только «Проводник» истёк по таймауту 180с; lock `QA_TESTING.lock` mtime 10:02 UTC (≈9.3 ч, протух) снять не смог — mounted-файл из песочницы не удаляется, Проводник требует CU; на 19:10 UTC: `list_granted_applications` пуст; `unrealeditor.exe` → `notInstalled` = редактор ЗАКРЫТ → запрос short-circuit; повторный запрос только «Проводник» истёк по таймауту 180с; на 19:05 UTC: `unrealeditor.exe` → `notInstalled` = редактор ЗАКРЫТ → запрос short-circuit; повторный запрос только «Проводник» истёк по таймауту 180с; `list_granted_applications` пуст; на 19:00 UTC: `unrealeditor.exe` → `notInstalled` = редактор ЗАКРЫТ → запрос short-circuit; повторный запрос только «Проводник» истёк по таймауту 180с), грантов нет (`list_granted_applications` пуст). `a548272` всё ещё НЕ покрыт. Условия не изменились (см. ниже). Дубликаты-отчёты не плодил — копится этот же блокер. Сталый `QA_TESTING.lock` (mtime 10:02 UTC, ≈8 ч, протух) снять не смог: mounted-файл из песочницы не удаляется (`rm: Operation not permitted`), Проводник требует CU (тот же таймаут). **Прошу game-lead: открыть редактор и оставить ОТКРЫТЫМ + снять сталый lock (10:02), либо дождаться Рината у машины для апрува CU.**

ИТОГ: ⏸️ НЕ ТЕСТИРОВАЛ — **блокер CU**. Плановое пробуждение по таймеру. В STATUS (верх 20:28) и `GL_SIGNAL.txt` свежий **🟢 a548272 «ФИКС ПОГОНИ»** (база DLL 20:25) — он НОВЕЕ моего последнего теста (`3b85e42`, ~20:17) и ещё не покрыт. Хотел протестить, но **`request_access` («Проводник») истёк по таймауту 180с** — апрува Computer Use нет (Ринат не присутствует на плановом прогоне). `list_granted_applications` пуст (грантов нет). Без CU редактор не открыть, PIE не погонять. Редактор НЕ трогал, lock НЕ создавал, git/STATUS/INBOX не трогал.

ЧТО В ЗАДАНИИ (жду окна с CU, HEAD `a548272`):
1. **Погоня-фикс (корень: `MoveToActor` вызывался каждый кадр → рестарт path-request → пешка стояла; теперь переотдача только при не-движении/раз в 0.35с):** `V`→Логово → волки **ДОГОНЯЮТ и атакуют** (`QA: <wolf> chasing player dist=..` с **убывающим** dist + HP игрока падает). ← закрывает моё замечание 20:17 (погоня стартовала, но не сустейнилась).
2. `Z`→база → бандиты `navmesh=yes` (после ~1.2с задержки спавна, инвокер успевает построить навмеш) + догоняют/атакуют. ← закрывает «база = floor-trace».
3. Спринт-лог НЕ спамит каждый кадр (был спам `Sprint state changed to: true`).

⛔ БЛОКЕР game-lead/Ринату: **автономный тест по таймеру невозможен без апрува Computer Use** (request_access висит и отваливается 180с, когда Ринат не за машиной). Чтобы прогон состоялся — Ринат у машины одобряет CU в окно теста, ЛИБО редактор оставлен ОТКРЫТЫМ + заранее одобренный грант CU. Иначе каждый 🟢 копится непроверенным.

⚠️ СТАЛЫЙ LOCK: `QA_TESTING.lock` на месте, mtime **10:02 UTC** (≈7.5 ч, протух >90 мин) — тест им НЕ идёт, очередь редактора свободна. Снять не смог: mounted-файл из песочницы не удаляется (`rm: Operation not permitted`), а Проводник требует CU (тот же таймаут). **Прошу game-lead снять маркер.**

🔵 ПРОГОН ПРОПУЩЕН — РЕДАКТОР НЕ ТРОГАЛ / СВОБОДЕН. Жду окно с одобренным CU для `a548272`.

---

## [2026-06-14 ~20:17] ШИРОКИЙ ПРОГОН (3b85e42): навмеш-погоня + экран смерти + спринт + A/V
ИТОГ: смешанный. **Экран смерти #26 — ПОЛНЫЙ PASS. Спринт-расход #2 — PASS (направление).** **Навмеш-погоня — ЧАСТИЧНО: на Логове погоня СТАРТУЕТ (лог `chasing player` есть, волки `navmesh=yes`), но НЕ сустейнится; на БАЗЕ фикс НЕ сработал — бандиты по-прежнему `navmesh=floor-trace`, погони нет вообще.** Редактор открыл Ринат (я не дублировал). Пруфы — живые `QA:`-строки из UE-лога + скрин экрана смерти.

### Контекст
PIE старт чистый (`Play in editor total start time 0.536s`). Спавн на полу (`playerLoc Z=88.206, navmesh=yes`). Ввод доходит до PIE — подтвердил (`QA: overlay on` по `O`). God НЕ включал (для #26). Замеры по UE-логу `ContrarySurvivor.log` (новая сессия редактора, отсчёт с baseline).

### 🟠 #1 Навмеш-погоня (Navigation Invoker) — ЧАСТИЧНО
1. **[FAIL] База (`Z`) — бандиты `navmesh=floor-trace`, погони НЕТ.** Все 3 спавна базы: `BanditSpawnSubsystem: spawned BP_EnemyBandit_C_1 at base X=300 Y=-1500 (navmesh=floor-trace)`, `_C_2 ... (navmesh=floor-trace)`, `_C_3 ... (navmesh=floor-trace)`. Ожидалось `navmesh=yes` + `QA: <enemy> chasing player dist=..`. Фактически: ни одной строки `chasing player` для бандитов за ~25с (стоял + двигался W; ввод доходит — `overlay on` подтверждён). HP игрока не падал, бандиты не дошли. **Инвокер у базы навмеш к моменту спавна не построил; органической погони на базе нет.**
2. **[PASS частично] Логово (`V`) — волки `navmesh=yes`, погоня СТАРТУЕТ, но не сустейн.** Все 5 волков `navmesh=yes` (`WolfSpawnSubsystem: spawned WolfCharacter_0..4 at den ... navmesh=yes`). Погоня логнулась: `QA: WolfCharacter_1 chasing player dist=1439`, `QA: WolfCharacter_2 chasing player dist=1482` + `QA: lock target -> WolfCharacter_1 (auto)`. **НО:** строки `chasing` появились ОДИН раз (оба на кадре [521], на спавне) и больше НЕ повторялись за ~25с (несмотря на «дроссель 1с» = ожидалось продолжение с убыванием dist). `lock target cleared` через ~1.4с. Ни сокращения dist, ни урона игроку, волки до игрока не дошли. → **Погоня инициируется, но не поддерживается** (волки встали; либо лог только на вход в стейт, либо ИИ теряет цель/упирается в край навмеш-пузыря инвокера). Через наклонную top-down камеру визуально сближение не подтвердил (та же ненадёжность).

### ✅ #26 Экран смерти — ПОЛНЫЙ PASS
3. **[PASS] `P` → экран «ВЫ ПОГИБЛИ» со статами** [скрин]. `QA: death -> death screen (lived 335.4s, killer 'Неизвестно', kills 0)`; `QA: all UI windows closed on player death`; `QA: death screen opened (input disabled)`; `QA: DEATH SCREEN shown - lived 335s, killer 'Неизвестно', money 50, quests 0, kills 0`; `Player took 100.0 dmg ... Health: 0.0/100.0`. На экране: Прожито 05:35, Убийца Неизвестно, Деньги 50, Квестов выполнено 0, Врагов убито 0 + зелёная кнопка «ВОЗРОДИТЬСЯ» + «Enter / Пробел — возродиться».
4. **[PASS] Enter → респаун у костра, управление вернулось.** `QA: respawn key pressed`; `APlayerCharacter: respawn (from death screen)`; `Campfire 'BP_Campfire_C_1' autosave for player: OK`; `Respawn stats: HP 100.0/100.0, Hunger 100.0, Thirst 100.0`; `QA: RESPAWN done - HP 100, money 50`. После респауна HUD живой (голод тикает), mouse-capture восстановлен (`Viewport MouseCaptureMode Changed ... CapturePermanently`), игрок управляем у костра.

### ✅ #2 Спринт-расход — PASS (направление подтверждено)
5. **[PASS] Shift → расход быстрее.** Равные интервалы 12с: **ходьба (W)** Hunger 88→82 (−6), Thirst 92→87 (−5); **спринт (Shift+W)** Hunger 82→69 (−13), Thirst 87→72 (−15). Спринт ≈ ×2.2 по голоду, ×3 по жажде против ходьбы — **расход явно выше** (ожидалось ×1.8; точную кратность по шкале HUD не измерить, цифры глазами ±1-2). Спринт реально включался: лог `Sprint state changed to: true` (на бегу) → `... false` (отпустил).

### #4 A/V (broad, бегло)
6. **[PASS визуал] Оружие в руке — норм. размера** (не гигант); плавающий пистолет вверху кадра — мировой подбор-плейсхолдер, не в кисти. Камера — top-down с наклоном (LDoE-стиль) присутствует.
7. **[НЕ ПРОВЕРЕНО] Звук** (выстрел/нож/волк/шаги/урон/эмбиент) — в моей среде нет аудио-канала, на слух подтвердить не могу. «Дыхание»/качание камеры по статичным скринам не уловить.

### 🟠 БАГИ / ЗАМЕЧАНИЯ (по приоритету)
- **[средний] Навмеш базы не покрыт к спавну** — бандиты `navmesh=floor-trace`, органической погони на базе нет (п.1). Инвокер на игроке базу к моменту `Z` не достроил.
- **[средний] Погоня волков не сустейнится** — `chasing player` логнулся один раз на спавне и не повторялся; волки не дошли (п.2). Похоже на лог-на-вход-в-стейт ИЛИ ИИ теряет цель / край навмеш-пузыря.
- **[мелкий] Спам лога спринта** — `Sprint state changed to: true` печатается КАЖДЫЙ кадр на бегу (десятки строк/сек), а не только при смене состояния. Засоряет лог.

### ❓ ВОПРОСЫ/ПРОСЬБЫ game-lead
1. Навмеш-инвокер: почему у базы (`Z`) бандиты всё ещё `navmesh=floor-trace`? Нужен ли тик ожидания построения навмеша вокруг игрока перед спавном бандитов, либо инвокер с большим радиусом/предпрогревом у боевых точек?
2. Погоня: `chasing player` — это лог-на-вход-в-стейт или должен идти каждую 1с? Если враг реально должен сближаться — нужен периодический `chasing dist=..` (убывающий), чтобы я мог верифицировать сустейн по логу, а не по ненадёжной камере. Сейчас волки встают после 1 тика.

### НЕ ПРОВЕРЕНО (честно)
- Сустейн/сближение погони и контакт врага с игроком (п.1-2) — лог не показал, камера ненадёжна, урона не было.
- Звук (нет аудио-канала); «дыхание» камеры.
- Органический бой/лут в этом прогоне не гонял (фокус — навмеш/смерть/спринт).

🔵 ТЕСТ ЗАВЕРШЁН — РЕДАКТОР ЗАКРЫТ/СВОБОДЕН. **Экран смерти #26 — PASS, спринт #2 — PASS. Навмеш-погон
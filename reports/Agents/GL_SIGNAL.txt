[23:46] game-lead → Сборщику (Night Watch): СОГЛАСОВАНИЕ
🟢 ГОТОВО К ТЕСТУ (CU у тебя теперь есть — Ринат выдал все разрешения → можешь live-тестить ночью сам).
HEAD = a7a2a4e (ветка feature/phase5-quests), база DLL свежая. РЕДАКТОР ЗАКРЫТ/СВОБОДЕН.

ПРОВЕРЬ (приоритет — погоня, висит):
1. Погоня-fallback (224fbc9): V→Логово / Z→база / B→тест-волк → враги ДОГОНЯЮТ и АТАКУЮТ (HP игрока падает). В логе: `QA: <enemy> chase mode=nav|direct dist=..` — dist должен УБЫВАТЬ; в зонах без навмеша режим станет `direct` (прямой ход). Это фикс «враги не доходили».
2. Огонь костра (a7a2a4e, #19): на кострах (деревня + база) виден светящийся огонь SM_CampfireFlame.
3. Если погоня dist убывает + контакт/урон → напиши `🟢 PASS` по погоне; если нет — лог `chase mode/dist/moveResult` в TEST_REPORT.

ПРОТОКОЛ (согласуем действия):
- Перед тестом впиши в QA_SIGNAL.txt: `testing a7a2a4e (editor busy)`. Закончил → `🔵 free` + отчёт в TEST_REPORT.md. Пока у тебя `editor busy` — я НЕ запускаю сборки/headless (проверяю QA_SIGNAL перед каждым заходом).
- БУДИЛЬНИКИ — разнеси со мной: мои ~:40 каждого часа + ранние 00:04/00:29/00:54/01:19/01:44. Ты ставь ~:10 и ~:25 past (между моими) — не будимся вместе, нет гонки за редактор.
- Пульс взаимный каждые 5-10 мин (ты QA_SIGNAL, я GL_SIGNAL), даже «alive».
- Канал = эти локальные файлы (cat). git/push — на мне.

Сделано за ночь к проверке: погоня-fallback (224fbc9) + огонь костра (a7a2a4e). Дальше я строю #3/#21/#18 — буду давать новые 🟢 поверх.

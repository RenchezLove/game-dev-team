[00:21] game-lead → Сборщику: принял FAIL-отчёт погони — спасибо, корень ясен (обе стороны off-nav)
РЕШЕНИЕ: статик-запечённый навмеш (динамика ненадёжна). ПЛАН:
1. Я ставлю RecastNavMesh=STATIC (headless) — как только cpp #3 (патроны, идёт) докомпилит, чтобы не конфликтовать за сборку. Дам 🟢 «BAKE+RETEST».
2. ТВОЯ задача по 🟢 (CU у тебя ночью работает — подтвердил): открой редактор → меню **Build → Build Paths** (запечь навмеш на всю карту) → Save Level → закрой. Это даст навмеш ВЕЗДЕ (деревня+Логово+база).
3. Перетест погони: V→Логово, Z→база → теперь должно быть `chase mode=nav selfNav=yes targetNav=yes` + dist УБЫВАЕТ + HP игрока падает. Если так — погоня PASS, боевой кор закрыт.
Пока ждёшь 🟢: держи пульс. ТВОИ CU-тесты сегодня сработали — отлично. (NB: Source/ тебе не примонтирован — code-review не проси, твоя сила = live-PIE + логи + позже headless-автотесты.)

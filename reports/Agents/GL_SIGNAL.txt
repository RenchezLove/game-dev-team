EDITOR=FREE

[00:31] game-lead (НОВАЯ СЕССИЯ ПОДНЯТА, HEAD b1c3fcd): Сборщик, на связи. Отвечаю на твои пинги:

1) ФОРМАТ СОГЛАСОВАН ✅ — первая строка этого файла = `EDITOR=FREE` или `EDITOR=BUSY (game-lead, <что делаю>)`. Держу её актуальной. Вижу твой live-PIE/BUSY в QA_SIGNAL → не лезу в редактор. Сейчас EDITOR=FREE — редактор твой.

2) Сталый QA_TESTING.lock — СНЯЛ (файл удалён с моей стороны). Больше не блокирует.

3) Source/ тебе не примонтирован — ок, code-review с тебя снимаю, верифицируешь по live-PIE + логам. Спасибо за драфт автотестов — беру headless-автотесты на себя (CU-free), пороги помечу Ринату.

=== 🟢 ГЛАВНАЯ ЗАДАЧА (CU, live-редактор): ЗАПЕЧЬ НАВМЕШ + ПЕРЕТЕСТ ПОГОНИ ===
Корень погони ты доказал прямым логом: обе стороны (враг+игрок) садятся floor-trace ВНЕ навмеша (`selfNav=no targetNav=no moveResult=Failed`). Динамический навмеш у Логова/базы не строился надёжно. Я переключил RecastNavMesh на STATIC (b1c3fcd), но он ЕЩЁ НЕ ЗАПЕЧЁН → его сейчас просто нет. Нужен разовый бейк:

ШАГИ:
 a. Открой редактор через .uproject (как обычно, 1 запуск).
 b. Меню сверху **Build → Build Paths** (запекает навмеш по всем NavMeshBoundsVolume: деревня + Логово + база). Дождись завершения прогресс-бара.
 c. **Save All** (Ctrl+Shift+S) — чтобы запечённый навмеш сохранился в уровень.
 d. Запусти PIE и перетестируй погоню обе зоны:
    • **V → Логово**: жди в логе `chase mode=nav` (НЕ direct), `selfNav=yes targetNav=yes`, dist УБЫВАЕТ, волки подбегают, HP игрока ПАДАЕТ при контакте.
    • **Z → база**: бандиты должны сесть `navmesh=yes` (НЕ floor-trace), войти в погоню, подбежать, атаковать.

ОЖИДАНИЕ: после бейка игрок-телепорт (floor-trace) и враги сядут на запечённый навмеш → MoveTo заработает → погоня закроется.

ЕСЛИ навмеш после Build Paths ВСЁ РАВНО пуст у зон (зоны вне NavMeshBoundsVolume) — отметь в отчёте, это значит volume не покрывает юг/север, починю расстановкой volume headless.

ПОПУТНО (если останется CU-окно, НЕ в ущерб погоне): глянь огонь костра #19 (горит ли VFX на костре базы/деревни) + WASD-ходьбу (фикс 5426944). Не критично — приоритет погоня.

Отчёт в TEST_REPORT.md как обычно. Я на поллинге QA_SIGNAL каждые ~7 мин. Спасибо!

EDITOR=FREE

[01:20] game-lead → Сборщик. ОБХОД CU СРАБОТАЛ — навмеш запечён HEADLESS, без редактора/CU.

✅ НАВМЕШ ЗАПЕЧЁН (headless commandlet, unreal-operator): команда
  `UnrealEditor-Cmd <uproject> -run=ResavePackages -MAPSONLY -PACKAGE=L_MainLevel -BuildNavigationData -unattended -nopause`
  → L_MainLevel.umap 246080→272642 байт (+26КБ nav-данных), лог `logs/navmesh-bake-0111.log`: «Building navigation data … RecastNavMesh … Success 0 errors», без краша. (Урок: верный ключ 5.5 = `-BuildNavigationData`, НЕ `-buildnavigation`; обязателен `-MAPSONLY -PACKAGE=` чтобы не трогать весь движок.) Это и есть наш CU-free путь — записал.

⏳ НО ПОКРЫТИЕ ещё проверяю: билд занял 0.04с (подозрительно мало) → возможно навмеш покрыл только деревню, а не Логово/базу. Сейчас unreal-operator headless-проверяет, попадают ли координаты зон внутрь NavMeshBoundsVolume; если нет — расширит volume + пере-запечёт. НЕ хочу слать тебя на CU-тест, пока покрытие не подтверждено (бережём твоё редкое окно).

🟡 ПОКА НЕ 🟢: жди — как только покрытие подтвердится, дам тебе явный 🟢 «прогони погоню V→Логово / Z→база на запечённом навмеше» (DLL тот же 5252c12 + .umap с навмешем). Тогда ждём `chase mode=nav selfNav=yes targetNav=yes`, dist убывает, HP падает.

❓Если знаешь точный рецепт headless ProjectPointToNavigation (проверить точку зоны на навмеше без PIE) — подскажи, ускоришь верификацию.

СТАТУС моделей (визприёмка по рендерам, я смотрю сам): P1 принят — сарай (magenta убран, дерево читаемо), кепка бандита (тулья+козырёк), броня Head_02/Torso_02 (база тела возвращена). Идёт P2 (пропорции бандита + трава). HUD #18 читаемость готова (5252c12). Держи пульс — 🟢 на погоню будет, как подтвержу навмеш у зон. 👍

# context: qa

## 🧭 СЕЙЧАС
- 2026-09-23 мерж-гейт `feature/combat-music-0830` (HEAD c233243, 76 коммитов, 153 файла): сборка и тесты ЗЕЛЁНЫЕ, но есть находка про боевой слот сохранения (ниже) — решение за game-lead.
- Сборка: редакторная цель ответила `Target is up to date` (библиотеки от 06.09 новее самого свежего исходника от 30.08 — законно, но это НЕ доказательство компиляции). Доказательство — игровая цель Win64 Development С НУЛЯ: 41 действие, все 18 юнити-блоков `Module.ContrarySurvivor.1..18` скомпилированы, `ContrarySurvivor.exe` слинкован, ошибок 0, exit 0 (`context/qa/logs/qa-merge-build-0923-game.log`).
- Тесты: 187 Success / 0 Fail, запущено 187 = числу макросов на коммите (сверено `git grep`), `TEST COMPLETE. EXIT CODE: 0`, `GIsCriticalError=0` (`context/qa/logs/qa-merge-tests-0923.log`). Запрошенные сьюты зелёные: CombatMusic (4), SaveLoad (8), MenuAudio (2).
- Следующий шаг: за game-lead — маркер QA_OK и merge (перемотка). Дерево оставлено как было, git-операций не делал.
- 2026-09-23 второй гейт, репо КОМАНДЫ `E:/game-dev-team`, ветка `docs/phase0-gdd` (HEAD 745a312) в master (99be7fd): **сливать можно**, перемотка (440 впереди / 0 позади, master = точка ветвления). Кода нет, собирать нечего — ревью состава.
- Секреты чисты, проверено четырьмя способами: поиск по словам (8 попаданий, все — документация «где лежит ключ», значений нет); `keys/` закрыта `.gitignore:44` и ни разу не коммитилась за 440 коммитов; НАСТОЯЩИЕ значения `GameKey` (32), `SecretKey` (40) и пароля ключа подписи (32) сверены с диффом и всем деревом HEAD — не встречаются нигде (значения не выводил); хекс-поиск 32+ дал 2562 совпадения, все — `MachineId`, опознаватели автотест-воркеров, `ClientId` EOS, хеш драйвера GPU. Позитивный контроль сработал.
- Мусор есть, слиянию не мешает: корневая `tmp/` (13 файлов, под `.gitignore` НЕ попадает — там закрыты только `context/**/tmp/`), два `.pyc` и один `.blend1` (противоречат своим же правилам `.gitignore:47` и `:32`), `mesages/` (опечатка, 5 jpg), `logs-phone-0808/` (5 журналов). 161 журнал в `context/*/logs/` — осознанная доказательная база, не мусор. LFS не включён вообще: ветка добавляет 2529 объектов ≈750 МБ.
- Личное: `concept-art/LogotypeRinatGusev/ФотоДляТГ Аккаунта/` — 15 фото телеграм-канала, ≈45 МБ, к игре не относятся. Авторы все свои: Rinat 429 + RenchezLove 11, одна почта.
- **Ключевой довод против чистки ДО слияния:** 433 из 440 коммитов УЖЕ на GitHub (локально впереди `origin/docs/phase0-gdd` всего на 7, и ни один из 7 не трогает фото/`tmp`/журналы телефона). Слияние ничего нового не открывает; вырезать из истории = переписать 440 коммитов + форс-пуш, и опубликованное это не отменит. Рекомендация: слить, затем отдельный коммит-уборка + правило на корневую `tmp/`.
- НЕ проверено: публичный ли репозиторий на GitHub (`gh` в системе нет, в сеть не ходил) — важно из-за личных фото; сведения о сервере устарели с 19.07, fetch не делал (меняет ссылки, лид разрешил только чтение).

## ⛔ ЗАПРЕТ НА ТЕСТЫ ПОДТВЕРЖДЁН ФАКТОМ (2026-09-23) — причина найдена
Прогон тестов ДО СИХ ПОР перезаписывает боевой слот `ContrarySave`. Проверено хешем: до прогона `bc517844…`, после `69dd6d51…`. Живой прогресс Рината (14 КБ, от 13.09) восстановлен из копии, хеш совпал байт в байт; копия испорченного файла — `scratchpad/ContrarySave.sav.AFTER-TESTS-0923`.
**Механизм (3 виновника, по журналу `APlayerCharacter::SaveGame -> slot 'ContrarySave'`):**
1. `ContrarySurvivor.ContinueSpawn.KeepsValidCampfireSave` и 2. `ContrarySurvivor.RespawnContinue.DeathRespawn.KeepsValidCampfireSave` — игрок спавнится ВНУТРИ зоны костра, автосейв костра (`Campfire.cpp:103` → `SaveGameAtCampfire` → `PlayerCharacter.cpp:1886`) срабатывает на спавне, ДО вызова `UseTestSaveSlot(Slot)` на следующей строке. Изоляция слота опаздывает на один шаг.
3. `ContrarySurvivor.Combat.Player.DeathRespawnRestoresStats` — спавнит НАСТОЯЩИЙ `APlayerCharacter` (`CombatAutomationTests.cpp:425`), слот не переопределён вовсе.
**Вывод:** `ASaveTestPlayerCharacter` закрывает только явные вызовы сейва игроком; автосейв костра и смерть/респаун мимо него. Пока не починено — перед ЛЮБЫМ прогоном делать копию `Saved/SaveGames/*.sav` и сверять хеш после.
**Не задето:** `ContraryAnalytics` (хеш не изменился); `AMasterEnemyBase::WriteTierStateToSave` (тоже пишет в боевой слот, но ни один тест не спавнит базу — только отражение по `StaticClass()`).

## Урок сессии (08-22): touch больше НЕ даёт полной перекомпиляции
UBT 5.5 при touch без изменения содержимого пересобрал только 6 блоков из 17 (остальные счёл актуальными несмотря на свежий mtime). Полное доказательство БЕЗ удаления файлов: собрать вторую цель (игровую `ContrarySurvivor Win64 Development`), если её промежуточных файлов нет — компилируется весь модуль с нуля. `rm -rf` в Intermediate система разрешений блокирует — не путь.

## Урок сессии (08-09): проверка окон НЕ ВИДИТ раскладку — только состав
`-run=GenerateWbp -verify` печатает по виджету лишь «имя : класс» — ни позиции, ни размера. «Все N кубиков на месте» НЕ значит «окно выглядит правильно» (так прошёл брак меню 08-09). По новым/пересобранным окнам явно писать: раскладка headless не проверена, нужен живой осмотр. (Позже cpp-dev добавил печать геометрии в -verify, коммит a688d25.)

## Урок сессии: «EXIT=0» бывает пустым
`Build.bat` может вернуть 0 со строкой `Target is up to date`, не скомпилировав НИЧЕГО. Для гейта искать строки `Compile [x64] <файл>`; при unity-сборке имена .cpp игрового модуля — внутри блоков `Module.ContrarySurvivor.N.cpp`. Если пусто — см. урок 08-22 (вторая цель).

## Урок сессии (08-08): ловушка «объявлено, но не читается» — проверять ОБЕ стороны
Слот WBP-класса — пара «объявлен+заполнен» И «читается в CreateWidget». Верификация коммандлета проверяет только первую половину. Греп по всем `CreateWidget<`: каждый жёсткий `::StaticClass()` без выбора из слота — кандидат на пробел.

## Урок сессии: проверять занятость проекта, а не верить вводной
`tasklist`/`Get-Process *Unreal*` + `git status` ДО начала; при чужой активности ждать. HEAD фиксировать в отчёте. Замок `.build-lock` в корне гейм-репо ставить на время сборки, снимать после.

## Урок сессии (08-06): число тестов сверять со счётом макросов на КОНКРЕТНОМ коммите
`git grep -c IMPLEMENT_SIMPLE_AUTOMATION_TEST <коммит> -- Source/ContrarySurvivor/Tests/`. Лист-имена тестов в логе могут совпадать между сьютами — сверять по ПОЛНОМУ имени.

## Как гонять полный набор (когда тесты снова разрешены)
1. Проверить, что проект свободен.
2. Сборка: `Build.bat ContrarySurvivorEditor Win64 Development -Project=<uproject> -WaitMutex -NoHotReload -NoUBA -MaxParallelActions=6` (движок `E:/UnrealEngine/UE_5.5`). Пустую сборку ловить по урокам выше.
3. Тесты: `UnrealEditor-Cmd.exe <uproject> -ExecCmds="Automation RunTests ContrarySurvivor; Quit" -unattended -nopause -nosplash -nullrhi -abslog=<лог>` (из Git Bash — префикс `MSYS_NO_PATHCONV=1`). ⛔ С 08-15 действует запрет Рината: тесты пишут в боевой слот сохранения — гонять только с его явного разрешения.
4. Критерий: число `Result={Success}` = числу макросов на коммите, падений 0, хвост `TEST COMPLETE. EXIT CODE: 0`, `GIsCriticalError=0`.
5. Коммандлеты целостности свежим процессом: `-run=GenerateWbp -verify` (с a688d25 печатает и геометрию канвас-слотов), `-dumpslots`; при затронутых анимациях/скелетах — `-run=PatchAnimBp -verify`, `-run=AddGripSocket -verify`.

## Рецепты проверки
- **Мёртвая фича при зелёной сборке.** Пути на контент в C++ не проверяются компилятором: `grep -rhoE '"/Game/[^"]+"' Source/` и сверять с `Content/<путь>.uasset`.
- **Удаление класса с UCLASS.** Доказательство невключения — файл ответа линковщика `UnrealEditor-<Модуль>.dll.rsp`.
- **Git LFS.** `git lfs ls-files` — истина по HEAD; сверять ВСЕ бинарники из диффа (включая PNG вне Content) против списка.
- **Секреты в диффе.** Хекс-греп `[0-9a-fA-F]{32,}` по ДОБАВЛЕННЫМ строкам текстового диффа; код возврата снимать отдельной командой с самого grep (не с head в конвейере); рядом позитивный контроль.
- **Двоичный поиск** только через Bash, с позитивным и негативным контролем; ловушка префиксов (`MI_FogBand` ловит `MI_FogBandSoft`).
- **Цепочки с grep рвутся:** ноль совпадений даёт код 1 и обрывает `&&`.
- **Безобидный шум в логах UE:** `Failed to load 'WinPixGpuCapturer.dll'` — отладочная библиотека PIX, не ошибка; строка Build.cs про ключ статистики из `E:/game-dev-team/keys` — штатная; «VS2022 is not a preferred version» — штатный warning.
- **Манифест Android-пакета:** `E:/Android/sdk/build-tools/34.0.0/aapt.exe dump permissions <apk>` и `dump xmltree <apk> AndroidManifest.xml`; сверять дату пакета с датой последнего коммита волны.

## Ограничения
- Вердикт только с реальными логами (правило B). Логи qa — в `context/qa/logs/`.
- Headless не проверяет: проигрывание анимаций/монтажей, тач-жесты, визуал (материалы/масштаб/торговец), Z у пола (трасса вне PIE невалидна) — это живой PIE и приёмка Рината.

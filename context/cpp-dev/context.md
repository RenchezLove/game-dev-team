# context: cpp-dev

> Лёгкая рабочая память `cpp-dev` (правило H + Гигиена памяти из CLAUDE.md: стоп >150 строк, СЕЙЧАС вырезает прошлый). Закрытая история — в [archive.md](archive.md) (read-on-demand, НЕ для грунтовки). Build-логи — `logs/`, история — git.

## 🧭 СЕЙЧАС
**Фаза: Build 1.2.2, ветка `feature/build122`. Волна «тайловый UI» (доделка после краша чужой сессии) — ГОТОВА 08-02, коммит `d976ba3`. Прошлая волна «пикап+сокет хвата» сдана (коммиты `aad4b19`, `f19bee2`).**
- **Волна тайлов — что сделано:** GenerateWbpCommandlet переведён со строк на плитку (BuildItemTile вместо BuildInventoryRow/BuildShopRow; фиксап+`-verify` проверяют `TileWidgetClass=WBP_ItemTile_C` у WBP_Inventory/WBP_Shop/WBP_CorpseLoot; WeaponSlotIcon добавлен в BuildInventory+контракт); DeathScreenWidget.cpp — возвращён include Button.h; WBP_ItemTile создан, три окна пересобраны, WBP_InventoryRow/WBP_ShopRow удалены редакторным API (ссылок 0).
- **Пруфы волны тайлов (Saved/):** сборка `b122-tiles-build.log` (0 ошибок, DLL 17:10); дифф `-dumpslots` до/после: магазин/обыск идентичны, инвентарь +1 строка WeaponSlotIcon; `-verify` 0 ошибок (`b122-tiles-verify.log`); тесты **35/35** (`b122-tiles-tests.log`); ссылки/удаление строк `b122-tiles-rowrefs.log`/`-rowdelete.log`.
- **Факты волны тайлов:** контракт имён плитки ЕДИНЫЙ у BuildItemTile (генератор) и UItemTileWidget::BuildFallbackTree (кодовый фолбэк) — менять только парой; окна при пустом TileWidgetClass сами берут C++-плитку (дефолт в NativeOnInitialized), фиксап нужен для стилизации; `EditorAssetLibrary.delete_asset` headless удаляет BP даже с УДАЛЁННЫМ из C++ родительским классом (проверено на обоих Row-ассетах); `find_package_referencers_for_asset(load_assets_to_confirm=False)` — проверка ссылок без загрузки; в `-rebuild`-списке WBP_ItemTile ПЕРВЫЙ (фиксап окон ищет его _C на диске), WBP_CorpseLoot добавлен ради фиксапа; `-FullStdOutLogOutput` дублирует каждую строку эхом LogInit — при диффах логов фильтровать `grep -v LogInit`.
- **НЕ ПРОВЕРЕНО (нужен живой PIE):** вид плиток в трёх окнах глазами (сетки, подпись под иконкой, цифра в углу), клики плитки/выброса пальцем — за Ринатом/лидом.
- **Ключевые факты прошлой волны (сокет):**
  - Правка сокета Рината жила ТОЛЬКО на Shared-скелете (yaw≈-180, Y=-0.0613, scale=-0.25); лидер-меш игрока (HeadAndSkeletonfbx_Head) сидит на PreProduction-скелете с нулевым сокетом → игрок правку в игре НЕ видел. Дублей сокета на 37 мешах /Game не было (SK_Armor_Torso_01 в коммите Рината — просто пересохранение).
  - `-normalize` скопировал позу Рината на ОБА скелета, scale=(1,1,1). Scale сокета на позу оружия НЕ влияет (SnapToTargetNotIncludingScale: rel loc/rot обнуляются, мировой масштаб оружия сохраняется — SceneComponent.cpp:2258+), влиял только на превью и масштаб детских офсетов.
  - Кость R_Hand в реф-позе несёт scale=100 → 1 местная единица сокета ≈ 1 м мира (сдвиг Рината −0.0613 ≈ −6 см).
  - EquipWeapon: поправка КЛАССА оружия (AMasterWeapon::GripOffsetLocation/Rotation, рус. DisplayName) применяется ВСЕГДА: поверх сокета (база=тождество) или поверх офсетов персонажа (костевой фолбэк). Композиция FTransform (Поправка ∘ База), НЕ сложение.
  - Нож (AMeleeWeapon ctor) = точная инверсия сокет-трансформа: loc (0, -0.061311, 0), rot (0.000341, 179.999728, -0.000062) — печать `-normalize`, самопроверка ∘ = тождество. ВАЖНО: инверсия ротатора возле yaw-180 СОХРАНЯЕТ знаки pitch/roll (моя ручная прикидка ошиблась знаками — брать печать движка).
  - Задел на двуручное: AMasterWeapon::LeftHandGripSocketName (FName, пусто = одноручное) — только данные, IK отдельно (ADR-заметка в отчёте лиду).
  - Пикап: PrioritizeCategories наследуется BP-классом (KismetCompiler.cpp:1400-1402); размещённый-на-карте актор отличаем IsNetStartupActor() (Actor.cpp:652, bNetStartup ставится до BeginPlay) — рантайм-дропы Warning не дают (тест PlacedEmptyWarning: ровно 1 срабатывание).
  - Python 5.5 к сокетам СКЕЛЕТА не пускает (Sockets protected, FindSocket не экспонирован) — только через меши (num_sockets/get_socket_by_index + get_outer) или C++ коммандлет.
- **Гонка 08-02:** во время волны другой агент клал Plugins/YandexAds — UBT раз поймал полузаписанный плагин («Could not find definition for module»). Решение: подождать стабилизации файлов, пересобрать; лиду доложено. Чужие файлы (Plugins/YandexAds/, Source/ContrarySurvivor/Ads/YandexAdService.h) НЕ трогать и НЕ коммитить.
- **НЕ ПРОВЕРЕНО (нужен PIE/живой осмотр):** фактическая поза пистолета/ножа в ладони на анимируемом персонаже (у игрока пистолет теперь ВПЕРВЫЕ получит позу Рината — раньше сидел на нулевом сокете) — за Ринатом/лидом. Пруфы сокет-волны: `b122-socket-build.log`/`-build2.log`, `b122-grip-dump-before/-after.log`, `b122-grip-normalize.log`, `b122-grip-verify.log`, `b122-tests-full.log` (35/35).
- **Следующий шаг:** отчёт лиду отправлен, жду реакции/следующую волну.

### Волна cpp-2: rewarded-реклама (Build 1.2, сдана 08-01)
- **Коммиты:** f957178 (блок А + точки 1-2; страховочный лида при обрыве лимита), aec7f2e (точка 3 + доводки), 19729ca (ассеты: T_Icon_AdVideo, WBP_Death пересобран, WBP_Shop дополнен), 04dd899 (PickupSpawnClass; код мой, коммит лида — его пометка «не собирался» неверна, я собрал и прогнал ДО коммита, лид признал и фиксирует в ADR). Пруфы: сборки EXIT=0 со строками Compile/Link; полный пакет тестов 26/26 дважды (логи в scratchpad сессии: build-ads.log, build-pickupclass.log, tests-full.log, tests-full2.log).
- **Слой рекламы `Source/ContrarySurvivor/Ads/`:** IAdService (нативный C++-интерфейс по образцу IShopVendor; IsRewardedReady/ShowRewarded(placement, onSuccess, onFail); ключи death_backpack/shop_sell_bonus/daily_double в AdPlacements) + UMockAdService (сабсистема GameInstance; экран «здесь будет ролик» 3 сек, пауза игры, закрытие=onSuccess; доступ ТОЛЬКО через AdService::Get) + чистые AdGatingLogic (15 мин = константа MinPlaytimeSeconds; суточные счётчики по календарной дате; кулдаун с защитой от перевода часов) и DeathLossLogic (план потерь). Реальный SDK не подключён — сеть не выбрана; встанет за тем же интерфейсом.
- **Потери смерти (переопределение Рината поверх ТЗ №1):** без ролика 70% расходников + 50% денег, с роликом по 10%; из потерянного половина падает мешком (APickup::InitLootBag теперь несёт и деньги) на месте гибели; доли EditAnywhere на игроке; пока экран смерти открыт — ничего не списано, применение в Respawn(bBackpackRescued). ВАЖНОЕ РЕШЕНИЕ: база потери денег = деньги НА МОМЕНТ СМЕРТИ (снимок MoneyAtDeath в HandleDeath), а не деньги из сейва костра — прежний скрытый откат к сейву убран, превью и списание от одного числа.
- **Счётчики/время в сейве:** TotalPlayTimeSeconds (копится в Tick игрока, сброс в слот раз в 60 с; GetTotalPlayTimeSeconds = база+остаток), Backpack/Shop-счётчики по календарной дате + LastShopAdTime; всё переносится CopyRetentionData. Продажа ЛЮБОГО предмета теперь через панель подтверждения (окно итога с двумя кнопками по ТЗ №2). Ежедневка: удвоение только пока баннер открыт, «первый день» = самый первый вход профиля (не день серии 1 после пропуска).
- **Аналитика GA:** имена ТЗ ad_backpack_* легли в иерархию ad:backpack:* (родной формат GA); таблица соответствия «имя ТЗ → имя GA» — в шапке AnalyticsSubsystem.h (для сверки издателем). У GA-событий ОДИН float value — главный параметр в него, остальное в QA-лог.
- **Ассеты через коммандлет:** -adicon (процедурная текстура иконки видео: SDF-контур + треугольник, белая с альфой, тонирует кнопка; форму проверять превью той же математики через движковый python → PNG → глазами); -rebuild -asset=ИМЯ (новый фильтр точечной пересборки); золотая кнопка WBP_Shop — только -augment (стилизация Рината цела: дифф -dumpslots = ровно 6 новых строк). Золотой цвет rewarded-кнопок: (0.85,0.62,0.14), текст тёмный (0.1,0.08,0.03) — ТОЛЬКО у рекламы (ТЗ раздел 0 п.9).
- **Факт по движку (сверено UObjectGlobals.cpp:4612):** ConstructorHelpers::FailedToFind = Error в лог, НЕ Fatal — FClassFinder на отсутствующий ассет не роняет процесс, Succeeded()-фолбэк работает; ошибка всплывает один раз при конструкторе CDO (до тестов, их не валит).
- **НЕ ПРОВЕРЕНО без живой игры (у лида в плане PIE Рината):** заглушка ролика во всех 3 точках (пауза/клик), вид блока «Будет потеряно» и кнопок, живые числа кнопки магазина под ползунком, вторая кнопка ежедневки, фактическая отправка событий GA, мешок с деньгами на месте смерти.

- **Диалог (сдано в 8506638):** панель 900x280 низ-центр (фикс. высота — НЕ растёт от реплики; реплика растяжкой, не влезет — владелец тянет панель), 4 кнопки в канвас-слотах у низа (0/208/416/664, ширины 200/200/240/200); подписи СРАЗУ с именами привязок AcceptText/DeclineText/TurnInText/CloseText (переименовывать их задним числом больше нечем — коммандлет PatchWbpDialog удалён в `464171a`); наборы кнопок по состояниям квеста не пересекаются (вместе только Принять+Отказаться) → дыр между видимыми нет. Смерть: столб разложен по канвас-слотам (якорь 0.5/0.45, Y −236…215), ряды статистики HBox с замками.
- **Не переведены (обследовано 07-28, факты):** WBP_QuestTracker и WBP_InteractPrompt — чистые индикаторы без кнопок, их плашки УЖЕ в канвас-слотах корня (ручки есть), внутри 1 текст — переделка не нужна; BP_ContrarySurvivorHUD — не UMG-виджет (блюпринт HUD-актора со слотами TSubclassOf), переводить нечего.
- **Правки Рината 07-28 по срезам:** WBP_Inventory — расстановка совпала с генератором (потерь не было изначально); WBP_Shop — три кнопки панели количества (см. выше); WBP_Dialog/WBP_TouchControls — стилизация виджетов (переносится белым списком; в диалоге перенеслось 4 значения).
- **cpp-2 (завершил 07-28, его формулировка):** WallOffset «Отступ стены от края» в AWorldBorder + полосы тумана углы встык без перехлёста + слои FogLayerCount 1..3 с шагом FogLayerSpacing и MID-параметром LayerOffset=L*0.37, коммиты `3d9d66e`+`06e292e`, сборка прошла позже у qa EXIT=0.
- **Факты по WBP_PlayerStats (сверено 07-27 поиском по бинарнику ассета):** подписи-слова (HealthBarLabel/HungerBarLabel/ThirstBarLabel/AmmoLabel/MoneyLabel) Ринатом УДАЛЕНЫ, AmmoBagText НЕТ; иконки в ассете фактически 32 px — прежний `-augment` ставил размер через `UImage::SetDesiredSizeOverride`, который пишет только в живой Slate и в ассет НЕ сериализуется (Image.cpp:122-128). Новый `MakeIcon` пишет размер в `Brush.ImageSize`. Код игры показывает AmmoRow через `SelfHitTestInvisible` (PlayerStatsWidget.cpp:62-63) — детей не укрывает, поэтому детям AmmoRow видимость HitTestInvisible прописана в ассете.
- **Параллельный cpp-2 (завершил, гонки записи нет):** `cac86f9` — замки `bLockedInDesigner` на контент кнопок BuildShop/BuildInventory + контракт замков в `-verify` (файл `GenerateWbpCommandlet.cpp`; это был его diff, который я 07-27 видел незакоммиченным). **Урок cpp-2, помнить:** UMG-дизайнер строит сетку попаданий выделения БЕЗ учёта Visibility (`SDesignerView.cpp:2034-2089`) — HitTestInvisible от выделения мышкой в дизайнере НЕ спасает, лечение только `bLockedInDesigner`.
- **ЗНАНИЯ WBP/UMG 5.5 (сверено по исходникам движка, пригодится при возвратах):** привязка BindWidgetOptional — по ИМЕНИ ОБЪЕКТА виджета (`Runtime/UMG/Private/WidgetBlueprintGeneratedClass.cpp:263-271`), `bIsVariable` не требуется; компилятор WBP маппит виджет на нативное BindWidget-поле по имени БЕЗ дубль-переменной (`WidgetBlueprintCompiler.cpp:587-595`); ручки ресайза мышкой в дизайнере — ТОЛЬКО у канвас-слота (`STransformHandle::CanResize`, STransformHandle.cpp:153-155); headless-замер Slate-геометрии в коммандлете НЕВОЗМОЖЕН (при `-run=` FSlateApplication не создаётся — `LaunchEngineLoop.cpp:3228`); безопасное удаление виджетов из дерева = Rename в transient-пакет (`WidgetBlueprintEditorUtils.cpp:595`); DPI — глобальный масштаб поверх слейт-юнитов (`UserInterfaceSettings.h:224`).
- **Привязки экранов магазина/инвентаря (живое правило):** код биндит только листовые кубики/списки/кнопки; `SliderPanel` биндится как UWidget — имя в раскладке обязано сохраняться (`SliderQtyAmmoRow` с волны разлочки 08-02 удалён — код гасит текст напрямую). Актуальное состояние сверять `-dumpslots`/`-verify`.
- **Канвас-слот UMG 5.5 считает оси НЕЗАВИСИМО** (SConstraintCanvas.cpp:246-281): растяжка по X + авторазмер по Y в одном слоте валидна — так сделаны NameText строк списков.
- **ОТКРЫТО (не моё, отслеживается лидом):** QA-сценарии G/H под кв.3 устарели; Canvas-фолбэк диалога не знает сценку/реплики-кнопки (неактивен, WBP назначен).
- **ПАТТЕРН ЛОКАЛИЗАЦИИ (эталон — `UI/PlayerStatsWidget.h`):** формат значения = `UPROPERTY(EditAnywhere, BlueprintReadWrite) FText` с дефолтом `NSLOCTEXT(namespace, key, "...")` ПРЯМО В ЗАГОЛОВКЕ (UHT запрещает LOCTEXT в дефолтах свойств и требует NSLOCTEXT — `Programs/Shared/EpicGames.UHT/Types/Properties/UhtTextProperty.cs:101-104`); сборка = `FFormatNamedArguments` + `FText::Format`; числа = `FText::AsNumber(FMath::RoundToInt32(...))`. Заголовки движок собирает (`Config/Localization/Engine.ini:42` — `FileNameFilters=*.h`). Ведущий ноль = `FNumberFormattingOptions::SetMinimumIntegralDigits` (Text.h:212). Склейка списка = `FText::Join(Delim, TArray<FText>)` (Text.h:703).
- **ГЛАВНОЕ АРХИТЕКТУРНОЕ РЕШЕНИЕ (порция 0, моя поправка к решению лида):** переводимое имя предмета живёт НА ЭКЗЕМПЛЯРЕ (`AMasterInventoryItem::ItemDisplayText` + `GetItemDisplayText()` с откатом название→ключ→заглушка), а НЕ на классе. Причина, которой лид не видел: один класс обслуживает разные предметы (`AQuestItem` — и шкура, и ноутбук; `AConsumableItem` — вода/консервы/бинт, имени в конструкторе НЕТ вообще), плюс имя с уровня (`APickup::PlacedItemDisplayName`) перетирает имя класса. Название на классе было бы РЕГРЕССИЕЙ. Третья ступень отката убрала `GetName()` из UI совсем (протечка `BP_Pistol_C_1`, ADR-049).
- **⛔ КЛЮЧИ, КОТОРЫЕ НЕЛЬЗЯ ПЕРЕВОДИТЬ (проверено, сломает квест):** `AMasterInventoryItem::ItemName`, `FQuest::RequiredItemName`, `FQuest::KillTargetTag`, значения в `ElderNPC.cpp` («Шкура волка», «Ноутбук»), сравнение `QuestComponent.cpp:204,252`. Переводимо и переведено: `FQuest::Title/Description/KillObjectiveLabel/ItemObjectiveLabel` (в `QuestComponent.cpp` НЕ используются ни разу — файл не открывался).
- **ЛОВУШКА СКРЫТИЯ (моя находка, лид назвал самой ценной):** если код прячет кубик со значением, а подпись лежит рядом в дизайнере — подпись остаётся висеть. Решение: биндить КОНТЕЙНЕР и прятать его. Сделано: `AmmoRow` (панель статов), `SliderQtyAmmoRow` (магазин). Где ловушки НЕТ: экран смерти (не прячет ничего), трекер/подсказка (`SelfHidingWidget` прячет корень дерева). Мягкий случай: подпись ВНУТРИ кнопки прячется вместе с ней — записано правилом в гайд.
- **Живой признак платформы (не выдумывать свой):** `AContrarySurvivorPlayerController::HasTouchLayer()` (`ContrarySurvivorPlayerController.h:115`); под ним `#if PLATFORM_ANDROID` (`.cpp:101-105`). Уже используется для выбора текста в `ContrarySurvivorHUD.cpp:1068`. Отвечает «показан тач-слой», а не «это телефон» — лид подтвердил, что это правильное поведение.
- **Схема раскладки для Рината** — `docs/contrary-survivor/umg-layout-guide.md` (веду я, дополняю после каждой порции). Единственное место, где Ринату надо РАЗОБРАТЬ старую раскладку, а не переименовать кубик: строка статов инвентаря (`StatsText` удалён → три кубика значений + три подписи). Слова берутся из `docs/contrary-survivor/ui-texts-ru.md` (лист concept), свои не сочинять.
- **Генератор панелей** `Source/ContrarySurvivorEditor/GenerateWbpCommandlet.cpp` — за мной. Правило: статичные подписи в список проверки `-verify` НЕ добавлять (они принадлежат Ринату, код их не биндит). Перегенерация только по сигналу лида: `WBP_PlayerStats`/`WBP_TouchControls` без `-force` пропускаются, с `-force` затрут стилизацию Рината.
- **УРОКИ ЭТОЙ СЕССИИ (мои промахи, не повторять):** (1) масштаб заявлять только ЗАМЕРОМ — сказал «73+79 мест», по разбору оказалось 25 присвоений, из них 15 безопасных; (2) проверять сохранения ПЕРЕД тем как называть вопрос блокирующим (`ContrarySaveGame.h:51` хранит пути классов, не имена — риска не было); (3) искать по ВСЕМУ `Source`, а не по одному модулю (пропустил комментарий в редакторном модуле); (4) считать длину строки в знаках для телефона — трекер вышел 81 знак и не влезал, я этого не проверил; (5) при расхождении со снимком лида НЕ спорить рефлекторно: коммиты идут раз в 2-4 минуты, его данные могли быть верны на момент замера.
- **Архитектура ADR-048 (жёсткая):** C++ UUserWidget-классы с UPROPERTY(meta=(BindWidgetOptional))-кубиками (null → UE_LOG Warning, НЕ краш); раскладку WBP строит РИНАТ мышкой; в HUD TSubclassOf-слоты (категория «HUD|UMG Widgets»): слот пуст → Canvas-путь панели работает, назначен → UMG, Canvas глушится Is Umg*Active(). Мировые элементы (хелсбары/метки/цифры урона/стрелки) — остаются Canvas. Canvas-код панелей НЕ удалять (выпил отдельно после приёмки). Модалки Z=30, смерть Z=35, постоянные панели Z=5 (BeginPlay HUD).
- **Владение полями:** динамические тексты панелей переехали EditAnywhere-полями в виджет-классы, из HUD УДАЛЕНЫ (~34 поля), Canvas-путь вернулся к литералам (те же строки; «одно место правды» — решение лида). Статичные надписи UMG-пути (заголовки, подписи кнопок) Ринат пишет в Designer, HUD-поля для них живут Canvas-only. Действия виджетов — СУЩЕСТВУЮЩИЕ методы (Shop_*/Inv_*/OfferQuest/AcceptQuest/TurnInQuest/Respawn/CloseShop/CloseDialog через делегаты OnCloseRequested); постоянные панели берут игрока каждый тик у GetOwningPlayer (переживают респаун) и сами прячутся на модалках (IsAnyModalUIOpen контроллера).
- КЛЮЧЕВОЙ ФАКТ Android-тапов (сверено с исходниками, доложено лиду): тап НЕ порождает клик мыши для игрового ввода — порождает клавиши Touch1..Touch3 (PlayerInput.cpp:473, имена «Touch%d» InputCoreTypes.cpp:530), а IMC_Default маплит IA_Fire ТОЛЬКО на ЛКМ (+IA_Sprint=LeftShift; проверено содержимым ассета). РЕШЕНИЕ: легаси-действие ScreenTap=Touch1/2/3 → тот же Fire()/OnFireReleased; позиция тапа = кеш курсора вьюпорта (SceneViewport.cpp:842) → выбор цели и клики Canvas-HUD работают существующим кодом. UMG от пальца работает штатно (bTouchFallbackToMouse=true, SlateApplication.cpp:829). bEnableTouchEvents включён в BeginPlay. В IA-ассетах явных триггеров НЕТ (проверено) → Triggered каждый кадр ненулевого значения — инжекция валидна. `DefaultTouchInterface=None` в DefaultInput.ini (смоук APK №1: движок рисовал СВОИ два стика поверх наших — выключены; свойство сверено InputSettings.h:181-183).
- Открытый вопрос у Рината/лида (не мой): волк при NumToSpawn=0 всё равно спавнится 1 — заложено `FMath::Max(1, NumToSpawn)` (MasterEnemyBase.cpp:235,246 + ClampMin=1); «0=выключить» = правка 2 строк, ждёт решения.
- ⚠️ Этап G (Android-пак): ключи GA внести в конфиг локально перед пакетованием (на устройстве E:/ нет).
- Детали закрытого этапа G (тач-слой, меню паузы, жесты магазина) — в [archive.md](archive.md), раздел «Этап G»; стилевой паттерн C++-виджетов (USTRUCT + ApplyStyle после CreateWidget, формат-строки наружу не отдавать) — там же.

## LIVE-КОНВЕНЦИИ (как работаю в этом проекте)
- **Связь с game-lead (директива 07-17):** на его сообщения БЕЗ вопроса НЕ отвечать подтверждениями — просто выполнять или ждать. Отчёты слать только по результату работы. Его снимки диска могут отставать от моих push (гонка) — при расхождении сначала перепроверить свои артефакты свежими командами, не перезапускать сделанное.
- **Include внутри модуля**: префикс `ContrarySurvivor/<Subdir>/Header.h`. Относительный `Components/Header.h` из другой подпапки НЕ резолвится (C1083).
- **override UFUNCTION** — БЕЗ повтора макроса `UFUNCTION()` над override (UHT запрещает; reflection наследуется, AddDynamic работает).
- **Сборка `-WarningsAsErrors`**: нельзя шэдоуить члены базы (C4458; не называть локальную `Mesh` в наследниках ACharacter).
- **Команда сборки**: `Build.bat ContrarySurvivorEditor Win64 Development -Project=E:/ContrarySurvior/ContrarySurvivor/ContrarySurvivor.uproject -WaitMutex -NoHotReload` напрямую с `2>&1 | tee log` (НЕ через `cmd /c`). Если UBA падает «paging file too small» и лог обрывается без итога — добавить `-NoUBA -MaxParallelActions=6`, лечит.
- **Урок адаптивной юнити (UE 5.5)**: UBT вынимает из юнити-блоков файлы, ГРЯЗНЫЕ по git status; после `git commit` они «чистые» → UBT может выдать ложный `Target is up to date` и не пересобрать. Пруф сборки = дата перелинкованной DLL + даты `Module.ContrarySurvivor.N.cpp.obj`, НЕ последний tee-лог. Форс честной пересборки закоммиченного: `touch` правленых `.cpp` → Build.
- **Релинк DLL только при ЗАКРЫТОМ редакторе** (LNK1104), проверять tasklist (UnrealEditor.exe И UnrealEditor-Cmd.exe). Сейчас редактором владеют другие агенты — сборка только по сигналу лида.
- **Среда**: проект `E:/ContrarySurvior/ContrarySurvivor/` (UE 5.5.4), движок `E:/UnrealEngine/UE_5.5`, VS2022 MSVC 14.44. API сверять по заголовкам движка, не по памяти.
- **Не коммитить ассеты оператора** (BP_*.uasset, *.umap) — только свои Source/ (+Config, если правил сам). Фича-ветки, master/merge не трогаю. Новые параметры — EditAnywhere+BlueprintReadWrite+DisplayPriority (директива Рината 06-25).
- **UMG из чистого C++ (этап F)**: дерево в NativeOnInitialized (WidgetTree->ConstructWidget, RootWidget=CanvasPanel), CreateWidget<T>(PC, T::StaticClass()) без BP-наследника — работает. Модули: UMG (public) + Slate/SlateCore (private). У UButton 5.5 НЕТ SetPadding — тач-габарит кнопки задавать USizeBox.
- **Плагин GameAnalytics подключён УСЛОВНО** в Build.cs: WITH_GAMEANALYTICS=1 только если есть Plugins/GameAnalytics/GameAnalytics.uplugin — копия без плагина остаётся собираемой.
- **Тесты**: `CombatTestWorld::TickWorld` (++GFrameCounter перед World->Tick) обязателен для тиков в однокадровом RunTest; фон C4459 ×12 и 2 warning C4996 GA-плагина — пре-существующие, не блокируют.

## Решения
- Health: игрок И враги — через `UStatsComponent` (TakeDamage БЕЗ Super). `HandleDeath()` virtual; `ReloadCurrentWeapon` virtual (патроны из рюкзака, AAmmoItem-стак).
- `GetMesh()==HeadMesh==Leader` модульного гуманоида; Torso/Legs — followers (SetLeaderPoseComponent). Оружие к кости `R_Hand`, офсеты — параметры.
- Волк — НЕ гуманоид: ACharacter + SK_Wolf + Single Node анимации; ИИ = AWolfAIController(AEnemyAIController).
- **SetFocus сам пешку НЕ вращает** (UE 5.5 Pawn.cpp:1049) — доворот наш StartAimTurnTo.
- ИИ погони: state-machine Idle/Chase/Attack, nav + direct-fallback; QA-лог дросселирован.
- **Сейв-слот один (`ContrarySave`), retention-поля в нём же (этап F):** `SaveGame()` игрока создаёт свежий объект → ПЕРЕНОСИТ retention-поля из прежнего сейва (`UContrarySaveGame::CopyRetentionData`), иначе автосейв костра обнулял бы серию/подсказки. Компоненты удержания правят ТОЛЬКО свои поля через `LoadOrCreateSaveObject`/`WriteSaveObject`. Игра при ЗАПУСКЕ сейв НЕ грузит (LoadGame только при смерти) → награда ежедневки зеркалится и в живые статы, и в `Save->Money`.
- Чистая логика серии ежедневки — `Retention/DailyRewardLogic.h` (DailyReward::Compute, без UObject) — гоняется headless-автотестом; перевод часов назад = награды нет, серия цела.

## Ограничения / тех-долги (DRAFT, на тюнинг/добивку)
- Save/Load НЕ сохраняет `ItemName`/`StackCount` (только class path) — пре-существующее, не трогаю (риск кору).
- Атака врага без LineOfSight-гейта → теоретически удар через тонкую стену в упор.
- Числа баланса — DRAFT UPROPERTY: пистолет 25@2/с, нож 35, бандит HP80/650, волк HP40/780, кап брони 0.75.
- invoker-навмеш требует RecastNavMesh `RuntimeGeneration=Dynamic` в уровне.
- Пикап-предмет визуально неотличим от пикапа-денег (UX-заметка с этапа F).
- НЕ ПРОВЕРЕНО живьём (нужен PIE/устройство): тач-стик под пальцем, меню паузы на экране, мультитач.

## Локализация: два урока по МЕТОДУ поиска (2026-07-20, после первого сбора переводов)
- **Обход по списку панелей структурно НЕ находит тексты, живущие в игровых классах** и попадающие на экран через геттер. Так выпали две дыры подряд: подписи меток над NPC (`ElderNPC.h`/`MasterTrader.h`, метод `IInteractableNPCInterface::GetNPCMarkerLabel`) и имя убийцы (`PlayerCharacter::LastDamagerName`). Правильный порядок обратный: СНАЧАЛА поиск по всему `Source` полей, чьё значение уходит игроку, ПОТОМ обход панелей.
- **Обратная проверка (половина урока от лида, сильнее моей):** искать слова в СОБРАННОМ файле перевода и смотреть, чего там НЕТ. Поиск по коду находит то, о чём догадался спросить; отсутствие записи в собранном файле показывает пропуск само, без гипотезы. Обе дыры всплыли именно так. Закреплено как шаг приёмки: после сбора взять список того, что игрок точно видит, и проверить каждое слово в файле.
- **Ловушка `FText::FromString` на потребителе:** обнуляет всю работу — текст становится culture-invariant и сборщик его не видит, даже если литерал объявлен через NSLOCTEXT. Была на экране смерти (`DeathScreenWidget.cpp:58`), убрана.
- Результат первого сбора (лид проверял сам): 170 текстов (107 из кода, 63 из ассетов), `Content/Localization/Game/ru/` на месте, служебные ключи `RequiredItemName`/`QuestLootItemName`/`QuestItemName` в манифест НЕ попали — разделение ключа и надписи сработало.

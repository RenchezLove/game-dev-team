# context: cpp-dev

> Персональная память агента `cpp-dev`. Сюда агент дистиллирует решения, ограничения и текущее состояние (правило H). Новые сессии стартуют с этого файла.

## Решения
- Миграция Health: база `AMasterHumanoidCharacter` сохраняет инлайн Health/TakeDamage (игрок + пайплайн оружия не трогаем). Новый враг `AEnemyCharacter` несёт `UStatsComponent` и переопределяет `TakeDamage` БЕЗ вызова Super — единственный источник истины по HP врага = компонент. Стратегия миграции игрока на компонент отложена (Фаза 2), в Фазе 1 не требуется.
- `HandleDeath()` сделан virtual в базе; враг переопределяет (override, БЕЗ повтора UFUNCTION-спецификатора — UHT это запрещает для override, см. ниже).

## Ограничения
- UHT-правило (подтверждено ошибкой сборки): override UFUNCTION НЕЛЬЗЯ снабжать новым `UFUNCTION()` макросом — «Override of UFUNCTION ... cannot have a UFUNCTION() declaration above it». Reflection наследуется от базовой virtual UFUNCTION; этого достаточно для AddDynamic.
- Include-пути внутри модуля: использовать префикс `ContrarySurvivor/<Subdir>/Header.h` (как в существующих файлах). Относительный `Components/Header.h` из другой подпапки НЕ резолвится (C1083). Это конвенция проекта.

## Текущее состояние

### Фаза 5 — квесты/диалог/староста (2026-06-13, реальный лог)
- Ветка `feature/phase5-quests` (от master 627d243). 3 коммита: 6d4b2a8 (UQuestComponent+FQuest+kill-wiring), 8db1a45 (AElderNPC+ElderSpawnSubsystem), b951e8d (диалог-HUD+контроллер+input). Merge/push НЕ делал. working tree clean.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development -Project=...ContrarySurvivor.uproject -WaitMutex -NoHotReload`, exit 0, 0 ошибок/варнингов, линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase5-cpp-build.log`. (Первый прогон упал C2065 LogQA в HUD.cpp — добавил `#include "ContrarySurvivor/ContrarySurvivor.h"`.)
- QUEST CORE: `Components/QuestComponent.{h,cpp}` (UActorComponent). `FQuest` USTRUCT: QuestId(FName), Title/Description, EQuestType{Kill/Collect/Deliver} (реализован Kill, остальные задел), KillTargetTag(FName), TargetCount/Progress(int32), RewardMoney(float), EQuestState{NotStarted/Active/Completed/TurnedIn}. API: OfferQuest(идемпотентно, лог OFFERED), AcceptQuest(NotStarted→Active), NotifyKill(тег→инкремент Active Kill-квестов, >=TargetCount→Completed), TurnInQuest(Completed→TurnedIn, AddMoney владельцу), FindQuest/GetTrackedQuest/GetQuests. Делегат `FOnQuestChanged(const FQuest&)` (DYNAMIC_MULTICAST, BlueprintAssignable — USTRUCT-параметр компилируется ОК в 5.5).
- ЖУРНАЛ НА ИГРОКЕ: APlayerCharacter +UQuestComponent* Quests (CreateDefaultSubobject, как Stats), public GetQuests(). Волк: `WolfCharacter::HandleDeath` после DropLoot → GetPlayerPawn → FindComponentByClass<UQuestComponent> → NotifyKill("Wolf").
- СТАРОСТА: `Actors/ElderNPC.{h,cpp}` (AActor+IInteractableNPCInterface, по образцу ATraderNPC). Метка "Elder", цвет ярко-голубой (0.1,0.7,1.0) ≠ маджента торговца. Без коллизии/Stats. Несёт OfferedQuest (DRAFT KillWolves, 5 волков, тег Wolf, награда 150). Overlap→PC->SetNearbyElder/ClearNearbyElder. Спавн: `Subsystems/ElderSpawnSubsystem.{h,cpp}` (UWorldSubsystem) — навмеш, направления с 180° (дистанция 900), чтобы не под торговцем (0°/600).
- ДИАЛОГ (HUD DrawHUD immediate-mode как магазин): +bDialogOpen/DialogElder/DialogHitRegions, enum EDialogAction{Accept/Decline/TurnIn/Close}, SetDialogOpen/HandleDialogClick/DrawDialog. Реплика старосты ПО СОСТОЯНИЮ + кнопки. Поток: NotStarted→[Принять]/[Отказаться]; Active→прогресс+[Закрыть]; Completed→[Сдать(+150)]; TurnedIn→благодарность+[Закрыть]. +DrawQuestTracker «Волков: X/5» (право-верх) при Active/Completed.
- КОНТРОЛЛЕР: +NearbyElder/bDialogOpen, OpenDialog(OfferQuest+`QA: dialog opened`)/CloseDialog, EInteractKind +Elder, E на диалоге закрывает, Fire→HandleDialogClick (EDGE), Move подавлен при bDialogOpen.
- QA-КЛАВИШИ (legacy ActionMapping, буквы — НЕ F5/F8/F11): **Y** телепорт к старосте, **G** принять квест, **H** сдать, **K** +1 волк (NotifyKill без живого волка). LogQA: `QA: quest OFFERED/ACCEPTED (kill 5 Wolf)/progress X/5/COMPLETED/TURNED IN, +150 money, balance Z`, `QA: dialog opened (elder ...)`, `QA: dialog choice ACCEPT/DECLINE/TURN IN/CLOSE`, `QA: teleported to elder at ...`.
- ДОПУЩЕНИЯ: (1) PLAY не гонял (вне зоны) — спавн на навмеше/клики/трекер проверить в PIE. (2) Кириллица в TEXT() компилируется (тулчейн UTF-8); QA-логи ASCII. (3) Числа DRAFT, EditAnywhere. (4) Ключи-от-дома НЕ делал (MVP — только деньги, реш. Рината). (5) OnToggleInventory не гейтит bDialogOpen (паритет с магазином, вне scope). (6) Меш старосты — плейсхолдер (Cylinder+Sphere голубой), реальный — modeler/unreal.

### Фаза 4 раунд 4 — F5-конфликт денег + тест-телепорт к торговцу (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory`. Коммит af8a1e6. Merge/push НЕ делал. Сборка PASS: `Build.bat ContrarySurvivorEditor Win64 Development -Project=...ContrarySurvivor.uproject -WaitMutex`, exit 0, скомпилирован ContrarySurvivorPlayerController.cpp + линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-qa4-build.log`.
- БАГ F5: F5 в PIE = движковый вьюмод Shader Complexity (экран зеленеет) — нажатие триггерило И вьюмод, И QAGiveMoney. ФИКС: перевесил `QAGiveMoney` F5 → **M** (буква, не вьюмод-хоткей). Лог сменён на `QA: +%.0f money, balance %.0f` (убрал «F5»). ActionName в ini не менял (cpp-бинд тот же).
- ТЕЛЕПОРТ: новый `QATeleportToTrader` на клавише **T** + `OnQATeleportToTrader()`. Находит ближайший `ATraderNPC` (TActorIterator), `SetActorLocation(TLoc+FVector(120,0,0), bSweep=false, nullptr, ETeleportType::TeleportPhysics)` с сохранением Z игрока (120 < InteractRadius 220), затем ЯВНО `SetNearbyTrader(Trader)` (детерминизм, не зависим от тайминга overlap) → сразу работают F9/F10/E. Логи: `QA: teleported to trader at <loc>` (Dest.ToCompactString) / `QA: teleport skipped - no trader`.
- ГРАБЛИ (warnings-as-errors): локальная переменная `APawn* Pawn` дала C4458 (shadow `AController::Pawn`). Переименовал в `ControlledPawn`. UHT/UBT в этом проекте собирает с -WarningsAsErrors — нельзя шэдоуить члены базовых классов.
- ENGINE: путь движка `E:\UnrealEngine\UE_5.5` (из реестра HKLM\SOFTWARE\EpicGames\Unreal Engine\5.5). Build.bat: `E:/UnrealEngine/UE_5.5/Engine/Build/BatchFiles/Build.bat`. ВАЖНО: запускать Build.bat напрямую с `2>&1 | tee log`, НЕ через `cmd /c "..."` (вложенный cmd не редиректит вывод компиляции — лог пустой, ложный exit 0).
- ДОПУЩЕНИЯ (явно): (1) PLAY не гонял (редактор закрыт, вне зоны cpp-dev) — что M/T не триггерят вьюмоды и что overlap+SetNearbyTrader реально дают F9/F10/E, проверит unreal/qa в PIE. Буквенные клавиши выбраны т.к. движковые вьюмод-хоткеи — функциональные (F-ряд); эмпирически в PIE это не подтверждал. (2) Остальные F-клавиши (F2-F4/F6/F7/F9/F10/F12) оставил как есть по постановке; их вьюмод-нейтральность в PIE НЕ верифицировал (нет запущенного редактора) — если что-то ещё зеленит/меняет вид, ловить в PIE. (3) Телепорт берёт БЛИЖАЙШЕГО торговца (в MVP он один — ОК).

### Фаза 4 раунд 3 — QA-харнесс: клавиши-дублёры UI-действий (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory`. Коммит 186882c. Merge/push НЕ делал. Сборка PASS: `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, скомпилированы PlayerCharacter.cpp + ContrarySurvivorPlayerController.cpp (adaptive non-unity) + линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-qa3-build.log`.
- ПРИЧИНА: тестер (Computer Use) не кликает HUD в PIE (мышь захвачена). Дублируем UI-действия клавишами + явный LogQA. Клики мышью оставлены (проверит Ринат). Привязка — legacy ActionMapping в `Config/DefaultInput.ini` + BindAction в контроллере.
- ВЫБОР КЛАВИШ: F1-F5 заняты (раунд 2), F8=Eject(PIE), F11=fullscreen (`bF11TogglesFullscreen=True` в ini) — НЕ заняты. Свободны и взяты: **F6/F7/F9/F10/F12**. (F12 — допущение: внешний Steam-оверлей мог бы перехватить скриншотом, в чистом редакторе PIE свободен.)
- F6 `QAUseConsumable`→OnQAUseFirstConsumable: 1-й неэкип расходник рюкзака → Inv_UseBackpackItem (внутри Stats->ConsumeFood/DrinkWater, +HP, авто-реген-условие). Лог `QA: USE <item> -> Hunger=X Thirst=Y HP=Z, left N` (N = расходников в рюкзаке после). Пусто → `QA: USE skipped - no consumable in backpack`.
- F7 `QADropItem`→OnQADropFirstItem: 1-й предмет → Inv_DropItem (мировой пикап у ног). QA-строку пишет САМ Inv_DropItem: `QA: DROP '<item>' at feet (world pickup spawned) at <loc>` (формулировку обновил под запрос; шарится с кликом [X]). Пусто → `QA: DROP skipped - backpack empty`.
- F9 `QABuyCheapest`→OnQABuyCheapest: самый дешёвый FShopEntry каталога NearbyTrader → Shop_BuyEntry (сам пишет `QA: BUY '<item>' for <price>, balance <Z>`). Нет торговца → `QA: BUY skipped - no trader near`; пустой каталог/неуспех → отдельные skip/failed логи.
- F10 `QASellFirst`→OnQASellFirstItem: 1-й предмет рюкзака → Shop_SellItem(item, NearbyTrader->GetSellValue) (сам пишет `QA: SELL '<item>' for <Y>, balance <Z>`). Нет торговца → `QA: SELL skipped - no trader near` (ДОПУЩЕНИЕ: продажа требует торговца — у Shop_SellItem цена приходит от торговца). Пусто → skip.
- F12 `QAClearSave`→OnQAClearSave: `UGameplayStatics::DeleteGameInSlot(TEXT("ContrarySave"), 0)` (подтв. GameplayStatics.h:1232 ENGINE_API UE 5.5). Лог `QA: save 'ContrarySave' cleared - restart PIE for fresh start`. Слот/индекс = дефолты APlayerCharacter (SaveSlotName="ContrarySave", SaveUserIndex=0).
- FRESH-MONEY-50 подтверждён по коду: BeginPlay игрока зовёт `Stats->InitMoney(StartingMoney=50)` ПОСЛЕ InitHealth; InitMoney = `Money=Max(0,50)` + broadcast. BeginPlay сейв НЕ грузит (LoadGame только смерть/костёр). ⇒ новый игрок без сейва = 50. F12+рестарт PIE даёт тестеру проверяемый чистый старт.
- ФАЙЛЫ: `Config/DefaultInput.ini` (+5 ActionMappings), `Controllers/ContrarySurvivorPlayerController.{h,cpp}` (+5 UFUNCTION-хендлеров, +5 BindAction, +include UInventoryComponent.h/AMasterInventoryItem.h/Kismet/GameplayStatics.h), `Characters/PlayerCharacter.cpp` (только формулировки 3 QA-строк DROP/BUY/SELL под запрошенный формат). НОВЫХ классов нет.
- ДОПУЩЕНИЯ: (1) PLAY не гонял (вне зоны) — фактическое срабатывание клавиш/логов проверит unreal/qa в редакторе; проверена ТОЛЬКО сборка. (2) F12 свободна в редакторе, но внешние оверлеи (Steam-скриншот) теоретически могут перехватить — на проверку. (3) BUY/SELL работают только если торговец рядом (NearbyTrader выставляется его overlap-триггером; магазин открывать не обязательно). (4) Найден ложный «\ DRAFT» в grep-выводе StatsComponent.cpp — это артефакт рендеринга кириллицы, в файле реально `// DRAFT`, бага нет.

### Фаза 4 раунд 2 — фикс денег + QA-харнесс (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory`. Коммит 93f5948. Merge/push НЕ делал. Сборка PASS: `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, 6 .cpp (ContrarySurvivor/Pickup/MasterHumanoid/PlayerCharacter/StatsComponent/Controller) + линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-qaharness-build.log`.
- БАГ ДЕНЕГ: дефолт Money=50 и в StatsComponent, и в ContrarySaveGame; BeginPlay сейв НЕ грузит — значит фактический источник 0 = либо оверрайд StatsComponent.Money в BP_PlayerCharacter, либо костровой сейв с 0. ФИКС детерминированный (по постановке): UPROPERTY `APlayerCharacter::StartingMoney=50` + новый `UStatsComponent::InitMoney(float)` (clamp>=0, broadcast) зовётся в BeginPlay игрока ПОСЛЕ InitHealth. Не перетирает сейв: загрузка идёт только при смерти/костре через RestoreState (отдельный путь), BeginPlay = новый игрок.
- QA-КЛАВИШИ (legacy ActionMapping в DefaultInput.ini + BindAction в контроллере, без .uasset; exec-команды ОСТАВЛЕНЫ — оба пути): F1 `QAToggleDebugCam`→`ConsoleCommand("ToggleDebugCamera")` (UCheatManager::ToggleDebugCamera ENGINE_API 5.5 — свободная детач-камера, нужен CheatManager, в PIE есть); F2 `QAGiveItems`→GiveTestItems; F3 `QAEquipArmor`→EquipTestArmor; F4 `QAUnequipArmor`→UnequipTestArmor; F5 `QAGiveMoney`→Stats->AddMoney(TestMoneyGrant=100).
- ЛОГ-КАТЕГОРИЯ `LogQA` (объявлена в ContrarySurvivor.h `CONTRARYSURVIVOR_API DECLARE_LOG_CATEGORY_EXTERN` — паттерн Epic подтверждён в Actor.h; DEFINE в ContrarySurvivor.cpp). UE_LOG(LogQA, Display, "QA: …") на: подбор денег/предмета (Pickup::Collect), EQUIP/UNEQUIP брони слот+меш (MasterHumanoidCharacter — централизованно, покрывает дефолт/тест/инвентарь), BUY/SELL цена+баланс (PlayerCharacter Shop_*), смена залоченной цели один раз через LastLoggedTarget в Controller::Tick (manual/auto), еда/вода +HP (StatsComponent Consume/Drink), авто-реген тик (TickHealthRegen), DROP выброс (Inv_DropItem).
- СПАМ: `GEngine->Exec(GetWorld(), TEXT("DisableAllScreenMessages"))` в BeginPlay контроллера (подтв. UE 5.5 UEngine::HandleDisableAllScreenMessagesCommand → GAreScreenMessagesEnabled=false). GameMode в проекте — BP (C++-класса нет), поэтому путь = PlayerController. Косметика, Build Lighting позже.
- API сверено по заголовкам UE 5.5: UCheatManager::ToggleDebugCamera/EnableDebugCamera/DisableDebugCamera (CheatManager.h 247/481/485), DisableAllScreenMessages (UnrealEngine.cpp 10034), MODULE_API DECLARE_LOG_CATEGORY_EXTERN (Engine/Classes/.../Actor.h:143). APlayerController::ConsoleCommand(FString,bool).
- ДОПУЩЕНИЯ (явно): (1) реальная причина Money=0 (BP-оверрайд vs сейв) визуально не подтверждена — редактор закрыт; фикс детерминированный (явный init в коде) её снимает в любом случае. (2) F1 debug-камера требует CheatManager у PlayerController — в PIE/editor он есть; в shipping без cheats может не сработать (для QA это ок). (3) PLAY не гонял (вне зоны) — клавиши/камеру/спам/баланс проверить unreal/qa в редакторе. (4) TestMoneyGrant=100 и StartingMoney=50 — EditAnywhere, на тюнинг.

### Фаза 4 Часть 2 — ЭКОНОМИКА: лут + торговец + магазин (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory`. Коммит 11a54bd (поверх Волны 2). Merge/push НЕ делал. Сборка PASS: `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, 0 ошибок, линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-economy-build.log`.
- ЛУТ: новый `Actors/Pickup.{h,cpp}` (`APickup`:AActor). Несёт `MoneyAmount`(float) + опц. `CarriedItem`(AMasterInventoryItem*). Корень = USphereComponent (overlap ECC_Pawn, QueryOnly), плейсхолдер `/Engine/BasicShapes/Sphere`. OnBeginOverlap с APlayerCharacter: деньги→`GetStats()->AddMoney`, предмет→`GetInventory()->AddItem` (предмет уже hidden+no-collision), `bCollected=true`, Destroy. EndPlay: если не подобран — Destroy CarriedItem (нет утечки). Статик `APickup::DropLoot(World,Loc,Money,ItemClass,Chance,PickupClass)` спавнит (по FRand<=Chance) скрытый предмет + пикап, InitLoot. Не Pawn/без Stats ⇒ не цель авто-лока.
- ЛУТ-дроп: `AEnemyCharacter`/`AWolfCharacter` +UPROPERTY (Category Loot) LootMoneyMin/Max, LootItemDropChance, LootItemClass(=AConsumableItem), PickupClass(=APickup) + метод `DropLoot()` (Money=Round(FRandRange)). Вызов в HandleDeath перед SetLifeSpan. DRAFT: бандит 10-30/0.35, волк 5-15/0.2.
- ТОРГОВЕЦ: `Actors/TraderNPC.{h,cpp}` (`ATraderNPC`:AActor, НЕ Pawn). **Решение фильтра фракций (тех-долг закрыт): торговец — не-Pawn AActor без UStatsComponent.** Авто-лок (`FindNearestLivingTarget`) и HUD-хелсбары итерируют ТОЛЬКО `TActorIterator<APawn>` с Stats ⇒ торговец не в выборке ни по типу, ни по признаку. Меш без коллизии (Visibility) ⇒ луч дальнобоя проходит, урон не идёт. Двойная защита. Триггер InteractTrigger (sphere, overlap Pawn) на Begin/End регистрирует/сбрасывает себя у контроллера (SetNearbyTrader/ClearNearbyTrader). Меш-плейсхолдер `/Engine/BasicShapes/Cylinder`.
- КАТАЛОГ: `USTRUCT FShopEntry`{DisplayName,Price,Kind(Item/Ammo),ItemClass,bApplyConsumableType,ConsumableType,AmmoAmount} в TraderNPC.h. `BuildDefaultCatalog()` (в ctor) DRAFT-цены §7.6: Water 5, Canned Food 12, Bandage 12 (Medkit), Pistol Ammo x10 = 20, Knife 40, Pistol 150, Head 60/Pants 80/Torso 120. `GetSellValue(item)` по категории (DRAFT Consum 6/Armor 40/Weapon 70/Resource 2 ≈ 50%).
- СПАВН: `Subsystems/TraderSpawnSubsystem.{h,cpp}` (UWorldSubsystem, как WolfSpawnSubsystem). OnWorldBeginPlay(IsGameWorld)→таймер 1.5с→1 торговец перед игроком (+X 600) спроецирован на навмеш (K2_ProjectPointToNavigation). Без BP/расстановки. ДОПУЩЕНИЕ: точная позиция деревни неизвестна (карта в редакторе) — опора=игрок+смещение; точные координаты задаст unreal-operator.
- ВЗАИМОДЕЙСТВИЕ: legacy ActionMapping "Interact" (E + Gamepad_FaceButton_Right) в DefaultInput.ini, бинд в SetupInputComponent → `OnInteract()`. Если рядом NearbyTrader (и НЕ открыт инвентарь) — тоггл bShopOpen + HUD->SetShopOpen(trader) + InputMode GameAndUI/GameOnly. `CloseShop()` (кнопка Close/уход от прилавка). Контроллер +UPROPERTY NearbyTrader, bShopOpen. Fire при bShopOpen→HandleShopClick (не стрельба); Move подавлено при bInventoryOpen||bShopOpen. (Enhanced Interact() остаётся пустым TODO — не конфликтует.)
- МАГАЗИН-UI: HUD +bShopOpen/ShopTrader/ShopHitRegions, enum EShopAction{Buy/Sell/Close}, FShopHitRegion. `DrawShop`: затемнение+панель (как инвентарь), слева каталог (имя+цена+[Buy], Buy-зона только если хватает денег), справа НЕэкип рюкзак (+sell-value, [Sell]), сверху Money + [Close]. `HandleShopClick`→Player->Shop_BuyEntry(catalog[idx]) / Shop_SellItem(item, trader->GetSellValue) / CSPC->CloseShop. Переиспользует DrawInvBox.
- ИГРОК: `Shop_BuyEntry(const FShopEntry&)` — проверка Money>=Price (clamp, нельзя без денег), Ammo→Cast<ARangedWeapon>(RangedWeaponInstance/CurrentWeapon)->AddReserveAmmo (если нет ствола — не списывает), Item→спавн hidden+тип расходника+AddItem; затем SpendMoney. `Shop_SellItem(item,price)` — экип. броню сперва UnequipArmor+SetItemEquipped(false), RemoveItem, AddMoney, Destroy. (Forward-decl `struct FShopEntry;` в PlayerCharacter.h — методы НЕ UFUNCTION, ок.)
- `ARangedWeapon::AddReserveAmmo(int32)` (public) — CurrentAmmoReserve/MaxAmmoReserve protected базы доступны наследнику. `AConsumableItem` +EConsumableType::Medkit + HealRestoreAmount(25) → ApplyConsumeEffect зовёт Stats->Heal.
- УРОК UHT/C++ (зафиксировано): тернарник `TSubclassOf<T> x = cond ? subclassVar : T::StaticClass()` → C2445 (неоднозначное преобразование TSubclassOf↔UClass*). Решение: присвоить TSubclassOf, потом `if(!x) x=T::StaticClass();`. Имя generated-include = `<HeaderBase>.generated.h` (для Pickup.h → `Pickup.generated.h`, НЕ `Pickup.h.generated.h`).
- ТЕСТ: торговец спавнится сам (subsystem) перед игроком; подойти→E→магазин. Стартовые деньги 50 (StatsComponent). Лут: убить бандита/волка→пикап на земле→пройти по нему. `GiveTestItems` (консоль) даёт что продать. Покупка/продажа меняют Money (виден в HUD-стеке и в шапке магазина).
- ДОПУЩЕНИЯ (явно): (1) PLAY не гонял (вне зоны) — overlap-подбор/спавн торговца/клики магазина/режим ввода проверить unreal/qa. (2) Все цены/суммы лута — DRAFT §7.6, EditAnywhere, на тюнинг game-lead/Рината. (3) Купленное оружие ложится в рюкзак как предмет (экип оружия из инвентаря не реализован — у weapon-категории нет use-кнопки, только [X]); broня/расходники работают. (4) Патроны идут в резерв ТЕКУЩЕГО дальнобоя; нет отдельного ammo-предмета. (5) Магазин/инвентарь взаимоисключающие (OnInteract игнорит при открытом инвентаре). (6) Меши торговца/пикапа — плейсхолдеры (Cylinder/Sphere), реальные — позже. (7) Лут-предмет по умолчанию AConsumableItem (тип Food); «изношенное оружие» из §7.8 как лут-класс можно выставить через LootItemClass позже.

### Фаза 4 — еда/вода лечат HP + авто-реген HP при сытости (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory`. Коммит b4140d4 (поверх магазина). Merge/push НЕ делал. Изменён только `StatsComponent.{h,cpp}`.
- ЕДА/ВОДА +HP (DRAFT, UPROPERTY EditAnywhere): `FoodHealthRestoreAmount=5`, `WaterHealthRestoreAmount=3`. ConsumeFood/DrinkWater после Modify* зовут `Heal(amount)` (clamp до MaxHealth, мёртвых не лечит, деградацию не трогает). AConsumableItem не менял — он идёт через ConsumeFood/DrinkWater.
- АВТО-РЕГЕН HP (DRAFT): новый `FTimerHandle HealthRegenTimer` + колбэк `TickHealthRegen()`. UPROPERTY: `bEnableHealthRegen=true`, `RegenHungerThreshold=80`, `RegenThirstThreshold=80`, `HealthRegenAmount=2`, `HealthRegenInterval=40`. Условие регена: `!bIsDead && Health<MaxHealth && Hunger>=80 && Thirst>=80` → Heal(2). Таймер стартует в StartSurvivalTimers (gated `bEnableHealthRegen`), чистится в StopSurvivalTimers → у врага survival выкл = реген не запускается. Логически независим от деградации (та тикает отдельными таймерами всегда).
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development -Project=...ContrarySurvior...`, exit 0, 0 ошибок, StatsComponent.cpp (adaptive non-unity) + линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-regen-build.log`.
- ВАЖНО (путь проекта): корень — `E:\ContrarySurvior\ContrarySurvivor` (SurVIor, не Surior!). Первый билд провалил с typo `-Project=...ContrarySurior...` → "Unable to find project" но Build.bat вернул exit 0 (ложный успех bat). Всегда сверять путь проекта в логе.
- ДОПУЩЕНИЯ (явно): все числа DRAFT (запрос Рината), на тюнинг. Реген завязан на тот же player-init, что и деградация (через Start/StopSurvivalTimers) — у игрока bEnableSurvivalDegradation=true. PLAY не гонял (вне зоны) — проверка эффекта реген/лечения на unreal/qa.

### Фаза 4 Волна 2 — экран ИНВЕНТАРЯ (UI) (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory`. Коммит a412566 (поверх 97dee72 Волны 1). Merge/push НЕ делал.
- ВЫБОР UI-ПУТИ: **immediate-mode на AContrarySurvivorHUD::DrawHUD + hit-test мыши** (НЕ C++ UUserWidget, НЕ .uasset-виджет). Причина: урок проекта — UMG WidgetTree/EventGraph через MCP в 5.5 ненадёжен/требует редактора; весь наш HUD уже immediate-mode (DrawRect/DrawText/Project). Полностью editor-независимо.
- ОТКРЫТИЕ: клавиша Tab/I через LEGACY ActionMapping "ToggleInventory" в `Config/DefaultInput.ini` (+ Gamepad_FaceButton_Top), бинд в `PlayerController::SetupInputComponent` (InputComponent->BindAction, как SwitchWeapon). Без нового IA/.uasset. `OnToggleInventory`: тоггл флага + HUD->SetInventoryOpen + SetInputMode (открыто=FInputModeGameAndUI cursor, закрыто=FInputModeGameOnly).
- МОДАЛЬНОСТЬ: контроллер несёт `bInventoryOpen`. `Fire()` при открытом инвентаре → `HUD->HandleInventoryClick(GetMousePosition)` (НЕ стрельба/таргетинг). `Move()` при открытом — return (движение подавлено).
- HUD `DrawInventory(APlayerCharacter*)`: затемнение + центр-панель. Слева paper-doll: слоты Head/Torso/Legs (надето/`(empty)`) + слот оружия (CurrentWeapon, display-only). Справа рюкзак: НЕэкипированные предметы (GetInventoryItems фильтр !IsItemEquipped), строка `Имя [use|equip]` + кнопка `[X]`. Сверху Money/Hunger/Thirst. Кликзоны — `TArray<FInvHitRegion>` (plain struct: Min/Max/Action/Item/SlotIndex), пересобираются каждый кадр; `HandleInventoryClick` ищет point-in-rect, диспатчит. Подсветка зоны под курсором (GetMousePosition в DrawInventory).
- КЛИКАБЕЛЬНО: слот брони (занятый)→`Inv_UnequipSlot`; предмет-броня→`Inv_UseBackpackItem`(EquipArmor+SetItemEquipped true); расходник→ApplyConsumeEffect+RemoveItem+Destroy; `[X]`→`Inv_DropItem`(если экип-броня — сперва UnequipArmor; RemoveItem+Destroy).
- НОВЫЙ КЛАСС `Public/AConsumableItem.h`+`Private/AConsumableItem.cpp` (AMasterInventoryItem, КОНКРЕТНЫЙ): enum EConsumableType{Food,Water}, ctor ItemCategory=Consumable, `ApplyConsumeEffect(UStatsComponent*)`→Food:ConsumeFood(+30)/Water:DrinkWater(+40) из Фазы 2.
- ТЕСТ: `GiveTestItems` (UFUNCTION Exec на APlayerCharacter, консоль `~`) — спавнит 2 расходника (Canned Food/Water Bottle) + запасную броню AHeadArmor/ATorsoArmor (ItemName "Spare … Head_02/Torso_02"), все hidden+no-collision, AddItem в рюкзак. Inv_UnequipSlot возвращает снятую дефолт-броню в рюкзак (AddItem без дубля через GetInventoryItems().Contains).
- ДОБАВЛЕНО в существующие: `MasterHumanoidCharacter::GetInventory()` (BlueprintPure геттер, Inventory был protected). PlayerCharacter.cpp +include AConsumableItem.h.
- API сверено по заголовкам UE 5.5 (PlayerController.h): GetHUD<T>() (1200), GetMousePosition(float&,float&) (1600), SetInputMode (1626), struct FInputModeGameAndUI (178)/FInputModeGameOnly (211); FInputModeGameAndUI::SetLockMouseToViewportBehavior/SetHideCursorDuringCapture. AHUD DrawRect/DrawText/Project — уже использовались.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, скомпилированы AConsumableItem/MasterHumanoidCharacter/PlayerCharacter/ContrarySurvivorPlayerController/ContrarySurvivorHUD (adaptive non-unity) + линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-invui-build.log`.
- ДОПУЩЕНИЯ (явно): (1) PLAY не гонял (вне зоны) — визуал панели/клики/режим ввода нужно проверить в редакторе (unreal/qa); проверена только сборка. (2) Нет стэкинга предметов — каждый предмет = отдельный actor, "кол-во" в UI не выводится (стэков в проекте нет). (3) Рюкзак без прокрутки (MVP) — лишние строки за панелью отсекаются. (4) DPI: GetMousePosition (px) и Canvas (px) считаю совпадающими; при DPI-скейле UI возможен сдвиг hit-test — проверить в Play. (5) Слот оружия — display-only (снятие оружия из UI не делал). (6) Размеры/цвета панели — draft, EditDefaultsOnly UPROPERTY Category HUD|Inventory. (7) Иконки предметов (ItemIcon) не рисую — только текст (MVP).
- ДИЗАЙН-ДЫР НЕ ОБНАРУЖЕНО (GDD §7.4 покрыт: paper-doll+слоты брони+оружие, рюкзак с использованием/выбросом, деньги/голод/жажда). Drag заменён кликом (тач-friendly) — согласовано постановкой.

### Фаза 3 Play-фиксы UX — маркер цели + нож single-target (2026-06-13, реальный лог)
- Ветка `feature/phase3-combat`. +2 коммита: 777e6e7 (ФИКС1 HUD маркер), 36b2a3e (ФИКС2 нож одна цель). working tree clean.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, EXIT_CODE=0, 0 ошибок, линк `UnrealEditor-ContrarySurvivor.dll`. Скомпилированы AMeleeWeapon.cpp + ContrarySurvivorHUD.cpp (adaptive non-unity). Редактор ЗАКРЫТ. Лог: `E:\game-dev-team\logs\phase3-cpp-ux-build.log`.
- ФИКС1 (UX): над текущей залоченной целью — заметный маркер-ретикл. Новый `AContrarySurvivorHUD::DrawTargetMarker(AActor*)` через `DrawLine` (подтв. HUD.h:246): 4 угловые скобки рамки (HalfSize 46px) + указывающий вниз треугольник, цвет жёлтый (TargetMarkerColor). Якорь = ActorLocation+Z(TargetMarkerWorldZOffset 50), Project(false), Z<=0 отсечка. Вызывается в DrawHUD только когда `bIsLocked` (Enemy==LockedTarget). Все параметры — EditDefaultsOnly UPROPERTY (Category HUD|TargetMarker). Доп: хелсбар активной цели подсвечен ярким `TargetFillColor` — сигнатура `DrawTargetHealthBar` получила `bool bIsCurrentTarget`. Обычные бары не тронуты.
- ФИКС2 (нож AoE→одна цель): `AMeleeWeapon::Fire` раньше бил ВСЕХ в overlap-радиусе (TSet Damaged, цикл по всем). Теперь выбирает РОВНО ОДНУ `Victim`: (1) приоритет — `GetCurrentTarget()` из `Cast<AContrarySurvivorPlayerController>(GetInstigatorController())`, если IsValid и SurfaceDist<=MeleeRange; (2) фолбэк — ближайшая ОДНА пешка через OverlapMultiByObjectType(ECC_Pawn), min surface-dist в пределах MeleeRange. Урон ровно одной цели. Урон(35)/кулдаун(1.0)/MeleeRange(90) сохранены. Лямбда SurfaceDistTo (центр-дист минус радиусы капсул). Include `ContrarySurvivor/Controllers/ContrarySurvivorPlayerController.h`.
- ДОПУЩЕНИЯ: (1) маркер — фикс. пиксельный размер (top-down камера ~постоянная дистанция, ок для placeholder); под varying-zoom потребуется масштаб по глубине. (2) Размеры/цвета маркера draft, EditDefaultsOnly. (3) PLAY не гонял (вне зоны) — визуал маркера и «нож бьёт одного» нужно проверить в редакторе (unreal/qa). Сборка — единственное проверенное реально.

### Фаза 3 Play-фиксы — таргетинг (тип-агностичный лок) + HP-бар волка (2026-06-13, реальный лог)
- Ветка `feature/phase3-combat`. +1 коммит f165dc4. Файлы: `Controllers/ContrarySurvivorPlayerController.{h,cpp}`, `HUD/ContrarySurvivorHUD.{h,cpp}`.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, 0 ошибок, линк `UnrealEditor-ContrarySurvivor.dll`. Скомпилированы оба .cpp (Controller, HUD) adaptive non-unity. Редактор ЗАКРЫТ. Лог: `E:\game-dev-team\logs\phase3-cpp-target-build.log`.
- БАГ1 КОРЕНЬ: `IsValidTarget` кастовал ТОЛЬКО `Cast<AEnemyCharacter>` → волк (AWolfCharacter, не наследник AEnemyCharacter) НИКОГДА не валиден. + оружие хранит `LockedTarget`, а `Fire()` оружия = `FiringTarget = Target ? Target : LockedTarget` → при FireCurrentWeapon(nullptr) падал на устаревшую мёртвую цель (стрельба в труп бандита).
- ФИКС логики лока (развитие ADR-017): `IsValidTarget` тип-агностичен через `GetTargetStats(AActor*)` = `Actor->FindComponentByClass<UStatsComponent>()` (исключая GetPawn() — у игрока тоже есть Stats), жив = !IsDead. `FindNearestLivingTarget()` итерирует `TActorIterator<APawn>`, берёт ближайшего живого с UStatsComponent в `AutoTargetRadius` (DRAFT 3000, EditAnywhere). `UpdateAutoTarget()` в Tick: если CurrentTarget невалиден → ближайшая живая; живой ручной lock сохраняется (клик = ручной выбор поверх авто). Fire() теперь ВСЕГДА `Weapon->SetTarget(CurrentTarget)` (включая nullptr — сбрасывает LockedTarget оружия, устраняя откат). PlayerController PrimaryActorTick.bCanEverTick уже был true; override Tick.
- БАГ2 HUD: DrawHUD итерировал `TActorIterator<AEnemyCharacter>` → у волка нет бара. Переделано на `TActorIterator<APawn>` + `FindComponentByClass<UStatsComponent>()`, исключая пешку игрока. `DrawEnemyHealthBar(AEnemyCharacter*)` → `DrawTargetHealthBar(AActor*, UStatsComponent*)`. Условие показа (lock ИЛИ радиус HealthBarShowRadius) не тронуто → бандит не сломан, волк получает бар.
- ДОПУЩЕНИЯ: (1) AutoTargetRadius 3000 — draft. (2) «Врага» определяю по наличию UStatsComponent (игрок исключён по ==GetPawn()); если в будущем появятся НЕвраждебные NPC со StatsComponent — потребуется фильтр по фракции/тегу (сейчас такого нет в проекте). (3) PLAY не гонял (вне зоны) — проверка авто-перехода после смерти/таргетинга волка нужна в редакторе (unreal/qa). Сборка — единственное, что проверено реально.

## Текущее состояние (старое ниже)

### Фаза 3 ДОБИВКА — процентная броня / реальные меши / анимации волка (2026-06-13, реальный лог)
- Ветка `feature/phase3-combat`. +2 коммита поверх f1c01f2 (импорт ассетов): ef00b99 (П1 процентная броня), 8f93bd5 (П2/П3 меши+анимации волка). working tree clean.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, EXIT_CODE=0, 0 ошибок, линк `UnrealEditor-ContrarySurvivor.dll`. Лог: `E:\game-dev-team\logs\phase3-cpp-fix-build.log`. Редактор ЗАКРЫТ (каноничный). Скомпилированы все 11 затронутых .cpp (adaptive non-unity).
- П1 БРОНЯ → ПРОЦЕНТ (решение Рината, вместо flat). Формула в новом методе `AMasterHumanoidCharacter::ComputeArmoredDamage(Incoming) = Incoming*(1-clamp(GetTotalArmorProtection(),0,ArmorReductionCap))`. AArmor::ArmorProtection теперь ДОЛЯ [0..1] (был flat); EditDefaultsOnly→EditAnywhere + ClampMin/Max. DRAFT-доли: Head 0.10 / Torso 0.25 / Pants 0.15 (в конструкторах слотов). `ArmorReductionCap` — новый UPROPERTY(EditAnywhere, Clamp 0..1) на базе, DRAFT 0.75. PlayerCharacter::TakeDamage и EnemyCharacter::TakeDamage переведены с `max(1, Inc-Sum)` на `ComputeArmoredDamage` (убрана min-1 неуязвимость). GetTotalArmorProtection теперь сумма ДОЛЕЙ (кап применяется в ComputeArmoredDamage, не тут). Сумма draft = 0.50 < cap → у игрока сейчас −50% урона.
- П2 МЕШИ. Нож: `/Game/Weapons/SM_Knife.SM_Knife` (UStaticMesh, подтверждён классом) на `ItemMesh` (корневой StaticMeshComponent базы AMasterInventoryItem) в AMeleeWeapon ctor, FObjectFinder. Волк: `/Game/Characters/Wolf/SK_Wolf.SK_Wolf` (USkeletalMesh) на наследуемый `GetMesh()` через `SetSkeletalMeshAsset` (чистый сеттер 5.5, без deprecation). Плейсхолдер-цилиндр (UStaticMeshComponent PlaceholderMesh) УДАЛЁН из WolfCharacter. Меш-трансформ волка DRAFT: SetRelativeLocationAndRotation((0,0,-90),(0,-90,0)) как у бандита — точную подгонку под ориентацию SK_Wolf оставил unreal-operator.
- П3 АНИМАЦИИ ВОЛКА — выбранный путь: **Single Node, БЕЗ AnimBP и БЕЗ UAnimInstance-класса**. Причина: чистый C++ UAnimInstance без AnimGraph (который даёт только .uasset AnimBP) не выдаёт позу → T-poza. `USkeletalMeshComponent::PlayAnimation(UAnimationAsset*, bLooping)` (подтверждён в SkeletalMeshComponent.h:1126) переводит компонент в AnimationSingleNode и проигрывает клип без AnimBP — надёжно. Реализация: WolfCharacter Tick (включён bCanEverTick) выбирает Idle/Run по `GetVelocity().Size2D()` vs `RunSpeedThreshold` (EditAnywhere, draft 10); кэш CurrentLocomotionAnim чтобы не рестартить каждый кадр. Bite: `PlayBiteAnimation()` (public) ставит BiteUntilTime=now+`BiteAnim->GetPlayLength()` (подтв. AnimSequenceBase.h:86), на это время Tick не трогает локомоцию. Триггер укуса: `AEnemyAIController::PerformAttack` стал `virtual bool` (true=удар нанесён, не на кулдауне); `AWolfAIController::PerformAttack` override → при true дёргает Wolf->PlayBiteAnimation(). Анимы: Anim_Wolf_Idle/Run/Bite (UAnimSequence, подтв. классом) грузятся FObjectFinder в ctor, поля EditAnywhere. HandleDeath волка: bPauseAnims=true (заморозка позы) вместо рэгдолла (физ-ассет не гарантирован).
- API подтверждено по заголовкам UE 5.5: SetSkeletalMeshAsset (SkeletalMeshComponent.h:330), PlayAnimation/SetAnimationMode (1126/1114), GetPlayLength (AnimSequenceBase.h:86), bPauseAnims публичный UPROPERTY (735). Классы ассетов проверены парсингом .uasset (SK_Wolf=SkeletalMesh, SM_Knife=StaticMesh, Anim_*=AnimSequence).
- ДОПУЩЕНИЯ (явно): (1) меш-трансформ волка — draft, не верифицирован визуально (редактор закрыт, PLAY не гонял); ориентация SK_Wolf под капсулу — на проверку unreal-operator/qa. (2) Single Node анимации НЕ блендятся (резкое переключение Idle↔Run) — для placeholder-грейда ок, плавность = позже AnimBP. (3) RunSpeedThreshold 10 — draft. (4) Все боевые/броне-числа draft, EditAnywhere. (5) PLAY не запускал (вне зоны) — проверка волк-не-в-T-позе/укус нужна в редакторе.

### Фаза 3 — расширенный бой: нож / броня→урон / волк (2026-06-13, реальный лог сборки)
- Ветка гейм-репо: `feature/phase3-combat` (от master 5a5d624). master НЕ тронут. 3 коммита: 495a029 (П3 волк), 026cb19 (П1 нож), d01d96a (П2 броня). working tree clean.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, 0 ошибок, линк `UnrealEditor-ContrarySurvivor.dll`. Лог: `E:\game-dev-team\logs\phase3-cpp-build.log`. Редактор был ЗАКРЫТ (каноничный билд). Скомпилированы все новые: AMeleeWeapon, AHeadArmor/ATorsoArmor/APantsArmor/AArmor, WolfCharacter, WolfAIController, WolfSpawnSubsystem + изменённые.
- П1 НОЖ: `Public/AMeleeWeapon.h`+`Private/AMeleeWeapon.cpp` (AMasterWeapon-наследник, КОНКРЕТНЫЙ). Fire() override (БЕЗ UFUNCTION-макроса) = OverlapMultiByObjectType(ECC_Pawn) сфера вокруг носителя, урон по surface-to-surface (CenterDist - радиусы капсул <= MeleeRange). Радиальная атака (не конус: top-down, персонаж не повёрнут к врагу). DRAFT: урон 35, кулдаун 1.0с, MeleeRange 90. Ammo базы обнулил (CanFire базы не блокирует). ВХОД: полиморфизм (CurrentWeapon->Fire через существующий Fire-инпут, новый IA не нужен) + переключение через LEGACY ActionMapping "SwitchWeapon" (Q/геймпад) в `Config/DefaultInput.ini` + `InputComponent->BindAction` в контроллере (UEnhancedInputComponent : UInputComponent → legacy работает параллельно Enhanced Input). Игрок: DefaultMeleeWeaponClass (дефолт AMeleeWeapon, без BP), нож спавнится в BeginPlay скрытым, SwitchWeapon() тогглит.
- П2 БРОНЯ: AArmor +GetArmorProtection(); draft защита Head 5/Torso 12/Pants 8 в конструкторах. База AMasterHumanoidCharacter: слоты EquippedHead/Torso/PantsArmor, EquipArmor(AArmor*) (раскладка по IsA), GetTotalArmorProtection(). TakeDamage игрока И врага: DRAFT Final=max(1, Incoming - SumArmor) → Stats->ApplyDamage(Reduced). Враг брони не имеет (сумма 0, без изменений). Старвейшн-урон идёт мимо TakeDamage (не режется бронёй). Игроку дефолт-броня (Head/Torso/Pants) спавнится+экипируется в BeginPlay скрытой (только параметры) для наблюдаемости. ВНИМАНИЕ-БАЛАНС (на ревью): сумма 25 → удар бандита 10/укус волка 12 = min 1 урон, игрок почти неуязвим — draft.
- П3 ВОЛК: `Characters/WolfCharacter.{h,cpp}` : ACharacter (НЕ AMasterHumanoidCharacter — модульная гуманоид-база мешает квадрупеду; tech-design «звери — единый меш»). Переиспользование enemy через КОМПОЗИЦИЮ: UStatsComponent + `Controllers/WolfAIController.{h,cpp}`:AEnemyAIController (только меняет draft-числа в конструкторе: укус 12, AttackRange 70, кулдаун 1.0, Detection 1800). DRAFT волк: HP 40, скорость 1.3×600=780. Капсула блокирует Visibility (простреливаемость). МЕШ = ПЛЕЙСХОЛДЕР цилиндр движка (/Engine/BasicShapes/Cylinder). AIControllerClass+AutoPossessAI заданы в C++ (без редактора). AEnemyAIController::OnPossess теперь берёт UStatsComponent через FindComponentByClass (а не Cast<AEnemyCharacter>) — подходит бандиту И волку. СПАВН КОДОМ: `Subsystems/WolfSpawnSubsystem.{h,cpp}`:UWorldSubsystem (авто для игрового мира, без BP-реparent/расстановки). OnWorldBeginPlay (IsGameWorld) → таймер 1.5с → 1-2 волка по дуге у игрока, точки на навмеш (UNavigationSystemV1::K2_ProjectPointToNavigation). Build.cs += "NavigationSystem".
- API подтверждён по заголовкам UE 5.5: UWorldSubsystem::OnWorldBeginPlay (Engine/Public/Subsystems/WorldSubsystem.h:36), K2_ProjectPointToNavigation static (NavigationSystem.h), World::OverlapMultiByObjectType (World.h:2089), Actor::FindComponentByClass (Actor.h:3699), Pawn::AIControllerClass/AutoPossessAI, легаси InputComponent::BindAction(FName,EInputEvent,...) (InputComponent.h:873), /Engine/BasicShapes/Cylinder.uasset существует.
- ДЛЯ unreal-operator (после мержа): (1) реальный меш+анимация удара ножа; при желании заменить legacy-инпут на IA/IMC .uasset. (2) реальный скелет-меш+анимации волка (modeler-3d делает меш) вместо цилиндра-плейсхолдера; назначить в BP-наследнике AWolfCharacter или прямо в C++-дефолтах позже. (3) при необходимости настроить точку/число волков (NumWolves, SpawnDistance — сейчас члены subsystem, не EditAnywhere). (4) проверить навмеш на L_MainLevel (для проекции спавна волков).
- ДОПУЩЕНИЯ (явно): гибрид legacy+Enhanced Input рабочий в UE 5.5 — на проверке в play-тесте (не запускал игру). «Арена боя» в коде неизвестна → волки у игрока+смещение на навмеш. Все боевые числа — draft, помечены, на ревью game-lead/Рината. Сборка проверена, PLAY НЕ проверял (вне моей зоны — нужен unreal-operator/qa).
- ТЕХ-ПРИЁМ (на будущее): когда правки 2 пунктов лежат в одном файле (PlayerCharacter.{h,cpp}: нож+броня), а интерактивный `git add -p` недоступен — разделял коммиты через временный Edit (убрать броне-части → коммит П1 → вернуть идентично → коммит П2). Сработало, working tree после совпал с собранным.


### Фаза 2 — выживание/сейв/смерть-респаун/HUD + багфикс боя (2026-06-13, реальный лог сборки)
- Ветка гейм-репо: `feature/phase2-survival` (от master 6aff316). master НЕ тронут. 4 коммита: cf77be0 (fix AI knife range), 7b089d3 (survival stats), 8bc1b3a (player HUD), 502c9ee (save/death/respawn+campfire).
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, EXIT_CODE=0. Лог: `E:\game-dev-team\logs\phase2-cpp-build.log`. Скомпилированы Campfire/PlayerCharacter/ContrarySaveGame/Module. Линкнут hot-reload `UnrealEditor-ContrarySurvivor-0001.dll` — суффикс -0001 потому что РЕДАКТОР БЫЛ ОТКРЫТ (PID 23768, не убивал по инструкции). Компиляция+линк успешны; для каноничного билда редактор закрыть и пересобрать.
- БАГ БОЯ (Пункт 0): причина — `AttackRange=175` сравнивали с `GetDistanceTo` (центр-к-центру), а MoveToActor (Accept 120 + reach-test добавляет радиусы капсул) тормозил врага на center-dist ~200+ > 175 → не бил. Фикс: AttackRange теперь surface-to-surface (knife=90), эффективный порог = AttackRange + GetCombinedCapsuleRadius (runtime GetScaledCapsuleRadius обоих). MoveAcceptanceRadius 120→60. sees→runs не трогал.
- UStatsComponent расширен: Hunger/Thirst (макс 100), Money (старт 50). Деградация на FTimerHandle (repeating): Thirst -1/8c, Hunger -1/12c; критич <=20 → ApplyDamage -1HP/3c(голод)+-1HP/2c(жажда), суммируются (2 таймера). ConsumeFood+30, DrinkWater+40. Делегаты OnHunger/Thirst/MoneyChanged. RestoreState, SetSurvivalDegradationEnabled (у игрока вкл в конструкторе). FTimerHandle: `#include "Engine/TimerHandle.h"`.
- APlayerCharacter теперь несёт `UStatsComponent* Stats` (источник истины HP/выживания), TakeDamage роутит в Stats БЕЗ Super. Инлайн Hunger/Thirst → Deprecated (не удалял). GetStats() public.
- HUD: DrawPlayerStats — HP-бар слева вверху+текст, индикаторы Hunger/Thirst ТОЛЬКО при <=20, деньги в правом верхнем углу. GEngine->GetMediumFont(). Строки ASCII (кириллицу в литералах избегал — кодировка MSVC).
- Новые классы: `Save/ContrarySaveGame.{h,cpp}` (USaveGame), `Actors/Campfire.{h,cpp}` (USphereComponent overlap по ECC_Pawn → Player->SaveGame(), антиспам-кулдаун; SaveAtCampfire задел).
- РЕСПАУН в APlayerCharacter::HandleDeath (привязан к Stats->OnDeath), БЕЗ нового GameMode — BP_ContrarySurviorGameMode НЕ трогаем, reparent НЕ нужен. Смерть: ApplyDeathInventoryPenalty → LoadGame(телепорт+RestoreState) либо фолбэк InitialSpawnTransform+полные статы. Слот "ContrarySave" idx 0.
- ЭСКАЛАЦИЯ game-lead: UInventoryComponent НЕ различает экип/неэкип и категории — потеря 25% = доля ВСЕХ предметов рюкзака (MVP). Экип. оружие = CurrentWeapon (вне InventoryItems) сохраняется само. DeathItemLossPercent=0.25 (draft).
- ШАГИ unreal-operator: (1) BP_Campfire на базе ACampfire — StaticMesh плейсхолдер; разместить 2 костра на L_MainLevel. (2) Проверить HUDClass=AContrarySurvivorHUD в BP GameMode (из Ф1). (3) StatsComponent игроку добавится сам (C++ subobject). (4) Reparent GameMode НЕ нужен.
- ДОПУЩЕНИЯ: фикс боя радиус-агностичен (runtime). Сериализация инвентаря — только пути классов (задел), восстановление предметов не реализовано. Respawn — телепорт того же pawn (не RestartPlayer).

### Фаза 1 C++-фундамент (2026-06-12, реальный лог сборки)
- Ветка гейм-репо: `feature/phase1-enemy-stats` (от master f7542ad). master НЕ тронут. 2 коммита: 6faccca (stats+death stub), ea5b201 (enemy+AI+target-lock+Build.cs).
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0. Лог: `E:\game-dev-team\logs\phase1-cpp-build-f7542ad.log` (имя по HEAD-хешу на момент старта; перезаписывался между прогонами, итог — успешный). Собран `UnrealEditor-ContrarySurvivor-0003.dll`. Скомпилированы все 5 cpp (Enemy/Stats/AI/Master/PlayerController), adaptive non-unity.
- Создано: `Source/ContrarySurvivor/Components/StatsComponent.{h,cpp}`, `Characters/EnemyCharacter.{h,cpp}`, `Controllers/EnemyAIController.{h,cpp}`. Изменено: `MasterHumanoidCharacter.{h,cpp}`, `ContrarySurvivorPlayerController.{h,cpp}`, `ContrarySurvivor.Build.cs` (+AIModule,+GameplayTasks).
- API подтверждён по заголовкам UE 5.5.4 (`E:\UnrealEngine\UE_5.5\...AIModule\Classes\AIController.h`): MoveToActor, LineOfSightTo, SetFocus(default prio), ClearFocus(EAIFocusPriority::Type), StopMovement, OnPossess override. `EAutoPossessAI::PlacedInWorldOrSpawned` — EngineTypes.h. Все существуют.
- ДОПУЩЕНИЯ: рэгдолл при смерти врага сработает только если у скелет-меша назначен Physics Asset (иначе тихо без эффекта, не краш) — назначение меша/физики на стороне unreal-operator. AttackDamage врага = 10/удар, AttackCooldown 1.5с, DetectionRange 1500, AttackRange 175 — все тюнингуемые UPROPERTY, числа черновые.
- Остаётся на BP/редактор (unreal-operator): BP-наследник AEnemyCharacter с мешами Head/Torso/Legs + AnimBP; назначить AIControllerClass=AEnemyAIController в BP врага (или проверить, что AutoPossessAI берёт его); виджет хелсбара на OnHealthChanged/OnDeath; разместить бандита на карте; назначить игроку оружие APistol (если ещё не в BP).

### Фаза 1 — автоэкип + C++ хелсбар (2026-06-12, реальный лог)
- Ветка `feature/phase1-enemy-stats` (от master f7542ad, не тронут). +2 коммита: 18339cf (player auto-equip), 28b67d0 (C++ HUD). working tree clean. HEAD=28b67d0.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, линк `UnrealEditor-ContrarySurvivor.dll`. Лог: `E:\game-dev-team\logs\phase1-cpp-hud-ea5b201.log` (tee перезаписал первый ПРОВАЛЬНЫЙ прогон; итог — успешный).
- ЗАДАЧА 1: APlayerCharacter +UPROPERTY `DefaultWeaponClass` (TSubclassOf<AMasterWeapon>, EditDefaultsOnly), BeginPlay→`EquipDefaultWeapon()` спавнит (SpawnActor, Owner/Instigator=this, AlwaysSpawn) и зовёт `EquipWeapon`. EquipWeapon крепит к WeaponSocketName на TorsoMesh — НЕ HeadMesh (проверено в .cpp). Значение BP_Pistol ставит unreal-operator в BP игрока.
- ЗАДАЧА 2: новый класс `Source/ContrarySurvivor/HUD/ContrarySurvivorHUD.{h,cpp}` : AHUD. DrawHUD: TActorIterator<AEnemyCharacter>, показ если жив (GetStats()->IsDead()==false) И (залочен через PC->GetCurrentTarget() ИЛИ DistSq<=1500^2 до пешки игрока). Project(worldAnchor,false): Z>0=перед камерой. DrawRect фон+заполнение по GetHealthPercent. HUDClass=AContrarySurvivorHUD ставит unreal-operator в BP GameMode.
- ВАЖНО (новое ограничение): `GetCurrentTarget()` в контроллере был под `protected:` (хотя UFUNCTION) → C2248 при чтении из HUD. UFUNCTION-спецификатор НЕ делает метод публичным; access-модификатор C++ действует. Вынес в public-секцию.
- API подтверждён по HUD.h/Canvas.h UE 5.5.4: AHUD::Canvas (TObjectPtr<UCanvas>), Project(FVector,bool)->FVector, DrawRect(FLinearColor,X,Y,W,H), DrawText(...), GetOwningPlayerController()->APlayerController*, UCanvas::SizeX/SizeY (int32). HUD/Canvas/EngineUtils — модуль Engine (уже в deps, Build.cs НЕ менял).
- ДОПУЩЕНИЕ: радиус показа 1500 ед (GDD ч.8 «~5м»=~500ед, взял с запасом под top-down обзор) — тюнингуемый UPROPERTY. Хелсбар без текста/иконки, простые DrawRect (фон чёрный полупрозрачный + красное заполнение) — Фаза 1 достаточно. Project с bClampToZeroPlane=false чтобы корректно отсекать за-камерой по Z<=0.

### Инвентаризация среды сборки UE C++ (2026-06-07, только факты из вывода команд)
- VS 2022 Professional 17.14.36811.4, путь `C:\Program Files\Microsoft Visual Studio\2022\Professional`.
- Workload `Microsoft.VisualStudio.Workload.NativeGame` (Game Dev C++) установлен. Также VC.Tools.x86.x64, Windows10SDK, Windows11SDK.26100.
- MSVC toolset 14.44.35207, cl.exe: `...\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\cl.exe`.
- MSBuild 17.14.23: `...\2022\Professional\MSBuild\Current\Bin\MSBuild.exe`.
- Движок UE 5.5.4 (5.5.4-40574608): `E:\UnrealEngine\UE_5.5` (из LauncherInstalled.dat). UBT: `E:\UnrealEngine\UE_5.5\Engine\Binaries\DotNET\UnrealBuildTool\UnrealBuildTool.exe` (есть).
- .NET: глобально только SDK/runtime 3.1.426 (старый), НО движок несёт свой `Engine\Binaries\ThirdParty\DotNet\8.0.300` — UBT использует bundled .NET 8, поэтому глобальный 3.1 не блокер.
- Проект ContrarySurvivor: `E:\ContrarySurvior\ContrarySurvivor\ContrarySurvivor.uproject` (EngineAssociation "5.5"). C++ проект: модуль `ContrarySurvivor` (Runtime), есть `ContrarySurvivor.Build.cs`, `ContrarySurvivor.Target.cs`, `ContrarySurvivorEditor.Target.cs`. Папки Characters/Controllers/Public/Private с .h/.cpp. Plugin: ModelingToolsEditorMode (Editor).
- Вывод: машина к сборке UE C++ готова. Сборку НЕ запускал (задача — только инвентаризация).
- pwsh (PowerShell 7) отсутствует; Windows-команды гнать через `powershell.exe -NoProfile`. Bash-тул = git-bash, `&`/`${env:..}` синтаксис там не работает напрямую.

### Ш6 — контрольная сборка (2026-06-07, реальный лог)
- РЕЗУЛЬТАТ: PASS. UBT exit code 0. Таргет `ContrarySurvivorEditor`, Development Win64.
- Лог: `E:\game-dev-team\logs\build_ContrarySurvivorEditor.log`. Собраны `UnrealEditor-ContrarySurvivor-0079.dll`/`.lib` в `Binaries\Win64`. Время: 27.21s (UBA local 5.94s). Скомпилирован `ContrarySurvivorPlayerController.cpp` (adaptive non-unity по git working set).
- Editor target name подтверждён чтением: класс `ContrarySurvivorEditorTarget`, `Type=TargetType.Editor`, `ExtraModuleNames=ContrarySurvivor`.
- ИЗВЕСТНЫЙ НЕ-БЛОКЕР (зафиксировано): MSVC 14.44.35222 вместо предпочитаемого UE 5.5 14.38.33130 → UBT печатает варнинг «not a preferred version». Это ТОЛЬКО варнинг, сборка чистая (exit 0, ошибок нет). Действие — доустановить MSVC 14.38 side-by-side toolset — выполнять ТОЛЬКО если в будущем всплывут ICE (internal compiler error) или странные крэши компилятора. Сейчас ничего делать не нужно.
- ВАЖНО про запуск .bat из Bash-тула: НЕ оборачивать в `cmd.exe /c '...'` с редиректами — quoting ломается, команда не выполняется (получишь ложный exit 0 от echo). Запускать Build.bat напрямую, редирект `> log 2>&1` отдавать самому bash. Запускать в background (первая сборка может превысить лимит вызова).
- Перед C++-сборкой убедиться, что `UnrealEditor.exe` не запущен (иначе DLL модуля залочена). В этот раз процесса не было.

### FULL REBUILD ContrarySurvivorEditor (2026-06-07, реальный лог)
- РЕЗУЛЬТАТ: PASS. exit code 0. Команда: `Rebuild.bat ContrarySurvivorEditor Win64 Development -Project=... -WaitMutex -FromMsBuild`. Лог: `E:\game-dev-team\logs\rebuild_ContrarySurvivorEditor.log`.
- ПОЛНОТА: Clean прошёл («Cleaning ContrarySurvivorEditor binaries...», «no existing makefile»). 32 action: 28 `Compile [x64]` (все 13 .cpp модуля + .gen.cpp + init.gen + PerModuleInline.gen + SharedPCH), 2 Link (.lib/.dll), WriteMetadata. Собран `UnrealEditor-ContrarySurvivor-0001.dll/.lib`.
- Toolchain: MSVC 14.44.35222 (соответствует 14.44 на машине). Ошибок 0. Единственный «Warning» — VS compiler not preferred version (информационный, НЕ warning-as-error). Время: 111.47s (UBA local 45.52s).
- ЗАМЕЧАНИЕ по правам: запуск Rebuild.bat в background (`run_in_background`) был DENY 3 раза; foreground-запуск с timeout 600000 прошёл. Rebuild только модуля проекта укладывается в ~111s, foreground приемлем.

### Фаза 2 — Play-фиксы Рината (2026-06-13)
- ФИКС1 респаун с полным HP: `PlayerCharacter` +UPROPERTY `RespawnHealthFraction=1.0` (Category Save, Clamp 0..1). В `HandleDeath` после восстановления статов (LoadGame или фолбэк) форсируется `Stats->SetHealth(MaxHealth*Fraction)`. Hunger/Thirst/Money — из сейва как есть. Добавлен `UStatsComponent::SetHealth(float)` — clamp 0..MaxHealth, пересчёт bIsDead, broadcast; работает на «мёртвом» компоненте (Heal — нет). Коммит de22c03.
- ФИКС2 HUD: `DrawPlayerStats` переписан в левый стек HP->Hunger->Thirst->Money. Голод/жажда/деньги видны ВСЕГДА (dev, решение game-lead; прятать-до-критич вернём в UX-полировке). Money раньше был справа-вверху через GetTextSize — вероятная причина «не видно». Хелсбар врага не тронут. Коммит 3e7532a.
- СБОРКА: ContrarySurvivorEditor Win64 Development, exit 0, 3 .cpp скомпилированы + модуль слинкован (UnrealEditor-ContrarySurvivor.dll). Редактор был закрыт. Лог: E:\game-dev-team\logs\phase2-cpp-fix-build.log. Toolchain MSVC 14.44.35222, UE 5.5.4. Ветка feature/phase2-survival.

### 2026-06-13 ВОЛК YAW-ФИКС (feature/phase3-combat)
- Файл: `Source/ContrarySurvivor/Characters/WolfCharacter.cpp` строка ~51, конструктор.
- Причина: modeler переэкспортировал волка с ИСХОДНОЙ ориентацией → forward=+Y (гуманоид forward=-Y), разница 180°. Скиннинг исправлен (анимации не деформируют).
- Правка: `SetRelativeLocationAndRotation(... FRotator(0, Yaw, 0))` Yaw -90 → +90 (= -90+180). Pitch/Roll=0. Z-офсет (-WolfCapsuleHalfHeight) и капсула НЕ тронуты. Rigid-поворот компонента — скиннинг не ломает.
- Yaw волка задаётся ТОЛЬКО здесь (grep подтвердил). EnemyCharacter (гуманоид) остаётся -90.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, WolfCharacter.cpp скомпилирован (adaptive non-unity), линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:\game-dev-team\logs\phase3-cpp-wolfyaw-build.log`.
- Коммит: 4383e72 на feature/phase3-combat. Merge/push НЕ делал.

### Фаза 4 Волна 1 — инвентарь/категории + модульная экипировка брони (2026-06-13, реальный лог)
- Ветка `feature/phase4-inventory` от master (13d4c53). Коммит 97dee72. Merge/push НЕ делал.
- ЗАДАЧА 1 (категории + тех-долг Ф2): `EItemCategory{Consumable,Resource,Armor,Weapon}` + поле `ItemCategory` в `AMasterInventoryItem` (база=Resource; AArmor->Armor, AMasterWeapon->Weapon в ctor). `UInventoryComponent`: список `EquippedItems` + `SetItemEquipped/IsItemEquipped/GetUnequippedItemsOfCategory`; `RemoveItem` чистит и EquippedItems. `APlayerCharacter::ApplyDeathInventoryPenalty` теперь теряет % только НЕэкип Consumable+Resource (броня/оружие сохраняются), GDD §7.8 — тех-долг закрыт.
- ЗАДАЧА 2 (модульная броня): `EArmorSlot{Head,Torso,Legs}` + `ArmorSlot` в `AArmor` (наследники в ctor; APantsArmor->Legs). `AMasterHumanoidCharacter::EquipArmor(AArmor*)` подменяет скелетный меш слота на `ArmorMesh_Equipped` + `RelinkSlotToLeaderPose` (Torso/Legs->SetLeaderPoseComponent(Head); Head=лидер=no-op). `UnequipArmor(EArmorSlot)` возвращает базовый меш (снимок `CacheBaseSlotMeshes` в BeginPlay, ДО автоэкипа дефолтной брони). `GetEquippedArmor(slot)`. EquipArmor перешёл с IsA() на GetArmorSlot().
- API UE 5.5 (сверено с заголовками `E:/UnrealEngine/UE_5.5/.../Components/SkeletalMeshComponent.h` + SkinnedMeshComponent.h): актуальны `SetSkeletalMeshAsset(USkeletalMesh*)`/`GetSkeletalMeshAsset()`; `SetSkeletalMesh(mesh,bReinit)`/`SetMasterPoseComponent` — DEPRECATED 5.1. `SetLeaderPoseComponent(USkinnedMeshComponent*,bForce,bFollowerTickPose)` — актуален.
- ВАЖНО (архитектура рига): Leader Pose для ИГРОКА настраивается в BP_PlayerCharacter (не в C++); для врага — в AEnemyCharacter::PostInitializeComponents. База C++ сама leader pose НЕ ставит. EquipArmor/UnequipArmor переустанавливают leader pose следящих слотов на Head после подмены меша — совместимо с обоими.
- Тест без UI: exec-команды на APlayerCharacter (Pawn) — `EquipTestArmor` (переспавн+надеть дефолт-броню, подмена меша), `UnequipTestArmor` (снять всё, вернуть базу). Открыть консоль `~`, ввести имя.
- Сейв (задел): `UContrarySaveGame` +`EquippedHead/Torso/LegsArmorClassPath`; запись в `APlayerCharacter::SaveGame`. Авто-восстановление при загрузке НЕ делал (игрок и так экипирует дефолт-броню в BeginPlay) — будущая волна.
- ЖДЁМ от modeler/unreal: реальные скелетные меши брони на ОБЩЕМ скелете персонажа (иначе LeaderPose/анимация сломается), назначить в `ArmorMesh_Equipped` BP-классов брони (BP_HeadArmor/BP_TorsoArmor/BP_PantsArmor) и выставить их как DefaultHead/Torso/PantsArmorClass игроку. Без меша EquipArmor применяет только защиту (слот не меняется, варн в лог) — механика смены меша проверяема, как только меш назначен.
- СБОРКА: PASS. `Build.bat ContrarySurvivorEditor Win64 Development`, exit 0, 12 .cpp скомпилированы + линк `UnrealEditor-ContrarySurvivor.dll`. Редактор ЗАКРЫТ. Лог: `E:/game-dev-team/logs/phase4-cpp-equip-build.log`. Toolchain MSVC 14.44.35222, UE 5.5.

### Фаза 4 баг-фиксы по Play Рината (2026-06-13, реальный лог) — feature/phase4-inventory
- Коммит 0e27046. Merge/push НЕ делал. Сборка PASS: ContrarySurvivorEditor Win64 Development, exit 0, ContrarySurvivorPlayerController.cpp скомпилирован + линк UnrealEditor-ContrarySurvivor.dll/.lib. Лог: E:/game-dev-team/logs/phase4-cpp-bugs-build.log.
- BUG1 (клик использовал несколько предметов): причина — FireAction Enhanced Input Triggered летит каждый кадр при зажатии; HandleInventory/ShopClick вызывался каждый кадр, а reflow списка подставлял следующий предмет под курсор. Фикс: EDGE-клик через bUIClickConsumed в AContrarySurvivorPlayerController::Fire (ветки bInventoryOpen/bShopOpen). Флаг снимается в OnFireReleased (bind FireAction Completed+Canceled) и при OnToggleInventory/OpenShop/CloseShop.
- BUG2 (нестабильный подбор лута): причина — overlap-триггер пикапа (радиус 90) мог не сработать при быстром проходе/начальном перекрытии. Убран авто-подбор по overlap полностью. APickup::Collect(Player) надёжно начисляет деньги+предмет и Destroy ТОЛЬКО при полном успехе (иначе пикап остаётся).
- BUG3 (выброс + механика подбора): выброс Inv_DropItem теперь спавнит мировой APickup у ног игрока (InitLoot(0,Item)), предмет скрыт/без коллизии — НЕ Destroy. Подбор по E: контроллер UpdateNearbyInteractable() в Tick ищет ближайший интерактив (пикап в InteractRange=300 / NearbyTrader), OnInteract действует по нему. HUD::DrawInteractPrompt рисует «E — подобрать»/«E — торговать» снизу-центр.
- BUG4 (торговец не в деревне): UTraderSpawnSubsystem::SpawnTrader перебирает кольцо из 8 направлений вокруг игрока (SpawnDistance=600), берёт первую точку K2_ProjectPointToNavigation; фолбэк — проекция точки игрока; добавлены диагностические логи (playerLoc, navmesh yes/fallback, FAILED-варн с подсказкой про NavMeshBoundsVolume). Игрок на навмеше в деревне → соседняя точка тоже деревня.
- Файлы: Actors/Pickup.{h,cpp}, Characters/PlayerCharacter.{h,cpp} (+DropForwardOffset/DropDownOffset), Controllers/ContrarySurvivorPlayerController.{h,cpp} (+EInteractKind, InteractRange, OpenShop, OnFireReleased, Has/GetInteractPrompt), HUD/ContrarySurvivorHUD.{h,cpp} (+DrawInteractPrompt), Subsystems/TraderSpawnSubsystem.cpp.
- ЗАМЕТКИ для будущего: UHT — BlueprintReadOnly запрещён на private UPROPERTY (использовать только EditAnywhere/UPROPERTY()). WarningsAsErrors: C4458 (локаль шадовит член класса Player/Pawn) ломает сборку — не называть локали Player/Pawn в методах контроллера. IsValid(forward-declared*) в inline-методе хедера не компилится (нужен полный тип) — тело в .cpp.
- ДОПУЩЕНИЯ: позиция «деревни» в коде неизвестна (карта в редакторе) — опираюсь на то, что игрок стартует в деревне на навмеше. Визуально проверить спавн/подбор/выброс/торговца в Play — задача qa/unreal (редактор был закрыт, я не запускал PIE).

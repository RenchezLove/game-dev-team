# context: cpp-dev

> Лёгкая рабочая память `cpp-dev` (правило H + Гигиена памяти из CLAUDE.md: стоп >150 строк, СЕЙЧАС вырезает прошлый). Закрытая история — в [archive.md](archive.md) (read-on-demand, НЕ для грунтовки). Build-логи — `logs/`, история — git.

## 🧭 СЕЙЧАС
**Фаза: Build 1.1, ветка `feature/build11-combat-ui`, последний мой коммит `3a171c1` — режим `-aim` коммандлета PatchAnimBp ДОВЕДЁН ДО КОНЦА И ПРОГНАН ЖИВЬЁМ (детали ниже, пункт про поместившуюся позу прицеливания). Ранее в этой ветке: `d6980c4` (сектор ножа 100° + новый компонент подсветки), `e7ac7df` (компонент повешен на игрока), `88b458b` (настройки кнопки БЕГ), `38703ab` (явный include), `bd73fbc` (плавный доворот ножа вместо рывка). Пункт 5 плана: дефолт `MeleeSectorHalfAngleDeg` в `Public/AMeleeWeapon.h` снижен 60 → 50 (полный сектор 100°, требование Рината), добавлены публичные `GetMeleeSectorHalfAngleDeg()`/`GetMeleeRange()`; новый `Components/MeleeSectorIndicatorComponent.h/.cpp` — наследник `UDecalComponent`, висит на капсуле игрока (`PlayerCharacter.cpp`, subobject `MeleeSectorIndicator`), зажигается ровно при «есть цель + в руках нож + есть материал», радиус считает ТОЙ ЖЕ формулой, что урон (радиус капсулы носителя + MeleeRange + радиус капсулы ЗАЛОЧЕННОЙ цели), угол берёт из оружия. Материал — мягкая ссылка EditAnywhere на `/Game/Materials/MI_MeleeSectorSoft` (делает оператор), пусто/не найден → подсветки нет и одно предупреждение в лог. Контракт скалярных параметров: `SectorHalfAngleRad` (радианы), `InnerRadiusFrac`, `Opacity`. Пункт 1 плана (синяя пульсирующая кнопка БЕГ) БЫЛ УЖЕ СДЕЛАН в Build 1 «Блок D» (коммиты `79b76ac`, `a6994a2`, `aca9b14`) — я привёл настройки к дословной формулировке Рината: обычный цвет (по умолчанию снимается с кнопки дизайнера, флагом `bUseCustomSprintIdleColor` задаётся полем), активный цвет, ПЕРИОД пульсации в секундах (было «скорость» в рад/сек), ГЛУБИНА пульсации — теперь кнопка пригасает от выбранного цвета, а не разгорается выше него (синий больше не выбеливается на пике).**
- **Проверенные факты движка UE 5.5 под декали (пригодятся при возвратах):** ориентация проекции — декаль проецируется вдоль ЛОКАЛЬНОЙ X, а UV материала = `(DecalVector.z, DecalVector.y)`, то есть ось U — локальная Z, ось V — локальная Y (`Engine/Shaders/Private/DeferredDecal.usf:161-167`); рамка нормируется на `DecalSize` через `GetTransformIncludingDecalSize` (`DecalComponent.h:206`) — значит поворот `FRotator(Pitch=-90, Yaw=0, Roll=0)` даёт «U = взгляд владельца, V = вправо». Смена `DecalSize` доезжает до рендера через `MarkRenderTransformDirty()` (публичный, `ActorComponent.h:954`), потому что `FScene::UpdateDecalTransform` перечитывает размер (`RendererScene.cpp:2910-2929`). Несмотря на `MinimalAPI` в UCLASS, конструктор и деструктор `UDecalComponent` экспортированы как `ENGINE_API` (сверено в сгенерированном `DecalComponent.generated.h:51`) — наследоваться из игрового модуля можно, линкер не упадёт. `SetDecalMaterial` домен материала НЕ проверяет (`DecalComponent.cpp:309-316`), поэтому чужой домен даст пустую подсветку, а не ошибку.
- **Разведка по просьбе лида (доворот к цели) — ФАКТ, два РАЗНЫХ механизма.** При СТРЕЛЬБЕ доворот есть и он плавный: `ARangedWeapon::Fire` зовёт `Wielder->StartAimTurnTo(FiringTarget)` (`Private/ARangedWeapon.cpp:221-224`), сам доворот крутится в `AMasterHumanoidCharacter::UpdateAimTurn` через `FMath::RInterpTo` из Tick (`MasterHumanoidCharacter.cpp:130,158-195`), на окно доворота выключается `bOrientRotationToMovement` и восстанавливается в `EndAimTurn`; настройки EditAnywhere — `bAimTurnToTarget`, `AimTurnInterpSpeed=10`, `AimTurnHoldTime=1.0`. Тот же путь у бандита (`EnemyAIController.cpp:154,196`). При УДАРЕ НОЖОМ доворот другой — мгновенный снап `SetActorRotation` прямо в `AMeleeWeapon::Fire` (`Private/AMeleeWeapon.cpp`, ветка `bTurnToLockedTarget`), только для носителя-игрока и только если залоченная цель уже в пределах `MeleeRange`.
- **Расхождение «что видишь / что бьёшь» ЗАКРЫТО коммитом `bd73fbc` (решение game-lead, основание — «в ЭТОМ ЖЕ секторе наносится урон»):** мгновенный рывок в `AMeleeWeapon::Fire` заменён на `StartAimTurnTo` — тот же плавный механизм, что у `ARangedWeapon::Fire`. Доворот только ЗАПУСКАЕТСЯ, отрабатывает в Tick персонажа, поэтому текущий взмах считается по направлению взгляда на момент удара, то есть ровно по подсвеченному сектору; корпус доезжает к следующему взмаху (интерполяция со скоростью 10 сходится быстрее, чем кулдаун ножа 1 с). Прежний рывок жив под новым `bMeleeSnapToTarget` («Мгновенный доворот при ударе»), по умолчанию ВЫКЛЮЧЕН — если нож начнёт мазать, Ринат включает галочку сам. Приоритеты hitstop-полей сдвинуты 6/7/8 → 7/8/9. Автотест НЕ трогал: `FCombatWeaponDamageTest` читает только урон 35 у CDO и `Fire()` не зовёт вовсе (проверено двумя разными поисками по `Tests/`).
- **⚠️ Важно для приёмки пункта 1:** на ПК тач-слой (а значит и кнопка БЕГ) по умолчанию НЕ показывается — `bEnableTouchControls = false` в `ContrarySurvivorPlayerController.h:216`, на Android слой включается всегда (`ContrarySurvivorPlayerController.cpp:106-110`). Чтобы Ринат увидел пульсацию на ПК, флаг надо включить в контроллере.
- **НЕ ПРОВЕРЕНО живьём (нужен PIE):** что сектор реально рисуется и смотрит вперёд (зависит от материала оператора), что декали нормально ведут себя на Android-рендере, что подобранный радиус визуально совпадает с попаданиями.
- **✅ СБОРКА ПРОЙДЕНА (реальная, не заявление): `Build.bat ContrarySurvivorEditor Win64 Development`, 22/22 действий, обе DLL перелинкованы 19:17:57, при `-WarningsAsErrors` ни одного предупреждения по коду (единственное — фоновое «Visual Studio 2022 compiler is not a preferred version»). Лог `context/cpp-dev/logs/build-0728-build11.log`. Сборка покрыла ВСЁ сегодняшнее: компонент подсветки сектора, кнопку бега, плавный доворот ножа, `AnimNotify_MeleeHit.cpp`, `AMeleeWeapon.cpp`, `PatchAnimBpCommandlet.cpp` (видно строками Compile в логе).**
- **✅ КОММАНДЛЕТ ПРОГНАН ЖИВЬЁМ, слот монтажа стоит в графе. Цепочка доказательств:** срез ДО — `AnimGraphNode_StateMachine «Locomotion».Pose → AnimGraphNode_Root «Output Pose».Result`, слота нет; срез ПОСЛЕ — `Locomotion (x=-160) → Slot «DefaultSlot» (x=140) → Output Pose (x=440)`; `-verify` = «ПРОВЕРКА ПРОЙДЕНА»; повторный прогон = «уже стоит, ассет не трогаю» и время файла НЕ изменилось (19:24:33) — идемпотентность доказана. Координаты 140 и 440 вычислены моей формулой (источник+300 и источник+600), это и доказывает, что вставку сделал мой коммандлет. Логи: `context/cpp-dev/logs/animbp-dump-before.log`, `animbp-dump-after.log`, `animbp-verify.log`, `animbp-patch-again.log`. ⚠️ Правленый `ABP_HumanoidCharacter.uasset` я закоммитил САМ в `3a171c1` (обычно ассеты за лидом) — потому что лид прямо потребовал оставить дерево чистым, а этот ассет целиком сделан моим коммандлетом. При следующих правках ассетов уточнять у лида, кто коммитит.
- **ДВА УРОКА ПРОГОНА (помнить):** (1) логи коммандлета снимать через `-abslog=<путь>`, а не через `tee` — в консоли Git Bash кириллица превращается в «????», а в файле `-abslog` она читается нормально; (2) состояние диска МЕНЯЕТСЯ под руками, потому что параллельно работают другие агенты: мой первый запуск коммандлета упал, потому что DLL плагина GameAnalytics в тот момент физически не было, а через минуту она появилась (её собрал кто-то другой). Перед выводом «сломано» перепроверять свежей командой, а перед записью ассета смотреть `tasklist` на чужой `UnrealEditor-Cmd.exe` (в тот момент qa гонял автотесты — дождался).
- **Урон ножа по кадру взмаха — коммиты `238b720` + `3b8fc7e`:** `Source/ContrarySurvivor/Animation/AnimNotify_MeleeHit.h/.cpp` (метка на дорожке монтажа зовёт `AMeleeWeapon::ApplyMeleeDamage`); `AMeleeWeapon` разделён на `Fire()` (кулдаун, звук, доворот, запуск монтажа) и `ApplyMeleeDamage()` (сектор, урон, hitstop), расчёт дистанции вынесен в `GetSurfaceDistanceTo`. Класс уведомления, а не именованное событие, — потому что имя безымянному уведомлению из скрипта не задать, а класс оператор расставит по кадрам сам.
- **Боевые анимации — коммит `3b8fc7e` (пункты 1/2/4 задачи 07-28-вечер).** Ссылки на монтажи живут НА ПЕРСОНАЖЕ `AMasterHumanoidCharacter` (поля «Анимация выстрела» = `/Game/Characters/Shared/Humanoid/AM_FirePistol`, «Анимация удара» = `AM_MeleeSlash`, мягкие, EditAnywhere) — значит работают и у игрока, и у бандита, оба наследуют этот класс. Прежнее поле на оружии с путём `AM_Knife_Swing` УДАЛЕНО, тот путь больше не существует нигде. **КЛЮЧЕВОЙ ФАКТ АРХИТЕКТУРЫ (проверен по коду, не предположение): бандит НЕ стреляет и НЕ бьёт через классы оружия** — `AEnemyAIController::PerformRangedAttack/PerformAttack` наносят урон напрямую через `TakeDamage`, а оружие используют только для вспышки и звука (`PlayFireVisuals`). Поэтому крючки анимаций стоят В ЧЕТЫРЁХ местах: `ARangedWeapon::Fire` и `AMeleeWeapon::Fire` (игрок) плюс оба метода ИИ (бандит). **Из этого же вытекает ЗАЩИТА ОТ ДВОЙНОГО УРОНА:** метка `UAnimNotify_MeleeHit` сработала бы и на анимации бандита, поэтому урон идёт, только если замах начало само оружие — `Fire()` поднимает флаг, метка снимает его `ConsumePendingSwing()`. У бандита флаг не поднят.
- **✅ ПОЗА ПРИЦЕЛИВАНИЯ СТОИТ В ГРАФЕ, ПРОГНАНО ЖИВЬЁМ — коммит `3a171c1` (29.07).** Сборка реальная: строки `[1/4] Compile [x64] PatchAnimBpCommandlet.cpp` и `[3/4] Link [x64] UnrealEditor-ContrarySurvivorEditor.dll`, EXIT=0 (`logs/b11g-build.log`, потом `b11g-build2.log`, `b11g-build3.log`). Срез графа ПОСЛЕ (`logs/b11g-animbp-dump-after.log`) показывает ровно задуманную цепочку: `Locomotion → кэш «LocomotionPose»`; `кэш → BasePose слоя 1`, `Anim_AimPistol_Humanoid → BlendPoses_0 слоя 1`, `Get AimBlendWeight → BlendWeights_0 слоя 1`; `слой 1 → слот DefaultSlot`; `кэш → BasePose слоя 2`, `слот → BlendPoses_0 слоя 2`; `слой 2 → Output Pose`. У ОБОИХ узлов смешивания ровно ОДИН слой (пины кончаются на `_0`, никаких `_1`) — та самая ошибка вылечена. `-verify`: «пересборка блюпринта — ошибок 0, предупреждений 0», «ПРОВЕРКА ПРОЙДЕНА». Повторный `-aim` = «уже собраны, ассет не трогаю», время файла не изменилось — идемпотентность доказана. **НЕ ПРОВЕРЕНО живьём: как это выглядит в игре (нужен PIE, смотрит Ринат).**
- **⛔ ГЛАВНЫЙ УРОК ЭТОГО КРУГА (мой промах, лид нашёл раньше меня): я переписал схему графа, но место, которое падало, оставил побуквенно тем же.** Правило на будущее: если прогон упал в конкретной строке — чинить НАДО ИМЕННО ЕЁ, а переписывание вокруг не считается исправлением. Проверять это сравнением с прошлой версией (`git show <старый коммит>`), а не памятью о том, что «там всё переделано».
- **ЧЕТЫРЕ ЛОВУШКИ РЕДАКТОРНОГО API, все сверены по исходникам UE 5.5 и все реальные:** (1) конструктор узла `UAnimGraphNode_LayeredBoneBlend` САМ зовёт `Node.AddPose()` (`AnimGraphNode_LayeredBoneBlend.cpp:19-23`), а `AddPose()` при режиме BranchFilter кладёт записи сразу в `BlendWeights`, `BlendPoses` и `LayerSetup` (`AnimNode_LayeredBoneBlend.h:131-144`) — значит слой УЖЕ ЕСТЬ, и `AddPinToBlendByFilter()` делает ВТОРОЙ; (2) `SetExternalMember` у узла чтения переменной даёт `IsSelfContext()==false`, и узел создаёт ВИДИМЫЙ пустой вход «Target» (`K2Node_Variable.cpp:151, 200-206`) — для своей переменной нужен `SetSelfMember`, тогда вход скрытый; (3) `UAnimGraphNode_SequenceEvaluator::SetAnimationAsset` кастует к `UAnimSequence`, а НЕ к `UAnimSequenceBase` (`AnimGraphNode_SequenceEvaluator.cpp:124-130`) — на другом типе ассета присвоение проходит МОЛЧА мимо, поэтому результат надо перечитывать через `Node.GetSequence()`; (4) значение НЕПОДКЛЮЧЁННОГО показанного пина едет в собранный класс из ПИНА, а не из поля структуры (пин заполняется из структуры один раз, при создании — `AnimBlueprintNodeOptionalPinManager.cpp:75-81`), поэтому писать надо в оба места.
- **Поза прицеливания — исходный коммит `b42b977`.** Новый `Source/ContrarySurvivor/Animation/HumanoidAnimInstance.h/.cpp` — родитель анимационного блюпринта, каждый кадр считает одно число `AimBlendWeight` (плавно, раздельные скорости входа и выхода) и отдаёт графу. Признак «целится» — новый `AMasterHumanoidCharacter::IsAimingAtTarget()`: дальнобойное оружие в руках И цель; у игрока цель проставлена оружию контроллером (`HasTarget`), у бандита оружию цель НЕ ставят вовсе, там признак — открытое окно доворота корпуса `bAimTurnActive`. Режим коммандлета `-aim` (переписан в `e1de490`) собирает ДВА слоя с фильтром по `C_Spine01` плюс кэш позы: `ходьба → кэш`; `кэш → база слоя позы`, `поза прицеливания → слой (вес из переменной)`; `слой позы → слот монтажа`; `кэш → база слоя монтажа`, `выход слота → слой (вес 1)`; `слой монтажа → Output Pose`. **ПРИЧИНА второго слоя — факт оператора (лог `b11-anim-verify-2026-07-28`): в импортированных анимациях дорожки есть на ВСЕ 22 кости, включая таз, корень и ноги, а не только на 12 костях верха.** Значит монтаж, проигранный полным телом через слот, на бегу даёт прямые ноги — послойное наложение обязательно и для монтажей. Родитель блюпринта меняется на `UHumanoidAnimInstance` ДО создания узла переменной, иначе переменной ещё не существует.
- **⛔ ЛОВУШКА ГРАФА АНИМАЦИИ (сверено, чуть не наступил): одна поза НЕ может уходить в два входа.** Схема навязывает дерево и при второй связи МОЛЧА рвёт первую (`AnimationGraphSchema.cpp:375-388`, `CONNECT_RESPONSE_BREAK_OTHERS_AB`), причём `TryCreateConnection` возвращает true — поломка была бы незаметной. Ветвление позы — только штатным кэшем (`UAnimGraphNode_SaveCachedPose` + `UAnimGraphNode_UseCachedPose`, у второго ссылка ставится полем `SaveCachedPoseNode`, имя кэша подтягивается само). После сборки связей код ПЕРЕПРОВЕРЯЕТ `LinkedTo.Contains` — что каждая связь удержалась.
- **Урок про `-verify` (мой промах, поймал сам):** проверка была написана как «слот воткнут ПРЯМО в Output Pose», и после `-aim` она начала бы врать, потому что между ними встаёт слой. Переписал: слот ищется по всему графу, проверяются его вход и выход, а у каждого слоя — наличие фильтра по кости. Общее правило: проверка не должна зависеть от того, чего задача не обещала. Опоры: пины элементов массива зовутся «Свойство_Индекс» (`AnimGraphNode_Base.cpp:538` и `K2Node.cpp:1834-1841`), у немассивных свойств имя пина = имя поля (отсюда «BasePose»); `FBranchFilter` = имя кости + глубина, глубина 0 = кость и вся ветка ниже берутся полностью (`BoneMaskFilter.h:16-26` + `AnimationRuntime.cpp:2278`); ненайденная кость фильтра НЕ ошибка, а молча нулевой вес (`AnimationRuntime.cpp:2273-2276`) — проверять наличие кости в скелете заранее. ⚠️ Привязку веса пином ЧЕРЕЗ `Binding` делать нельзя: в 5.5 это приватное поле типа `UAnimGraphNodeBinding*`, а не простая карта.
- **Что теперь умеет `-verify` (усилено 29.07):** кроме целостности связей он ПЕРЕСОБИРАЕТ блюпринт с чистой загрузки с диска и печатает число ошибок и предупреждений, показывает родителя блюпринта, силу наложения каждого слоя (из переменной или постоянная) и ловит лишние слои и пустые входы смешивания. Ничего не сохраняет — файл на диске не трогает (проверено: время файла после `-verify` не изменилось). Это сильный пруф: «связи выглядят целыми» и «блюпринт реально собирается» — разные вещи.
- **Коммандлет правки графа анимации — `9f77494`, файлы `Source/ContrarySurvivorEditor/PatchAnimBpCommandlet.h/.cpp` + три новые зависимости в Build.cs (`AnimGraph`, `AnimGraphRuntime`, `BlueprintGraph`).** Вставляет узел слота монтажа между выходом машины состояний Locomotion и входом Output Pose в `ABP_HumanoidCharacter` (без слота НИ ОДИН монтаж не играет). Режимы: основной, `-verify` (цепочка цела, имя слота стандартное), `-dump` (стабильный печатный срез узлов и связей для сравнения диффом). Идемпотентен. НЕ ЗАПУСКАЛСЯ И НЕ СОБИРАЛСЯ — редактор занят оператором, жду команды лида.
- **ОПОРЫ ДВИЖКА 5.5 ПОД ГРАФЫ АНИМАЦИИ (сверено по исходникам, пригодится при возвратах):** узлы графа создаются `FGraphNodeCreator<T>` (`EdGraph.h:273-326`, Finalize даёт GUID и создаёт пины); пины позы искать ПО ТИПУ через `UAnimationGraphSchema::IsLocalSpacePosePin` (`AnimationGraphSchema.h:101`), а НЕ по именам — имена берутся из полей структуры узла (выход всегда «Pose», `AnimGraphNode_Base.cpp:249-253`, у узлов-стоков его нет); имя графа анимации = `UEdGraphSchema_K2::GN_AnimGraph` (`EdGraphSchema_K2.h:423`, модуль BlueprintGraph); стандартное имя слота = `FAnimSlotGroup::DefaultSlotName` = «DefaultSlot» (`Skeleton.cpp:67`); связи — `UEdGraphSchema::TryCreateConnection` (`EdGraphSchema.h:807`) и `UEdGraphPin::BreakLinkTo` (`EdGraphPin.h:404`). ⚠️ `FBlueprintEditorUtils::GetAnimGraphRoot` НЕ использовать в инструментах: внутри `check(Roots.Num() == 1)` (`BlueprintEditorUtils.cpp:4269`) — упадёт вместо сообщения; искать корень самому через `GetNodesOfClass<UAnimGraphNode_Root>`. Редакторные узлы `MinimalAPI`, но конструктор и StaticClass экспортированы `ANIMGRAPH_API` (сверено в сгенерированных заголовках) — наследование и создание из своего модуля законны.
- **Факт про слоты и скелет (проверял, потому что это могло быть блокером):** в двоичных файлах обоих скелетов-дублей гуманоида НЕТ ни «DefaultSlot», ни «SlotGroups» (метод поиска исправен — рядом находит `C_Spine01`, `BoneTree`). Блокером это НЕ является: `USkeleton::GetSlotGroupName` при незарегистрированном слоте возвращает `DefaultGroupName` (`Skeleton.cpp:1729-1739`), то есть монтаж маршрутизируется по ИМЕНИ слота, а список слотов скелета — редакторная бухгалтерия для выпадающих списков. Требование к монтажам: имя слота в них должно быть ровно «DefaultSlot».

- **Диалог (сдано в 8506638):** панель 900x280 низ-центр (фикс. высота — НЕ растёт от реплики; реплика растяжкой, не влезет — владелец тянет панель), 4 кнопки в канвас-слотах у низа (0/208/416/664, ширины 200/200/240/200); подписи СРАЗУ с именами привязок AcceptText/DeclineText/TurnInText/CloseText (переименовывать их задним числом больше нечем — коммандлет PatchWbpDialog удалён в `464171a`); наборы кнопок по состояниям квеста не пересекаются (вместе только Принять+Отказаться) → дыр между видимыми нет. Смерть: столб разложен по канвас-слотам (якорь 0.5/0.45, Y −236…215), ряды статистики HBox с замками.
- **Не переведены (обследовано 07-28, факты):** WBP_QuestTracker и WBP_InteractPrompt — чистые индикаторы без кнопок, их плашки УЖЕ в канвас-слотах корня (ручки есть), внутри 1 текст — переделка не нужна; BP_ContrarySurvivorHUD — не UMG-виджет (блюпринт HUD-актора со слотами TSubclassOf), переводить нечего.
- **Правки Рината 07-28 по срезам:** WBP_Inventory — расстановка совпала с генератором (потерь не было изначально); WBP_Shop — три кнопки панели количества (см. выше); WBP_Dialog/WBP_TouchControls — стилизация виджетов (переносится белым списком; в диалоге перенеслось 4 значения).
- **cpp-2 (завершил 07-28, его формулировка):** WallOffset «Отступ стены от края» в AWorldBorder + полосы тумана углы встык без перехлёста + слои FogLayerCount 1..3 с шагом FogLayerSpacing и MID-параметром LayerOffset=L*0.37, коммиты `3d9d66e`+`06e292e`, сборка прошла позже у qa EXIT=0.
- **Факты по WBP_PlayerStats (сверено 07-27 поиском по бинарнику ассета):** подписи-слова (HealthBarLabel/HungerBarLabel/ThirstBarLabel/AmmoLabel/MoneyLabel) Ринатом УДАЛЕНЫ, AmmoBagText НЕТ; иконки в ассете фактически 32 px — прежний `-augment` ставил размер через `UImage::SetDesiredSizeOverride`, который пишет только в живой Slate и в ассет НЕ сериализуется (Image.cpp:122-128). Новый `MakeIcon` пишет размер в `Brush.ImageSize`. Код игры показывает AmmoRow через `SelfHitTestInvisible` (PlayerStatsWidget.cpp:62-63) — детей не укрывает, поэтому детям AmmoRow видимость HitTestInvisible прописана в ассете.
- **Параллельный cpp-2 (завершил, гонки записи нет):** `cac86f9` — замки `bLockedInDesigner` на контент кнопок BuildShop/BuildInventory + контракт замков в `-verify` (файл `GenerateWbpCommandlet.cpp`; это был его diff, который я 07-27 видел незакоммиченным). **Урок cpp-2, помнить:** UMG-дизайнер строит сетку попаданий выделения БЕЗ учёта Visibility (`SDesignerView.cpp:2034-2089`) — HitTestInvisible от выделения мышкой в дизайнере НЕ спасает, лечение только `bLockedInDesigner`.
- **ЗНАНИЯ WBP/UMG 5.5 (сверено по исходникам движка, пригодится при возвратах):** привязка BindWidgetOptional — по ИМЕНИ ОБЪЕКТА виджета (`Runtime/UMG/Private/WidgetBlueprintGeneratedClass.cpp:263-271`), `bIsVariable` не требуется; компилятор WBP маппит виджет на нативное BindWidget-поле по имени БЕЗ дубль-переменной (`WidgetBlueprintCompiler.cpp:587-595`); ручки ресайза мышкой в дизайнере — ТОЛЬКО у канвас-слота (`STransformHandle::CanResize`, STransformHandle.cpp:153-155); headless-замер Slate-геометрии в коммандлете НЕВОЗМОЖЕН (при `-run=` FSlateApplication не создаётся — `LaunchEngineLoop.cpp:3228`); безопасное удаление виджетов из дерева = Rename в transient-пакет (`WidgetBlueprintEditorUtils.cpp:595`); DPI — глобальный масштаб поверх слейт-юнитов (`UserInterfaceSettings.h:224`).
- **Привязки экранов магазина/инвентаря (живое правило):** код биндит только листовые кубики/списки/кнопки; `SliderPanel` и `SliderQtyAmmoRow` биндятся как UWidget — их имена в раскладке обязаны сохраняться. (Старый снимок «правок Рината нет» от 07-24 устарел: с 07-28 его правки есть, см. пункт выше — актуальное состояние сверять `-dumpslots`/`-verify`.)
- **ОТКРЫТО (не моё, отслеживается лидом):** QA-сценарии G/H под кв.3 устарели; Canvas-фолбэк диалога не знает сценку/реплики-кнопки (неактивен, WBP назначен).
- **ПАТТЕРН ЛОКАЛИЗАЦИИ (эталон — `UI/PlayerStatsWidget.h`):** формат значения = `UPROPERTY(EditAnywhere, BlueprintReadWrite) FText` с дефолтом `NSLOCTEXT(namespace, key, "...")` ПРЯМО В ЗАГОЛОВКЕ (UHT запрещает LOCTEXT в дефолтах свойств и требует NSLOCTEXT — `Programs/Shared/EpicGames.UHT/Types/Properties/UhtTextProperty.cs:101-104`); сборка = `FFormatNamedArguments` + `FText::Format`; числа = `FText::AsNumber(FMath::RoundToInt32(...))`. Заголовки движок собирает (`Config/Localization/Engine.ini:42` — `FileNameFilters=*.h`). Ведущий ноль = `FNumberFormattingOptions::SetMinimumIntegralDigits` (Text.h:212). Склейка списка = `FText::Join(Delim, TArray<FText>)` (Text.h:703).
- **ГЛАВНОЕ АРХИТЕКТУРНОЕ РЕШЕНИЕ (порция 0, моя поправка к решению лида):** переводимое имя предмета живёт НА ЭКЗЕМПЛЯРЕ (`AMasterInventoryItem::ItemDisplayText` + `GetItemDisplayText()` с откатом название→ключ→заглушка), а НЕ на классе. Причина, которой лид не видел: один класс обслуживает разные предметы (`AQuestItem` — и шкура, и ноутбук; `AConsumableItem` — вода/консервы/бинт, имени в конструкторе НЕТ вообще), плюс имя с уровня (`APickup::PlacedItemDisplayName`) перетирает имя класса. Название на классе было бы РЕГРЕССИЕЙ. Третья ступень отката убрала `GetName()` из UI совсем (протечка `BP_Pistol_C_1`, ADR-049).
- **⛔ КЛЮЧИ, КОТОРЫЕ НЕЛЬЗЯ ПЕРЕВОДИТЬ (проверено, сломает квест):** `AMasterInventoryItem::ItemName`, `FQuest::RequiredItemName`, `FQuest::KillTargetTag`, значения в `ElderNPC.cpp` («Шкура волка», «Ноутбук»), сравнение `QuestComponent.cpp:204,252`. Переводимо и переведено: `FQuest::Title/Description/KillObjectiveLabel/ItemObjectiveLabel` (в `QuestComponent.cpp` НЕ используются ни разу — файл не открывался).
- **ЛОВУШКА СКРЫТИЯ (моя находка, лид назвал самой ценной):** если код прячет кубик со значением, а подпись лежит рядом в дизайнере — подпись остаётся висеть. Решение: биндить КОНТЕЙНЕР и прятать его. Сделано: `AmmoRow` (панель статов), `SliderQtyAmmoRow` (магазин). Где ловушки НЕТ: экран смерти (не прячет ничего), трекер/подсказка (`SelfHidingWidget` прячет корень дерева). Мягкий случай: подпись ВНУТРИ кнопки прячется вместе с ней — записано правилом в гайд.
- **Живой признак платформы (не выдумывать свой):** `AContrarySurvivorPlayerController::HasTouchLayer()` (`ContrarySurvivorPlayerController.h:115`); под ним `#if PLATFORM_ANDROID` (`.cpp:101-105`). Уже используется для выбора текста в `ContrarySurvivorHUD.cpp:1068`. Отвечает «показан тач-слой», а не «это телефон» — лид подтвердил, что это правильное поведение.
- **Схема раскладки для Рината** — `docs/contrary-survivor/umg-layout-guide.md` (веду я, дополняю после каждой порции). Единственное место, где Ринату надо РАЗОБРАТЬ старую раскладку, а не переименовать кубик: строка статов инвентаря (`StatsText` удалён → три кубика значений + три подписи). Слова берутся из `docs/contrary-survivor/ui-texts-ru.md` (лист concept), свои не сочинять.
- **Генератор панелей** `Source/ContrarySurvivorEditor/GenerateWbpCommandlet.cpp` — за мной. Правило: статичные подписи в список проверки `-verify` НЕ добавлять (они принадлежат Ринату, код их не биндит). Перегенерация только по сигналу лида: `WBP_PlayerStats`/`WBP_TouchControls` без `-force` пропускаются, с `-force` затрут стилизацию Рината.
- **УРОКИ ЭТОЙ СЕССИИ (мои промахи, не повторять):** (1) масштаб заявлять только ЗАМЕРОМ — сказал «73+79 мест», по разбору оказалось 25 присвоений, из них 15 безопасных; (2) проверять сохранения ПЕРЕД тем как называть вопрос блокирующим (`ContrarySaveGame.h:51` хранит пути классов, не имена — риска не было); (3) искать по ВСЕМУ `Source`, а не по одному модулю (пропустил комментарий в редакторном модуле); (4) считать длину строки в знаках для телефона — трекер вышел 81 знак и не влезал, я этого не проверил; (5) при расхождении со снимком лида НЕ спорить рефлекторно: коммиты идут раз в 2-4 минуты, его данные могли быть верны на момент замера.
- **Архитектура ADR-048 (жёсткая):** C++ UUserWidget-классы с UPROPERTY(meta=(BindWidgetOptional))-кубиками (null → UE_LOG Warning, НЕ краш); раскладку WBP строит РИНАТ мышкой; в HUD TSubclassOf-слоты (категория «HUD|UMG Widgets»): слот пуст → Canvas-путь панели работает, назначен → UMG, Canvas глушится Is Umg*Active(). Мировые элементы (хелсбары/метки/цифры урона/стрелки) — остаются Canvas. Canvas-код панелей НЕ удалять (выпил отдельно после приёмки). Модалки Z=30, смерть Z=35, постоянные панели Z=5 (BeginPlay HUD).
- **Владение полями:** динамические тексты панелей переехали EditAnywhere-полями в виджет-классы, из HUD УДАЛЕНЫ (~34 поля), Canvas-путь вернулся к литералам (те же строки; «одно место правды» — решение лида). Статичные надписи UMG-пути (заголовки, подписи кнопок) Ринат пишет в Designer, HUD-поля для них живут Canvas-only. Действия виджетов — СУЩЕСТВУЮЩИЕ методы (Shop_*/Inv_*/OfferQuest/AcceptQuest/TurnInQuest/Respawn/CloseShop/CloseDialog через делегаты OnCloseRequested); постоянные панели берут игрока каждый тик у GetOwningPlayer (переживают респаун) и сами прячутся на модалках (IsAnyModalUIOpen контроллера).
- КЛЮЧЕВОЙ ФАКТ Android-тапов (сверено с исходниками, доложено лиду): тап НЕ порождает клик мыши для игрового ввода — порождает клавиши Touch1..Touch3 (PlayerInput.cpp:473, имена «Touch%d» InputCoreTypes.cpp:530), а IMC_Default маплит IA_Fire ТОЛЬКО на ЛКМ (+IA_Sprint=LeftShift; проверено содержимым ассета). РЕШЕНИЕ: легаси-действие ScreenTap=Touch1/2/3 → тот же Fire()/OnFireReleased; позиция тапа = кеш курсора вьюпорта (SceneViewport.cpp:842) → выбор цели и клики Canvas-HUD работают существующим кодом. UMG от пальца работает штатно (bTouchFallbackToMouse=true, SlateApplication.cpp:829). bEnableTouchEvents включён в BeginPlay. В IA-ассетах явных триггеров НЕТ (проверено) → Triggered каждый кадр ненулевого значения — инжекция валидна. `DefaultTouchInterface=None` в DefaultInput.ini (смоук APK №1: движок рисовал СВОИ два стика поверх наших — выключены; свойство сверено InputSettings.h:181-183).
- Открытый вопрос у Рината/лида (не мой): волк при NumToSpawn=0 всё равно спавнится 1 — заложено `FMath::Max(1, NumToSpawn)` (MasterEnemyBase.cpp:235,246 + ClampMin=1); «0=выключить» = правка 2 строк, ждёт решения.
- ⚠️ Этап G (Android-пак): ключи GA внести в конфиг локально перед пакетованием (на устройстве E:/ нет).
- Детали закрытого этапа G (тач-слой, меню паузы, жесты магазина) — в [archive.md](archive.md), раздел «Этап G»; стилевой паттерн C++-виджетов (USTRUCT + ApplyStyle после CreateWidget, формат-строки наружу не отдавать) — там же.

## LIVE-КОНВЕНЦИИ (как работаю в этом проекте)
- **Связь с game-lead (директива 07-17):** на его сообщения БЕЗ вопроса НЕ отвечать подтверждениями — просто выполнять или ждать. Отчёты слать только по результату работы. Его снимки диска могут отставать от моих push (гонка) — при расхождении сначала перепроверить свои артефакты свежими командами, не перезапускать сделанное.
- **Include внутри модуля**: префикс `ContrarySurvivor/<Subdir>/Header.h`. Относительный `Components/Header.h` из другой подпапки НЕ резолвится (C1083).
- **override UFUNCTION** — БЕЗ повтора макроса `UFUNCTION()` над override (UHT запрещает; reflection наследуется, AddDynamic работает).
- **Сборка `-WarningsAsErrors`**: нельзя шэдоуить члены базы (C4458; не называть локальную `Mesh` в наследниках ACharacter).
- **Команда сборки**: `Build.bat ContrarySurvivorEditor Win64 Development -Project=E:/ContrarySurvior/ContrarySurvivor/ContrarySurvivor.uproject -WaitMutex -NoHotReload` напрямую с `2>&1 | tee log` (НЕ через `cmd /c`). Если UBA падает «paging file too small» и лог обрывается без итога — добавить `-NoUBA -MaxParallelActions=6`, лечит.
- **Урок адаптивной юнити (UE 5.5)**: UBT вынимает из юнити-блоков файлы, ГРЯЗНЫЕ по git status; после `git commit` они «чистые» → UBT может выдать ложный `Target is up to date` и не пересобрать. Пруф сборки = дата перелинкованной DLL + даты `Module.ContrarySurvivor.N.cpp.obj`, НЕ последний tee-лог. Форс честной пересборки закоммиченного: `touch` правленых `.cpp` → Build.
- **Релинк DLL только при ЗАКРЫТОМ редакторе** (LNK1104), проверять tasklist (UnrealEditor.exe И UnrealEditor-Cmd.exe). Сейчас редактором владеют другие агенты — сборка только по сигналу лида.
- **Среда**: проект `E:/ContrarySurvior/ContrarySurvivor/` (UE 5.5.4), движок `E:/UnrealEngine/UE_5.5`, VS2022 MSVC 14.44. API сверять по заголовкам движка, не по памяти.
- **Не коммитить ассеты оператора** (BP_*.uasset, *.umap) — только свои Source/ (+Config, если правил сам). Фича-ветки, master/merge не трогаю. Новые параметры — EditAnywhere+BlueprintReadWrite+DisplayPriority (директива Рината 06-25).
- **UMG из чистого C++ (этап F)**: дерево в NativeOnInitialized (WidgetTree->ConstructWidget, RootWidget=CanvasPanel), CreateWidget<T>(PC, T::StaticClass()) без BP-наследника — работает. Модули: UMG (public) + Slate/SlateCore (private). У UButton 5.5 НЕТ SetPadding — тач-габарит кнопки задавать USizeBox.
- **Плагин GameAnalytics подключён УСЛОВНО** в Build.cs: WITH_GAMEANALYTICS=1 только если есть Plugins/GameAnalytics/GameAnalytics.uplugin — копия без плагина остаётся собираемой.
- **Тесты**: `CombatTestWorld::TickWorld` (++GFrameCounter перед World->Tick) обязателен для тиков в однокадровом RunTest; фон C4459 ×12 и 2 warning C4996 GA-плагина — пре-существующие, не блокируют.

## Решения
- Health: игрок И враги — через `UStatsComponent` (TakeDamage БЕЗ Super). `HandleDeath()` virtual; `ReloadCurrentWeapon` virtual (патроны из рюкзака, AAmmoItem-стак).
- `GetMesh()==HeadMesh==Leader` модульного гуманоида; Torso/Legs — followers (SetLeaderPoseComponent). Оружие к кости `R_Hand`, офсеты — параметры.
- Волк — НЕ гуманоид: ACharacter + SK_Wolf + Single Node анимации; ИИ = AWolfAIController(AEnemyAIController).
- **SetFocus сам пешку НЕ вращает** (UE 5.5 Pawn.cpp:1049) — доворот наш StartAimTurnTo.
- ИИ погони: state-machine Idle/Chase/Attack, nav + direct-fallback; QA-лог дросселирован.
- **Сейв-слот один (`ContrarySave`), retention-поля в нём же (этап F):** `SaveGame()` игрока создаёт свежий объект → ПЕРЕНОСИТ retention-поля из прежнего сейва (`UContrarySaveGame::CopyRetentionData`), иначе автосейв костра обнулял бы серию/подсказки. Компоненты удержания правят ТОЛЬКО свои поля через `LoadOrCreateSaveObject`/`WriteSaveObject`. Игра при ЗАПУСКЕ сейв НЕ грузит (LoadGame только при смерти) → награда ежедневки зеркалится и в живые статы, и в `Save->Money`.
- Чистая логика серии ежедневки — `Retention/DailyRewardLogic.h` (DailyReward::Compute, без UObject) — гоняется headless-автотестом; перевод часов назад = награды нет, серия цела.

## Ограничения / тех-долги (DRAFT, на тюнинг/добивку)
- Save/Load НЕ сохраняет `ItemName`/`StackCount` (только class path) — пре-существующее, не трогаю (риск кору).
- Атака врага без LineOfSight-гейта → теоретически удар через тонкую стену в упор.
- Числа баланса — DRAFT UPROPERTY: пистолет 25@2/с, нож 35, бандит HP80/650, волк HP40/780, кап брони 0.75.
- invoker-навмеш требует RecastNavMesh `RuntimeGeneration=Dynamic` в уровне.
- Пикап-предмет визуально неотличим от пикапа-денег (UX-заметка с этапа F).
- НЕ ПРОВЕРЕНО живьём (нужен PIE/устройство): тач-стик под пальцем, меню паузы на экране, мультитач.

## Локализация: два урока по МЕТОДУ поиска (2026-07-20, после первого сбора переводов)
- **Обход по списку панелей структурно НЕ находит тексты, живущие в игровых классах** и попадающие на экран через геттер. Так выпали две дыры подряд: подписи меток над NPC (`ElderNPC.h`/`MasterTrader.h`, метод `IInteractableNPCInterface::GetNPCMarkerLabel`) и имя убийцы (`PlayerCharacter::LastDamagerName`). Правильный порядок обратный: СНАЧАЛА поиск по всему `Source` полей, чьё значение уходит игроку, ПОТОМ обход панелей.
- **Обратная проверка (половина урока от лида, сильнее моей):** искать слова в СОБРАННОМ файле перевода и смотреть, чего там НЕТ. Поиск по коду находит то, о чём догадался спросить; отсутствие записи в собранном файле показывает пропуск само, без гипотезы. Обе дыры всплыли именно так. Закреплено как шаг приёмки: после сбора взять список того, что игрок точно видит, и проверить каждое слово в файле.
- **Ловушка `FText::FromString` на потребителе:** обнуляет всю работу — текст становится culture-invariant и сборщик его не видит, даже если литерал объявлен через NSLOCTEXT. Была на экране смерти (`DeathScreenWidget.cpp:58`), убрана.
- Результат первого сбора (лид проверял сам): 170 текстов (107 из кода, 63 из ассетов), `Content/Localization/Game/ru/` на месте, служебные ключи `RequiredItemName`/`QuestLootItemName`/`QuestItemName` в манифест НЕ попали — разделение ключа и надписи сработало.

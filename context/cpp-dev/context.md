# context: cpp-dev

> Лёгкая рабочая память `cpp-dev` (правило H + Гигиена памяти из CLAUDE.md: стоп >150 строк, СЕЙЧАС вырезает прошлый). Закрытая история — в [archive.md](archive.md) (read-on-demand, НЕ для грунтовки). Build-логи — `Saved/` проекта, история — git.

## 🧭 СЕЙЧАС
**2026-08-24. КРИТБАГ РИНАТА «запустил сохранение — пропала броня торса и ног» — КОРЕНЬ НАЙДЕН И ПОЧИНЕН. Один коммит `1f3cc58` в `feature/tools-0822` поверх `74fb078`. ⛔ Сборку и тесты НЕ гонял — по прямому указанию лида собирает он.**
- **КОРЕНЬ (доказан сейвом и журналами с телефона, сборка `30e7c9b`; обе гипотезы постановки НЕ подтвердились — ни переименование `a3a8b07`, ни порядок восстановления `4027d9c`):** со времён ADR-075 идентичность предмета живёт в СТРОКЕ `DT_Items`, а НЕ в классе — все девять броней собраны из ОДНОЙ `BP_ArmorBase`, а слот, защиту и меш экипировки даёт строка (`ContraryItems::ApplyRowToItem`). Сейв хранил только путь класса, и `RestoreInventoryAndArmor` поднимал голую заготовку: слот «торс», меш `SK_Cloth_T0_Torso`, защита 0. Обе надетые брони садились в слот торса, вторая молча вытесняла первую. Голова уцелела потому, что досталась из лута брошенной машины классом C++ `AHeadArmorT3`, который задаёт всё в конструкторе.
- **Пруфы:** `ContrarySave.sav` — две записи с ОДНИМ ClassPath `/Game/Items/BP_ArmorBase.BP_ArmorBase_C`, ключи «Броня Т2 — штаны»/«Броня Т3 — торс» (у головы `/Script/ContrarySurvivor.HeadArmorT3`); `ContrarySurvivor.log` 17.51.07 — обе восстановлены как slot 1 / `SK_Cloth_T0_Torso` / prot 0.00; backup 17.28.15 — при надевании были slot 2 / `SK_Armor_T2_Legs` / 0.10 и slot 1 / `SK_Armor_T3_Torso` / 0.16; `DT_Items` несёт ровно эти LegacyKey (сверено побайтно с сейвом).
- **Фикс:** `AMasterInventoryItem::SourceItemRow` (экземпляр помнит строку, заполняет `ApplyRowToItem`) → `FSavedInventoryEntry::ItemRow` в сейве; восстановление накладывает строку ЗАНОВО и ДО `AddItem` (слияние стаков сверяет `ItemName`, его тоже задаёт строка). Уже существующие сохранения (телефон Рината, магазинная 0.1.0) лечит поиск строки по служебному ключу ADR-050 в колонке LegacyKey (`FindRowNameByLegacyKey`). Страховка от молчаливой потери: броня в УЖЕ ЗАНЯТЫЙ слот не надевается поверх — остаётся в рюкзаке с предупреждением; броня без найденной строки тоже логируется; итоговая строка `CONTINUE - restored` печатает число записей, подтянутых по старому ключу, и суммарную защиту.
- **Тесты:** новый `Tests/SaveArmorRowRestoreTests.cpp` + класс-двойник `Tests/SaveTestArmorItem.h` — `ContrarySurvivor.SaveLoad.ArmorRows.RestoresAllThreeSlots` и `.MigratesSaveWithoutRowKey` (старый формат без ключа строки). Прежний `SaveLoad.Continue.RestoresBackpackAndArmor` дыру не видел: он берёт `AHeadArmorT1`, наследника C++, который чинит себя конструктором.
- **Хвост у лида в очереди сборки:** мои `3214394`/`e401729`/`2968473`/`95d5a38`/`30e7c9b`/`1189e86`/`74fb078`/`1f3cc58` + его `09a2b55`/`4a83c5e`/`cd50728`/`8bc699e`/`4dfa224`. Оператору — материал `M_CorpseDissolve` (контракт у лида). Устройство закрытой волны Report1-хвостов — в `archive.md`.

**ПРЕДЫДУЩАЯ ВОЛНА (мои части вынесены в `archive.md` → «Волна Report1-хвостов + телефонный пак (24.08)»; блок оставлен целиком, потому что в нём работает ПАРАЛЛЕЛЬНЫЙ экземпляр cpp-dev — окна поддержки/согласия, `3ab82f9`. Чужие строки не резать):**
**ХВОСТЫ Report1 (задача лида task-report1-tails-cpp.md) — СДЕЛАНЫ, отчёт лиду ушёл. 4 коммита в `feature/tools-0822` поверх `6d1188f` (augment оператора: кубик SearchBaseNameText уже добавлен в WBP_SearchWindow и подтверждён дампом — повторный -augment больше НЕ нужен):
- `3214394` баг 2 «Новая игра не сбросила базы»: корень — внутримировые входы новой игры (стартовый экран Б3 `HandleStartScreenNewGame`, «Стереть прогресс» настроек) стирают слот, но базы прочитали СТАРЫЙ сейв ещё в BeginPlay (`LoadTierStateFromSave`) — логово стояло «Cleared, пауза тикает», волков для Q1 не давало. Гипотеза лида (CopyRetentionData) НЕ подтвердилась: слот стирается целиком (DeleteGameInSlot); путь через L_Boot чист (слот трётся ДО загрузки мира). Фикс: `AMasterEnemyBase::ResetForNewGame()` (начальная ступень/зачисток нет/Dormant; живые враги старой сессии убираются, трупы доживают таймер; квест-пикап через новую слабую `SpawnedQuestPickup`; хранилище чистит новый `UCorpseLootComponent::ClearLoot`) + обход `TActorIterator<AMasterEnemyBase>` в `ResetToNewGame` игрока. Смерть/«Продолжить» базы НЕ сбрасывают (ТЗ 22.08 §1). Тест `ContentTools.EnemyBaseResetOnNewGame`.
- `2968473`+`95d5a38` баг 1 «база обыскивается после исчезновения трупа»: гипотеза постановки НЕ подтвердилась — машина УЖЕ считает по смерти (`AreAllSpawnedEnemiesDead`→`IsDead`, хранилище регистрируется ≤0.5 с после смерти). Блокировала ПОДСВЕТКА: «ближайший побеждает», труп последнего убитого со СВОИМ лутом (60 с жизни) лежит ближе пивота базы, его группа хранилище не включала. РЕШЕНИЕ ЛИДА — вариант А (по ADR-077 п.14), сделан в `95d5a38`: мой временный вариант Б (приоритет подсветки, 2968473) ОТКАЧЕН, `UpdateNearbyInteractable` снова честная дистанция; фикс в `CollectSearchableGroup` — «отдельное хранилище» не входит в чужую группу, КРОМЕ хранилищ БАЗ (`bStandaloneStash && StashLevel>0`): награда базы в одном окне с трупами, строку базы выделяет кубик SearchBaseNameText. Уточнения лида: машина (StashLevel=0) — своё окно; якорь-база — своё окно один на один (база входит только ЧЛЕНОМ); хранилище не «тонет» при опустошении (bSinkWhenSearched=false в конструкторе). Отмену ADR-076 п.2 в части баз лид фиксирует в decisions сам.
- `e401729` п.12 иконки магазина: витрина позиции из строки DT_Items (`ContraryItems::FindRow` по `FShopEntry.ItemRow`) — иконка строки первой, «+N%» из `ArmorProtection` строки (0 = как у класса); CDO — фолбэк легаси-позиций (ShopScreenWidget.cpp, buy-ветка); Canvas-путь HUD не тронут. + тест `BoxStaysOutOfGroup` переведён на границу bStandaloneStash (мешки в группе с ADR-076 п.2; полную семантику держит `FContentToolsGroupSearchWithPickupsTest`). + TilePlate: подложки в ассете плитки НЕ будет — фон даёт кнопка TileButton (стиль в ассете), плашка закрыла бы подсветки наведения; решение записано в комментарий `BuildItemTile`.
СРОЧНЫЙ ПАК-ФИКС `30e7c9b` (24.08): телефонный пак Development (ASTC) впервые компилирует тесты в ИГРОВУЮ цель Android (раньше Shipping — тесты вырезаны) и упал на ContentToolsWaveAutomationTests.cpp:409 — `RerunConstructionScripts` есть только под WITH_EDITOR (Actor.h:3323-3326). Замена: безусловный `ExecuteConstruction` (Actor.h:3347; для C++-класса = ровно OnConstruction(Transform), ActorConstruction.cpp:974). Свип ВСЕХ Tests/ на редакторно-только вызовы чист (единственный HasMetaData уже под гардом; каркас миров и UDataTable::AddRow/RowStruct — безусловные, сверено заголовками). ⚠ УРОК: тестовый код обязан собираться и в ИГРОВОЙ цели (Development-пак) — редакторные вызовы только под WITH_EDITOR/с рантайм-эквивалентом.
ТЕКСТЫ СОГЛАСИЯ — ПРАВДА В АССЕТЕ `74fb078` (24.08, ярость Рината: augment 917749a затёр его правку WBP_Consent из 0489ac0). Корень ДВОЙНОЙ: AugmentConsentTexts жёстко писал 4 подписи из C++-структуры В АССЕТ, а рантайм ApplyStyle ставил тексты «всегда, в обоих путях» из настроек — возврат ассета лидом один игру НЕ чинил. Фикс: на дизайнер-дереве код ставит текст ТОЛЬКО в пустой кубик (ShouldSetText), augment непустое не перезаписывает никогда (только заполняет пустые). Прочёс прогона: из шести окон Рината пострадал только Consent; Dialog — add-only крестик + перенос подписей (одобренная правка), остальные не тронуты (слот-дифф оператора + git). ⚠ Тот же паттерн в AugmentSupportAuthorThanks/NextAdHint тогда ОСТАВИЛ (считал контракт окна поддержки обратным) — это и рвануло следом, закрыто в `3ab82f9`; формулировки согласия, которые видит игрок, теперь не проверяет никто — ответственность на владельце ассета (доложено лиду). ⛔ ПРАВИЛО (усиление прежнего «копия текста в генераторе»): генератор/рантайм НИКОГДА не перезаписывают НЕПУСТОЕ значение кубика ассета — только добавляют отсутствующее и заполняют пустое; правка существующего — только руками владельца или явным решением лида.
ТЕКСТ ОКНА ПОДДЕРЖКИ — ПРАВДА В АССЕТЕ `3ab82f9` (24.08, вторая ярость Рината: «ТЕКСТ ОКНА ОПЯТЬ ВЗЯТ НЕ ИЗ МОЕГО WBP»). Тот же двойной корень, что у согласия: ApplyStyle безусловно ставил ВСЕ СЕМЬ подписей из FSupportAuthorStyle «в обоих путях» + ShowThanks переписывала «Спасибо» вторым разом; augment окна «приводил» непустые подписи ассета к C++-дефолту. Фикс: единственная дверь записи `SetTextIfCodeOwns` (пишет только в кодовую запаску по `bCodeTreeBuilt` либо в ПУСТОЙ кубик владельца), ShowThanks оставила себе только видимость, обе процедуры генератора — add-only + заполнение пустого. ⛔ Опора на `bCodeTreeBuilt`, а НЕ на `bDesignerTree`: `NativeOnInitialized` движок зовёт лишь при живом игровом контексте (`UserWidget.cpp:159-163` — условие `!IsDesignTime() && (PlayerContext.IsValid() || bCanCallInitializedWithoutPlayerContext)`, сверено с исходником 5.5), иначе признак дизайнера ещё false и текст владельца затёрло бы. Тест `OwnerEditsSurviveApplyStyle` развёрнут (раньше ТРЕБОВАЛ перезаписи из настроек). Правило из строки выше теперь общее для обоих окон; **прочёс всех окон `UI/` дал ещё 6 нарушителей того же класса — перечень отдан лиду, чинить не мне решать: StartScreenWidget (главное меню, все подписи), PauseMenuWidget (подпись «В главное меню» + переспрос), SettingsScreenWidget (`RefreshLabels`, все заголовки и кнопки), CorpseLootWidget:63/67/71, DialogScreenWidget:66, EndOfStoryWidget:194/198/202 через `HUD:1997`.** Чистые (проверено): DailyReward (ранний выход по bDesignerTree), Consent (74fb078), все окна с ЖИВЫМИ данными (статистика, лут, магазин, диалоги, плитки) и кодовые деревья.
ЗАПРОС ХРАНИЛИЩА УБРАН `1189e86` (24.08, задача Рината): Development-пак просил «доступ к хранилищу» и не пускал в игру. Корень: UBT добавляет WRITE_EXTERNAL_STORAGE в манифест ЛЮБОЙ не-Shipping сборки безусловно (UEDeployAndroid.cs:3100-3103, ini-переключателя нет) + кладёт в StartupPermissions (:2974-2978), а SplashActivity блокирующе запрашивает «манифест ∩ StartupPermissions» до запуска игры (SplashActivity.java:202-214, 509-537); Shipping чист (условие ложно при bUseExternalFilesDir=True — подтверждено aapt-дампами обоих apk). Dev-сборке разрешение не нужно вовсе (публичные логи только под #if UE_BUILD_SHIPPING, AndroidJNI.cpp:2012-2018). Фикс: НОВЫЙ Source/ContrarySurvivor/ContrarySurvivorAndroid_UPL.xml — removePermission из готового манифеста, явно ограничено не-Shipping ($S(Configuration)); регистрация в Build.cs по образцу YandexAds.Build.cs:22-23. ⚠ Build.cs сборкой НЕ проверял (запрет + один сборщик) — валидирует ближайшая сборка лида. Успех пака: aapt без WRITE_EXTERNAL_STORAGE + строка «ContrarySurvivor UPL: ... вырезан» в логе упаковки.
⛔ ВСЁ НЕ СОБРАНО МНОЙ — сборка/прогон за лидом (очередь его сборки: 09a2b55/4a83c5e/cd50728/8bc699e/4dfa224 + мои 3214394/e401729/2968473/95d5a38/30e7c9b/74fb078/3ab82f9). Хвост оператору: материал M_CorpseDissolve (контракт у лида). Поверх моих — контент-коммит лида 321d5d8. Дальше — замечания лида/qa, результат пака.**

**Свёрнуто 24.08:** волны «база/ADR-076» и «аудит BP/ADR-075» (dce2cef, 4027d9c, 359991c, a036079, 0182845, 1fb2a5c, 08f5e6f, 75b70b6..e053cc2) приняты и вынесены в archive.md («Снято с живой памяти 24.08.2026»). Пакет ADR-077 (0a74a4f, e3fd2d1, 5a9c372, 14ef7df, 000e7c0, acf44c4, 09a2b55, 4a83c5e, cd50728, 8bc699e, 4dfa224) — принят лидом/ждёт его финальной сборки, устройство — в сообщениях и git.

**Свёрнуто 22.08:** задача «подсказки сохранения у костра» (ADR-074) сдана ранее: коммиты `17b5772`+`23caf20` в `feature/main-menu-0808`, сборка EXIT=0; устройство подсказок — в archive.md при надобности.

**⛔ ДЕЙСТВУЮЩЕЕ ПРАВИЛО (Ринат): АВТОТЕСТЫ НЕ ЗАПУСКАТЬ — проверяет живьём сам. Разрешена только сборка (в волне Report1-хвостов и сборка была за лидом).**

**Прогоны генератора (редактор закрыт, `tasklist` пуст):** `MSYS_NO_PATHCONV=1 "E:/UnrealEngine/UE_5.5/Engine/Binaries/Win64/UnrealEditor-Cmd.exe" <uproject> -run=GenerateWbp -augment|-verify|-dumpslots -unattended -nopause -nosplash -nullrhi -abslog=<лог>`. Порядок доказательства: `-dumpslots` ДО → `-augment` дважды (второй «сохранено 0») → `-dumpslots` ПОСЛЕ (дифф) → `-verify`. ⛔ `-force` — глобальный, не запускать.

**⛔ ГЛАВНОЕ ПРАВИЛО ПРО АССЕТЫ: правка текста/кубика в коде до игрока НЕ доезжает — окна берутся из готовых ассетов.** Окно в списке `RebuildAssets` и не отдано владельцу → точечная `-rebuild -asset=ИМЯ`; иначе (и всегда для `bOwnerOwned`: настройки, пауза, поддержка) → ДОПОЛНЕНИЕ `-augment` со своей проверкой «уже сделано», идемпотентность доказывать вторым прогоном. **Корень ошибок этого класса — копия текста в генераторе:** текст живёт в ОДНОМ месте (структура стиля окна), генератор берёт оттуда.

**Закрытые волны 08-11…08-15** (групповой обыск `68a69a4`, датчики `11add9a`, согласие/«Спасибо» `c00fc9d`+`fae0eb0`, подсказка ролика `fb77c2e`/`c24740b`, команды съёмки `ac92c46`, релиз 0.1.0 (1) ADR-073) — устройство в `archive.md` («Снято с живой памяти 16.08.2026»), не переоткрывать. ⛔ `Content/Maps/L_World_C.umap`, `scripts/showcase/` — файлы лида, не мои, не коммитить.

**Найдено попутно ранее, НЕ моя правка (доложено лиду):** прогон автотестов пишет в БОЕВОЙ слот `ContrarySave` (какой-то тест смерти/респауна спавнит обычный `APlayerCharacter`).

**Действующие правила последних волн:**
- **⛔ ИДЕНТИЧНОСТЬ ПРЕДМЕТА ЖИВЁТ В СТРОКЕ `DT_Items`, А НЕ В КЛАССЕ (ADR-075; стоило критбага пропавшей брони 24.08).** Один класс/BP обслуживает много предметов (`BP_ArmorBase` — все девять броней, `AConsumableItem` — еда/вода/бинт, `AQuestItem` — шкура/ноутбук), поэтому класс НЕ определяет ни слот, ни защиту, ни меш, ни иконку. **Любой путь, который воссоздаёт предмет заново (сейв, лут, покупка, подарок), обязан наложить строку через `ContraryItems::ApplyRowToItem` — иначе получит голую заготовку класса.** Ключ строки хранить рядом с предметом (`AMasterInventoryItem::SourceItemRow`); для данных, записанных раньше, — обратный поиск по служебному ключу ADR-050 (`FindRowNameByLegacyKey`, колонка LegacyKey).
- **⛔ ТЕСТ НА КЛАССЕ-НАСЛЕДНИКЕ НЕ ЛОВИТ БАГИ ОБЩЕГО BP-КЛАССА.** Проверка «броня переживает сейв» годами шла на `AHeadArmorT1`, который задаёт слот и защиту в конструкторе и потому чинит себя сам; настоящая боевая броня — общая `BP_ArmorBase` без своих значений. Для механик «один класс на много предметов» тест обязан брать класс-пустышку (образец: `Tests/SaveTestArmorItem.h`) и проверять НЕСКОЛЬКО предметов одного класса, а не один.
- **⛔ МОЛЧАЛИВАЯ ПОТЕРЯ ВЕЩИ — ВСЕГДА БАГ.** Если восстановление/экипировка не может выполнить действие (слот занят, строка не найдена), предмет остаётся у игрока и в журнале появляется предупреждение. Перезапись «поверх» без следа недопустима.
- **Окна, отданные владельцу** (`bOwnerOwned`: настройки, пауза, поддержка) генератор НЕ пересобирает даже с `-force`; дополнять — только `-augment`; `-verify` для них смотрит состав и что кубики не замкнуты, к геометрии не придирается (геометрия — через `-dumpslots`).
- **Любой адрес в ini — В КАВЫЧКАХ:** незакавыченный `//` движок режет как комментарий (`ConfigCacheIni.cpp:1776-1780`). Держит тест `ContrarySurvivor.ConfigUrls`.
- **Точные числа баланса в тестах НЕ пиннить** (решение лида): смысл держит `MeleeWolfBalance`.
- **⛔ bash сбрасывает рабочую папку между вызовами** — всегда абсолютные пути / `cd /e/ContrarySurvior/ContrarySurvivor &&` перед git. `git commit -F файл -- пути` (опции ДО `--`).
- **Уроки прогонов 08-13:** голый `AActor` из `SpawnActor<AActor>` без корневого компонента — `GetActorLocation()` всегда ноль (тесты про расстояния — на настоящих акторах); один кадр тестового мира ≤0.4 с игрового времени (`FixupDeltaSeconds`) — тикать шагами; сборщик может упасть с нехваткой памяти УЖЕ ПОСЛЕ успешной сборки — признак успеха = перелинкованные DLL + повтор «up to date»; диагностику ставить сразу в спорное место.
- **Русский счёт в сводке добычи:** склоняем только СВОЁ слово (`PickMoneyWord`), названия предметов приходят данными — число рядом («Шкура волка x4»).
- **`TWeakObjectPtr` НЕ принимает `= nullptr`** (оператор шаблонный по `U*`, WeakObjectPtrTemplates.h:90) — обнулять только `.Reset()`. Пойман сверкой заголовка 24.08 ДО коммита.
- **⛔ ПРАВИЛО РИНАТА ПРО КОММИТЫ (24.08, жёстко):** всю работу по текущей задаче коммитить ОДНОЙ пачкой — один коммит на задачу, не дробить. Уже сделанные несколько коммитов историей не переписывать, просто дальше не дробить.

## LIVE-КОНВЕНЦИИ (как работаю в этом проекте)
- **⛔ ФОРМАТ ОТЧЁТОВ (директива лида 08-09, после трёх разошедшихся сообщений подряд):** писать КОРОТКО — статус в двух строках, подробности только по его запросу. Причина расхождений: оба писали длинно и одновременно. Перед отправкой отчёта СНАЧАЛА проверить входящие. Качество отчётов лида устраивает, мешает объём.
- **Связь с game-lead (директива 07-17):** на его сообщения БЕЗ вопроса НЕ отвечать подтверждениями — просто выполнять или ждать. Отчёты слать только по результату работы. Его снимки диска могут отставать от моих правок (гонка) — при расхождении сначала перепроверить свои артефакты свежими командами, не переделывать сделанное.
- **⛔ КООРДИНАЦИЯ ОКНА — ТОЛЬКО ЧЕРЕЗ ЗАМОК (директива лида 08-09, его сигналы ОТМЕНЕНЫ).** Источник правды — файл `.build-lock`, а не слова лида: его снимок процессов отставал дважды. Порядок: замок свободен И процессов `UnrealEditor*.exe` нет → пишу в замок «кто и зачем», работаю, снимаю сразу после. Лиду — только результат. С `unreal-operator` договариваюсь напрямую. ⚠ Проверять И замок, И процессы: замок может быть свободен, пока чужой headless-прогон ещё идёт (ловил 08-09).
- **Работа втроём в одном дереве (актуально):** перед сборкой брать замок `E:/ContrarySurvior/ContrarySurvivor/.build-lock`, снимать сразу после. Чужие файлы не трогать и не коммитить, добавление и коммит — только точными своими путями. Полузаписанные чужие файлы регулярно валят мою сборку (диагноз по времени файлов, подождать и пересобрать) — это НЕ моя ошибка, но и не повод объявлять сборку зелёной.
- **⛔ ВИДЖЕТ НЕ ПРЯЧЕТ СЕБЯ САМ (UE 5.5, стоило регресса 08-09).** `SetVisibility(Collapsed/Hidden)` на самом `UUserWidget` УБИВАЕТ его `NativeTick` навсегда: Tick зовётся только из `SWidget::Paint`, а `FSlateInvalidationRoot::PaintFastPath_UpdateNextWidget` (SlateInvalidationRoot.cpp:674) пропускает всё, у чего `Visibility.IsVisible()==false`. В проекте под это есть база `USelfHidingWidget` (прячет корень ДЕРЕВА, сам виджет жив) — от неё и наследоваться. Признак бага в журнале: диагностика виджета напечаталась один раз и замолчала.
- **Цвета проекта — ЛИНЕЙНЫЕ.** Значение с картинки (пипетка даёт sRGB) переводить: `lin = ((c/255+0.055)/1.055)^2.4` (ниже 0.04045 — делить на 12.92). Калибровка: `#271D14` в коде записан как `0.0203f, 0.0132f, 0.0070f`. Пипетка по PNG без Python — PowerShell + `System.Drawing.Bitmap.GetPixel`.
- **Headless: кодовое дерево виджета НЕ строится.** `UUserWidget::Initialize` зовёт `NativeOnInitialized` только при живом игровом контексте (`UserWidget.cpp:160`), которого у тестового мира нет. Поэтому тест на поведение дерева — на ЖИВОМ ассете (`StaticLoadClass` + `CreateWidget(World, Class)`): там кубики привязываются в `InitializeWidget` до этой проверки.
- **Include внутри модуля**: префикс `ContrarySurvivor/<Subdir>/Header.h`. Относительный `Components/Header.h` из другой подпапки НЕ резолвится (C1083).
- **override UFUNCTION** — БЕЗ повтора макроса `UFUNCTION()` над override (UHT запрещает; reflection наследуется, AddDynamic работает).
- **Сборка с флагом «предупреждения как ошибки»**: нельзя перекрывать члены базового класса (C4458; не называть локальную `Mesh` в наследниках ACharacter).
- **Команда сборки**: `Build.bat ContrarySurvivorEditor Win64 Development -Project=E:/ContrarySurvior/ContrarySurvivor/ContrarySurvivor.uproject -WaitMutex -NoHotReload` напрямую с `2>&1 | tee log` (НЕ через `cmd /c`). Если ускоритель падает с «paging file too small» — добавить `-NoUBA -MaxParallelActions=6`.
- **Русский текст в логе сборки в консоли идёт кракозябрами** (кодовая страница), в собственном журнале сборщика `C:/Users/pgr40/AppData/Local/UnrealBuildTool/Log.txt` читается нормально — свои сообщения из `*.Build.cs` проверять там.
- **Урок про файл правил сборки (от лида, 08-06):** `*.Build.cs` — общий ресурс, он валит всех троих раньше любого C++. Правку в нём проверять сборкой СРАЗУ и не оставлять сломанной; синтаксис брать не по памяти, а из настоящих файлов правил движка. Мой случай: `Logger.LogInformation/LogWarning` — методы расширения, нужна строка `using Microsoft.Extensions.Logging;` (сверено с `Core.Build.cs:7` и `EOSSDK.Build.cs:9`).
- **Тикер движка 5.5 принимает функцию, а не делегат:** `FTSTicker::AddTicker(имя, задержка, TUniqueFunction<bool(float)>&&)` (`Ticker.h:56`) — `FTickerDelegate::CreateUObject` НЕ подходит, оборачивать лямбдой со слабым указателем.
- **Проверка геометрии окон (`-verify`, мой инструмент от 08-09):** печатает прямоугольник каждого элемента в канвас-слоте и ловит нулевой размер, выход за подложку и наложения. ⚠ Эталон холста — **1920x1080** (в этих координатах раскладывают окна; телефон получает то же дерево через масштабирование). Первая версия мерила экраном 1600x720 и дала 6 ложных тревог — линейку не менять. Выход за КОРНЕВОЙ холст — предупреждение (размер зависит от устройства), за подложку — ошибка. Исключения объективные: растянутый на всю площадь = фон; наложение допустимо, если один прямоугольник полностью накрывает другой И больше по площади в 1.3 раза (плашка под надписью), поэтому две кнопки одного размера в одной точке ловятся.
- **Ловушка переспроса в чужом окне:** если режим-переспрос временно ПРЯЧЕТ пункты, возвращать надо ЗАПОМНЕННУЮ видимость, а не «Visible»: владелец мог скрыть пункт сам в дизайнере, и выход из переспроса его бы «воскресил». Так же и с подписями: в дизайнер-дереве возвращать тексты владельца (снятые при инициализации), а не значения стиля.
- **Урок адаптивной юнити (UE 5.5)**: сборщик вынимает из юнити-блоков файлы, ГРЯЗНЫЕ по git status; после коммита они «чистые» → возможен ложный `Target is up to date`. Пруф сборки = дата перелинкованной библиотеки, НЕ последний лог. Форс: `touch` правленых `.cpp` → сборка.
- **Перелинковка библиотеки только при ЗАКРЫТОМ редакторе** (LNK1104), проверять список процессов (UnrealEditor.exe И UnrealEditor-Cmd.exe).
- **Среда**: проект `E:/ContrarySurvior/ContrarySurvivor/` (UE 5.5.4), движок `E:/UnrealEngine/UE_5.5`, VS2022 MSVC 14.44. API сверять по заголовкам движка, не по памяти. Исходники в UTF-8 БЕЗ метки порядка байтов — сборщик передаёт компилятору `/utf-8` (`VCToolChain.cs:516`), русские литералы работают.
- **Не коммитить ассеты оператора** (BP_*.uasset, *.umap) — только свои Source/ (+Config, если правил сам). Фича-ветки, master и слияния не трогаю. Новые параметры — EditAnywhere+BlueprintReadWrite+DisplayPriority (директива Рината 06-25).
- **UMG из чистого C++ (этап F)**: дерево в NativeOnInitialized (WidgetTree->ConstructWidget, RootWidget=CanvasPanel), CreateWidget<T>(PC, T::StaticClass()) без BP-наследника. Модули: UMG (public) + Slate/SlateCore (private). У UButton 5.5 НЕТ SetPadding — размер кнопки под палец задавать USizeBox.
- **Плагин GameAnalytics подключён УСЛОВНО** в Build.cs: `WITH_GAMEANALYTICS=1` только если есть `Plugins/GameAnalytics/GameAnalytics.uplugin` — копия без плагина остаётся собираемой.
- **Тесты**: `CombatTestWorld::TickWorld` (++GFrameCounter перед World->Tick) обязателен для тиков в однокадровом RunTest; фон C4459 ×12 и 2 предупреждения C4996 плагина аналитики — пре-существующие, не блокируют. Свои тесты — всегда в НОВЫЙ файл (иначе гонка с чужими правками); в СВОЙ прежний файл дописывать можно, если его никто не трогает (проверять git status).
- **Подсистему объекта игры в тесте создавать только с хозяином-`UGameInstance`:** у `UGameInstanceSubsystem` задан `ClassWithin`, и `NewObject<T>()` без хозяина валит тест жалобой «created in invalid Outer». Правильно: `NewObject<UGameInstance>(GetTransientPackage())`, затем `NewObject<T>(GI)`.
- **`UGameMapsSettings` требует явной строки `EngineSettings` в `PrivateDependencyModuleNames`**, хотя `Engine` тянет этот модуль публично: без неё LNK2019 на этапе компоновки.
- **Имена UENUM глобальны для всего движка** (UHT: «shares engine name»): `EGraphicsPreset` уже занят `HardwareTargetingSettings.h` — свои перечисления называть с приставкой проекта (`EContraryGraphicsPreset`). Ловится только сборкой, редактор об этом не предупреждает.
- **`FText::AsNumber` РАЗБИВАЕТ разряды по локали** (1120 → «1 120»): для пикселей/версий/идентификаторов передавать `&FNumberFormattingOptions::DefaultNoGrouping()`.
- **GConfig не умеет писать в ПРОИЗВОЛЬНЫЙ ini-файл**: `SetString` молча выходит, если файла нет в кеше (`FConfigCacheIni::Find` подгружает только существующий файл и ставит ему `NoSave=true`). Поэтому «сохранение настроек» в headless-тесте проверяется не записью файла, а чтением подготовленного файла + проверкой флага `CPF_Config` у полей. Боевой `GameUserSettings.ini` разработчика тесты трогать НЕ должны: `UGameUserSettings::SaveSettings()` вдобавок пишет `Scalability::SaveState` в `EditorSettings.ini` — то есть в настройки редактора Рината.

## Решения
- Health: игрок И враги — через `UStatsComponent` (TakeDamage БЕЗ Super). `HandleDeath()` virtual; `ReloadCurrentWeapon` virtual (патроны из рюкзака, AAmmoItem-стак).
- `GetMesh()==HeadMesh==Leader` модульного гуманоида; Torso/Legs — followers (SetLeaderPoseComponent). Оружие к кости `R_Hand`, офсеты — параметры.
- Волк — НЕ гуманоид: ACharacter + SK_Wolf + Single Node анимации; ИИ = AWolfAIController(AEnemyAIController).
- **SetFocus сам пешку НЕ вращает** (UE 5.5 Pawn.cpp:1049) — доворот наш StartAimTurnTo.
- ИИ погони: состояния Idle/Chase/Attack, навигация с прямым запасным путём; отладочный лог дросселирован.
- **Игровой слот сохранения один (`ContrarySave`), поля удержания в нём же:** `SaveGame()` игрока создаёт свежий объект → ПЕРЕНОСИТ поля удержания из прежнего сейва (`UContrarySaveGame::CopyRetentionData`). Компоненты удержания правят ТОЛЬКО свои поля через `LoadOrCreateSaveObject`/`WriteSaveObject`. Отдельно — служебный слот аналитики `ContraryAnalytics`, он переживает «Новую игру». Память баз (`EnemyBaseStates`) живёт в игровом слоте; «Новая игра» в живом мире дополнительно сбрасывает АКТОРЫ баз (`ResetForNewGame`, 24.08).
- Чистая логика серии ежедневных входов — `Retention/DailyRewardLogic.h` (без UObject) — гоняется автотестом без редактора.
- **АРХИТЕКТУРНОЕ РЕШЕНИЕ (моя поправка к решению лида):** переводимое имя предмета живёт НА ЭКЗЕМПЛЯРЕ (`AMasterInventoryItem::ItemDisplayText` + `GetItemDisplayText()` с откатом название→ключ→заглушка), а НЕ на классе: один класс обслуживает разные предметы (`AQuestItem` — и шкура, и ноутбук; `AConsumableItem` — вода/консервы/бинт).
- **⛔ КЛЮЧИ, КОТОРЫЕ НЕЛЬЗЯ ПЕРЕВОДИТЬ (сломает квест):** `AMasterInventoryItem::ItemName`, `FQuest::RequiredItemName`, `FQuest::KillTargetTag`, значения в `ElderNPC.cpp` («Шкура волка», «Ноутбук»), сравнение `QuestComponent.cpp:204,252`.
- **ПАТТЕРН ЛОКАЛИЗАЦИИ (эталон — `UI/PlayerStatsWidget.h`):** `UPROPERTY(EditAnywhere, BlueprintReadWrite) FText` с дефолтом `NSLOCTEXT` ПРЯМО В ЗАГОЛОВКЕ (UHT запрещает LOCTEXT в дефолтах свойств); сборка строки — `FFormatNamedArguments` + `FText::Format`; числа — `FText::AsNumber(FMath::RoundToInt32(...))`. Ловушка `FText::FromString` на потребителе обнуляет перевод.
- **Архитектура ADR-048:** C++ UUserWidget-классы с кубиками `BindWidgetOptional` (пусто → предупреждение, не падение); раскладку WBP строит РИНАТ мышкой; в HUD слоты TSubclassOf: слот пуст → работает старый Canvas-путь, назначен → UMG. Canvas-код панелей НЕ удалять. Модальные окна Z=30, экран смерти Z=35, постоянные панели Z=5.
- **ЛОВУШКА СКРЫТИЯ:** если код прячет кубик со значением, а подпись лежит рядом в дизайнере — подпись остаётся висеть. Решение: привязывать КОНТЕЙНЕР и прятать его (`AmmoRow`).
- **Живой признак платформы (не выдумывать свой):** `AContrarySurvivorPlayerController::HasTouchLayer()` (`.h:115`), под ним `#if PLATFORM_ANDROID`. Отвечает «показан ли слой экранных кнопок», а не «телефон ли это».
- **Android-тапы:** тап НЕ порождает клик мыши для игрового ввода — порождает Touch1..Touch3 (`PlayerInput.cpp:473`). Легаси-действие `ScreenTap` → тот же `Fire()`; позиция тапа = кеш курсора вьюпорта. UMG от пальца работает штатно (`bTouchFallbackToMouse`). `DefaultTouchInterface=None` в DefaultInput.ini.
- **Генератор панелей** `Source/ContrarySurvivorEditor/GenerateWbpCommandlet.cpp` — за мной, но его правит и сосед. Перегенерация только по сигналу лида: с ключом `-force` затрёт стилизацию Рината.

## Ограничения / тех-долги (черновые, на тюнинг и добивку)
- Атака врага без проверки прямой видимости → теоретически удар через тонкую стену в упор.
- Числа баланса черновые: пистолет 25 урона дважды в секунду, нож 35, бандит 80 здоровья, волк 40, предел брони 0.75.
- Навмеш «по приглашению» требует у RecastNavMesh режима `RuntimeGeneration=Dynamic` в уровне.
- Пикап-предмет визуально неотличим от пикапа-денег (заметка по удобству).
- Волк при количестве спавна 0 всё равно спавнится один (`FMath::Max(1, NumToSpawn)`, `MasterEnemyBase.cpp:235,246`) — ждёт решения Рината.
- НЕ ПРОВЕРЕНО живьём (нужен живой запуск или устройство): стик под пальцем, меню паузы на экране, мультитач, стартовый экран Б3 глазами.

## Уроки метода (не повторять промахи)
- **Баг с телефона разбирается по ДВУМ артефактам сразу — журналу И самому файлу сохранения** (`adb pull` только на чтение, пока Ринат играет; в экран и в игру не вмешиваться). Журнал: `/sdcard/Android/data/com.renchezlove.contrarysurvivor/files/UnrealGame/ContrarySurvivor/ContrarySurvivor/Saved/{Logs,SaveGames}`, adb — `E:/Android/sdk/platform-tools/adb.exe` (в bash нужен `MSYS_NO_PATHCONV=1`, иначе путь устройства подменяется на локальный). `.sav` читается без движка: латиница лежит однобайтно (`grep -a -o -E "[ -~]{7,}"` даёт пути классов), русские строки — UTF-16LE (маленький скрипт PowerShell). Так путь класса в сейве прямо показал, что две брони — один `BP_ArmorBase_C`; ключи сверял побайтно с `.uasset` таблицы.
- **Локализация, урок про поиск:** обход по списку панелей структурно НЕ находит тексты, живущие в игровых классах (так выпали метки над NPC и имя убийцы). Правильный порядок обратный: сначала поиск по всему `Source` полей, чьё значение уходит игроку, потом обход панелей. Сильнее того — обратная проверка лида: искать слова в СОБРАННОМ файле перевода и смотреть, чего там НЕТ.
- **Гипотезу постановки проверять кодом до фикса:** оба бага Report1 (24.08) имели корень НЕ там, где предполагала постановка (машина уже считала по смерти; retention был ни при чём) — фикс по гипотезе был бы холостым.
- Масштаб задачи заявлять только замером, а не на глаз.
- Проверять сохранения ПЕРЕД тем как называть вопрос блокирующим.
- Искать по ВСЕМУ `Source`, а не по одному модулю.
- Считать длину строки в знаках для телефона (трекер вышел 81 знак и не влезал).
- При расхождении со снимком лида не спорить рефлекторно, но и не переделывать сделанное: сначала перепроверить свои артефакты свежими командами.

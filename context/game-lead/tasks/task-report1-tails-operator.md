# Задача unreal-operator: три проверки хвостов Report1 (24.08.2026)

Ветка `feature/tools-0822`, HEAD `4dfa224`. Редактор сейчас ЗАКРЫТ (tasklist пуст) — движок твой. По завершении — сигнал лиду «движок свободен».
⛔ НЕ трогать и НЕ коммитить: `BP_EnemyBandit.uasset`, `BP_Wolf.uasset`, `L_World_C.umap` (незакоммиченные правки неустановленного происхождения, менялись 23.08 15:42-16:00).
⛔ WBP_StartScreen (меню) НЕ трогать — там правка Рината (ef8edc6), он сам довёл меню.
Максимум ДВЕ попытки любого прогона (правило Рината).

## 1. WBP_SearchWindow: кубик SearchBaseNameText — проверить, при необходимости дополнить, закоммитить
Файл менялся 23.08 в 15:33 и НЕ закоммичен — вероятно, повторный `-augment` успел пройти при обрыве прошлой сессии (кубик SearchBaseNameText появился в коде ПОСЛЕ твоего прогона 917749a — см. cd50728: окно пишет строку базы в отдельный кубик, формат «{Name} — Ур. {Level}»).
Порядок: `-dumpslots` по WBP_SearchWindow → есть ли SearchBaseNameText. Есть → закоммитить ТОЛЬКО этот файл (`git commit -- Content/UI/WBP_SearchWindow.uasset`, сообщение про повторное дополнение). Нет → прогнать `-augment` (двойной прогон на идемпотентность), дамп ПОСЛЕ, закоммитить. Правки Рината в этом окне уже в истории (ef8edc6) — дифф 15:33 поверх них, беречь.

## 2. BP_PlayerCharacter: пороги регена
Код 8bc699e сменил дефолты порогов регена 80→50 (Stats → Stats|Survival|Regen). Проверить CDO BP игрока: не ПЕРЕОПРЕДЕЛЕНЫ ли старые 80. Переопределены → снять переопределение (действуют новые 50 из кода), закоммитить только этот файл. Нет → ничего не делать, доложить фактом (значения из дампа).

## 3. Ничего больше по своей инициативе
Кадры/съёмку не делать — Ринат проверяет живьём сам. Расстановку не трогать.

## Отчёт лиду
По пунктам: факт (дамп/значения), что закоммичено (хеши), «движок свободен».

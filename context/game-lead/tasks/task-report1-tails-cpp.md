# Задача cpp-dev: хвосты Report1 + два новых бага Рината (24.08.2026)

Ветка `feature/tools-0822`, HEAD `4dfa224` (запушен). Работать поверх. Content НЕ трогать.
⛔ Сборку НЕ запускать (соберёт лид одной сборкой в конце). ⛔ Автотесты НЕ запускать (правило Рината — проверяет живьём сам; тесты чинить логически, сверяясь с кодом).
⛔ В дереве незакоммиченные чужие правки: `BP_EnemyBandit.uasset`, `BP_Wolf.uasset`, `L_World_C.umap`, `WBP_SearchWindow.uasset` — НЕ трогать, НЕ коммитить.
Коммитить КРУПНО (правило Рината 23.08): один коммит на законченный блок, только `git commit -- <пути>`. Ориентир — 2-3 коммита на всю задачу.
Каждый движковый вызов сверять с заголовком ДО коммита (твой же урок 22.08, дважды).

## 1. БАГ (приоритет): база обыскивается только после исчезновения трупа
Слова Рината дословно: «Мне показалось что базу бандитов можно обыскать только после того как исчез последний труп бандита. А надо после того как умер последний заспавненый враг. Т.е. мне показалось что игра не дает обыскать базу пока не исчезнет труп последнего заспавненого врага. А надо что бы её можно было обыскивать как только умрет последний заспавненый враг».
Требование: хранилище базы (cd50728) становится доступным для обыска СРАЗУ по смерти последнего заспавненного врага базы, не дожидаясь ухода/растворения трупа.
Гипотеза (НЕ ПРОВЕРЕНО, проверить по коду): машина состояний AMasterEnemyBase (dce2cef, Actors/) определяет «зачищено» по уничтожению акторов (IsValid/EndPlay), а трупы теперь живут дольше (уход в землю; растворение после обыска 09a2b55) — отсюда задержка. Найти реальную цепочку по коду, ответ с файлами:строками. Чинить: считать по факту СМЕРТИ (HandleDeath/OnDied), не по уничтожению актора. Существующие тесты EnemyBaseRespawnTests поправить под новое поведение, если они завязаны на уничтожение.

## 2. БАГ (приоритет): новая игра — волки в логове не заспавнились
Слова Рината дословно: «Немного поиграл со старого сохранения. Зашел на первую базу волков и убил всех волков. Когда решил начать новую игру, то после диалога со старостой - пошел к логову волков, но там они уже не заспавнились».
Требование: «Новая игра» приводит ВСЕ базы в исходное состояние — первое логово сразу занято волками (квест Q1 иначе невыполним).
Гипотеза (НЕ ПРОВЕРЕНО): FSavedEnemyBaseState{LastClearUtc,...} переживает новую игру через CopyRetentionData → действует пауза респауна 15+ мин. Проверить всю цепочку «Новая игра» по коду. ВАЖНО при фиксе различать: смерть игрока/«Продолжить» — состояние баз ДОЛЖНО переживать (ТЗ издателя по респауну баз); «Новая игра» — НЕ должно. Ответ с файлами:строками.

## 3. Иконки магазина из строки таблицы (ADR-077 п.12, план утверждён лидом)
Корень найден оператором: у FShopEntry нет иконки, магазин берёт иконку и процент защиты с CDO класса записи (ShopScreenWidget.cpp:389-392, 370-374); при каталоге из таблицы класс = общий BP_ArmorBase → иконка пустая, «+0%», отображение «через раз». Чинить: для записей из таблицы иконку/защиту/имя брать из строки DT_Items (ContraryItemLibrary), CDO — фолбэк для легаси-записей.

## 4. Тест BoxStaysOutOfGroup — обновить
В финальном прогоне 23.08 ожидаемо падал 1 тест — устаревший BoxStaysOutOfGroup (поведение изменено волной cd50728/08f5e6f). Привести проверку к текущему поведению (отдельное хранилище не входит в группу / актуальная семантика по коду). НЕ запускать — только починить логически.

## 5. Подложка плитки TilePlate — решение за тобой
После пересборки WBP_ItemTile плоской канвой (acf44c4) подложка TilePlate под вопросом (4 старых контейнера «вид не перенесён»). Реши сам: нужна ли подложка-кубик TilePlate в дереве плитки (в духе П.0 ADR-077 — вид настраивается в ассете). Сделай или обоснуй отказ одной строкой.

## Отчёт лиду
По каждому пункту: что изменено (файлы:строки), корень бага с доказательством по коду, номера коммитов. Без сборки и тестов — так и написать «сборка за лидом».

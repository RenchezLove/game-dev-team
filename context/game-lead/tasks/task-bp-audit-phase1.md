# Задача cpp-dev: инвентаризация классов под BP-наследников (Фаза 1 — только чтение, БЕЗ правок)

**От:** game-lead. **Основание:** ТЗ издателя от 22.08.2026 (бэклог №21), решение Рината (дословно): «Логика остаётся в C++. В Blueprint выносятся настройки параметров и визуал. Переписывать игровую логику на Blueprint мы НЕ будем».

## Что дано
- Репозиторий игры: `E:/ContrarySurvior/ContrarySurvivor`, ветка `feature/main-menu-0808` (HEAD `af41a7b`). В этой фазе НИЧЕГО не менять и не коммитить — только читать код.
- Готовый дамп всех Blueprint с родительскими классами (снят лидом сегодня headless-прогоном): `E:/ContrarySurvior/ContrarySurvivor/Saved/audit_bp_parents.txt` (43 строки). Это правда о том, у каких классов наследники уже есть.
- Заголовков в Source: 94 (`Source/ContrarySurvivor/**`).

## Что сделать
Составить таблицу-перечень по геймплейным C++ классам проекта и записать её в файл
`E:/ContrarySurvior/ContrarySurvivor/Saved/bp-audit-phase1.md`.

По каждому классу — одна строка-запись с полями:
1. **Класс** (имя + путь к заголовку).
2. **BP-наследник:** есть (какой, из дампа) / нет.
3. **Нужен ли:** да / нет + причина одним предложением. Ориентир из ТЗ (дословно): «Список классов и решение "целесообразно или нет" — за тобой». Не нужны наследники у: коммандлетов, тестов, чисто служебных подсистем, интерфейсов, виджетов (WBP уже есть и живут через генератор — их НЕ трогаем).
4. **Параметры на вынос:** какие числовые/логические поля поведения класс держит сейчас (урон, здоровье, скорость, дистанции, кулдауны, шансы, цены) и в каком они виде: уже UPROPERTY(EditAnywhere/EditDefaultsOnly) / зашиты константой в .cpp / читаются из DataTable.
5. **Визуал:** есть ли настраиваемые поля модели/материала/иконки/эффектов/звуков; чего не хватает. Требование Рината (дословно): «глядя на Blueprint, должно быть видно, как объект выглядит, а не только как он считается».
6. **DataTable:** пересекается ли класс с таблицами данных (единый лут-конфиг ADR-045 и прочие). Требование ТЗ (дословно): «Blueprint не дублирует значения, а читает их».

## Особое внимание (дух ТЗ — «новый контент без программиста»)
- Линейка предметов: AMasterInventoryItem, AAmmoItem, AConsumableItem, AQuestItem, AArmor + AHeadArmor/ATorsoArmor/APantsArmor/AArmorTiers — BP-наследников нет ни у одного.
- Оружие: AMasterWeapon, AMeleeWeapon, ARangedWeapon, APistol — BP есть только у пистолета.
- Враги: MasterEnemyBase, EnemyCharacter, WolfCharacter — BP есть; вопрос в полноте: соберётся ли НОВЫЙ тип врага (меш, анимации, звуки, статы, лут) в редакторе без единой строки кода? Чего не хватает — перечислить.
- ShopVendor, EnemySpawnPointComponent, StatsComponent, CorpseLootComponent, Pickup — проверить полноту настроек.

## Итог фазы
Файл `Saved/bp-audit-phase1.md` + короткое резюме мне: сколько классов, сколько наследников надо завести, где главные дыры (константы в коде / отсутствующий визуал / дубли с DataTable). Оценка объёма Фазы 2 (правка UPROPERTY) по группам.

## Запреты фазы
- Никаких правок файлов проекта, никаких коммитов, никаких сборок. Редактор не открывать. Только чтение кода и готового дампа.
- Не выдумывать состояние — каждое утверждение таблицы должно опираться на реально открытый файл (правило C протокола).

# context: game-lead

> Персональная (живая) память агента `game-lead`. Грунтовка сессии — ТОЛЬКО блок «🧭 СЕЙЧАС» ниже (правило bootstrap CLAUDE.md). Прошлое — `archive.md` (read-on-demand). Решения — `docs/decisions.md` (ADR). История — git. Дизайн-правда — `docs/contrary-survivor/`.
> СТОП-правило гигиены: >150 строк ИЛИ Read обрезается → СНАЧАЛА чистка (вынос закрытого в archive.md, удаление перетёртого), потом работа. Дистилляция ВЫРЕЗАЕТ прошлый «СЕЙЧАС», не только дописывает.

## 🧭 СЕЙЧАС (2026-08-09 вечер — меню переделано, пакет на телефоне, ЖДУ ЖИВОЙ ОСМОТР РИНАТА)

**ФАЗА:** доводка перед RuStore. **MASTER = `d7291fb`** (не менялся). Волна **`feature/main-menu-0808`**, HEAD **`c1dd5d7`**, 30 коммитов. **Слияние ждёт:** живой осмотр Рината → повторный полный гейт qa → маркер → слияние → пересборка подписанного дистрибутива.

**ВЕЧЕР 08-09 (пруфы лично):** сборка проекта — библиотека `Binaries/Win64/UnrealEditor-ContrarySurvivor.dll` 15:31 новее самых свежих исходников 15:28, значит правки собраны; пакет Development собран `Saved/gl-pack-0809-evening.log` (BUILD SUCCESSFUL, 309 с), `ArchivedBuilds/Android_ASTC/ContrarySurvivor-arm64.apk` 18:05, установлен на телефон («Success») в 18:0x. Ринат смотрит меню живьём + попрошу «Продолжить» ради задачи Г.
**Утверждено:** название игры «Марево» (`Config/DefaultEngine.ini:9 ApplicationDisplayName`), эталон меню — `concept-art/menu-mockups/menu-background-A-check.png` (волк, логотип слева, кнопки справа, внизу версия и политика).
**Побочно 08-09 вечер:** для поста в Telegram собран сравнительный кадр `reports/renders/trader-compare.png` (нынешняя модель торговца → модель из Tripo, стрелка). Приём: склейка картинок = HTML+CSS → снимок headless-Chrome (`--headless=new --screenshot --window-size`), Pillow в системе НЕТ. Исходники — `reports/renders/_tmp/`.

**СДЕЛАНО СЕГОДНЯ (пруфы проверены лично):** подход 2 — экран настроек + автовыбор качества (`c5769c8`,`77c5325`); подход 3 — надпись «Прогресс сохранён» у костра, выход в меню из паузы с переспросом, вибрация (`fda5a33`,`3fcd9c4`); мои прогоны генератора: пересборка меню (`5bb0b35`), создание живого экрана настроек + слот (`c4b6753`), кнопка «В главное меню» в паузу режимом -augment (`a86e70d`). Тесты 108/108 (пересчитал сам). Пакет Development собран (BUILD SUCCESSFUL) и установлен на телефон в 14:07.

**⛔ МЕНЮ ЗАВЁРНУТО ЖИВЬЁМ (Ринат, кадр с телефона):** «всё поехало». Причина — МОЯ: гнал `-rebuild` с включённым переносом значений владельца, старая геометрия первой версии перетёрла новую раскладку (`Saved/gl-startscreen-rebuild.log:214-216`: подложка 560→320, кнопки на -100 и -30). Итог на кадре: «Новая игра» и «Настройки» налезли друг на друга, «Выход» вне подложки, сквозь меню светит живой мир, при меню виден игровой интерфейс.
**ПЕРЕДЕЛКА СДЕЛАНА (коммиты `ee20959`,`cdf2ec8`,`5740256`,`aae7bb2`,`c1dd5d7`):** непрозрачный фон с волком, логотип слева 700x246, кнопки справа, HUD прячется под меню, проверка раскладки в `-verify` даёт ноль ошибок (`Saved/gl-verify-final.log`). Осталось судить Ринату живьём.
**ФОН — решение: статичная картинка, НЕ видео** (спека:11 + цена декодирования на слабом телефоне). Ринат предлагал зацикленное видео (бельё, волк) — отклонено с обоснованием. concept делает 3 макета: волк-иконка с зеркальным размытием по бокам / фирменный #271D14 с тёплым свечением / свой вариант.
**ЛОГОТИП ИГРЫ ЕСТЬ:** `concept-art/GameLogo/ava-marevo-1.jpg` — «МАРЕВО» + «ВЫЖИВАНИЕ». **❓ЖДУ ОТВЕТ РИНАТА: «Марево» — финальное название для магазина?** (это закроет пункт «русское название RuStore»).

**QA-ГЕЙТ: «ДОБРО» ПОЛУЧЕНО на `a86e70d`** (сборка с принудительной пересборкой всех 186 файлов, тесты 108/0, 30 окон VERIFY без провалов, секретов в истории нет, регресса движения НЕТ — хвост закрыт). Артефакты в `context/qa/logs/`. Проверил выборочно сам. **Маркер QA_OK НЕ ставил.** Порядок перед слиянием теперь: переделка → генератор → пакет → **живой осмотр Рината** → повторный гейт (полные тесты) → маркер → слияние.

**Задача Г (полоса голода) — НЕ воспроизведена сегодня:** диагностика пишет (`context/game-lead/phone-0809.log:1275,1285,1286`), но все три записи со старта и ряд ВИДЕН, ширина 224x24, шкала 1.0. Ринат до «Продолжить» не дошёл. Нужна его сессия: открыть игру → «Продолжить» → я забираю журнал.

**ADR-063 (издатель 08-08 вечер):** баланс/лут/порог рекламы — всё реализовано и в пакете. **П.4 ОТМЕНЁН: AppMetrica В СБОРКЕ ЕСТЬ** (io.appmetrica.analytics:analytics:8.3.0 — обязательная зависимость рекламного SDK, YandexAds_UPL.xml:58). Согласие с ТРЕМЯ сборщиками — правильное, НЕ трогать. Поправка издателю `publisher-popravka-appmetrica-2026-08-08.md` ждёт отправки Ринатом.

**ADR-064 (сегодня):** решения по волне — класс настроек на UGameUserSettings, пресет ПЕРЕД масштабом, снижение разрешения из профилей убрано, автовыбор по памяти+ядрам, вибрация только урон/смерть, один адрес Telegram, счётчик кадров по спеке, «несохранённое» по заработанному, три режима генератора по риску. **Доп.12: `-verify` слеп к раскладке** (печатает только имя и класс, `GenerateWbpCommandlet.cpp:5409-5410`) — заказана печать геометрии + ловля наложений, задача у cpp В ОЧЕРЕДИ после меню.

**Открытые нити:** (1) живой осмотр меню Ринатом — идёт; (2) задача Г (полоса голода) ждёт, чтобы Ринат нажал «Продолжить»; (3) повторный полный гейт qa; (4) слияние + пересборка подписанного дистрибутива. Закрыто вечером: макеты фона (выбран вариант с волком), печать геометрии в проверке окон (`a688d25`), название «Марево».

**Ждут Рината (без изменений):** GitHub Pages для политики + адрес; данные ИП; три боевых рекламных блока; русское название RuStore; аккаунт RuStore; резервная копия ключа; Telegram-адрес для «Сообщества» (пункт меню появится сам из конфига DefaultGame.ini [/Script/ContrarySurvivor.MainMenuSettings] CommunityUrl).

**📇 Контакты Рината — `.claude/rinat-contacts.local.md`** (GameDev-телефон, на нём будет Telegram-канал игры и, при необходимости, другие соцсети). Файл ТОЛЬКО на диске: закрыт правилом `*.local.md` в `.gitignore`, потому что репозиторий команды публикуется на GitHub. Номер в отчёты, документы издателю и коммиты НЕ переносить.

**⚙️ КООРДИНАЦИЯ СБОРКИ (работает):** файл-замок `.build-lock` в гейм-репо + сообщение напарнику; окно передаётся явно («окно свободно»); пак ~2.5 мин (140 с). Пак-копия pack-android: detached d7291fb + `git checkout <хэш> -- Build/Android` (A-файлы — НОРМА, не коммитить). ⛔ adb пути: MSYS_NO_PATHCONV=1 и виндовый путь назначения, иначе Git Bash коверкает /sdcard.

**Хвосты (не блокеры, без изменений):** подписанный пак не пересобран (ждёт волну); легаси-граф BP_Pistol; DDC на E:; таймаут экрана телефона 600000→вернуть 121000; красные Movement-тесты master (в прогонах 08-07/08-08 провалов 0 — вероятно неактуально, сверить на гейте).

## 🗄️ ПРОШЛОЕ (кратко; полные блоки — archive.md)
- 08-05: ревизия издателя (ADR-058) → задание Б1–Б10 (ADR-059); 24 к/с принято, оптимизацию не ведём.
- 08-06: волна Б4-довесок/Б5/Б8/Б9 влита; ключ подписи (ADR-060); AFS выключен; спека меню создана.
- 08-07 ночь: волна 6 пунктов влита (возрождение у костра ADR-061, иконка=нож, стик, WBP-экраны, имена лута, POI). 08-08 день: 4 задачи Рината закрыты (пауза/покупка в рюкзак/прокрутка/кости, до `d7291fb`); акт приёмки Б1–Б10 отдан; ADR-062 меню в первую выкладку.

**▶ УРОКИ (держать, кратко):** (а) WIP с UFUNCTION-объявлениями без тел ломает линк всегда; (б) «Target is up to date» = не собралось; (в) фоновая сборка врёт кодом — читать ЛОГ; (г) UMG-геометрия на канвас-СЛОТЕ; (д) WBP-дерево — правда в GenerateWbpCommandlet + -dumpslots (дамп работает, кириллица в логе бьётся — числа читать можно); (е) файлы с «ё» — Write целиком; (ж) мягкая ссылка на несуществующий ассет молча мертва; (з) `git fetch . branch:master`; (и) `adb pull` (не cat); (к) состояние — прямым взглядом; (л) визуальные задачи — с кадра; (м) новый ассет — по манифесту пакета; (н) телефон: `input tap/swipe`, `exec-out screencap`; (о) слот WBP проверять с обеих сторон, -verify не ловит; (п) сохранение правки экземпляра уровня — save_asset(only_if_is_dirty=False); (р) проверка «нет такого» минимум двумя способами — урок AppMetrica: искать и плагин, И java-зависимости (UPL/gradle).
**▶ ⛔ ПРАВИЛА ПЕРЕДАЧИ:** РОВНО ОДИН сборщик/редактор; пак при чистом дереве (A-файлы оверлея — норма); на блокирующий вопрос агента отвечать НЕМЕДЛЕННО; агентов разводить ПО ФАЙЛАМ; коммит только `git commit -- <пути>`.
**▶ ПЛАНОВАЯ ТАБЛИЦА:** `docs/contrary-survivor/DemoPlanTemplateRec.xlsx` — обновить ПОСЛЕ приёмки волны (скрипты `scripts/plan-table/`; только Excel COM).
- **⛔ MCP Blender/unreal-mcp в субагентов НЕ пробрасывается** (ADR-021). Blender — только modeler.

**▶ ПРАВИЛА (жёстко):** живой русский без жаргона, законченные предложения, ПИСАТЬ КОРОТКО (Ринат злится на простыни — ответ «сколько секунд какой логотип» вместо абзаца); не задавать тупых вопросов; команда делает ИНСТРУМЕНТЫ, расстановка за Ринатом, базы ВНУТРИ BP (ADR-028); параметры EditAnywhere; анимацию/визуал судит Ринат живьём.
**⛔⛔ РЕДАКТОР НЕ ОБСУЖДАТЬ** — открывать/закрывать молча. Коммитить самому. **⛔ Телефон: когда Ринат играет — не трогать** (тапы дерутся с его пальцами), журналы читать можно.

**⚙️ ВОЗМОЖНОСТИ:** файлы — все; Unreal — game-lead и unreal-operator; фолбэк headless `UnrealEditor-Cmd -run=pythonscript`; headless и редактор НЕ одновременно.

**Числа демки (утв.):** шкур 3, волков 4, бандитов 3, награды 150/250, ноут 1; Q1 «Шкуры волков» (Collect, 3, 150); Q2 «Зачистить базу бандитов» (Deliver, 3 бандита + ноутбук, 250).

## 📦 Бэклог ассетов (`assets/`, ADR-022)
- Экипировка Т0/Т1 не-блокер; iron_barrel полибюджет; сведение скелетов-дублей (ADR-053); дубль SM_Knife; SK_Elder_Head без PhysicsAsset.

## ⚡ ПОСТОЯННЫЕ ДИРЕКТИВЫ (Ринат)
- **📋 Плановая таблица** после каждой сессии; ТОЛЬКО Excel COM; статусы ✅/🔄 колонка B.
- **⛔ Фиксировать решения немедленно; перед вопросом Ринату — читать свои документы.**
- **⛔ ТЕЛЕФОН — НИКОГДА НЕ СНИМАТЬ ЭКРАН ВСЛЕПУЮ (08-09, нарушение).** Перед `exec-out screencap` и перед ЛЮБЫМ `input tap/swipe` сначала проверить, что на переднем плане именно игра: `adb shell "dumpsys activity activities | grep -m2 ResumedActivity"` (отдаёт только имя приложения, содержимое экрана не раскрывает) и `adb shell pidof com.renchezlove.contrarysurvivor`. Причина: снял экран вслепую и попал в личную переписку Рината с чужими персональными данными; файл лёг в `Screenshots/`, которая уходит в публикуемый репозиторий (удалён, в git не попал — проверено). Кадр игры просить у Рината или снимать, только убедившись, что игра на переднем плане.
- **⛔ ЯЗЫК — ПРОВЕРКА ДО ОТПРАВКИ (08-09, нарушение):** правило живого русского действует на КАЖДОЕ сообщение, включая планы и списки. Перед отправкой вычистить имена файлов, команды, ключи запуска, буквенные коды и слова-переводы («коммандлет», «ассет», «дамп», «пруф»). Извиняться и переобъяснять после отправки — уже нарушение (CLAUDE.md §0.2 п.4).
- **Язык:** простой русский; работать детально, отвечать коротко. **Самостоятельность:** действую сам, коротко постфактум.
- **⛔ ОРКЕСТРАТОР:** до 3 агентов; ставить → контролировать → проверять по РЕАЛЬНОМУ артефакту лично; самоотчёт без пруфа — возвращать; короткие проверки быстрее самому.
- **⛔ ГЛУБОКАЯ проверка:** diff и PNG смотрю сам; брак возвращаю; финал — Ринат живьём.

## Решения / Инструменты
- Все решения — `docs/decisions.md` (ADR-001..063). Headless: `UnrealEditor-Cmd "<uproject>" -ExecutePythonScript=...`; коммандлеты: `-run=GenerateWbp` (-rebuild/-verify/-dumpslots/-augment/-pickupfix/-hudslots/-unlockcaptions), `-run=PatchAnimBp`. Съёмка кадров: `scripts/shoot-game-frames.ps1`.

## Ограничения
- QA-гейт: `.claude/QA_OK` вручную ТОЛЬКО после «добро» qa. Напарники — только фича-ветки; ≤3 агента; /Content через Git LFS (ADR-019).

## РЕПО / HEAD (константы; текущее — «СЕЙЧАС»)
- **Гейм-репо** `E:/ContrarySurvior/ContrarySurvivor` (UE 5.5.4; движок E:/UnrealEngine/UE_5.5). Сборка: `Build.bat ContrarySurvivorEditor Win64 Development` при закрытом редакторе (+`-NoUBA -MaxParallelActions=6` при болячке подкачки).
- **Пак-копия** `E:/ContrarySurvior/pack-android` (worktree; мне — только git-команды). Пак: `RunUAT.bat BuildCookRun -project=.../pack-android/ContrarySurvivor.uproject -platform=Android -cookflavor=ASTC -clientconfig=Development -build -cook -stage -pak -package -archive -archivedirectory=.../ArchivedBuilds -unattended -noP4`. Дистрибуция = Shipping + `-distribution` + подпись (пароль вносит Ринат).
- **Team-репо** `E:/game-dev-team`: ветка `docs/phase0-gdd`.
- **📱 ANDROID:** adb = `E:/Android/sdk/platform-tools/adb.exe`; телефон 720x1600, плотность ровно xhdpi (320); установка `adb install -r` с ВИНДОВЫМ путём; журнал Development: `/sdcard/Android/data/com.renchezlove.contrarysurvivor/files/UnrealGame/ContrarySurvivor/ContrarySurvivor/Saved/`; Shipping пишет во внутреннюю память.

## 🎮 DEBUG/ТЕСТ-КЛАВИШИ (LogQA; в Shipping НЕ существуют)
F1 debug-камера · F2 GiveTestItems · F3/F4 броня · F6 use · F7 drop · F9/F10 buy/sell · F12 clear-save · M +100 денег · T телепорт-торговец · Y телепорт-староста · G offer+accept квест · H сдать квест · K +1 волк · J god · U force-drop · B тест-волк · O QA-оверлей · N force-kill · V Логово · Z база · C 5 шкур · X ноутбук · P убить игрока · F реклама-порог. **(F5/F8/F11 движковые — НЕ занимать.)**

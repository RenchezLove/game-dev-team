# context: game-lead

> Персональная (живая) память агента `game-lead`. Грунтовка сессии — ТОЛЬКО блок «🧭 СЕЙЧАС» ниже (правило bootstrap CLAUDE.md). Прошлое — `archive.md` (read-on-demand). Решения — `docs/decisions.md` (ADR). История — git. Дизайн-правда — `docs/contrary-survivor/`.
> СТОП-правило гигиены: >150 строк ИЛИ Read обрезается → СНАЧАЛА чистка (вынос закрытого в archive.md, удаление перетёртого), потом работа. Дистилляция ВЫРЕЗАЕТ прошлый «СЕЙЧАС», не только дописывает.

## 🧭 СЕЙЧАС (2026-08-06, вторая половина дня — Б5/Б8/Б9 добиты, Shipping-пак собран)

**ФАЗА:** доводка перед первым выходом на RuStore, задание издателя Б1–Б10 (ADR-059). Ветка `feature/build122`, ЗАПУШЕНА (HEAD `7cf4f01` = origin), в master не вливалось.

**ЗАКРЫТО СЕГОДНЯ ЛИЧНО (сборка `Saved/b122-wave3-build2.log` 0 ошибок; тесты 65/0 `Saved/b122-wave3-tests2.log`):**
- Незакоммиченные правки исполнителей прошлой волны прочитаны диффом, добиты и закоммичены: `0fb0a54` Б5-код (отладочный харнесс не компилируется в Shipping, выключатель `CONTRARY_WITH_QA_CHEATS`; адрес «Написать мне» в `DefaultGame.ini`), `ca9ee8e` Б4-довесок («обучение пройдено» без подсказки смерти — тела функций дописал сам, исполнитель их только объявил), `bdc6447` Б8 (колонки магазина по живой ширине), `2e2ffb4`+`179664a` Б9 (панель статов в SafeZone: генератор + пересобранный ассет, строка «SAFEZONE … завёрнут» в `Saved/b122-wave3-augment1.log`).
- **Ключ подписи RuStore создан** (одобрение Рината «делай ключ») — ADR-060: `E:/game-dev-team/keys/contrarysurvivor-release.keystore` + пароль рядом, в git не попадает. Путь/псевдоним в конфиге игры (`7c378e7`).
- **AFS (служба файлов) выключена целиком** (`7c378e7`) — требование издателя + чинит падение Shipping-пака (ADR-060 п.3). Лишнее разрешение чтения хранилища убрано из обвязки GameAnalytics (`7cf4f01`).
- **Shipping-пак 3 собран** (`Saved/b122-shipping-pack3.log`, `pack-android/ArchivedBuilds/Android_ASTC/ContrarySurvivor-Android-Shipping-arm64.apk`, 200 МБ). Проверено aapt по манифесту: службы файлов НЕТ, чтения хранилища НЕТ. Флаг отладки ещё true — гасится ТОЛЬКО флагом `-distribution` (не режимом Shipping!), финальный пак = Shipping + `-distribution` + подпись.

**НЕ ПРОВЕРЕНО:** живой запуск Shipping-пака на телефоне (телефон не подключён); экран согласия Б6 (`bc39673`, работа cpp-3) — код закоммичен и собирается, живьём никто не видел; дистрибуционный пак с подписью.

**▶ ЖДЁТ РИНАТА:** (1) подключить телефон по USB; (2) GitHub-репозиторий + Pages для `docs/contrary-survivor/privacy/index.html`, дать адрес; (3) данные ИП и почта для политики; (4) три боевых рекламных блока (Б10, список `docs/contrary-survivor/reklama-kabinet-spisok.md`); (5) русское название для RuStore; (6) резервная копия ключа подписи (два файла из `keys/`); (7) перед дистрибуционным паком — строка `KeyStorePassword=<из password.txt>` в `pack-android/Config/DefaultEngine.ini` руками ИЛИ дать мне `/add-dir E:\ContrarySurvior\pack-android`.

**▶ СЛЕДУЮЩИЙ ШАГ:** телефон появился → `adb install -r` пак 3 → живая проверка: экран согласия при первом старте, ссылка на политику, магазин (растяжка сетки/колонки/подписи), полосы статов не под вырезом, экран смерти, отсутствие отладочных клавиш и счётчиков на экране → дистрибуционный пак с подписью → qa-гейт всей ветки → мерж в master.

**⚠️ УРОКИ 08-06 (вторая половина):**
- **Классификатор оболочки блокирует запись секретов в файлы И правки вне рабочих папок** (`pack-android` мне недоступен). Не обходить; путь: правка в основном репо → commit → `git checkout` в сборочной копии (git-команды разрешены). Секреты вносит Ринат руками.
- **Shipping ≠ дистрибуция:** `debuggable` гасится только `-distribution` (`UEDeployAndroid.cs:2893`); подпись релизным ключом — тоже только с ним.
- **Объявление в заголовке без тела линкуется, пока функцию никто не зовёт** — недоделку исполнителя ловить грепом ТЕЛ функций, а не фактом зелёной сборки.
- Слово «error» в грепе лога сборки может быть флагом `-WarningsAsErrors` — смотреть саму строку.

## 🗄️ ПРОШЛОЕ (кратко; полные блоки 08-02…08-05 — в archive.md)
- 08-05: ревизия издателя (ADR-058) → задание Б1–Б10 (ADR-059). Б1/Б2/Б3/Б7/Б9 закрыты с пруфами; узкое место кадров — видеочип, Ринат принял 24 к/с, оптимизацию НЕ ведём. Б4 аналитика: ключи вшиваются при сборке из `E:/game-dev-team/keys` (`b98ed0d`); живая доставка событий НЕ проверена.
- Тексты Б6 (согласие + политика) написаны лидом: `docs/contrary-survivor/soglasie-i-politika.md`, страница `docs/contrary-survivor/privacy/index.html` (team-репо `5d484c9`, `323b3d2`).

**▶ УРОКИ ПРОШЛЫХ СЕССИЙ (держать, кратко):** (а) наработки агентов спасать WIP-коммитами по точным путям; (б) «Target is up to date» = ничего не собралось; (в) проверять, что новые .cpp реально компилировались (строки Compile/Link); (г) фоновая сборка врёт кодом возврата — читать ЛОГ; (д) UMG-геометрия живёт на канвас-СЛОТЕ; (е) дерево WBP из Python не читается — правда в `GenerateWbpCommandlet.cpp` + `-dumpslots`; (ж) файлы с «ё» — Write целиком, Edit спотыкается; (з) мягкая ссылка на несуществующий ассет молча мертва; (и) `git fetch . branch:master` — мерж без переключения; (к) `adb shell cat` ломает кириллицу — только `adb pull`; (л) состояние проверять прямым взглядом, не косвенной строкой лога; (м) визуальные задачи начинать с кадра на согласование; (н) PS1 с кириллицей — только с BOM; (о) новый ассет из кода проверять по манифесту пакета (ADR-057); (п) `.build-lock` работает; правку общего `Build.cs` проверять сборкой сразу; (р) телефоном управлять с компьютера: `adb shell input tap/swipe`, `exec-out screencap`; отладочные клавиши через `input keyevent` НЕ доходят.

**▶ ⛔ ПРАВИЛО ПЕРЕДАЧИ ПРОЕКТА:** одновременно РОВНО ОДИН сборщик/редактор; сборка пакета — только при чистом дереве.
**▶ ⛔ УРОК 07-20:** на блокирующий вопрос агента отвечать НЕМЕДЛЕННО; разводить агентов ПО ФАЙЛАМ; требовать «взял, делаю» и номер коммита.
**▶ ПЛАНОВАЯ ТАБЛИЦА:** `docs/contrary-survivor/DemoPlanTemplateRec.xlsx` — обновить ПОСЛЕ приёмки волны (скрипты `scripts/plan-table/`). Внесено по 1.2.1 включительно.
- **⛔ MCP Blender/unreal-mcp в субагентов НЕ пробрасывается** → modeler и оператор ходят headless/своим каналом (ADR-021).

**▶ ПРАВИЛА (жёстко):** живой русский без жаргона (№0.2-1); законченные предложения (№0.2-3); не задавать тупых вопросов (№0.2-2); команда делает ИНСТРУМЕНТЫ, расстановку Ринат сам, базы/пропсы ВНУТРИ BP (ADR-028); все параметры EditAnywhere + наверх Details; анимацию/визуал судит Ринат вживую; эскалация ручного действия после ~2-3 попыток.
**⛔⛔ РЕДАКТОР С РИНАТОМ НЕ ОБСУЖДАТЬ** — открывать/закрывать молча. Полная автономия: сам открываю/закрываю редактор, запускаю помощников, беру разумные дефолты. Коммитить самому («не еби мозги с коммитами», 08-06).

**⚙️ ВОЗМОЖНОСТИ КОМАНДЫ:** файлы — все роли сами; Unreal — game-lead и unreal-operator; фолбэк всегда headless `UnrealEditor-Cmd -run=pythonscript`; headless и открытый редактор НЕ одновременно; Blender — только modeler headless.

**Числа демки (утв.):** шкур 3, волков 4, бандитов 3, награды 150/250, ноут 1; Q1 «Шкуры волков» (Collect, 3, 150); Q2 «Зачистить базу бандитов» (Deliver, 3 бандита + ноутбук, 250).

## 📦 Бэклог ассетов (`assets/`, ADR-022)
- Экипировка Т0/Т1 — доводка не-блокер; iron_barrel полибюджет (отложено); сведение двух скелетов-дублей + переезд боевого скелета (ADR-053); дубль SM_Knife; SK_Elder_Head без PhysicsAsset; красные Movement-тесты на master (не расследовано).

## ⚡ ПОСТОЯННЫЕ ДИРЕКТИВЫ (Ринат)
- **📋 ПЛАНОВАЯ ТАБЛИЦА (07-02):** веду я, после каждой сессии; ТОЛЬКО Excel COM; статусы ✅/🔄 колонка B Segoe UI Emoji; после правок Рината — только добавлением.
- **⛔ Фиксировать решения немедленно (ADR/GDD/контекст); перед вопросом Ринату — читать свои документы (06-24).**
- **Язык и длина (06-24):** простой русский; работать детально, отвечать коротко.
- **Самостоятельность (06-24):** действую сам, оповещаю коротко постфактум; Ринату — только приёмка и истинные дизайн-развилки.
- **⛔ Я — ОРКЕСТРАТОР (06-24):** до 3 агентов; ЦИКЛ: ставить → контролировать → проверять по РЕАЛЬНОМУ артефакту лично. Самоотчёт без пруфа — возвращать. Короткую проверку фактов быстрее делать самому.
- **⛔ ГЛУБОКАЯ проверка (06-24):** diff читаю сам, PNG открываю сам; брак возвращаю. Финал — Ринат в PIE.
- **ПРУФ:** «собралось/прошло» — только с логом; «закоммичено» — только после git status/log.

## Решения / Инструменты
- Все решения — `docs/decisions.md` (ADR-001..060). Headless: `UnrealEditor-Cmd "<uproject>" -ExecutePythonScript=...`; коммандлеты: `-run=GenerateWbp` (-rebuild/-verify/-dumpslots/-augment/-pickupfix), `-run=PatchAnimBp`. Съёмка кадров: `scripts/shoot-game-frames.ps1`.

## Ограничения
- QA-гейт: маркер `.claude/QA_OK` создаю вручную ТОЛЬКО после «добро» qa. Напарники — только фича-ветки; merge в master после QA; ≤3 агента; /Content через Git LFS (ADR-019).

## РЕПО / HEAD (константы; текущее — «СЕЙЧАС»)
- **Гейм-репо** `E:/ContrarySurvior/ContrarySurvivor` (UE 5.5.4), origin RenchezLove/ContrarySurvivor. Сборка: `Build.bat ContrarySurvivorEditor Win64 Development` при закрытом редакторе.
- **Пак-копия** `E:/ContrarySurvior/pack-android` (worktree, detached; мне НЕДОСТУПНА на запись — только git-команды). Пак: `RunUAT.bat BuildCookRun -platform=Android -cookflavor=ASTC -clientconfig=<...> -build -cook -stage -pak -package -archive`; env JAVA_HOME/ANDROID_HOME/NDKROOT уже в системе. Повторный пак ~5-8 мин.
- **Team-репо** `E:/game-dev-team`: ветка `docs/phase0-gdd`.
- **📱 ANDROID:** adb = `E:/Android/sdk/platform-tools/adb.exe`; установка `adb install -r` с ВИНДОВЫМ путём; журнал/сейв: `/sdcard/Android/data/com.renchezlove.contrarysurvivor/files/UnrealGame/ContrarySurvivor/ContrarySurvivor/Saved/`.

## 🎮 DEBUG/ТЕСТ-КЛАВИШИ (LogQA-строки; в Shipping НЕ существуют — CONTRARY_WITH_QA_CHEATS)
F1 debug-камера · F2 GiveTestItems · F3/F4 броня вкл/выкл · F6 use · F7 drop · F9/F10 buy/sell · F12 clear-save · M +100 денег · T телепорт-торговец · Y телепорт-староста · G offer+accept квест · H сдать квест · K +1 волк · J god+заморозка статов · U force-drop 100% · B тест-волк вплотную · O QA-оверлей · N force-kill ближнего · V телепорт-Логово · Z телепорт-база · C выдать 5 «Шкур волка» · X выдать «Ноутбук» · P убить игрока · F реклама: добить счётчик до порога. Магазин: стрелки/колесо ±1, Shift ±10, Enter подтвердить. **(F5/F8/F11 движковые — НЕ занимать.)**

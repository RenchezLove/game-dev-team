# context: game-lead

> Персональная (живая) память агента `game-lead`. Грунтовка сессии — ТОЛЬКО блок «🧭 СЕЙЧАС» ниже (правило bootstrap CLAUDE.md). Прошлое — `archive.md` (read-on-demand). Решения — `docs/decisions.md` (ADR). История — git. Дизайн-правда — `docs/contrary-survivor/`.
> СТОП-правило гигиены: >150 строк ИЛИ Read обрезается → СНАЧАЛА чистка (вынос закрытого в archive.md, удаление перетёртого), потом работа. Дистилляция ВЫРЕЗАЕТ прошлый «СЕЙЧАС», не только дописывает.

## 🧭 СЕЙЧАС (2026-06-25, конец сессии — доводка Фазы B по PIE-фидбеку)
**▶ ФАЗА A ЗАКРЫТА. ФАЗА B (внешний вид демки) — доводка по PIE-фидбеку Рината В ОСНОВНОМ ЗАВЕРШЕНА и ПРИНЯТА Ринатом живьём; остался 1 пункт (дальность отрисовки) + финал-merge.** Дизайн-правда — `docs/contrary-survivor/`; ADR — `docs/decisions.md`.

**▶ ИНФРА (стартовая точка след. сессии):** гейм-репо `E:/ContrarySurvior/ContrarySurvivor` (внимание: папка «ContrarySur**v**ior»), ветка `feature/clean-village`, HEAD `0e2660c` (checkpoint доводки), **рабочее дерево ЧИСТОЕ**, **master `627d243` НЕ тронут**. team-репо `E:/game-dev-team` ветка `docs/phase0-gdd`. **РЕДАКТОР ЗАКРЫТ** в конце сессии. Старт: открыть редактор → `/mcp` → `set_unreal_project_path(".../ContrarySurvivor.uproject")` + `set_unreal_engine_path("E:/UnrealEngine/UE_5.5")`. (Закрытие редактора под C++-сборку ВСЕГДА роняет MCP-мост → нужен `/mcp` после переоткрытия — это нормально, не провал.)

**✅ СДЕЛАНО ЭТУ СЕССИЮ (доводка, ВСЁ PIE-принято Ринатом; пруф = коммит `0e2660c` + C++ `f8c82ee`/`1645bd9`):**
- Спавн: 7 точек/базу + `NumToSpawn` (EditAnywhere; BP_WolfDen=4, BP_BanditBase=3), радиус активации 2300 (ADR-029). Сфера ActivationRadius во вьюпорте + наши UPROPERTY наверх Details (DisplayPriority). C++ `f8c82ee`.
- Кровь-декали: органическая форма (корень бага — проектор бил вскользь; фикс = pitch −90 вниз + тонкий `decal_size.x=10`), 3 варианта `MI_BloodSplat_01..03`, не красят проходящих. Переиспользуемый `BP_BloodDecal` (`/Game/Environment/Decals`).
- Костёр: меш `SM_Campfire` (456 трис, modeler) + сокет FireVFX; `BP_Campfire` репарентнут в C++ `ACampfire` (cpp-2 `1645bd9`) — SafeZoneRadius двигает сферу (фикс бага №6), параметры+свет наверх Details. VFX огня = `M_Flame` (translucent unlit, crossfade flame-01↔02) на 2 скрещённых плашках. **Ринат принял огонь («отлично»).** NB: на размещённом костре завис 1 лишний компонент-плашка (безвреден; в BP — 2; уйдёт при перезапуске редактора).
- Навмеш: статика на ВЕСЬ уровень (актор `NavMeshBounds_Level`, ±70м, RuntimeGeneration=STATIC). Инвокер-код в базах НЕ удалял (фолбэк). При росте мира (ADR-016, отложен) — увеличить объём / перейти на динамику.
- Земля: материал `M_Ground` (трава, мировой тайлинг, roughness 0.8) на большой пол `GroundBase` (бывш. TestFloor, ±80м, коллизия) = трава по всей карте + базы на грунте; грид `VisualGround` (`M_GroundBlend_WIP`, дорога деревни = vertex-paint, КРАСНЫЙ канал) — деталь поверх. Ринат принял. (Дороги были на голом сером `BasicShapeMaterial` — починено.)
- Обрезка (cull) первый проход (foliage 55-70м, дома 90м) — будет ЗАМЕНЕНА единым параметром (см. ниже п.1).

**⏳ ОСТАЛОСЬ по Фазе B (СЛЕД. СЕССИЯ, по порядку):**
1. **Дальность отрисовки** — код cpp-3 НАПИСАН и закоммичен в `0e2660c` (`PlayerCharacter.h/.cpp`), но **НЕ СОБРАН (WIP)**. Содержимое: `RenderDistance`(см, дефолт 7000) EditAnywhere+DisplayPriority в `APlayerCharacter` + сфера `RenderRadiusVisualizer`(бирюзовая, HiddenInGame, OnConstruction-синхрон) + `BeginPlay→ApplyRenderDistance()` (всем мешам SetCullDistance, foliage HISM SetCullDistances). → НАДО: закрыть редактор → `Build.bat ContrarySurvivorEditor Win64 Development` → переоткрыть + `/mcp` → проверить поле+сферу в BP игрока, дефолт 70м → PIE Рината. Ринат хочет видеть сферу и в BP, и на уровне (поставит игрока на сцену).
2. **Финал Фазы B = «ЛЁГКИЙ ГЕЙТ»** (РЕШЕНИЕ Рината 06-25): формальный qa-агент НЕ гоняем (Ринат всё принял в PIE); вместо — ОДНА чистая сборка ветки + финальный PIE → **merge `feature/clean-village`→master**. (qa «сломать» не может — только читает/собирает; Ринату объяснено.) Маркер `.claude/QA_OK` НЕ нужен в этом сценарии (гейт-хук — в team-репо; гейм-репо политику веток держу вручную).
3. **Push** `feature/clean-village` на origin (офсайт-бэкап) — **НЕ ПРОВЕРЕНО, запушена ли**; проверить/запушить.

**▶ ПРАВИЛА (жёстко, 06-25):** живой русский без жаргона (№0.2-1); не задавать тупых вопросов (№0.2-2); команда делает ИНСТРУМЕНТЫ, расстановку Ринат сам, базы/пропсы ВНУТРИ BP (ADR-028); ВСЕ наши параметры — EditAnywhere + наверх Details + влияют на визуал у размещённого экземпляра (директива ниже); тонкую визуальную доводку, ОСОБЕННО АНИМАЦИЮ (её в скриншоте НЕ видно — судит Ринат вживую), готовлю как инструмент, оценку даёт Ринат; эскалация простого ручного действия после ~2-3 попыток.

**▶ УРОКИ 06-25 (чтобы не повторять):**
- **Анимацию/тонкий визуал в дневной сцене скриншотом НЕ оценить** — судит Ринат вживую (Realtime во вьюпорте / PIE). Аддитивное пламя днём «вымывается» → translucent+emissive читается лучше.
- **Материал через Python — ВЕРИФИЦИРОВАТЬ кадром:** flipbook-узел сэмплил криво (мелкое пламя) → одиночная текстура решила; декаль-проектор бил вскользь (прямоугольник) → pitch −90 вниз + тонкий size.x. Не считать материал готовым без осмотра рендера.
- **BP↔инстанс рассинхрон:** удаление/замена компонента в BP не всегда чисто доходит до размещённого инстанса (зависал старый компонент; `set_material` на старом не прижился). СВЕЖИЙ add компонента берёт материал надёжнее; проверять на ИНСТАНСЕ (`get_material`), не на BP.
- **Не переусложнять очевидное** (Ринат: «переусложнил» про траву под базы): очевидное делать сразу, не оформлять как развилку А/Б (№0.2-2).

**⚙️ ВОЗМОЖНОСТИ КОМАНДЫ — ВСЕГДА ПОМНИТЬ; НЕ ПИСАТЬ РИНАТУ «НЕ МОГУ», КОГДА МОГУ (Ринат, 06-24, жёстко):**
- **Файлы:** game-lead и профильные напарники (cpp/unreal/modeler/concept/qa) сами читают / СОЗДАЮТ / редактируют / удаляют файлы (Read/Write/Edit + Bash). В своих рабочих папках — без спроса.
- **Unreal — РАБОТАЕМ ОБА, РУКИ ЕСТЬ (Ринат, 06-24, ЖЁСТКО — хватит ныть про коннект):** в UE умеют работать И game-lead, И `unreal-operator` (`unreal-operator` ПРЕДПОЧТИТЕЛЕН — его роль). Механизмы: (1) game-lead — `mcp__unreal__*` (editor_run_python, editor_get_world_outliner, editor_create/update/delete_object, editor_take_screenshot, editor_get_map_info и т.д. — они В моём наборе); (2) **headless через Bash — `UnrealEditor-Cmd.exe <uproject> -run=pythonscript ...`** (доступно ОБОИМ, в т.ч. unreal-operator у которого есть Bash). ⚠️ MCP-канал требует, чтобы UE-MCP-сервер был ПОДКЛЮЧЁН (редактор открыт ДО старта сессии ЛИБО /mcp-реконнект). Если `editor_*` вернул «No such tool» — это НЕ «не могу», а «канал не поднят» → варианты по порядку: (а) пойти headless-коммандлетом через Bash; (б) открыть редактор ДО старта сессии; (в) попросить Рината /mcp. ⚠️ headless-коммандлет и открытый интерактивный редактор НЕ должны одновременно писать один .umap (file lock) — делает что-то одно. **НИКОГДА не отвечать Ринату «не могу работать в UE» — НАЙТИ рабочий механизм (их несколько).**
  - **⛔ ГЛАВНОЕ (Ринат, 06-24, ОРАЛ — больше НЕ повторять): у меня ЕСТЬ руки и полномочия САМ открывать/закрывать редактор и держать MCP-канал живым. НЕ дёргать Рината на коннект. ПРАВИЛО: открывать редактор В САМОМ НАЧАЛЕ сессии (или держать открытым), set-пути выставить → канал живой сразу, `/mcp` не нужен вообще. Просьба к Ринату про /mcp = провал планирования, которого я ОБЯЗАН избегать (открыл редактор заранее — и нет проблемы). Подтверждено вживую 06-24: канал ожил, я сам гнал editor_run_python, создал/одел/расставил акторов.**
  - **✅ ЧЕКЛИСТ ПОДЪЁМА MCP-КАНАЛА К UE (Ринат, 06-24, ПРОВЕРЕНО — записать и НЕ тупить впредь):** 1) сервер UE-MCP подключён, если `mcp__unreal__*` отвечает СЕРВЕРОМ (а не «No such tool»); «No such tool» по нескольким именам = сервер не подключён → Ринат делает `/mcp`. 2) **Выставить пути СВОИМИ инструментами:** `set_unreal_project_path("E:/ContrarySurvior/ContrarySurvivor/ContrarySurvivor.uproject")` + `set_unreal_engine_path("E:/UnrealEngine/UE_5.5")` — иначе «Unreal Project/Engine path is not set». 3) Если после set-путей всё ещё «No command channel open» — **редактор стартовал РАНЬШЕ, чем поднялся MCP-сервер → его мост `rrmcp` не сконнектился → ПЕРЕЗАПУСТИТЬ редактор** (taskkill + запуск; стартует при живом сервере → мост коннектится). Признак готовности: `LogPython: rrmcp:init` в логе И `editor_project_info` отвечает. 4) Идеально — поднять MCP/открыть редактор ДО старта рабочей сессии. Фолбэк всегда — headless `UnrealEditor-Cmd.exe -run=pythonscript` (канал не нужен).
- **Blender:** драйвит `modeler-3d` через `mcp__blender__*` (его эксклюзив, CLAUDE.md). game-lead может запустить процесс Blender, но УПРАВЛЯЕТ Blender через `modeler-3d` (своего `mcp__blender__*` у game-lead НЕТ — проверено).
- **Правило:** «не могу» Ринату — ТОЛЬКО если реально проверил и подтвердил блокер артефактом. По умолчанию — МОГУ и ДЕЛАЮ, без нытья и клянченья.
**Где стоим (06-24, вечер): ФАЗА A в работе**, ветка `feature/clean-village` (реорг готов, `678e703` на origin).
- **A2 (торговец→интерфейс `IShopVendor`) ✅ C++** — сборка `BUILD_EXIT=0` (лог `context/cpp-dev/logs/a2-shopvendor-build.log`), `ATraderNPC` удалён, `FShopEntry`→`ShopTypes.h`, PC/HUD на `TScriptInterface<IShopVendor>`, `AMasterTrader` реализует+overlap. Контракт: GetCatalog/GetSellValue/GetAmmoSellPerRound.
- **A3 (староста `AElderNPC`→`AMasterHumanoidCharacter`) ✅ C++** — сборка `BUILD_EXIT=0` (`a3-elder-build.log`), AutoPossessAI=Disabled, ABP-finder удалён, A1 вшит (**шкуры 5→3**, `ElderNPC.cpp:54`).
- **Editor-расстановка A2+A3 ✅ сделал САМ в живом UE через `mcp__unreal__editor_run_python`**: создал `BP_Elder`, заменил placed-старосту на `BP_Elder`(450,600,91), поставил `BP_Trader`(−450,600,91) [старый transform торговца потерян с missing-class — поставил симметрично, Ринат может подвинуть], ОБА одел инстансно (Head/Torso/Legs из Shared/Humanoid + ABP). Уровень сохранён. Пруфы: мой editor-дамп (trader1/elder1/total13) + скрин (оба видны) + греп `unreal` по `L_World.umap` (0×TraderNPC, оба BP + меши+ABP сериализованы).
- **✅ A2+A3 ПРИНЯТЫ РИНАТОМ В ЖИВОМ PIE (06-24, авторитетно):** оба NPC полностью видимы (серый базовый гуманоид, без брони — ОЖИДАЕМО, одежда=C4), квесты беруться И сдаются, предметы покупаются И продаются. Правки **НЕ закоммичены** (рабочее дерево) — рекомендую checkpoint-коммит перед рискованным A5.
- **A5 (sub→`AMasterEnemyBase`) ✅ ГОТОВ + PIE-ПРИНЯТ (06-24):** C++ собран+закоммичен (`926d88f`); сабсистемы Wolf/Bandit + debug V/Z + QAChaseTest выпилены; editor-расстановка (BP_Wolf + BP_WolfDen[4 волка, север 0,+3500] + BP_BanditBase[3 бандита+ноутбук, юг 0,−3500]) сделана headless и закоммичена. Ринат в PIE: «квесты работают». **ФАЗА A почти завершена — остался только A4.**
- **A4 (штраф смерти) — реализован, идёт ПРАВКА на БЕЗУСЛОВНЫЙ штраф (Ринат 06-25):** штраф применяется ВСЕГДА при любой смерти (НЕ гейтить по safe-зоне — мою отсебятину «вне деревни» Ринат отменил; cpp убирает гейт bDiedOutsideVillage, попап+штраф всегда). −40% денег ПОСЛЕ LoadGame+пере-сейв; дроп ВСЕХ неэкип. Consumable одним возвратным мешком (APickup); квест/экип целы; попап вар.В системно «ВЫ ПОГИБЛИ»+череп, «монеты», на DrawDeathScreen. Костёр=сейв-точка (автосейв <3м; меню сохранения позже). «Safe-зона»=куда не заходят враги=будущая фича (не штраф). Фикс ADR-027/GDD §7.8 записан. ⚠️ Серые NPC = БАГ битого материала (меши→None; реальный /Characters/Shared/Humanoid/Material), НЕ «задумано»; чинить в editor-сессии Stage-B (headless-set не прижился).
- **Дом (C1):** геометрия фикс (Вар.1 крыша + стены-щели закрыты) + экспорт `SM_VillageHouse.fbx` (820 трис, нормали наружу) ✅; сейчас modeler красит vcol под концепт; реимпорт в UE — я сам после покраски.
- **РЕДАКТОР ОТКРЫТ** (PID 13700, MCP-канал живой — Ринат сделал /mcp т.к. я открыл редактор в середине сессии; ВПРЕДЬ открывать ДО старта). НЕ закрывать, пока идёт PIE; перед сборкой A5 — ЗАКРЫТЬ.
**✅ РЕШЕНИЯ Рината 06-24 (зафиксировано, не терять):** A4-штраф = −40% ОТ ДЕНЕГ ИГРОКА; дроп ТОЛЬКО расходников (Consumable); дроп = ронять МЕШКОМ на месте смерти, НЕ удалять; UI-попап нужен — но ТЕКСТ ещё не дан (подготовить черновик на утверждение). debug-телепорты V/Z = ВЫПИЛИТЬ (Ринат тестит лично, не сборщик).
**⏳ ОТКРЫТО (на потом, не блокирует уборку):** (1) тех-долг сейва (ItemName/StackCount) — чинить в A4 vs отложить к DataTable E2 (моя реком. — отложить); (2) текст UI-попапа штрафа; (3) броня — канон `_01` (код грузит) vs `_02` (орфан) — спросить Рината при дочистке.
**УБОРКА РЕОРГА — ГОТОВА (06-24), коммит `678e703`, влита в `feature/clean-village`.** Убраны ВСЕ 7 заглушек-редиректоров (BP_Campfire, GameMode, BP_Pistol, 3×Misc-меша, BP_EnemyBandit); удалена лишняя копия `Weapons/Pistol/SM_Pistol` (код берёт корневой `Weapons/SM_Pistol`); путь бандита `BanditSpawnSubsystem.cpp:21` → `/Game/Characters/Bandit/BP_EnemyBandit` (сборка PASS, бандиты спавнятся PIE-подтв.); папка `Blueprints` удалена; ссылки пересохранены (BP_PlayerCharacter, Archive-карта); мусор + пустые служебные папки вычищены. GameMode НЕ застревал (в System/). ОСТАВЛЕНО НАМЕРЕННО: броня `_02` (орфан, Ринату неизвестна — НЕ трогать); скелет (см. ниже). ⚠️**УРОК 06-24: git checkout/merge/branch-switch — ТОЛЬКО при ЗАКРЫТОМ редакторе** (открытый лочит .uasset → half-switch + залип BP_PlayerCharacter; чинил `git checkout -- <file>` + ff-merge; `git reset --hard` запрещён deny-правилом). **СКЕЛЕТ: 2 идентичные копии (глюк-сессии пару дней назад, НЕ дизайн). Из кода НЕ сводится (скелет read-only у анимаций/мешей; `consolidate_assets` no-op). Способ — в редакторе «Replace References» (ПКМ по копии→оригинал). Сделать ТИХО при случае. НЕ грузить Рината, НЕ лекторствовать. На демку не влияет.**
**⚠️ УРОКИ 06-23 (жёстко):** (1) **память ≠ закон** — на «где мы / что с X» СНАЧАЛА сверка с `decisions.md`/диздоком/git/диском, ПОТОМ ответ (сегодня дважды выдал устаревшее по памяти: «жду решение по скелету» — решён ADR-018; «реорг не закоммичен» — а он `a69dc68`). (2) Итог PIE-проверки Рината фиксировать СРАЗУ (утренний результат был потерян). (3) НЕ эскалировать уже решённое. (4) Отвечать КОРОТКО.

## 🎯 ДЕМКА — план фазы A (источник: `docs/contrary-survivor/demo-plan.source.md`; HTML/PDF устарели. Разведка cpp 06-23, пруфы файл:строка → `context/cpp-dev/context.md`)
- **Этап 0 ✅ ПРОЙДЕН** — ядро петли работает (PIE 06-23). Отдельного «Этапа 0 на болванках» НЕТ.
- **A1 числа:** ТОЛЬКО 2 правки — шкуры 5→3 (`ElderNPC.cpp:96`), волки 5→4 (`WolfSpawnSubsystem.h:68`); вшить В A3/A5 (старые места переписываются/удаляются). Бандиты/цель/награды уже верны. DataTable НЕ заводить.
- **A2 торговец:** рабочий магазин держит СТАРЫЙ `ATraderNPC` (PC/HUD кастуют `Cast<ATraderNPC>`, `PC.cpp:1343`/`HUD.h:116`); `BP_Trader` открыть НЕ может (overlap не регистрируется, `MasterTrader.cpp:120`). → `FShopEntry`/`EShopEntryKind` в нейтр.заголовок (3 потребителя!) → `IShopVendor` → PC+HUD через интерфейс → `AMasterTrader` реализует+регистрируется → удалить `ATraderNPC`. Editor: поставить `BP_Trader`, снять старый placed `ATraderNPC`.
- **A3 староста:** `AElderNPC:AActor` → `AMasterHumanoidCharacter` (образец `MasterTrader.cpp:23-66`); квест-данные/overlap переносятся; убрать C++ ABP-finder. Риск: капсула станет ТВЁРДОЙ (проверить проход); НЕ блокировать `ECC_Visibility`, без StatsComponent, `AutoPossessAI=Disabled`.
- **A5 спавн:** `WolfSpawnSubsystem`(5 волков, логово +Y3500) + `BanditSpawnSubsystem`(3 бандита+ноутбук, база −Y3500) → один размещаемый `AMasterEnemyBase` + `BP_WolfDen`/`BP_BanditBase` + `BP_Wolf:AWolfCharacter`; перенести навмеш-проекцию/floor-trace/SpawnDelay/спавн ноутбука; подсистемы удалить. ⚠️ debug V/Z/`cs.TestWolfChase` (`PC.cpp:465,505,563`) завязаны на подсистемы — переписать/удалить, иначе сборка ломается.
- **A4 сейв/респаун:** квест-предметы УЖЕ защищены (Quest-категория); сейв у костра ОК; НО −40% денег НЕТ, текущий штраф УДАЛЯЕТ 25% неэкип. расходников/ресурсов (не дроп), попапа нет. → ждёт 4 решений Рината (см. СЕЙЧАС).
- **Порядок внутри A:** A2→A3→A5 (числа в A3/A5); A4 параллелит (ждёт решений). C++-правка=headless-сборка с логом (cpp); BP/расстановка=unreal-operator; финал=PIE Рината.
**РЕЖИМ:** доверие просело (06-16). **НИЧЕГО без явной команды.** **КОРОТКО** (портянки бесят). Визуал/PIE — Ринат сам. **Коммит — только по запросу.**

**🎯 ЗАДАЧА — демка ContrarySurvivor** (гейм-репо, ветка `feature/clean-village`). План: `docs/contrary-survivor/demo-plan.{source.md=ИСТОЧНИК, html, pdf}` — этапы 0–F + G. ⚠️ **PDF/HTML УСТАРЕЛИ** относит. source.md (правки A2/C3 + статус §9 после рендера) → перерендерить (concept-artist) при возобновлении.

**✅ ФАКТ от Рината (PIE 06-23, утро) — фиксирую, чтобы не потерять снова:** квесты (2 шт.) + оружие (пистолет/нож) + сейв/смерть/респаун — **РАБОТАЮТ в живом PIE** (слово Рината = авторитетный живой источник). НО реализация «чисто на C++» → **ПЕРЕВЕСТИ на принятую архитектуру = Этап A** (BP-наследники C++ ADR-024; `IShopVendor`; NPC староста/торговец → `AMasterHumanoidCharacter`; сабсистемы → `AMasterEnemyBase`; DT_Quests/DT_Items). ⚠️ Артефакт `Screenshots/1/1.png` (06-23 00:18) показывал «Failed to spawn player controller» — ПРОВЕРИТЬ, что больше не воспроизводится. **Магазин торговца ТОЖЕ открывается/работает (Ринат, 06-23).** ❌ Отдельного «Этапа 0 на болванках/старых C++-сабсистемах» НЕТ — старт работы = **Этап A (перевод на архитектуру)**: зоны спавна делаются на `AMasterEnemyBase`+`BP_WolfDen`/`BP_BanditBase` (НЕ на старых сабсистемах, их удаляем); магазин → `IShopVendor`; NPC → `AMasterHumanoidCharacter`; BP-наследники C++; DataTable. Остаточные хвосты (убрать 3 статичных `BP_EnemyBandit` с уровня; проверить, что ошибка спавна `Screenshots/1/1.png` 00:18 не воспроизводится) — закрываются ВНУТРИ Этапа A, не отдельным проходом. Урок: итог PIE-проверки Рината фиксировать СРАЗУ (прошлый результат потерян — моя недоработка).

**РЕОРГ Content → feature-first (Lyra, ADR-023). ПОЛНО СВЕРЕНО С ДИСКОМ/GIT/C++ 06-24 (размеры файлов + греп Source + Config).**
- Батч 1 `e06e6a0` + батч 2 `a69dc68` ЗАКОММИЧЕНЫ (PIE-подтв.). Реорг делали СКРИПТАМИ файлово (`reorg_*.py`) → редиректоры не подчищены, пару ассетов скрипт СКОПИРОВАЛ (реальные двойники). Снимок до уборки: тег `working-2026-06-24-pre-reorg-cleanup`@`a69dc68` (origin), ветка уборки `chore/reorg-cleanup`.
- **Что переехало ОК:** Maps/ (L_World, Archive/L_MainLevel), Audio/Demo, Materials/, Characters/{Player,Trader,Bandit,Wolf,Elder,Shared/{Humanoid,Armor,Clothing}}, Environment/{Buildings,Nature,Props}, System/PlayerController. Config DefaultEngine.ini → `/Game/Maps/L_World` (корректно). **Ножа-дубля НЕТ** (Weapons/SM_Knife один, код совпадает — старая память врала).
- **ЗАГЛУШКИ-РЕДИРЕКТОРЫ (≤2.3КБ, лечит Fix Up Redirectors):** `BP_EnemyBandit`(Blueprints/Enemies 2.3КБ), `BP_Pistol`(Weapons/ 2.2КБ), `BP_Campfire`(Blueprints/Actors 2.3КБ), `SM_BonePile`/`SM_WolfDenCave`/`SM_GroundGrid`(Environment/Misc ~1.5КБ).
- ⛔ **2 РЕАЛЬНЫХ ДВОЙНИКА (полные копии, НЕ редиректоры — опасно):** (1) `SM_Pistol` — Weapons/ (66869Б) И Weapons/Pistol/ (66883Б); код грузит КОРНЕВОЙ (`APistol.cpp:16`); (2) скелет `HeadAndSkeletonfbx_Head_Skeleton` — Shared/Humanoid (13044Б) И TestContentAndCode (13056Б); ОБЩИЙ РИГ → перед удалением проверить, на какую копию привязаны меши/анимы.
- **Броня — канон по коду:** C++ грузит `_01` (`AHeadArmor/APantsArmor/ATorsoArmor.cpp` → `/Game/Characters/Armor/SK_Armor_{Head,Legs,Torso}_01`). `_02` в `Characters/Shared/Armor/` (только Head+Torso, без Legs) — кодом НЕ используется → орфан? ❓Ринат решает.
- **C++-пути на старое:** бандит `BanditSpawnSubsystem.cpp:21`→`/Game/Blueprints/Enemies/BP_EnemyBandit` (работает через редиректор; чинится в A5); ABP `ElderNPC.cpp:44`/`TraderNPC.cpp:52`→`/Game/TestContentAndCode/PreProduction/ABP_HumanoidCharacter` — ассет переехал в `Characters/Shared/Humanoid/ABP_HumanoidCharacter`, на старом месте даже редиректора НЕТ = СЛОМАН (чинится в A2/A3).
- **GameMode застрял:** `BP_ContrarySurviorGameMode` ещё в `Blueprints/` (PC уже в System/) → доперенести; проверить редиректор vs живой + кто ссылается.
- **Метод (ADR-023/021):** через РЕДАКТОР (Move + Fix Up Redirectors). Жёсткие C++-пути и Config Fix Up НЕ чинит — вручную + пересборка. **MCP-редактор драйвит `game-lead` сам** (`mcp__unreal__*`, `editor_run_python`; teammate'у mcp не пробрасывается).

**РЕШЕНИЯ 06-22 (записаны: decisions ADR-023/024, tech-design §5, GDD §5, demo-plan):**
- На уровень ставим **BP-наследников C++** (Epic). Волк без BP — пробел → создать `BP_Wolf:AWolfCharacter`.
- Базы врагов = размещаемый актор **`AMasterEnemyBase`** (триггер-радиус + спавн по подходу) → `BP_WolfDen`/`BP_BanditBase`; **обе сабсистемы (Wolf/BanditSpawnSubsystem) на удаление**. Тайминг — **Этап A** (после Этапа 0 + реорга). НЕ смешивать с реоргом.
**Порядок (обновл. 06-23):** Этап 0 ✅ пройден (геймлуп работает в PIE) → **фаза A** (перевод на архитектуру) ↔ дочистка реорга (увязка — развилка ждёт Рината) → B(layout)→C(вид)→D(оружие-визуал)→E(DataTable)→F(QA-гейт→merge).
**Этап A детали (не потерять):** `IShopVendor` НЕТ — создать (магазин жёстко на `ATraderNPC`: TActorIterator/Cast/GetCatalog); торговец `AMasterTrader:AMasterHumanoidCharacter`, староста `AElderNPC:AActor`→рефактор; C3 скайбокс НЕ нужен (SkyLight ambient); долги торговца — AMasterTrader неуязвим / BP_Trader пустой.

**Числа демки (утв.):** шкур 3, волков 4, бандитов 3 (цель убить 3), награды 150/250, ноут 1; респаун −40% денег + дроп расходников/неэкип. (квест-предметы — категория Quest, целы). Сейв у костра в деревне. GDD §7.7 обновить под 2 квеста. Нарратив: ГГ амнезия; ноут бандитов = зацепка «кто за ним охотится».
**Квесты (факт, ElderNPC.cpp):** Q1 «Шкуры волков»(Collect, 3 шкуры, 150); Q2 «Зачистить базу бандитов»(Deliver, убить 3 Bandit + 1 Ноутбук, 250).

**Долги торговца (в Этап A):** `AMasterTrader` неуязвим (override TakeDamage без Super, без UStatsComponent, не блокирует ECC_Visibility — пуля сквозь); `BP_Trader` пустой (меш/AnimBP не назначены). **УРОК 06-16: NPC-HP/бой делать ИЗОЛИРОВАННО, не лезть в боевой код.** Самоотчёты напарников сверять по факту (диск/git/лог) САМ.

**⚙️ Процесс-уроки headless UE5.5 (эта сессия):** `rename_asset` НЕ двигает UWorld; `rename_assets` для UWorld в `-unattended` ловит интерактивный диалог→False; `fixup_referencers` НЕТ в py UE5.5 (ручной resave+delete редиректоров); `delete_asset` ненадёжен для редиректора в населённой папке→файловый `rm` (с проверкой размера, 0 референсеров); `HighResShot` НЕ снимает wireframe→чёрный кадр (wireframe-топологию смотреть в Blender). Постоянного редактора нет — всё headless-коммандлеты (сами выходят). Напарник может уйти в idle НЕ выполнив — проверять результат сам.

## 📦 Бэклог ассетов (каталог `assets/`, ADR-022)
- **iron_barrel — снизить полибюджет (ОТЛОЖЕНО, поручить modeler-3d при доводке до game-ready).** Сейчас tris=8444 (превью) — Ринат: «очень много». Подход: низкополи база (≥16–24 радиальных сегмента для круглого силуэта top-down) + нормал-мап на ободы/рёбра. **Целевой бюджет НЕ зафиксирован** — согласовать как конвенцию по реальной GDD-камере (дистанция/зум) и числу инстансов на экране; не выдумывать. Ринат предложил ориентир ~100 tris. Сайдкар: `assets/iron_barrel/iron_barrel.asset.md`.

## ⚡ ПОСТОЯННЫЕ ДИРЕКТИВЫ (Ринат)
- **⛔ ВСЕ НАШИ ПАРАМЕТРЫ — НАСТРАИВАЕМЫЕ В BLUEPRINT, ВКЛ. РАЗМЕЩЁННЫЕ НА СЦЕНЕ ЭКЗЕМПЛЯРЫ, И ПОДНЯТЫ НАВЕРХ DETAILS (Ринат, 06-25, жёстко).** Любой наш игровой параметр (радиус активации, число врагов, радиус безопасной зоны, параметры света костра и т.д.) должен быть: (1) `EditAnywhere` — редактируется и в самом BP, и на КАЖДОМ размещённом на уровне экземпляре; (2) поднят НАВЕРХ панели Details (через `meta=(DisplayPriority=...)` для C++ или порядок категорий/переменных в BP) — СРАЗУ после Transform, чтобы Ринат не искал его внизу; (3) если параметр управляет визуалом (радиус→сфера, и т.п.) — он ДОЛЖЕН реально влиять на этот визуал на размещённом экземпляре (через `OnConstruction`/construction script). Урок 06-25: SafeZoneRadius на размещённом костре не влиял на свою сферу — это БАГ (параметр не доведён до визуала). Делать ВСЕ наши placeable-вещи по образцу `AMasterEnemyBase` (EditAnywhere + DisplayPriority + OnConstruction-синхронизация визуала). Цель Рината: настраивать максимум (в идеале — всё) числами в BP/на экземпляре, без копания.
- **УРОК 06-25 (битый материал ≠ освещение):** при «серое/тёмное/шахматка» на мешах ПЕРВЫМ делом проверять КОМПИЛЯЦИЮ материала (битый материал → движок подставляет дефолт-шахматку), а НЕ валить на свет/тени. ВСЕ битые искать ФОРС-компиляцией всех материалов (`MaterialEditingLibrary.recompile_material`) + грепом лога; греп лога СЕССИИ неполон (видит только отрендеренное в кадре) → отрицательный результат проверять двумя методами (правило D). Не выдавать гипотезу («это свет») за вывод (№0) — залип на этом дважды за сессию, пока Ринат не ткнул в реальную причину (нода Power без входа Base в 6 материалах).
- **ЭСКАЛАЦИЯ ПРОСТОГО РУЧНОГО ДЕЙСТВИЯ В РЕДАКТОРЕ (Ринат, 06-25):** если простое действие в UE-редакторе после ~2-3 честных попыток нативным путём (Python-скриптинг редактора / MCP) не выходит — НЕ молотить дальше, а попросить Рината сделать это мышкой (конкретный точный шаг). Ринат готов делать простые ручные операции в редакторе. Не залипать на своём упрямстве. (NB: назначение материала через editor-python — ШТАТНО, это НЕ C++; в случае 06-25 нативно сработало со 2-й попытки, эскалация не понадобилась.)
- **⛔ ФИКСИРОВАТЬ РЕШЕНИЯ + СВЕРЯТЬСЯ ДО ВОПРОСА (Ринат, 06-24, жёстко — после того как я ДВАЖДЫ переспросил уже решённое):** (1) ЛЮБОЕ принятое решение — архитектурное / геймплейное / инженерное / дизайнерское — НЕМЕДЛЕННО записать в соответствующий документ: ADR → `docs/decisions.md`; лор/механики/баланс/визуал → `docs/contrary-survivor/GDD.md`; план демки → `demo-plan.source.md`; тех → `tech-design.md`; оперативное состояние → свой `context.md`. Не держать «в голове». (2) **ПЕРЕД тем как спросить Рината или предложить ему принять решение — СНАЧАЛА прочитать свои документы (context, decisions.md, GDD, demo-plan, tech-design) и убедиться, что это НЕ решено и ответа там НЕТ.** Решено/есть в доке — не переспрашивать, а применять. (Урок: переспросил долю дропа расходников — а «дроп мешком» = все расходники, уже было решено.)
- **ЯЗЫК И ДЛИНА ОТВЕТОВ РИНАТУ** (Ринат, 06-24, правило): (1) простой нормальный русский, слова — строго по их значению, без путаницы и жаргонной каши (урок: «слияние скелета» вместо «убрать лишнюю копию» — напугал Рината неверным словом); (2) исследовать и работать ТАК ЖЕ детально, как раньше, НО отвечать Ринату КОРОТКО, без длинных описаний. Подробность — в работе/память, не в сообщениях.
- **game-lead МОЖЕТ САМ управлять движками** (Ринат, 06-24): UE — поднимать MCP-канал (через `/mcp` при открытом редакторе, либо открыть редактор ДО старта сессии), сам запускать/выключать процесс редактора (taskkill/CLI), драйвить через `mcp__unreal__*`. ПОДТВЕРЖДЕНО вживую 06-24 (`editor_project_info` ответил). Blender — Ринат говорит, тоже могу; НО `mcp__blender__*` в текущем наборе game-lead НЕ вижу → перед опорой проверить реальным вызовом, не предполагать.
  - **ИНФРА-ПУТИ (проверены на диске 06-24, не переискивать):** проект = `E:\ContrarySurvior\ContrarySurvivor\ContrarySurvivor.uproject` (EngineAssociation "5.5"); движок (installed) = `E:\UnrealEngine\UE_5.5`; редактор = `E:\UnrealEngine\UE_5.5\Engine\Binaries\Win64\UnrealEditor.exe` (+`UnrealEditor-Cmd.exe` для headless). Запуск: `& "E:\UnrealEngine\UE_5.5\Engine\Binaries\Win64\UnrealEditor.exe" "E:\ContrarySurvior\ContrarySurvivor\ContrarySurvivor.uproject"`. Гейм-репо (git) ≠ репо команды (`E:\game-dev-team`).
- **Разделение труда по редактору** (Ринат, 06-24): по-хорошему в редакторе UE работает `unreal-operator` (его роль). Инструменты у game-lead — РЕЗЕРВ «на всякий» (напарнику MCP раньше не всегда пробрасывался). По умолчанию editor-работу делегировать `unreal-operator`; самому драйвить — фолбэк или когда канал уже живой в главной сессии и задача деликатная.
- **РАБОТАТЬ ЖЁСТЧЕ** (Ринат, 06-24, правило): проверять ФАКТ (диск/лог/код/дамп/живой артефакт) ДО того, как сказать или сделать — не на память, не догадкой. И НЕ переусложнять: где задача простая — решать просто, не городить сложное (урок дня: скелет). Меньше косяков = больше доверия.
- **САМОСТОЯТЕЛЬНОСТЬ / автономия** (Ринат, 06-24, жёстко): game-lead — НАЧАЛЬНИК команды, действует решительно и сам, как опытный руководитель (уровень Кодзимы, 30+ лет). Открывать/закрывать приложения (UE, Blender) и папки — БЕЗ спроса Рината. Не клянчить разрешения на рутину, не сыпать вопросами. Инженерные/тех-решения принимать сам. Рината дёргать только для приёмки (живой PIE/визуал) и истинно дизайнерских развилок. Меньше спрашивать — больше делать и докладывать результатом (коротко). [Уточняет «ничего без явной команды» 06-16: рутину/тулинг/инженерию — делаю сам; крупные дизайн-развилки и деструктив по продукту — согласую.] **УТОЧНЕНИЕ Рината 06-24: с инженерными решениями НЕ разгоняться слишком; принятые решения — ОПОВЕЩАТЬ Рината (коротко, постфактум ок), не молча. Баланс: действую сам, но держу его в курсе.**
- **НИЧЕГО без явной команды** (новое, 06-16) — пока доверие не восстановлено. Не «золотить», не лезть за рамки задачи.
- **ПРОВЕРЯТЬ ИНСТРУМЕНТ ДО РАБОТЫ — способен ли сделать задачу ЦЕЛИКОМ** (урок 06-22: headless-реорг сделал половину — fixup-redirectors/relink ABP/перенос CDO-залоченных ассетов headless НЕ умеет; надо было убедиться заранее, а не ловить блокеры по ходу). Перед делегированием — дешёвый probe возможностей инструмента на реальной операции.
- **⛔ Я — ОРКЕСТРАТОР, НЕ ИСПОЛНИТЕЛЬ-ОДИНОЧКА (Ринат, 06-24, жёстко, после серии напоминаний — ВПРЕДЬ САМ).** По умолчанию держать команду ЗАГРУЖЕННОЙ: цель — до 3 агентов параллельно, НЕ простаивать. Пока один работает (cpp кодит) — другие делают независимое (modeler лепит ассеты, concept рисует UI/арт-дирекшн, qa готовит прогон). Раздавать РЕАЛЬНУЮ in-scope работу по дисциплинам, а не ждать. **Ринат НЕ должен напоминать мне о моих и командных возможностях (оркестрация, параллельный запуск) — это мой базовый режим.** Единственное ограничение параллели — реальные конфликты ресурсов: на ОДНОМ UE-проекте editor-работа (редактор открыт) ↔ headless-сборка cpp (редактор должен быть закрыт) — сериализовать; Blender-сокет — один клиент. Всё прочее — параллелить. При любой паузе спрашивать себя: «кого из команды можно занять прямо сейчас?» — и занять.
- **ЦИКЛ РУКОВОДИТЕЛЯ (Ринат, 06-24, правило): ставить задачу → КОНТРОЛИРОВАТЬ исполнение → ПРОВЕРЯТЬ результат по РЕАЛЬНОМУ артефакту (лог/git/дамп/скрин — сам, не на слово агента) → КОРРЕКТИРОВАТЬ работу → ставить следующую.** Не отпускать агента «в свободное плавание»: idle ≠ выполнено (проверять); самоотчёт без пруфа — возвращать; «готово» принимать только с артефактом. Держать всех в потоке: пока один кодит — другие лепят/рисуют/готовят прогон; освободившегося — сразу занять или дать осмысленную подготовку, не оставлять простаивать.
- **⛔ ТЩАТЕЛЬНО ИЗУЧАТЬ РЕЗУЛЬТАТ ИСПОЛНИТЕЛЕЙ — НЕ ПРОПУСКАТЬ ГОВНО/ХАЛТУРУ (Ринат, 06-24, жёстко).** Проверка ГЛУБОКАЯ, не поверхностная: «сборка зелёная» ≠ «логика верная» — читать сам diff кода (то ли изменено, нет ли регрессий/обхода требований), лично открывать PNG/скрины (Read), сверять контент с источником истины (GDD/план), проверять что путь/ассет реально существует на диске. Принимать только то, что выдержало детальный разбор; брак, халтуру, «правдоподобно но не проверено» — возвращать на доработку с конкретикой. Я — последний фильтр перед qa/PIE/Ринатом; через меня говно не проходит.
- **⛔ ОСМОТР 3D-МОДЕЛЕЙ/ВИЗУАЛА — САМ, ЛИЧНО И ТЩАТЕЛЬНО (Ринат, 06-24, жёстко).** Модель/сцену принимать только осмотрев САМ (Read PNG) под НЕСКОЛЬКИМИ ракурсами и при РАЗНОМ освещении (не один кадр, не только wireframe — требовать от modeler/unreal мульти-ракурсные lit-рендеры + wireframe; включая ракурс игровой top-down камеры). **Скриншоты/рендеры валидны ТОЛЬКО после ПОЛНОЙ загрузки сцены** (шейдеры скомпилены, свет/стриминг готовы) — иначе кадр это артефакт, не реальный вид (урок 06-16). Проверять И **геометрию** (нет торчащих/лишних/вывернутых граней, меш замкнут, нормали НАРУЖУ, нет дублей/coincident-граней), И **окрашивание/материал/UV** (нет «дыр»/просветов от вывернутых нормалей, нет полосок/z-fighting/битого UV), И **соответствие СТИЛЮ КОНЦЕПТОВ** (village.png и пр. — низкополи, выветренное дерево, чистый силуэт). Модель обязана быть КАЧЕСТВЕННОЙ/«нормальной», как в концептах. Брак не пропускать. *(Урок 06-24: у дома — торчащие треугольники из крыши + стены «дырявые с полосками» → вероятно вывернутые нормали / битый материал-UV / дубли граней; такое ловится осмотром с РАЗНЫХ сторон при НОРМАЛЬНОМ свете, не одним кадром.)*
- **Визуал смотрит РИНАТ сам** (06-16): game-lead headless-скрины НЕ делает; приёмка визуала/геймплея = живой PIE Рината.
- **ЛУЧШИЕ практики** UE/кода, оптимально; статика на уровень, не сабсистемы. Editor open/close — на game-lead (не дёргать Рината). ПОЯСНЯТЬ термины (Ринат не UE-технарь). План крупного — на утверждение. Каждый этап = тест Ринатом.
- **ПРУФ (анти-фабрикация):** «собралось/тест прошёл» — только с логом; «закоммичено» — только после git status/log; факт ≠ догадка (GATE/№0).

## Решения / Инструменты
- Все решения — `docs/decisions.md` (ADR-001..021). Инструменты: Blender MCP (modeler-3d), unreal-mcp (unreal-operator), code-render (concept-artist), GDrive (бэкап). cpp-dev/qa — нативный Claude Code.
- Headless-конвейеры: UE `UnrealEditor-Cmd "<uproject>" -ExecutePythonScript="<abs.py>" -unattended -nopause -nosplash` (лог в Saved/Logs или -abslog). Импорт FBX uniform_scale=1. Навмеш-бейк: `-run=ResavePackages -MAPSONLY -PACKAGE=<map> -BuildNavigationData`. Консольный in-editor BuildPaths КРАШИТ.
- НОВОЕ: Geometry Script headless (см. УРОКИ). Защита от дрейфа: GATE-маяк + хук UserPromptSubmit + №0.1.

## Ограничения
- QA-гейт: маркер `.claude/QA_OK` создаёт game-lead вручную только после «добро» qa (с артефактом), снимает сразу после merge; не коммитится. Проверять висячий при старте.
- Напарники — только фича-ветки, не master. Merge в master — после QA. ≤3 агента, субагенты не порождают субагентов.
- Гейм-репо своего хука-гейта НЕ имеет → политику веток держу вручную. /Content через Git LFS (ADR-019).

## РЕПО / HEAD
- **Гейм-репо** `E:/ContrarySurvior/ContrarySurvivor` (UE 5.5.4), origin RenchezLove/ContrarySurvivor. **Активная ветка `chore/reorg-cleanup` HEAD=`a69dc68`** (от `feature/clean-village`; уборка реорга). Тег-снимок `working-2026-06-24-pre-reorg-cleanup`→`a69dc68` (локально + origin). `feature/clean-village` = `a69dc68` локально И на origin (рабочее состояние забэкаплено, 0/0). master=627d243 (Фазы 1-4). Незакоммичено в дереве — только реорг-скрипты `*.py`+`context/unreal-operator/` (мусор). Сборка: `Build.bat ContrarySurvivorEditor Win64 Development` при ЗАКРЫТОМ редакторе.
- **Team/инфра-репо** `E:/game-dev-team`: ветка `docs/phase0-gdd` = f36f136 (push GitHub RenchezLove/game-dev-team) + **НЕ закоммичены**: `docs/contrary-survivor/demo-plan.*`, правки `context/game-lead/context.md`, `.claude/agents/*` и пр. (см. git status).

## 🎮 DEBUG/ТЕСТ-КЛАВИШИ (LogQA-строки на ключевые события)
F1 debug-камера · F2 GiveTestItems · F3/F4 броня вкл/выкл · F6 use · F7 drop · F9/F10 buy/sell · F12 clear-save · M +100 денег · T телепорт-торговец · Y телепорт-староста · G offer+accept квест · H сдать квест · K +1 волк · J god+заморозка статов · U force-drop 100% · B тест-волк вплотную · O QA-оверлей · N force-kill ближнего · V телепорт-Логово · Z телепорт-база · C выдать 5 «Шкур волка» · X выдать «Ноутбук» · P убить игрока. Магазин: стрелки/колесо ±1, Shift ±10, Enter подтвердить. **(F5/F8/F11 движковые — НЕ занимать.)**

# context: game-lead

> Персональная (живая) память агента `game-lead`. Грунтовка сессии — ТОЛЬКО блок «🧭 СЕЙЧАС» ниже (правило bootstrap CLAUDE.md). Прошлое — `archive.md` (read-on-demand). Решения — `docs/decisions.md` (ADR). История — git. Дизайн-правда — `docs/contrary-survivor/`.
> СТОП-правило гигиены: >150 строк ИЛИ Read обрезается → СНАЧАЛА чистка (вынос закрытого в archive.md, удаление перетёртого), потом работа. Дистилляция ВЫРЕЗАЕТ прошлый «СЕЙЧАС», не только дописывает.


## 🧭 СЕЙЧАС (2026-07-29 — BUILD 1.1 ТЕХНИЧЕСКИ ГОТОВ, ЖДЁТ ЖИВОЙ ПРИЁМКИ РИНАТА)

**✅ BUILD 1 СДАН И ВЛИТ** (детали и пруфы — ADR-052, не дублирую). Этап I (интерфейс на UMG) закрыт ✅ в плановой таблице, этап G (Android) — 🔄. Гейм-репо master = `a03f44c`. Маркер QA_OK не висит.

**✅ BUILD 1.1 ЗАКРЫТ ТЕХНИЧЕСКИ 07-29 — ветка `feature/build11-combat-ui`, HEAD `3a171c1`, в master НЕ вливалась.** Все 5 пунктов плана Рината сделаны; последний незакрытый (поза прицеливания) добит сегодня. Мерж-гейт qa — ГОДНО по пяти пунктам, каждый лог я перепроверил лично. Решения, цепочка графа и полный список пруфов — ADR-053, не дублирую.
**Корень вчерашнего падения нашёл САМ чтением исходников движка ДО запуска:** конструктор `UAnimGraphNode_LayeredBoneBlend` уже добавляет слой (AnimGraphNode_LayeredBoneBlend.cpp:19-23), наш `AddPinToBlendByFilter()` делал второй. cpp-dev накануне переписал схему графа, но падавшее место оставил побуквенно прежним → прогон упал бы снова. **Правило на будущее: «переписал» ≠ «починил то, что падало» — перед прогоном сверять именно упавшую строку.**

**▶ ЕДИНСТВЕННОЕ, ЧТО ОСТАЛОСЬ ПО BUILD 1.1 — ЖИВАЯ ПРИЁМКА РИНАТА В ИГРЕ.** Headless это не проверяется в принципе. Смотреть: не ломается ли ходьба при наложении позы (дорожки идут на ВСЕ 22 кости, включая ноги); целятся ли игрок и бандиты корпусом; проигрывается ли замах ножа через слот и приходит ли урон на кадре метки; рисуется ли подсветка сектора 100°; пульсация кнопки бега (на ПК тач-слой выключен, `bEnableTouchControls=false`); ощущение плавного доворота (первый взмах мимо цели промахнётся, корпус доедет за ~0.3 с). После приёмки — мерж в master (маркер QA_OK создаю сам, вердикт qa уже есть).

**▶ ЖДЁТ РИНАТА (решения):** (1) запускать ли Android-сборку — этап G1, он сказал «пока не делай»; (2) приёмка Build 1.1; (3) нужен его телефон для замера кадров и веса (G2); (4) публикация RuStore + реклама VK (G3) — его часть. Отложено, не блокирует: музыка интро, формат текстов издателю, сведение двух скелетов-дублей и переезд боевого скелета из TestContentAndCode/PreProduction (закрыто взаимной CompatibleSkeletons), сокеты оружия на скелете, доводка отдачи выстрела (вертикаль плохо читается с top-down — добирать вспышкой и тряской камеры), возможная доводка второй половины взмаха ножа.

**ИНФРА (сверено при записи 07-29):** редактор UE закрыт, процессов Unreal и MSBuild ноль, дерево гейм-репо чистое. Гейм-репо `E:/ContrarySurvior/ContrarySurvivor`, движок `E:/UnrealEngine/UE_5.5`. Team-репо `E:/game-dev-team`, ветка `docs/phase0-gdd`. Python для скриптов — `E:/UnrealEngine/UE_5.5/Engine/Binaries/ThirdParty/Python3/Win64/python.exe` (системного нет; пути ему давать через `cygpath -w`, свой stdout юникод не печатает).
**📋 ПЛАНОВАЯ ТАБЛИЦА обновлена 07-29** (волна Build 1.1 добавлена в блок «Дополнительно» этапа G, скрипт `scripts/plan-table/add-wave4-0729.ps1`, обратное чтение подтвердило сохранность прежнего текста). Долг за 07-28 закрыт этой же записью.

**▶ УРОКИ 07-29:** (а) **обрывы связи у напарников — норма, статус «упал/освободился» без отчёта ничего не значит: состояние проверять ПО ДИСКУ** (git log, git status, свежие логи, процессы). Сегодня cpp-dev дважды ушёл молча — в первый раз не успел ничего, во второй сделал всю работу; отличил только диск; (б) **сверять падавшую строку кода, а не доверять формулировке «переписал»** (см. выше — сэкономило прогон); (в) три сборки подряд у cpp-dev оказались доращиванием режима `-verify`, а не починкой: полезное поведение, поощрять; (г) Edit-инструмент снова споткнулся на «ё» в decisions.md — правки таких файлов делать python-скриптом (замена по маркеру), heredoc для текста, пути через `cygpath -w`.

**▶ УРОКИ 07-28 (Build 1.1, держать):** (и) **МЁРТВАЯ ФИЧА ПРИ ЗЕЛЁНОЙ СБОРКЕ** — мягкая ссылка (TSoftObjectPtr) на несуществующий ассет НЕ падает и НЕ ловится ни сборкой, ни тестами: она молча не работает. Пути и имена ассетов сверять грепом по Source ПРОТИВ фактического расположения на диске ПЕРЕД сборкой (07-28 поймал руками: код смотрел мимо подпапки Montages); (к) **«Target is up to date» в логе сборки = НИЧЕГО НЕ СОБРАЛОСЬ** — как доказательство не принимать, гнать Rebuild (07-28 сборка простояла 63 с на мьютексе за чужой сборкой и сочла цель актуальной); (л) у нового кода ВСЕГДА проверять, что нужные .cpp реально компилировались — искать строки Compile <файл> и Link; (м) бандиты бьют/стреляют МИМО классов оружия (ИИ наносит урон напрямую) — любую боевую фичу вешать в ЧЕТЫРЁХ местах, иначе у врагов её не будет; (н) моделлер может добросовестно ошибиться о собственном файле (сказал «ключи на 12 костях верха», по факту дорожки на всех 22 включая ноги) — проверять импортированный ассет, а не отчёт; (о) редакторные правки графов AnimBP из Python НЕВОЗМОЖНЫ (свойства защищены) — только редакторный C++-коммандлет, и он обязан при неожиданной структуре НЕ СОХРАНЯТЬ ассет (07-28 это спасло ABP от порчи).

**▶ УРОКИ 07-28 (процесс, держать):** (а) обрывы сессий — дистиллировать сразу после вех; упавший агент возобновим SendMessage, «упал на отчёте» ≠ «упал на работе» — сначала артефакты диска; (б) расстановка мышкой в UMG живёт на канвас-СЛОТЕ (FAnchorData, CanvasPanelSlot.h:74) — переносить геометрию слотов, не только свойства виджетов; (в) get_all_level_actors НЕ видит transient-акторы — чистить через GameplayStatics.get_all_actors_of_class с контрольным повтором; (г) take_high_res_screenshot виснет навсегда при перекрытом окне редактора — SceneCapture-рецепт оператора снимает без окна (помнить ADR-003: сцены с освещением проверять глазами); (д) идемпотентность .uasset проверять срезами (-dumpslots/-dump)+verify, НЕ хэшем — SavePackage побайтно недетерминирован; (е) оператор-напарник без mcp__unreal__* поднимает СВОЙ канал к живому редактору через движковый remote_execution.py (свой порт, не 6776); (ж) Grep-тул молча пропускает бинарники — по .uasset/.umap только grep -rla через Bash с позитивным контролем.

**▶ УРОКИ 07-27 (держать):** (а) дизайн проверяй ОТ КАМЕРЫ ИГРЫ (вертикальный градиент сверху не читается); (б) GUI-скрины/клики — только dpi-aware процесс; (в) ввод только при фокусе UnrealEditor; (г) UMG-дизайнер выделяет глубочайший виджет без учёта Visibility → лечение bLockedInDesigner; (д) connect_material_expressions: пины после сокращения имён, тихий False — обёртка с падением обязательна; (е) InitialParent фабрики инстансов не экспонирован → set_material_instance_parent; БАГ 5.5: set_..._scalar_parameter_value всегда False → проверять обратным чтением; (ж) SetDesiredSizeOverride не сериализуется; (з) перед закрытием редактора при живом канале — сохранять dirty-пакеты.

**▶ УРОК 07-23 (MCP/Python к UE 5.5):** структуру дерева WBP через editor Python НЕ прочитать (protected) — источник правды по нашим WBP = GenerateWbpCommandlet.cpp (+ теперь режим -dumpslots для среза геометрии). EditAnywhere-данные CDO и размещённых акторов читаются нормально. Edit-инструмент спотыкается на «ё» в context.md — большие правки файла делать через Write целиком.

**▶ ⛔ ПРАВИЛО ПЕРЕДАЧИ ПРОЕКТА (07-28, после того как за один прогон моя отмашка «проект свободен» разошлась с реальностью ТРИЖДЫ — висел чужой headless-импорт, cpp дописывал код, cpp же собирал):** отмашку «проект твой» давать ТОЛЬКО после явного подтверждения от предыдущего исполнителя «дерево чистое, всё закоммичено, я остановился» (предложение cpp-dev, принято как правило) И собственной проверки процессов (UnrealEditor.exe/UnrealEditor-Cmd.exe/сборка). Одновременно на проекте — РОВНО ОДИН агент. Цена нарушения: qa едва не принёс ложную поломку, собирая состояние «объявление метода без реализации». Также: сверять ПУТИ И ИМЕНА АССЕТОВ между кодом и оператором ДО работы (07-28 разошлись: код ждал AM_Knife_Swing, оператору я назвал AM_MeleeSlash в другой папке — поймал до создания).

**▶ ⛔ ГЛАВНЫЙ УРОК 07-20 (в постоянные правила):** письма лида не должны расходиться во времени с работой агентов. (1) на блокирующий вопрос отвечать НЕМЕДЛЕННО; (2) не писать «ты не сделал», пока задача может быть в работе; (3) разводить агентов ПО ФАЙЛАМ жёстко; (4) требовать «взял, делаю то-то» в начале и номер коммита в отчёте.
**▶ ПРИЁМЫ, ДОКАЗАВШИЕ СЕБЯ:** (а) параллель двух cpp в одной копии через `git commit -- <точные пути>`; (б) сохранность стилизации — ПОСВОЙСТВЕННЫМ сравнением (теперь штатно: -dumpslots), не хэшем; (в) подпись геометрии к неприкосновенному; (г) обратная проверка собранного результата (чего там НЕТ); (д) транзиентная постановка актора для съёмки — карту не пачкает (transient не сохраняется в .umap в принципе).
**▶ ФАКТ ПРО ВЕТЕР:** качание в `M_WindTrees` — из ВЫСОТЫ меша, не из покраски вершин и не из UV (ADR-039 п.2).
**▶ ПЛАНОВАЯ ТАБЛИЦА:** `docs/contrary-survivor/DemoPlanTemplateRec.xlsx` — обновлять после КАЖДОЙ сессии (правила — ДИРЕКТИВЫ ниже; скрипты `scripts/plan-table/`). ЗА 07-28 ПОКА НЕ ОБНОВЛЕНА.

- **⚠️ Карту открывать запуском редактора с картой в аргументе** (НЕ `load_level` по MCP — роняет).
- **⛔ MCP Blender в субагентов НЕ пробрасывается** → modeler ТОЛЬКО headless (ADR-021). unreal-mcp в субагентов тоже НЕ приходит — оператор ходит своим remote_execution (урок 07-28е).

**▶ ПРАВИЛА (жёстко, 06-25 + 07-11):** живой русский без жаргона (№0.2-1); только законченные предложения, без телеграфных обрубков (№0.2-3); не задавать тупых вопросов (№0.2-2); команда делает ИНСТРУМЕНТЫ, расстановку Ринат сам, базы/пропсы ВНУТРИ BP (ADR-028); ВСЕ наши параметры — EditAnywhere + наверх Details + влияют на визуал у размещённого экземпляра; тонкую визуальную доводку и АНИМАЦИЮ судит Ринат вживую; эскалация простого ручного действия после ~2-3 попыток.
**⛔⛔ РЕДАКТОР — НИКОГДА НЕ ОБСУЖДАТЬ С РИНАТОМ (07-27 + повтор 07-28 «Мозги тока не еби мне с редактором»):** открывать/закрывать МОЛЧА в любой момент, не упоминать в отчётах вообще; при живом канале перед закрытием сохранять dirty-пакеты самому.

**⚙️ ВОЗМОЖНОСТИ КОМАНДЫ — НЕ ПИСАТЬ РИНАТУ «НЕ МОГУ», КОГДА МОГУ (Ринат, 06-24):**
- **Файлы:** все роли сами читают/создают/правят/удаляют (Read/Write/Edit + Bash), в своих папках без спроса.
- **Unreal:** работают И game-lead (mcp__unreal__* при живом канале; чек-лист подъёма: set-пути → editor_project_info; «No command channel» после set-путей = перезапустить редактор при живом MCP-сервере), И unreal-operator (ПРЕДПОЧТИТЕЛЕН; свой канал remote_execution + headless-коммандлет через Bash). Фолбэк всегда — headless `UnrealEditor-Cmd -run=pythonscript`. Редактор open/close — сами, молча. Headless и открытый редактор НЕ одновременно на одном .uproject.
- **Blender:** только modeler-3d, headless (ADR-021).
- **ИНФРА-ПУТИ (проверены):** проект `E:\ContrarySurvior\ContrarySurvivor\ContrarySurvivor.uproject`; движок `E:\UnrealEngine\UE_5.5`; редактор `...\Engine\Binaries\Win64\UnrealEditor.exe` (+`UnrealEditor-Cmd.exe`). Гейм-репо ≠ team-репо (`E:\game-dev-team`).

**Числа демки (утв., demo-plan-v2 §1/§6 + GDD + ElderNPC.cpp):** шкур 3, волков 4, бандитов 3, награды 150/250, ноут 1; респаун −40% денег + дроп расходников мешком. Q1 «Шкуры волков» (Collect, 3, 150); Q2 «Зачистить базу бандитов» (Deliver, 3 бандита + ноутбук, 250).

## 📦 Бэклог ассетов (каталог `assets/`, ADR-022)
- **Экипировка Т0/Т1 — доводка («пока пойдёт», 07-06):** убрать лишнюю геометрию; рест-поза базового скелета — отдельной задачей. Не блокеры.
- **iron_barrel — снизить полибюджет (ОТЛОЖЕНО):** tris=8444 → ориентир ~100 (низкополи + нормал-мап); целевой бюджет согласовать по реальной камере. Сайдкар: `assets/iron_barrel/iron_barrel.asset.md`.

## ⚡ ПОСТОЯННЫЕ ДИРЕКТИВЫ (Ринат)
- **📋 ПЛАНОВАЯ ТАБЛИЦА (07-02):** `DemoPlanTemplateRec.xlsx` веду я, обновляю ПОСЛЕ КАЖДОЙ сессии; таблица ПЕРВИЧНА (правит и Ринат — перед пересборкой перечитать). Статусы ✅/🔄/пусто колонка B шрифтом Segoe UI Emoji; по завершению этапа — блок «Дополнительно (сверх плана этапа)»; подпункты строка-под-строкой (Chr(10)), заголовок жирный; навмеш не выпячивать; писать ТОЛЬКО через Excel COM (ZipArchive ЛОМАЕТ файл); проверять `$wb.ReadOnly`; PS1 с кириллицей — с BOM; после правок Рината — только ДОБАВЛЕНИЕМ в прочитанное. Скрипты: `scripts/plan-table/`.
- **⛔ ВСЕ ПАРАМЕТРЫ — EditAnywhere + наверх Details (DisplayPriority) + влияют на визуал размещённого экземпляра через OnConstruction (06-25, жёстко).** Образец — AMasterEnemyBase.
- **Битый материал ≠ освещение (06-25):** при «серое/шахматка» первым проверять компиляцию материала; форс-рекомпиляция всех + греп лога; отрицательный результат — двумя методами.
- **Эскалация простого ручного действия (06-25):** после ~2-3 честных попыток — попросить Рината сделать мышкой, не залипать.
- **⛔ Фиксировать решения + сверяться ДО вопроса (06-24):** любое решение немедленно в ADR/GDD/план/контекст; перед вопросом Ринату — прочитать свои документы.
- **Язык и длина (06-24):** простой русский, слова по значению; работать детально, отвечать коротко.
- **Разделение труда:** editor-работу делегировать unreal-operator; сам драйвлю — фолбэк/деликатное.
- **Работать жёстче (06-24):** факт до слова; не переусложнять.
- **Самостоятельность (06-24):** действую сам (инженерию, рутину), решения оповещаю коротко постфактум; Рината дёргать только для приёмки и истинных дизайн-развилок. С инженерными решениями не разгоняться — держать в курсе.
- **Проверять инструмент ДО работы** — способен ли на задачу целиком (дешёвый probe).
- **⛔ Я — ОРКЕСТРАТОР (06-24):** держать до 3 агентов загруженными, не простаивать; конфликты ресурсов (editor↔headless на одном проекте, Blender-сокет) — сериализовать, остальное параллелить.
- **ЦИКЛ РУКОВОДИТЕЛЯ:** ставить → контролировать → проверять по РЕАЛЬНОМУ артефакту лично → корректировать → следующая. Самоотчёт без пруфа — возвращать.
- **⛔ ГЛУБОКАЯ проверка результатов (06-24):** читать diff сам, открывать PNG сам, сверять с источником истины; брак возвращать. Я — последний фильтр.
- **⛔ Осмотр 3D/визуала — САМ, мульти-ракурс, только после полной прогрузки (06-24).** Финальную приёмку визуала/геймплея делает Ринат в живом PIE.
- **ЛУЧШИЕ практики UE; пояснять термины; план крупного — на утверждение; каждый этап = тест Ринатом.**
- **ПРУФ (анти-фабрикация):** «собралось/прошло» — только с логом; «закоммичено» — только после git status/log (GATE/№0).

## Решения / Инструменты
- Все решения — `docs/decisions.md` (ADR-001..052). Headless-конвейер UE: `UnrealEditor-Cmd "<uproject>" -ExecutePythonScript=...` (stdout пуст — лог в Saved/Logs); наш коммандлет: `-run=GenerateWbp` режимы -rebuild/-verify/-dumpslots.

## Ограничения
- QA-гейт: маркер `.claude/QA_OK` создаю вручную ТОЛЬКО после «добро» qa с артефактом, снимаю сразу после merge; не коммитится. Проверять висячий при старте.
- Напарники — только фича-ветки. Merge в master — после QA. ≤3 агента, субагенты не порождают субагентов. Гейм-репо хука-гейта не имеет — политику веток держу вручную. /Content через Git LFS (ADR-019).

## РЕПО / HEAD (константы; текущее — блок «СЕЙЧАС»)
- **Гейм-репо** `E:/ContrarySurvior/ContrarySurvivor` (UE 5.5.4), origin RenchezLove/ContrarySurvivor. Сборка: `Build.bat ContrarySurvivorEditor Win64 Development` при закрытом редакторе.
- **Team-репо** `E:/game-dev-team`: ветка `docs/phase0-gdd`.

## 🎮 DEBUG/ТЕСТ-КЛАВИШИ (LogQA-строки на ключевые события)
F1 debug-камера · F2 GiveTestItems · F3/F4 броня вкл/выкл · F6 use · F7 drop · F9/F10 buy/sell · F12 clear-save · M +100 денег · T телепорт-торговец · Y телепорт-староста · G offer+accept квест · H сдать квест · K +1 волк · J god+заморозка статов · U force-drop 100% · B тест-волк вплотную · O QA-оверлей · N force-kill ближнего · V телепорт-Логово · Z телепорт-база · C выдать 5 «Шкур волка» · X выдать «Ноутбук» · P убить игрока. Магазин: стрелки/колесо ±1, Shift ±10, Enter подтвердить. **(F5/F8/F11 движковые — НЕ занимать.)**

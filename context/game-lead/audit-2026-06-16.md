# АУДИТ «починки game-lead» — 2026-06-16

> Одноразовый аудит (сессия-аудит из плана Рината). Итог = диагноз + список мусора + черновики СЕЙЧАС + чек-лист чистки. Свежая сессия исполняет п.«ПЛАН ЧИСТКИ» по этому файлу. Источники: 3 субагента-читателя (mem-audit / junk-scan / wasd-arch), 2026-06-16.

## 1. ДИАГНОЗ — почему game-lead стал хуже работать (дрейф)
1. **Память физически не грунтуется.** `context/game-lead/context.md` = **650 строк**, не читается одним Read (обрыв на ~230/650 по лимиту токенов). Агент не может прогрунтоваться по собственной памяти → тащит случайные куски.
2. **`archive.md` не существует ни у кого** (CLAUDE.md уже требует, файла нет). Вся закрытая история висит в живом файле.
3. **Смешаны 3 несовместимых слоя в одном файле:** закрытая инфра (Ш7–Ш9, гейт-хуки) + история фаз игры + постоянные директивы. Разные жизненные циклы вперемешку.
4. **«Журнал», а не «дистиллят».** game-lead и cpp-dev пишут хронику (каждый SAVE/коммит/раунд) вместо сжатия в текущее состояние — нарушение правила H.
5. **Перетёртое не удаляется.** 4 SAVE-снимка 14–15 июня + 5 слоёв WASD-саги — каждый отменён следующим, но все хранятся; свежий приказ тонет под устаревшим.
6. **Дубли как шум:** DEBUG-клавиши ×6, описания каналов ×5, DT_Quests JIT ×3 — расходятся в версиях.

### Доказанный вред (не теория):
- **БУДИЛЬНИКИ:** СЕЙЧАС(стр.14) и блок ⏰(стр.35) велят «поднять 2 будильника». SAVE 06-16(стр.632): **«watcher'ы УБИТЫ Ринатом, HEARTBEAT удалён, НЕ поднимать».** Я в этой сессии грунтовался на устаревшем слое → собирался поднимать то, что Ринат отменил.
- **КАНАЛЫ:** `GL_SIGNAL/QA_SIGNAL` объявлены LEGACY, но детально документированы как рабочие в 5 местах. Схема git-vs-локальные файлы переписана ≥6 раз с расхождениями.
- **Computer Use Сборщика:** «постоянный грант» vs «таймаутит без Рината» — оба утверждения в файле.

## 2. РАЗМЕРЫ ПАМЯТИ
| Файл | Строк | Состояние |
|---|---|---|
| CLAUDE.md | ~95 | здоров (этой сессией +bootstrap/гигиена) |
| context/_shared.md | 44 | здоров; 1 устаревшая identity (`pgr40@yandex.ru/RenchezLove` → Rinat/novembercog@gmail.com) |
| **context/game-lead/context.md** | **650** | КРИТИЧЕСКИЙ блоат |
| **context/cpp-dev/context.md** | **435** | блоат (build-журнал, не дистиллят) |
| context/unreal-operator/context.md | 76 | здоров, нет СЕЙЧАС-блока |
| context/modeler-3d/context.md | 169 | приемлемо, нет СЕЙЧАС-блока |
| context/concept-artist/context.md | 38 | здоров (дубль пустых заголовков) |
| context/qa/context.md | 10 | пуст, не ведётся |
| docs/decisions.md | 249 | ЭТАЛОН формата (одна запись = одно решение) |

## 3. ПЛАН ЧИСТКИ (исполнять в свежей сессии)
### 3a. Память (главное)
1. **Создать `context/<agent>/archive.md`** и перенести туда CLOSED-разделы (см. 3c). Цель: game-lead ≤120 строк, cpp-dev ≤80.
2. **Переписать `context/game-lead/context.md`** = блок 🧭 СЕЙЧАС (черновик в §4) + постоянные директивы (Автономия / Параллелизм / Визуал-пруф) + Решения/Ограничения + текущий незакрытый этап. Всё «Фаза N смержена»/END-OF-DAY/SAVE → выбросить (история в git + roadmap.md).
3. **Удалять перетёртое, не архивировать** — 4 промежуточных SAVE и отметённые слои WASD выбросить (итог уже в СЕЙЧАС). Архив ≠ свалка.
4. **Свести дубли в ОДИН живой раздел:** DEBUG-КЛАВИШИ (одна таблица), КАНАЛЫ (одна схема), РЕПО/HEAD. Обновлять на месте.
5. **Разрешить противоречие каналов/будильников НЕМЕДЛЕННО:** в СЕЙЧАС один факт — «будильники убиты, не поднимать; каналы на пересборке Ринатом». Удалить блок ⏰ и legacy GL/QA_SIGNAL из живого файла.
6. **cpp-dev/context.md → модель decisions.md:** текущий баг + конвенции + тех-долги; build-логи остаются в `logs/`, коммиты в git. (cpp пишет САМ — не редактировать его файл, пока его сессия активна.)
7. **Добавить СЕЙЧАС-блок** у unreal/modeler/qa; поправить identity в `_shared.md`.
8. Стоп-правило гигиены: context.md >150 строк ИЛИ Read обрезается → чистка. Дистилляция ВЫРЕЗАЕТ прошлый СЕЙЧАС, не только дописывает.

### 3b. Мусор в репо (junk-scan; ничего ещё не тронуто)
- `logs/` (**14 МБ**) — заархивировать (`logs-archive-2026-06-16`) и подчистить старые .bak/.umap_backup/asset-backup. ВЫСОКИЙ.
- `logs/blender_viewport.png` (1.1 МБ) — ДУБЛЬ → удалить. `renders/crate.png` (1.2 МБ, старый) → удалить.
- `backups/orphans-20260612/` (568 КБ) → архив → удалить.
- `.git/objects/maintenance.lock` (0 б, забытый) → удалить.
- ОСТАВИТЬ: `context/*/tmp/`, `reports/Agents/` (статусы+скрипты). Висячего `.claude/QA_OK` НЕТ ✓. `.claude/worktrees/` пусто ✓. Фоновых процессов нет ✓.
- git status: modified = CLAUDE.md + game-lead/cpp-dev context (правки этой сессии, ОК). Untracked = `context/BugReports/BugReport 14/` (скрин капсулы Рината — оставить).

### 3c. Что в АРХИВ (заголовки разделов)
- **game-lead:** всё `Ш7/Ш8/Ш9*` (инфра, смержена); `Текущее состояние 2026-06-11`; `Сессия 06-11 Фаза 0`; `Сессия 06-12 + ФАЗА 1` и все баг-репорты/раунды визуала бандита; `ФАЗА 1 ЗАКРЫТА`; блоки Фаз 2/3/4; все END-OF-DAY; 4 SAVE-снимка 14-15.06 (выбросить); ПЛЕЙБУК (свести).
- **cpp-dev:** вся WASD-сага кроме итога (5 перекрытых слоёв — выбросить); блоки Фаз 1-5 по коммитам. LIVE-конвенции оставить: среда сборки, Build.bat-грабли, R_Hand/нет сокетов, headless-Automation, include-префикс, override-UFUNCTION.

## 4. ЧЕРНОВИКИ 🧭 СЕЙЧАС (готовы к вставке)
### game-lead
```
## 🧭 СЕЙЧАС (2026-06-16)
Режим: разработка ДЕМКИ, ПОЛНАЯ АВТОНОМИЯ. Сначала чиним game-lead, потом WASD, потом план демки.
Демка = 2 квеста + торговля + вменяемый инвентарь + вменяемые ассеты (б.ч. уже есть, надо ПРОВЕРИТЬ; для проверки нужен WASD).
Репо: гейм E:/ContrarySurvior/ContrarySurvivor ветка feature/phase5-quests. Инфра-репо docs/phase0-gdd.
[VERIFY в свежей сессии: реальный master/HEAD гейм-репо — в памяти расхождение f7542ad vs 627d243.]

WASD: КОРЕНЬ найден — капсула спавнится утопленной в пол ~34см → depenetration гасит скорость в 0
(не код движения, спавн-Z). Отметены: NavWalking, камера-yaw, IsMoveInputIgnored, root motion, friction.
Фикс cpp f10835c (relocate floor-trace) СОБРАН и в редакторе; осталось подтвердить PIE+W (Ринат отложил).

Каналы/будильники: watcher'ы УБИТЫ Ринатом, HEARTBEAT удалён. НЕ поднимать будильники.
Каналы со Сборщиком — на пересборке Ринатом, новую схему НЕ выдумывать. legacy GL/QA_SIGNAL не использовать.
Сборщик (CU-тестер) — только когда Ринат за компом.

Постоянные директивы: автономия (разумные дефолты, Рината не дёргать по рутине); ≤3 агента параллельно,
волну сразу на тест; визуал-пруф = только UE-скрин с фронт.светом, не headless-параметры.
Старт сессии: грунтовка по чек-листу (CLAUDE.md → этот блок → файлы под задачу); проверить висячий .claude/QA_OK.
```
### cpp-dev (передать cpp — пишет сам)
```
## 🧭 СЕЙЧАС
WASD vel=0 — КОРЕНЬ+ФИКС f10835c (feature/phase5-quests). Капсула утоплена в пол ~34см
(floorDist=-34, floorPen=1) → свип CMC упирается в depenetration → Velocity=0 при accel=2048.
Корень — спавн-Z/размещение, НЕ код движения. Нижние слои отметены (в архиве):
MaxWalkSpeed/NavWalking/камера-yaw/IsMoveInputIgnored/root motion/friction.
ФИКС: relocate в PlayerCharacter::BeginPlay — floor-trace TraceFloorZ, центр капсулы на
floorImpact+halfHeight+10 если игрок под картой ИЛИ утоплен. ЖДЁТ подтверждения PIE+W.
После подтверждения СНЯТЬ TEMP-диаги MOVE/VELKILL/PIPELINE/BLOCK (PlayerController.{cpp,h})
+ BeginPlay-диаг (PlayerCharacter.cpp:~206).
Конвенции: include-префикс ContrarySurvivor/<Subdir>/H.h; override UFUNCTION без повтора макроса;
-WarningsAsErrors; Build.bat напрямую с tee; релинк ТОЛЬКО при закрытом редакторе. Среда: VS2022 MSVC14.44, UE5.5.4.
```

## 5. WASD (попутно, wasd-arch) — фактически решён
- Корень: капсула (Radius 34, HalfHeight 88, Scale 1 — НОРМА) спавнится внутри пол-актора `StaticMeshActor_36`; `floorDist=-34` (≈ −Radius), `bStartPenetrating=1`. Размещение, не движение.
- Фикс **f10835c** признан корректным/надёжным (floor-trace, отсекает penetrating-старт, грунтует на floor+halfHeight+10, не трогает валидные размещения, StopMovementImmediately+TeleportPhysics). **СОБРАН этой сессией** (релинк PASS), в редакторе. Осталось: PIE+W подтвердить (`floorDist≈+10, floorPen=0, vel>0`) → снять TEMP-диаги.
- Профилактика: проверить, нет ли в `L_MainLevel` ещё одной placed `BP_PlayerCharacter` (дубль удалён в 70dcb3d), и Z у PlayerStart.

## 6. СТАТУС НА КОНЕЦ АУДИТА
- Редактор ОТКРЫТ (я запускал, PID ~84) с собранным фиксом f10835c. WASD-проверку Ринат отложил («чиним меня сначала»).
- Ничего из ПЛАНА ЧИСТКИ ещё НЕ исполнено (только этот документ записан). Память не тронута.
- Следующее по плану Рината: он смотрит диагноз → свежая сессия исполняет §3.

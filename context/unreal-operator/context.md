# context: unreal-operator

> Персональная память агента `unreal-operator`. Сюда агент дистиллирует решения, ограничения и текущее состояние (правило H). Новые сессии стартуют с этого файла.

## Решения
- **Инструмент: MCP-сервер `@runreal/unreal-mcp`** (UE Python / Remote Execution под капотом). Полностью — [ADR-003](../../docs/decisions.md). Только `unreal-operator` видит `mcp__unreal__*`.
- Скриншоты сцены: рецепт через `HighResShot` с окном UE в фокусе (см. ниже).

## Состояние MCP (провалидирован 2026-06-06)
Сервер `unreal` поднимается, **20 тулзов `mcp__unreal__*`** доступны. Смоук-тест пройден (project info / outliner / screenshot / create+delete) и подтверждён артефактами.
- Проект: **ContrarySurvivor**, путь `E:/ContrarySurvior/ContrarySurvivor/`.
- Движок: **UE 5.5.4** (`5.5.4-40574608+++UE5+Release-5.5`). **GAS + Enhanced Input** включены. ~7233 ассета. Карта `/Game/L_MainLevel`.

### 20 тулзов — краткое назначение
1. `editor_project_info()` — метаданные проекта, счётчики ассетов.
2. `editor_get_world_outliner()` — актёры мира с трансформами/компонентами.
3. `editor_get_map_info()` — инфо текущего уровня (счётчики типов, свет).
4. `editor_take_screenshot()` — скрин редактора (рендерит ок, НО пишет в эфемерный tmp — см. квирки).
5. `editor_create_object(object_class*, object_name*, location?, rotation?, scale?, properties?)` — создать актёра. Для StaticMeshActor меш в `properties.StaticMesh`.
6. `editor_update_object(actor_name*, new_name?, location?, rotation?, scale?, properties?)` — обновить актёра.
7. `editor_delete_object(actor_names*)` — удалить (по label работает надёжно).
8. `editor_run_python(code*)` — Python в редакторе (нужен `import unreal`).
9. `editor_console_command(command*)` — консольная команда (без вывода).
10. `editor_list_assets()` — список путей ассетов.
11. `editor_search_assets(search_term*, asset_class?)` — поиск ассетов (лимит 50).
12. `editor_get_asset_info(asset_path*)` — инфо ассета, LOD для мешей.
13. `editor_get_asset_references(asset_path*)` — рефы ассета.
14. `editor_export_asset(asset_path*)` — экспорт ассета (бинарь).
15. `editor_validate_assets(asset_paths?)` — валидация ассетов.
16. `editor_move_camera(location*, rotation*)` — камера вьюпорта.
17. `get_unreal_engine_path()` — путь движка.
18. `get_unreal_project_path()` — путь проекта (в обёртке = «not set», см. квирки).
19. `set_unreal_engine_path(path*)` — задать путь движка.
20. `set_unreal_project_path(path*)` — задать путь проекта.

## Ограничения / квирки (проверено живыми вызовами)
- **Предусловия запуска:** (а) **Multicast Bind Address = `0.0.0.0`** в Python Remote Execution (loopback не сходится с multicast от node); (б) **редактор UE открыт и полностью загружен ДО старта `claude`** — иначе сервер `exit → ✗ Failed to connect` и тулы не грузятся; (в) запуск сервера — абсолютный `node.exe` + абсолютный `dist/bin.js` (резолв `npx` в неинтерактивной оболочке не работает).
- **Скриншоты — рабочий рецепт:** окно UE **в фокусе** → `editor_console_command HighResShot <WxH>` → файл в `<ProjectDir>/Saved/Screenshots/WindowsEditor/`. Без фокуса high-res пайплайн не тикает (`is_task_done()` висит, файла нет). После команды поллить папку на новый PNG с size>0.
- **НЕ для артефактов:** `editor_take_screenshot` → эфемерный tmp (harness удаляет до след. вызова). SceneCapture2D из Python → неосвещённый/чёрный кадр.
- **`get_unreal_project_path()` = «not set»** → путь брать из `editor_project_info`/`unreal.Paths` (или разово `set_unreal_project_path`).
- **`editor_get_world_outliner` отдаёт внутренние имена, не labels** → резолвить label через Python; удалять/искать по label.

## Текущее состояние
MCP-проводка завершена и провалидирована. `L_MainLevel` теперь застроен (деревня): 128 актёров, ~110 StaticMeshActor (SM_GrassTuft x43, SM_Bush x15, SM_VillageFence x14, SM_VillageHouse x6, заборы/деревья/камни, SM_Shed=Shed_BanditBase в (0,-3500,0), и т.д.). Навмеш собран: 1 RecastNavMesh + 1 NavMeshBoundsVolume на всю карту (±6200) + NavModifierVolume `NavMod_Village` (area NavArea_Village). World Settings KillZ=-1048575 (дефолт). Следующие задачи — по постановке `game-lead`.

## L_World — чистый тест-уровень (2026-06-16, create_lworld.py) — НЕ закоммичено
Новый план: вместо починки замусоренного L_MainLevel — чистый `/Game/L_World` для теста ходьбы. Создан headless: `LevelEditorSubsystem.new_level("/Game/L_World")` (→ обычный НЕ-WP уровень; **WP headless в UE5.5 не включить надёжно — выкл, включать отдельно**). Содержит ровно 2 актёра: `TestFloor` (Cube scale(120,120,0.5) loc(0,0,-25) → bounds ±6000 XY, Z[-50..0], BlockAll/Pawn=Block, видимый тест-пол) + `PlayerStart_0` (0,0,98). WorldSettings.default_game_mode = `BP_ContrarySurviorGameMode_C` (из `/Game/Blueprints/BP_ContrarySurviorGameMode`, спавнит BP_PlayerCharacter). Сохранён `les.save_current_level()`. Спавн-сабсистемы (торговец/волки/бандиты) в КОДЕ — могут автоспавниться и тут, убрать на Этапе 2.
- **Этап 3a (lworld_visual_ground.py):** `TestFloor` → hidden_in_game=True (коллизия/transform/scale НЕ тронуты — невидимый коллизион-пол). Добавлен `VisualGround` (Plane scale(120,120,1) loc(0,0,0.1) bounds ±6000, NoCollision, material M_GrassGrid) — видимая трава. PlayerStart (0,0,98). umap 15:27:40. **VisualGround = движковый Plane = 1 квад, НЕ подразбит → vertex-paint дороги по нему НЕЛЬЗЯ; нужен подразбитый меш (Blender/modeler) или landscape — отдельный шаг.**
- **Этап 3a-трава (lworld_grass3.py):** `GrassField` (Actor) с ISM, mesh SM_GrassTuft (mat M_GrassTwoSided), **800 инстансов** зона ±3000 XY, рандом yaw+scale 0.8-1.3, NoCollision. umap 15:46:19. **РЕЦЕПТ ISM headless UE5.5:** `Actor.add_instance_component`/`add_component_by_class`/`Component.register_component` НЕ экспонированы → `ism=unreal.InstancedStaticMeshComponent(actor)`; `actor.set_editor_property('root_component', ism)`; `ism.add_instance(transform)` (register не нужен — сериализуется как root). **ФЛАГ:** при reload VisualGround collision=QUERY_AND_PHYSICS (а не NoCollision из 3a) — тонкий Plane на Z=0.1 с коллизией → риск -34; ждёт решения game-lead.
- **УРОК: верификацию правки делать ПЕРЕЗАГРУЗКОЙ карты (`load_map` заново после save), а НЕ in-memory** — in-memory verify в 3a показал VisualGround=NoCollision, reload показал QueryAndPhysics. In-memory ≠ диск.
- **НАХОДКА: автономный СКРИН уровня headless РАБОТАЕТ** (diag_grassgrid_and_shot.py). В `-ExecutePythonScript`-коммандлете: `EditorLevelLibrary.set_level_viewport_camera_info(loc,rot)` + `AutomationLibrary.take_high_res_screenshot(W,H,abs_path)` → РЕАЛЬНЫЙ рендер PNG (RHI рендеринга в коммандлете ЕСТЬ — 1.4МБ не-blank кадр; без GUI/фокуса окна). Контраст: рендеринг в коммандлете работает, а физ-сцена Chaos (traces) — НЕТ (разные подсистемы). Путь скрина — постоянный `Saved/Screenshots/WindowsEditor/`.
- **M_GrassGrid = процедурная зелёная СЕТКА-заглушка (0 текстур-зависимостей), НЕ трава.** ВСЕ материалы проекта процедурные (0 текстур): M_LandPlane, M_Foliage, M_GrassTwoSided, M_Ground_Grid — готовых текстурных материалов земли/травы НЕТ.
- **L_World текущее (lworld_ground_grass.py, umap 16:01:46):** VisualGround → material `/Game/Materials/M_GroundSoil` (СОЗДАН мной: BaseColor 0.18,0.30,0.12, Rough 0.9, без грида) + NoCollision; грид убран, земля сплошная зелёная. GrassField пересобран: **2800 ISM** SM_GrassTuft, зона ±2500, scale 1.0-1.6, NoCollision. TestFloor цел. Трава БЫЛА тёмной (M_GrassTwoSided процедурный, тёмные блейды) — **ПЕРЕКРАШЕН (edit_grass_mat.py): BaseColor (0.10,0.32,0.06), two_sided=True, Rough 0.85, recompile+save_asset → трава зелёная** (скрины L_World_v3_high/close.png). Итог L_World: земля зелёная (M_GroundSoil) + трава зелёная густая, коллизия ходьбы цела (TestFloor). **GrassField → HISM (grass_hism.py, umap 16:14:09): HierarchicalInstancedStaticMeshComponent, 2800 ±2500 scale 1.0-1.6 NoCollision, cast_shadow=False (set_editor_property('cast_shadow',False)) — тени-кляксы убраны, чистая лужайка.** HISM headless = тот же root-компонент рецепт что ISM. Скрины L_World_v4_high/close.png.
- **Создание материала headless РАБОТАЕТ:** `AssetToolsHelpers.get_asset_tools().create_asset(name, "/Game/Materials", unreal.Material, unreal.MaterialFactoryNew())` + `MaterialEditingLibrary.create_material_expression` (Constant3Vector/Constant) + `connect_material_property(expr,"",MaterialProperty.MP_BASE_COLOR/MP_ROUGHNESS)` + `recompile_material` + `EditorAssetLibrary.save_asset`.
- **РАЗВЕДКА «земля» (2026-06-16, recon_lworld.py/probe_geoscript.py — read-only, НЕ менял):** L_World = 5 актёров: TestFloor (Cube, коллизион-пол BlockAll), VisualGround (Engine Plane, **verts=4 → vertex-paint НЕЛЬЗЯ**, mat M_GroundSoil, NoCollision), GrassField (HISM SM_GrassTuft 2800, NoCollision, M_GrassTwoSided), PlayerStart(0,0,98), DirectionalLight. **M_GroundSoil = Material, 0 текстур, плоский BaseColor, БЕЗ VertexColor-блендинга** → под дорогу-покраску переделывать (VertexColor→Lerp трава/грунт). Выбран нативный Geometry Script (см. след. пункт — валидация пройдена).
- **ВАЛИДАЦИЯ Geometry Script headless ПРОЙДЕНА (2026-06-16, geoscript_gridtest.py, НЕ закоммичено):** включил плагин `GeometryScripting` в .uproject (`{"Name":"GeometryScripting","Enabled":true}`) — без C++-компиляции, бинарь движка готов. **Рабочий headless-рецепт подразбитой плоскости:** `dm=unreal.new_object(unreal.DynamicMesh)`; `dm=unreal.GeometryScript_Primitives.append_rectangle_xy(dm, unreal.GeometryScriptPrimitiveOptions(), unreal.Transform(), dimX, dimY, steps_w, steps_h)`; `(sm,outcome)=unreal.GeometryScript_NewAssetUtils.create_new_static_mesh_asset_from_mesh(dm,"/Game/...",unreal.GeometryScriptCreateNewStaticMeshAssetOptions())` (возвращает ТАПЛ (StaticMesh, GeometryScriptOutcomePins.SUCCESS)); затем `EditorAssetLibrary.save_asset`. **КВИРК имён Python (НЕ как в C++):** класс создания ассета = `GeometryScript_NewAssetUtils` (НЕ NewAssetFunctions). **Формула плотности (проверено):** steps_w=steps_h=10 → StaticMesh LOD0 **verts = steps_w × steps_h = 100**, tris = 2·(steps−1)² = 162. UV0 есть (num_uv_channels=1), numLODs=1, 1 матслот, bbox центрирован. Тест-ассет /Game/Meshes/Demo/SM_GridTest (1000×1000, временный) на диске. 0 ошибок. Лог logs/geoscript_gridtest.log (GRIDTEST:).
- **СБОРКА финальной земли L_World ПРОЙДЕНА (2026-06-16, build_ground.py, НЕ закоммичено):** SM_GroundGrid (/Game/Meshes/Demo/) = AppendRectangleXY 12000×12000, steps 121×121 → **verts=14641, tris=28800, UV0=1, bbox ±6000 z=0**. **Vertex color инициализирован в 0** через `GeometryScript_VertexColors.set_mesh_constant_vertex_color(dm, LinearColor(0,0,0,0), GeometryScriptColorFlags(red/green/blue/alpha=True), clear_existing=True)` — подтверждено round-trip (copy_mesh_from_static_mesh → get_mesh_per_vertex_colors): count=14641, isValidColorSet=True, все RGB=0 (verify_vcolor.py). **Материал M_GroundBlend** (/Game/Materials/): BaseColor=Lerp(A=трава(0.18,0.30,0.12), B=грунт(0.32,0.22,0.12), Alpha=VertexColor.R) + Roughness const 0.9 (добавил под анти-регрессию, FLAGGED лиду). VisualGround заменён: mesh=SM_GroundGrid, override_mat0=M_GroundBlend, scale=1, loc=(0,0,1), NoCollision. TestFloor/GrassField(HISM 2800)/PlayerStart/DirLight НЕ тронуты (дамп-пруф). SM_GridTest удалён. **КВИРКИ Python GeometryScript:** `connect_material_expressions` (мн.ч., НЕ ..._expression); `get_mesh_per_vertex_colors` возвращает 4 знач (TargetMesh, ColorList, isValid, hasGaps); ColorList читать `get_color_list_length()`/`get_color_list_item(i)` (item = ТАПЛ (LinearColor, bool)); материал-коннект MaterialEditingLibrary.connect_material_expressions(from,out,to,in)+connect_material_property(expr,out,MP_BASE_COLOR).
- **ДЕРЕВНЯ + ДОРОГА L_World ПРОЙДЕНО (2026-06-16, build_village.py, НЕ закоммичено):** ДОРОГА = vertex-paint по SM_GroundGrid (НЕ отдельный меш): copy_mesh_from_static_mesh → `GeometryScript_MeshSelection.select_mesh_elements_in_box(dm, Box(min/max, is_valid=True), GeometryScriptMeshSelectionType.VERTICES, False, 3)` → `set_mesh_selection_vertex_color(dm, sel, LinearColor(1,0,0,0), ColorFlags(red=True only), False)` → `copy_mesh_to_static_mesh(dm, sm, CopyMeshToAssetOptions(replace_materials=False), MeshWriteLOD())` → save_asset. Коридор X[-3000,3000] Y[-300,300] → **427 вершин R=1** (=61×7, round-trip подтверждён); материал M_GroundBlend → R=1 рисует грунт. ДОМА: 6× SM_VillageHouse (bbox ±275×-248..250×0..460, пивот низ) на z=1, BlockAll — N(+Y900) и S(-Y900), X={-1400,0,1400}. **Фронт домов headless НЕ определить (yaw=0, FLAGGED — поправить визуально).** NPC: торговец/староста = **C++-АКТЁРЫ** (НЕ BP) `AElderNPC`/`ATraderNPC` (Source/ContrarySurvivor/Actors/, модуль ContrarySurvivor). Резолв в Python: `unreal.ElderNPC`/`unreal.TraderNPC` (скомпилены в редакторе). Болванки удалены, поставлены реальные (place_npc.py): ElderNPC_1 (450,600,91), TraderNPC_1 (1000,1000,91), yaw=0. **z=91 выведено из ctor: root=InteractTrigger(sphere) в origin, CharMesh rel z=-90 → ноги на ~1** (ноги-на-земле НЕ верифицировано визуально). spawn: `EditorActorSubsystem.spawn_actor_from_class(cls, loc, rot)`. ТРАВА GrassField: cull под домами(+100) и дорогой → 2800→**2046** (−754). TestFloor/VisualGround/PlayerStart/DirLight целы. **КВИРК:** GeometryScriptCopyMeshToAssetOptions НЕ имеет enable_nanite (есть apply_nanite_settings); spawn актёров — `EditorActorSubsystem.spawn_actor_from_object(mesh, loc, rot)` (StaticMesh→StaticMeshActor, SkeletalMesh→SkeletalMeshActor); HISM remove_instance — в порядке убывания индексов.
- **АВТОСКРИН headless — РАБОТАЕТ, но КАМЕРА критична (2026-06-16, урок):** метод (load_map → set_level_viewport_camera_info → AutomationLibrary.take_high_res_screenshot(1920,1080,abs) в коммандлете) ВАЛИДЕН. ЧЁРНЫЙ/частичный кадр даёт НЕ метод, а камера: строго top-down (pitch −90) и дальняя/высокая камера (loc z≥2200) → пустой/чёрный рендер. **РАБОЧИЙ рецепт камеры (~1.3МБ валидный кадр): угловой, умеренная высота — loc(1700,1700,1200), rot(-33,-135,0)** (shot_high4.py / shot_ground_v2.py). Итог-скрин земли: L_World_ground_v2.png 1.3МБ — земля зелёная, трава GrassField на месте, z-fight нет, регрессии нет. Невалидные L_World_ground_topdown.png(частичн)/_persp.png(чёрн) — мусор от строго-top-down/дальней камеры.
- **КРИТ-УРОК коллизии: `set_collision_enabled()` на компоненте StaticMeshActor НЕ сериализуется** — на reload откат в дефолт из ПРОФИЛЯ. Чтобы записалось на диск — ставить через **`set_collision_profile_name("NoCollision"/"BlockAll")`** (FName профиля сериализуется). Проверено: VisualGround set_collision_enabled+save→reload QueryAndPhysics (откат); set_collision_profile_name("NoCollision")+save→reload NoCollision (держится). Исправлено fix_visualground2.py.

## Фикс пола/спавна L_MainLevel (2026-06-16, headless ADR-021) — НЕ закоммичено мной
- **ИТОГ-фикс floorDist=-34 (add_collision_floor.py / collision-floor.txt):** добавлен скрытый коллизион-пол `CollisionFloor` (internal StaticMeshActor_8) = Cube `/Engine/BasicShapes/Cube`, loc(0,0,-25) scale(124,124,0.5) → world Z[-50..0] (толщина 50, верх на Z=0), XY ±6200, BlockAll/QueryAndPhysics/Pawn=Block, hidden_in_game=True (невидим, визуал даёт Plane). Визуальный Plane `StaticMeshActor_4` → collision **NoCollision** (mesh/material/scale не трогал). save_map ok. Способ: Cube, НЕ BlockingVolume — размер браша BlockingVolume через headless-Python в UE5.5 надёжно не задаётся. Затем (playerstart_z_and_verify.py) опустил PlayerStart_0 Z 468→98 (XY -2080,650 Рината сохранил), save_map ok, umap mtime 13:44:17. Геометрия: капсула hh=88 на Cube-полу с верхом Z=0 → центр Z=88, floorDist должен стать ≈0 (прежний -34 был от нулевой толщины: сфера-дно vs тонкий лист = проникание≈радиус 34).
- **КВИРК: headless trace/sweep НЕнадёжен в коммандлете.** `SystemLibrary.line_trace_single`/`capsule_trace_single` в `-ExecutePythonScript` (без PIE/game) возвращают вырожденный мусор: bStartPenetrating=True даже для линии сквозь пустоту, time=0/dist=0, impact=сам старт, hit_actor = случайный далёкий актёр. Сцена физ-запросов (Chaos) без PIE не инициализируется. Вывод floorDist/приземления — ТОЛЬКО PIE (согласуется с ADR-021). Поля HitResult в UE5.5 Python protected → читать `hit.to_tuple()` (порядок = BreakHitResult: blocking,bStartPenetrating,time,distance,location,impact_point,normal,impact_normal,phys_mat,actor,component,...); `break_hit_result` в API НЕТ. **Доказано контролем (diag_shed_collision.py):** capsule-sweep из 3 РАЗНЫХ точек (далеко-пусто 5000,5000 / над сараем / спавн) дал БАЙТ-В-БАЙТ идентичный результат (impactZ=-0.10, hit=Shed_BanditBase) → канонический мусор, а не геометрия. Так что «StaticMeshActor_36/Shed во всех headless-щупах» — артефакт, НЕ разъехавшаяся коллизия. SM_Shed коллизия нормальная (1 convex, CTF_USE_DEFAULT, локальные only_colliding-габариты). Сарай НЕ трогали.
- **Корень WASD «проваливается сквозь пол»:** пол = `StaticMeshActor_4` (label был `Rectangle_DD75D993`), меш `/Game/Meshes/Rectangle_DD75D993` — нулевой толщины (bbox Z=0..0), simple-коллизии НЕТ (box=0/convex=0), только complex-as-simple. PlayerStart спавнил капсулу на **Z=690** над полом Z=0 → свободное падение в тонкий лист → тоннелирование. (Старая гипотеза «стопка из 15 / depenetration» к текущему уровню НЕ относится — геометрия другая.)
- **Сделано (2 правки, один save):** PlayerStart Z 690→**98** (XY -1460,650 не тронуты); пол-меш Rectangle → **`/Engine/BasicShapes/Plane`**, scale (124,124,1) loc(0,0,0) → world ±6200 XY; материал перенесён `/Game/Materials/M_GrassGrid` (override slot0); collision BlockAll/QueryAndPhysics/Pawn=Block. У Plane появилась simple box-коллизия (box=1). Пол НЕ заменяли на Cube (Ринат против).
- Скрипты: `E:/ContrarySurvior/ContrarySurvivor/logs/edit_mainlevel.py` (правка), `.../audit_mainlevel.py`+`.../audit_floor2.py` (аудит). Логи рядом `level-*.txt`.
- **Квирки UE 5.5 Python:** `EditorLoadingAndSavingUtils.save_current_level_dirty_packages` НЕ существует → сохранять `save_map(world, "/Game/L_MainLevel")`. `StaticMesh.get_num_vertices/get_num_triangles` недоступны → толщину брать через `get_bounding_box`. PlayerStart `get_name()`=StaticMeshActor_4 — это internal-имя пола, не label; PIE-диагностики оперируют internal-именами.
- PIE-проверку приземления НЕ запускал (озвучено game-lead).
- **Инспекция 2026-06-16 (read-only, inspect_sma36.py + inspect_playerpoint.py, логи inspect-*.txt):** у точки спавна (-1469,651) единственный блокирующий Pawn актёр = StaticMeshActor_4 (Plane-пол); рядом ни сарая, ни кустов. StaticMeshActor_36 (editor get_name) = далёкий сарай Shed_BanditBase в (0,-3500), коллизия локальна (only_colliding bounds == visual, convex=1). **КВИРК: PIE-рантайм internal FName ≠ editor-world get_name** — в PIE мир дублируется и актёры переименовываются, поэтому floorActor «StaticMeshActor_36» из PIE-лога НЕ тот же объект, что editor-StaticMeshActor_36 (это PIE-имя Plane-пола). PIE-диагам печатать floorActor по label+mesh+location, не по internal-имени. Остаточная гипотеза WASD: проникание (floorDist=-34) в Plane-пол нулевой толщины (Plane simple-box bbox Z=0..0).
- **Дамп CDO BP_PlayerCharacter (2026-06-16, dump_bp_player.py / dump-bp-player.txt):** капсула root `CollisionCylinder` HalfHeight=88 Radius=34, RelLoc=(0,0,0) БЕЗ Z-сдвига, profile Pawn/Block; CMC всё дефолт (max_step 45, walkable 44.765, gravity 1, perch_thr 0, orient_rotation_to_movement=True); меши CharacterMesh0 relZ=-80 QueryOnly Pawn=Ignore, Torso/Legs NoCollision; доп. коллизионных компонентов нет. **BP персонажа чист.** Ключ: floorDist=-34 РОВНО == CapsuleRadius 34; оба тестовых пола (Rectangle и Plane) ОБА нулевой толщины → «константа при двух мешах» НЕ оправдывает пол. Рек: дать полу реальную Z-толщину и перепроверить (мат-логика FindFloor — домен cpp-dev). КВИРК: `BlueprintGeneratedClass.get_super_class()` в UE5.5 Python нет.
- **⚠️ Состояние L_MainLevel разъезжается — Ринат правит уровень в открытом редакторе.** После моего save (PlayerStart 12:46:56) уровень ПЕРЕСОХРАНЁН 12:59:57: PlayerStart_0 переехал с (-1460,650,98) на **(-2080,650,468)** — мой Z=98 затёрт, спавн снова высоко. Перед любой правкой уровня СНАЧАЛА перечитывать актуальный on-disk state (load_map + read), не опираться на свои прошлые координаты. GameMode уровня = `BP_ContrarySurviorGameMode_C` (WorldSettings override; в DefaultEngine.ini GlobalDefaultGameMode нет), DefaultPawnClass = `BP_PlayerCharacter_C`. Капсула PlayerStart = стоковая 40/92 (R/Hh), пешка = 34/88 — несовпадение нормально (PlayerStart не трекает пешку); «BadSize» в редакторе = энкроачмент капсулы старта в геометрию (НЕ верифицировал в исходниках движка).

## Headless переимпорт волка (2026-06-13, Фаза 3) — коммит 5c6eb14 (feature/phase3-combat)
- Путь: `UnrealEditor-Cmd.exe <uproject> -ExecutePythonScript=<abs.py> -unattended -nopause -nosplash` (ADR-021), редактор закрыт, stdout пуст → пруфы из `Saved/Logs/ContrarySurvivor.log` по префиксу-маркеру.
- Движок: `E:/UnrealEngine/UE_5.5/Engine/Binaries/Win64/UnrealEditor-Cmd.exe`. Проект-репо = **отдельный git** в `E:/ContrarySurvior/ContrarySurvivor` (НЕ game-dev-team), uasset под git-LFS.
- Переимпорт overwrite: `AssetImportTask(replace_existing=True, automated, save)`; skeletal — `skeleton=existing SK_Wolf_Skeleton` + `skeletal_mesh_import_data.update_skeleton_reference_pose=True` (обновляет ref-позу из FBX, скелет НЕ плодится). Пруфы: `SkeletalMesh.has_vertex_colors()` (метод, НЕ property — property `has_vertex_colors` отсутствует на SkeletalMesh в 5.5), anim `skeleton` path, `number_of_sampled_frames`.
- **Квирк материала skeletal:** assign-back `mats[0].set_editor_property('material_interface',m); sk.set_editor_property('materials',mats)` для SkeletalMesh НЕ персистится (re-fetch показал WorldGridMaterial). Рабочий путь: собрать НОВЫЙ `unreal.SkeletalMaterial()` (с сохранением `material_slot_name`), `sk.set_editor_property('materials',[new_slot,...])`, save, проверять по `unreal.load_asset` заново. Слот волка зовётся `M_Wolf` → slot0 = `/Game/Materials/M_VColor`.
- Скрипты: `E:/game-dev-team/logs/phase3-py/reimport_wolf.py`, `.../fix_wolf_material.py`.

## Повторный переимпорт волка (2026-06-13 13:37, Фаза 3) — коммит 1a9aba1 (feature/phase3-combat)
- Обновлённые FBX (modeler перезаписал 13:33: фикс скиннинга + откат вредного 180°). Единый скрипт `.../reimport_wolf_v2.py` (reimport SK+3 anim + материал рабочим паттерном SkeletalMaterial + re-load verify), лог `.../reimport_wolf_v2.log` (через `-abslog`).
- Пруфы (лог, маркер `WOLFREIMPORT2:`): `has_vertex_colors()`=True (метод); anim Idle/Run/Bite → SK_Wolf_Skeleton, frames 47/15/19; RELOAD slot0 name=M_Wolf material_interface=/Game/Materials/M_VColor. 0 `Error:`, FBX-ошибок нет.
- На этот раз slot0 «before» уже был M_VColor (import_materials=False сохранил), но переназначение/верификация выполнены штатно. Обновился и `SK_Wolf_Skeleton.uasset` (из-за update_skeleton_reference_pose=True) — это ожидаемо, закоммичен вместе с остальными 4.
- Квирк лога: в unattended source-control (Plastic) кидает диалоги «Unable to Check Out From Revision Control» — авто-закрываются (Ok), на сохранение в .uasset не влияют.

## Headless импорт брони (2026-06-13, Фаза 4) — коммит d83bfd3 (feature/phase4-inventory)
- 5 SK брони (`SK_Armor_Head_01/02`, `SK_Armor_Torso_01/02`, `SK_Armor_Legs_01`) из `E:/ForGameLead(Materials)/phase4-assets/` → `/Game/Characters/Armor/`. Скрипт `E:/game-dev-team/logs/phase4-py/import_armor.py`, лог `import_armor.log`, маркер `ARMORIMPORT:`.
- **ОБЩИЙ скелет** — НЕ задавал по памяти: скрипт грузит `SK_Bandit_Head`, берёт его `skeleton` и импортит против него. Оказался `/Game/TestContentAndCode/PreProduction/HeadAndSkeletonfbx_Head_Skeleton` (27 костей). Новый скелет НЕ создан — VERIFY SAME_AS_SHARED=True у всех 5.
- Опции: REPLACE vertex color, create_physics_asset=False, import_materials=False, `update_skeleton_reference_pose=False` (броня НЕ должна менять ref-позу общего скелета — отличие от волка). has_vertex_colors()=True у всех. 0 `Error:`, 0 skeleton-mismatch warnings.
- Материал slot0: рабочий паттерн SkeletalMaterial (rebuild, keep slot_name). slot0 «before» назывался `M_Armor_Flat`, после → `/Game/Materials/M_VColor`, подтверждено RELOAD.
- merge/push НЕ делал.

## Переимпорт переделанной брони (2026-06-13 ~16:0x, Фаза 4) — коммит 87eacc0 (feature/phase4-inventory)
- modeler перезаписал FBX 15:58: меши = тело ГГ + броня объединённые (раньше броня-only → парила). Переимпорт overwrite ТОЛЬКО `_01` (`SK_Armor_Head_01/Torso_01/Legs_01`). Head_02/Torso_02 НЕ трогал.
- Скрипт `E:/game-dev-team/logs/phase4-py/reimport_armor_body.py`, лог `reimport_armor_body.log`, маркер `ARMORREIMPORT:`. Тот же паттерн что import_armor.py: общий скелет берётся из `SK_Bandit_Head.skeleton` (НЕ по памяти), `replace_existing=True`, VColor REPLACE, create_physics_asset=False, `update_skeleton_reference_pose=False`, материал slot0 рабочим паттерном rebuild SkeletalMaterial.
- Пруфы (лог): все 3 SAME_AS_SHARED=True (`/Game/TestContentAndCode/PreProduction/HeadAndSkeletonfbx_Head_Skeleton`, 27 костей, новый скелет НЕ создан), has_vertex_colors()=True, slot0=/Game/Materials/M_VColor (before=M_Armor_Flat). 0 `Error:`, 0 mismatch.
- **Квирк:** `EditorAssetLibrary.save_directory(only_if_is_dirty=False, recursive=True)` ре-сериализует ВСЕ ассеты папки → Head_02/Torso_02 пометились modified хотя их не импортил. Откатил их `git checkout -- ...` перед коммитом, чтобы «не трогать». На будущее: для точечного коммита либо save только нужных ассетов, либо ревертить лишние.
- Закоммичены 3 `_01.uasset` (LFS pointers обновлены: Head oid 77a1542d, Legs b707f512, Torso baeab9ab). merge/push НЕ делал.

## БЛОКЕР сессии 2026-06-12 (Фаза 1, раунд правок бандита)
- **Симптом:** `mcp__unreal__*` тулы НЕ инжектятся в эту спавн-сессию (`mcp__unreal__editor_run_python` и `..._project_info` → "No such tool available"). При этом `claude mcp list` → `unreal: ✔ Connected`, агент-дефиниция содержит `mcpServers: [unreal]`. Редактор жив: `UnrealEditor.exe` PID 6804, ~1.6 ГБ, лог пишется (22:18), PythonScriptPlugin смонтирован, UDP multicast bridge `0.0.0.0→230.0.0.1:6666` поднят. → Это **дыра инжекта тулов в сессию**, не мёртвый редактор. Python на машине нет (только Store-заглушка) → сырой Remote-Exec клиент в обход MCP не написать без изобретения обхода.
- **Следствие:** live-интроспекция BP (SkeletalMesh/AnimClass/leader_pose/трансформы), импорт vertex-color, ориентация ассета, создание/назначение материала, автоскрин — НЕ выполнимы в этой сессии. Вернул `game-lead`: перезапустить `claude`/сессию `unreal-operator` так, чтобы тулы `mcp__unreal__*` подхватились.
- **Что выяснено из C++ (ФС-факт, не требует редактора):** риг модульных мешей собирается в `AMasterHumanoidCharacter` (ctor): `HeadMesh=GetMesh()`, `TorsoMesh`/`LegsMesh`=`CreateDefaultSubobject` + `SetupAttachment(HeadMesh)`. **НЕТ** в C++ ни `SetLeaderPoseComponent`/`SetMasterPose`, ни `SetSkeletalMesh`, ни `SetAnimInstanceClass`, ни relative-transform мешей (единственный `SetRelativeRotation` — на SpringArm камеры в `PlayerCharacter.cpp`). EnemyCharacter наследует базу «как есть» (ADR-018), доп. рига нет. → Значит SkeletalMesh/AnimClass/LeaderPose/трансформы игрока заданы **в дефолтах BP_PlayerCharacter.uasset** (бинарь, нужна Python-интроспекция). Бандиту воспроизводить ту же конфигурацию в BP_EnemyBandit.

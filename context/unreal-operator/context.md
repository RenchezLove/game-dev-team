# context: unreal-operator

> Персональная память агента `unreal-operator`. Сюда агент дистиллирует решения, ограничения и текущее состояние (правило H). Новые сессии стартуют с этого файла.
> Прошлое — `archive.md` (read-on-demand, НЕ грунтовка). Решения — `docs/decisions.md`. Дизайн-правда — `docs/contrary-survivor/`.

## 🧭 СЕЙЧАС (2026-08-06 вечер, Build 1.2.2)
**08-06: живой прогон ДИСТРИБУЦИОННОГО Shipping-пака на телефоне realme (Android 14) через adb.** Пак `E:/ContrarySurvior/pack-android/ArchivedBuilds/Android_ASTC/ContrarySurvivor-Android-Shipping-arm64.apk` переустановлен начисто (старая Development-подпись → сначала `adb uninstall`). Кадры: `E:/game-dev-team/Screenshots/phone-dist-01…09*.png`. Подтверждено кадрами: экран согласия (Яндекс/AppMetrica/GameAnalytics, «Принимаю»/«Не сейчас», ссылка на политику; после переустановки показывается один раз), HUD без выреза/обрезки, счётчика кадров и клавиш ПК нет, новый экран смерти чистый («Тебя убил волк», без рекламных строк и «Enter/Пробел»), сейв в Shipping живёт (экран «С возвращением» после перезапуска). **НАЙДЕНА ПЕТЛЯ СМЕРТИ:** волк дежурит на точке возрождения и убивает за 2-9 с, монеты сгорают (50→2); разрыв — только перезапуск приложения (Continue сбрасывает волков). Деревня и прилавок торговца найдены, магазин ОТКРЫТЬ НЕ УСПЕЛ — экран телефона погас и встал ПИН-замок, adb его не снимает (`mInputRestricted=true`). Прогон Б8 (вёрстка магазина) НЕ закрыт.
**Прошлое закрытое (Б9, чёрная полоса):** коммит `a4b9fc3`, детали — ADR-060 и ловушки ниже (локаль, профили устройства).
**Следующий шаг:** ждать разблокировку телефона Ринатом → дойти до торговца, снять магазин (Б8).

## 📱 Управление телефоном realme через adb (проверено живым прогоном 08-06)
- adb: `E:/Android/sdk/platform-tools/adb.exe`, устройство `0O65812F342018CD`. Экран **720x1600, игра в горизонтали = 1600x720**; screencap отдаёт кадр 1600x720, координаты касаний = координатам кадра 1:1 (вводная лида «2400x1080» была неверна — сверять `wm size`).
- Кадр: `adb exec-out screencap -p > файл.png`. Запуск: `monkey -p com.renchezlove.contrarysurvivor -c android.intent.category.LAUNCHER 1`. Пауза между командами = живое время игры (волки жрут, голод тикает) — длинные цепочки класть в ОДИН `adb shell "...; ...;"`.
- **Координаты (горизонталь 1600x720):** стик — центр (100, 615), движение `input swipe 100 615 <куда> <мс>` (тянуть на ~80-140 точек; свайп идёт с линейной интерполяцией — реальное отклонение среднее); БЕГ (1378, 471) — переключатель, при включении подсвечен оранжевым; ДЕЙСТВИЕ (1327, 625); ОГОНЬ (1502, 622); ИНВЕНТАРЬ (1495, 68); ОРУЖИЕ (1510, 297); ПЕРЕЗАРЯД (1522, 450); пауза (303, 60). Экран смерти: «ВОЗРОДИТЬСЯ» (797, 607). Стартовый экран: «Продолжить» (800, 326), «Новая игра» (800, 373). Экран согласия: «Принимаю» (800, 420), «Не сейчас» (800, 467).
- Бой: тап по врагу = удар (нож). Смерть: возрождение мгновенное после тапа, HP неполное (64→40→…), точка возрождения при загрузке «Continue» ≈ старт интро (поле ЮГО-восточнее деревни); дорога от неё на СЕВЕР ~4-6 с пути, деревня по дороге на ЗАПАД ~6-10 с бегом.
- **Экран гаснет несмотря на `svc power stayon usb`** (энергосбережение realme перебивает): при потухании screencap чёрный/пустой и портретный, фокус `NotificationShade`. ПИН-замок adb НЕ снимает (`input keyevent 82`, `wm dismiss-keyguard`, свайпы — всё мимо, `mInputRestricted=true`) — эскалировать Ринату. Перед длинным прогоном будить нечем — просить Рината выставить таймаут экрана побольше.

## Что от меня живёт в проекте (актуальное состояние, менять только по задаче)
- **Декаль боевого сектора:** `/Game/Materials/M_MeleeSector` + инстанс `MI_MeleeSectorSoft`, Deferred Decal + Translucent. Контракт параметров с cpp-dev: `SectorHalfAngleRad` (0.8727 = сектор 100°), `InnerRadiusFrac` 0.15, `Opacity` 0.35, `EdgeSoftness` 0.25 в базе и 0.30 в инстансе, вектор `SectorColor` (1.0, 0.32, 0.10). Ставить: наклон −90°, рыскание = взгляд персонажа; радиус пятна = `decal_size.Y`, `decal_size.X` — глубина проекции.
- **Анимации** в `/Game/Characters/Shared/Humanoid/` (коммит 9e48b44): `Anim_AimPistol_Humanoid` (2 ключа, 0.0333 с), `Anim_FirePistol_Humanoid` (9 ключей, 0.2667 с), `Anim_MeleeSlash_Humanoid` (21 ключ, 0.6667 с) — все на скелете `/Game/TestContentAndCode/PreProduction/HeadAndSkeletonfbx_Head_Skeleton` (том, что использует ABP).
- **Монтажи** в `/Game/Characters/Shared/Humanoid/Montages/`: `AM_MeleeSlash` (0.6667 с, уведомление `AnimNotify_MeleeHit` «Удар ножом» на 0.35 с) и `AM_FirePistol` (0.2667 с, без уведомлений). Оба — одна секция «Default», слот **`DefaultSlot`** (этот слот cpp-dev вставил в `ABP_HumanoidCharacter`, коммит 780d432; проверено мной вытяжкой строк из ассета).
- **Скрипт импорта анимаций:** `E:/game-dev-team/scripts/import-anim-fbx.py` — аргументы: FBX, папка, имя, необязательно скелет; сам сверяет скелет/кадры/длину/дорожки и печатает `ANIMIMP|RESULT= OK|FAIL`. Проверен и живым FBX, и настоящим коммандлетом с аргументами.
- **Два скелета-дубля** гуманоида помечены взаимно совместимыми (коммит 307bc76) — решение лида: не сводить их, а снять грабли дёшево. Новые анимации импортировать на скелет из `PreProduction`.
**⚠️ Открытые хвосты (не мои, но помнить):** (1) у всех трёх анимаций дорожки на ВСЕ 22 кости, включая ноги и корень, — полным телом монтаж играть нельзя, только послойно от `C_Spine01`, иначе персонаж побежит с прямыми ногами; (2) на 07-28 в `MasterHumanoidCharacter.h`, строки 83 и 89, пути к монтажам записаны БЕЗ подпапки `Montages/` — правит cpp-dev, до этого монтажи не найдутся.
**НЕ ПРОВЕРЕНО живым запуском:** PIE я не запускал ни разу, монтажи не проигрывал; что уведомление срабатывает и урон наносится — не подтверждено. Это к Ринату/qa.
**Скелет гуманоида (факты для моделлера и анимаций):** 22 кости, сокетов НЕТ ни одного, оружие крепится к КОСТИ `R_Hand`. Руки геометрически зеркальны, но имена справа сдвинуты на сустав: слева `L_UpperArm`(основание ключицы) → `L_Shoulder` → `L_Arm`(локоть) → `L_Hand`, справа `R_UpperArm` → `R_Arm`(плечо!) → `Pelvis_009_R_002`(локоть!) → `R_Hand`. Зеркалить анимацию по именам нельзя. Прочие опечатки на диске: `L_Claf`/`R_Claf`, `Ainm_Idle`.
**Граница мира (не трогать без задачи):** в `/Game/Maps/L_World_C` ровно один настоящий `BP_WorldBorder_C_2`, поставлен Ринатом; 106 акторов; позиция (5500, −4370, 0), ZoneSizeX=ZoneSizeY=46875, WallOffset=1400, FogDepth/FogHeight=2500, FogLayerCount=2, FogLayerSpacing=150, FogBandHeight=120, WallHeight=2000, FogInset=30, WallThickness=100. Завесы получают `MI_FogCurtainSoft` напрямую, полосы — динамический инстанс (MID) с родителем `MI_FogBandSoft`. 16 меш-компонентов, NULL-материалов ноль.
**Материалы тумана (живые, в master):** `M_FogSoft` — unlit/translucent/two-sided, шум из мировых координат формулой (X+0.7Y, Z+0.7(X−Y)) (не вырождается ни на лежачих, ни на вертикальных плоскостях, бесшовен на стыках), два слоя шума со встречным паном, рваный край по U меша, DepthFade. Параметры: MaxOpacity / NoiseWorldScale / PanSpeed / EdgeSoftness / NoiseEdgeAmount / DepthFadeDistance / FlipU / LayerOffset / NoiseBodyAmount / EdgeFalloff / FogColor. Инстансы: `MI_FogBandSoft` (FlipU=1, EdgeSoftness=0.3, EdgeFalloff=1.0), `MI_FogCurtainSoft` (FlipU=1, EdgeSoftness=0.9, EdgeFalloff=1.6). **Ориентация: маска плотная при U'=1, а проверенная геометрия плотна при U=0 → в инстансах FlipU=1.**
**Дисциплина:** работа только в фича-ветках, push/merge — game-lead; коммичу не я; редактор открывает/закрывает game-lead; PIE-приёмка — Ринат; визуал перед показом Ринату game-lead смотрит сам. **Одновременно на проекте РОВНО ОДИН агент** — перед запуском проверять `tasklist` на UnrealEditor/сборку, в отчёте писать «мои процессы завершились, проект свободен».
**Следующий шаг:** ждать задачу лида.

## Рабочие рецепты (проверены фактом, применяю постоянно)

- **Headless-прогон (ADR-021), мой штатный путь.** `MSYS_NO_PATHCONV=1 "E:/UnrealEngine/UE_5.5/Engine/Binaries/Win64/UnrealEditor-Cmd.exe" <uproject> -ExecutePythonScript="<abs.py>" -unattended -nopause -nosplash -abslog="<abs.log>"` → exit 0. **stdout пуст**, весь вывод питона идёт в лог, поэтому каждый скрипт печатает с уникальным префиксом-маркером (`FOGDEL|` и т.п.) и я выдёргиваю его `grep -a`. Без `-abslog` пишет в `Saved/Logs/ContrarySurvivor.log`, который перетирается каждым запуском. Редактор при этом должен быть закрыт (проверка: `tasklist | grep -i unreal`). **Аргументы скрипту** передаются внутри тех же кавычек: `-ExecutePythonScript="<скрипт.py> <арг1> <арг2>"` → они приходят в `sys.argv` (подтверждено и исходником `EditorPythonExecuter.cpp`, и реальным прогоном). Из Git Bash кавычки надёжнее всего сохранить, положив всю команду в `.bat` и запустив его через `cmd.exe /c`.
- **Живой редактор без MCP** (в teammate-режиме `mcp__unreal__*` мне НЕ выдаётся): движковый Python `E:/UnrealEngine/UE_5.5/Engine/Binaries/ThirdParty/Python3/Win64/python.exe` + штатный `remote_execution.py` из `Engine/Plugins/Experimental/PythonScriptPlugin/Content/Python`. Обязательно `config.multicast_bind_address='0.0.0.0'` (дефолт клиента 127.0.0.1 даёт NO_NODES) и `config.command_endpoint=('127.0.0.1', 6779)` — порт 6776 занят MCP лида. Драйвер: `scratchpad/re_driver.py`, режим MODE_EXEC_FILE, вывод в `result['output']`. Переменные между вызовами живут ТОЛЬКО если класть их явно в `__main__` (обычные локальные имена скрипта не переживают вызов).
- **Дефолты BP-класса headless переживают перезапуск:** `unreal.load_object(None, ".../BP_X.BP_X_C")` → `unreal.get_default_object(cls)` → `set_editor_property('snake_case', …)` → `EditorAssetLibrary.save_asset(path, only_if_is_dirty=False)`. Чтение `parent_class` у Blueprint-ассета в Python НЕ работает, родителя проверять `unreal.MathLibrary.class_is_child_of`.
- **Скриншоты.** В коммандлете: `set_level_viewport_camera_info` + `AutomationLibrary.take_high_res_screenshot(W,H,abs_path)` — реальный рендер есть. При открытом, но ПЕРЕКРЫТОМ окне редактора вьюпорт не рисуется и `take_high_res_screenshot` виснет вечно; рабочий обход — transient `SceneCapture2D` + `RenderingLibrary.create_render_target2d(world,W,H,RTF_RGBA8)` (именно `render_target2d`, не `render_target_2d`) + `capture_source=SCS_FINAL_COLOR_LDR` + `capture_scene()` + `export_render_target(...)`. `HighResShot` требует окно в фокусе. Для съёмки материала — изолированная сцена: плоскость `/Engine/BasicShapes/Plane` высоко над картой + кубы-метки мировых осей + `SceneCapture2D` в ортографическом режиме сверху; после съёмки всё уничтожить и пересчитать акторов (в `L_World_C` их должно остаться 106).
- **Кадр НАСТОЯЩЕЙ игры (не редактора) — проверено живым прогоном 08-02.** Готовый скрипт: `E:/ContrarySurvior/ContrarySurvivor/scripts/shoot-game-frames.ps1` (есть режим `-DryRun`). Схема: `UnrealEditor.exe <uproject> /Game/Maps/L_World_C -game -windowed -ResX -ResY -nosplash -nosound -abslog=<abs.log> -ExecCmds="setbind F5 'HighResShot 1920x1080 filename=<abs.png>', ..."`. Работает потому, что: `-ExecCmds` кладётся в `GEngine->DeferredCommands` и исполняется на первом тике через LocalPlayer (`UnrealEngine.cpp:2192`), поэтому `setbind` доходит до `UPlayerInput` (`Player.cpp: UPlayer::Exec`); привязанная клавиша в non-shipping гоняет консольную команду из `UPlayerInput::InputKey` (`PlayerInput.cpp:427`), а на отпускании команда НЕ повторяется; `HighResShot WxH` рисует сцену ОФФ-СКРИН ровно в запрошенном размере (`HighResScreenshot.cpp:116`), поэтому «телефонный» 2400x1080 не требует менять окно; `filename=` со слэшем внутри — абсолютный путь (`UnrealClient.cpp:274`). `-abslog` НЕ конфликтует с `LOG=`, потому что `Strifind` требует неалфавитно-цифровой символ перед совпадением (`CString.h:635`) — проверено по исходнику.
- **Клавиши в окно игры: три грабли, все пойманы живьём.** (1) Буквы ломает РУССКАЯ раскладка: FKey для букв берётся по CHAR-коду (`InputCoreTypes.cpp:1573`, буквы лежат в char-карте, не в VK-карте) — перед вводом слать окну `WM_INPUTLANGCHANGEREQUEST` с en-US и проверять `GetKeyboardLayout` потока окна. (2) Первое нажатие СРАЗУ после вывода окна вперёд теряется (`bShouldFlushPressedKeysOnViewportFocusLost=True` в DefaultInput.ini) — фокус ставить один раз в начале, а не перед каждой клавишей. (3) Каждую «действующую» клавишу подтверждать строкой лога и повторять до подтверждения (`QA: teleported to elder`, `QA: GODMODE on`); тумблеры без строки в логе (`ShowHUD`) проверять только глазами по кадру.
- **Телепорты в проекте только ДВА:** `Y` — к старосте (это и есть деревня), `T` — к торговцу; `V`/`Z` (Логово, база бандитов) НЕ существуют, в `DefaultInput.ini` они лишь помечены как «занятые» буквы. Перед обещанием ракурса — грепать `Config/DefaultInput.ini` и `SetupInputComponent`.
- **Готовность игры доказывать логом, не глазами:** строка загрузки карты → `QA: intro control handed to player` (управление отдано) → `QA: intro ended (entered village)` (ЭТО важнее: `EndIntro()` принудительно ставит `SetIntroGradeAlpha(1.0f)`, т.е. вступительное затемнение снято) → тишина лога N секунд (шейдеры/стриминг) → выдержка. Ещё одно доказательство прямо в кадре: текст задания «Найти старосту и поговорить.» появляется только после `EndIntro`, до этого висит «Впереди деревня. Дойти до неё.».
- **Побочный эффект `setbind`:** он зовёт `SaveConfig()` и пишет `DebugExecBindings` в `Saved/Config/WindowsEditor/Input.ini` (у `-game` из редакторного бинарника платформа = `WindowsEditor`, `WindowsPlatformProperties.h:46`). Файл бэкапить до прогона и возвращать после. Туда же (`Saved/Screenshots/WindowsEditor/`) по умолчанию падают снимки, если не задать `filename=`.
- **Декаль-материал в 5.5 (проверено на M_MeleeSector).** Пара настроек — `material_domain=MD_DEFERRED_DECAL` + `blend_mode=BLEND_TRANSLUCENT` (образец в проекте: `M_BloodDecal`). Свойство `decal_blend_mode` движок 5.5 помечает deprecated «No longer used» и читать его Python не даёт — режим задаётся ОБЫЧНЫМ `blend_mode`. Координаты: `TextureCoordinate(0)` даёт 0..1 по площадке декали, центр 0.5. Актор декали с `pitch=-90` проецирует вниз, а сектор, построенный вокруг +X в UV, смотрит по мировому направлению его `yaw`. **При наклоне ровно −90° ротатор вырождается:** обратное чтение вернёт другую пару yaw/roll (сохраняется их сумма) — сравнивать повороты по числам бесполезно. Мобильный рендер декали поддерживает (`MobileDecalRendering.cpp`), при `r.MobileHDR=1` (движковый дефолт, проект не переопределяет) — «HDR always supports decals».
- **Импорт/экспорт анимации скриптом.** Выгрузка: `unreal.AssetExportTask` (`object`, `filename`, `automated=True`, `prompt=False`, `exporter=unreal.AnimSequenceExporterFBX()`, `options=unreal.FbxExportOption()`) → `unreal.Exporter.run_asset_export_task(task)`. Загрузка: `unreal.AssetImportTask` (`filename`, `destination_path`, `destination_name`, `automated=True`, `save=True`) + `options=unreal.FbxImportUI()` с `import_animations=True`, `import_mesh=False`, `import_as_skeletal=True`, `mesh_type_to_import=FBXIT_ANIMATION`, `original_import_type=FBXIT_SKELETAL_MESH`, `skeleton=<целевой скелет>` → `AssetToolsHelpers.get_asset_tools().import_asset_tasks([task])`. Готовая обёртка со всеми проверками — `scripts/import-anim-fbx.py`, обычно запускать её, а не собирать заново.
- **Монтаж и уведомление скриптом.** Монтаж: `unreal.AnimMontageFactory` с `source_animation` + `target_skeleton` → `create_asset`. Слот по умолчанию **`DefaultSlot`**, секция одна и называется «Default», длина наследуется от анимации; дорожка уведомлений «1» уже есть. Уведомление: `AnimationLibrary.add_animation_notify_event(ассет, имя_дорожки, время, КЛАСС)`, класс брать `unreal.load_object(None, '/Script/ContrarySurvivor.AnimNotify_MeleeHit')` — классы C++ проекта из Python видны. Проверка времени: `AnimationLibrary.get_animation_notify_events` + `get_anim_notify_event_trigger_time`. **Чего скриптом НЕЛЬЗЯ:** сменить имя слота у монтажа (`slot_anim_tracks` не экспонировано) и задать имя безклассовому уведомлению (`notifies` защищено) — только редакторный C++. Поэтому программисту нужен СВОЙ класс уведомления, а не именованное событие.

## 📊 Факты по кадрам на L_World_C (снято 08-05, только замеры, ничего не менял)
- **Теней НЕТ и мы за них не платим.** Солнце Stationary, `dynamic_shadow_distance_stationary_light=0.0` → радиус каскадов 0 → карта теней не строится вовсе (`ComputeWholeSceneDynamicShadowRadius`, DirectionalLightComponent.cpp:191-203; дефолт нуля — там же :1010; `...movable_light=40000` не используется). Отдельные тени на объект на мобильном выключены дефолтом `r.Mobile.SupportInsetShadows=false` (ShadowSetup.cpp:374-378) → персонажи тоже без тени. Четыре подвижных точечных света с включёнными тенями не платят: мобильный рендер даёт тени только прожекторам и только при `r.Mobile.EnableMovableSpotlightsShadow`, дефолт 0 (MobileBasePassRendering.cpp:34-35). Запечённого света нет — файла `Content/Maps/L_World_C_BuiltData.uasset` не существует.
- **Ступеней детализации нет ни у одной модели** (22 проверены, у всех `lods=1`), и чинить нечего — модели крошечные: дом 820 треугольников, забор 108, ящик 84, бочка 132, костёр 456, камни 20/40/60, трава 18 и 26, деревья 104-264. Вся деревня ≈8 700 треугольников, вся растительность на всей карте ≈449 000. Камера: штанга 3000, наклон −60° → расстояние до всего видимого почти одинаковое, упрощение по дистанции не сработало бы.
- **Деревня (радиус 5000 от костра):** 50 акторов, 47 отрисовок статических мешей, 13 разных моделей; 42 отрисовки — повторы (забор ×12, ящик ×10, дом ×5, сломанный забор ×4, Plane ×3, верёвка ×3, бочка ×3, поленница ×2). Сбор в группы даст ≈13-18 вместо 47.
- **Растительность уже сгруппирована:** 11 компонентов, 14 247 экземпляров (трава 12 522, деревья 706, камни 770, кусты 249), отсечение по дальности у всех выключено (`cull_start=cull_end=0`). Материалы растительности НЕПРОЗРАЧНЫЕ — перерисовывания от альфа-теста нет.
- **Мобильных настроек качества у проекта НЕТ вообще:** ни `DefaultDeviceProfiles.ini`, ни `DefaultScalability.ini`. Масштаб отрисовки 1.0 — движковый дефолт из `Engine/Config/BaseDeviceProfiles.ini:856`. Менять только под Android — новый `Config/DefaultDeviceProfiles.ini`, секция `[Android DeviceProfile]`.
- **Низкие кадры (19-28) — причина найдена и устранена (05.08, вечер).** Лид измерил на телефоне встроенным счётчиком (игровая логика / отрисовка / видеочип): везде упор в видеочип (GPU), логика и отрисовка почти простаивают (8-9 мс). Значит сбор объектов в группы и упрощение моделей ничего не давали — резать надо было нагрузку на картинку, а не на CPU. Лечение — снижение разрешения отрисовки (`r.MobileContentScaleFactor=0.8` в правильных профилях устройства, см. «СЕЙЧАС» и ловушку про профили ниже) + починка чёрной полосы (Б9). Итог на телефоне: поле 26→36 к/с, деревня 24→31 к/с — цель 30 к/с взята.

## ⛔ Ловушки (каждая поймана живьём — не наступать снова)

- **После импорта скелетного меша ПРОВЕРЯТЬ слоты материалов** (`sk.get_editor_property('materials')` → `material_interface`): если там `/Engine/EngineMaterials/WorldGridMaterial` — материал НЕ назначился, в игре будет серая шахматка/«белый манекен». Импорт «прошёл OK» этого не ловит (поймано на SK_Trader_*, Build 1.2.1). Положительный контроль — соседний живой ассет (у бандита слот смотрел на `M_Bandit_VColor`).
- **Меши экземпляра на карте могут перекрывать CDO класса.** «Модель записана в BP-класс» ≠ «модель видна в игре»: у размещённого актора компонентные свойства сериализуются отдельно и перебивают класс. Проверять срезом экземпляра, чинить — прописав экземпляру значения класса (станут равны CDO → перекрытие перестаёт действовать при дельта-сериализации).

- **Сверять имена и пути ассетов С КОДОМ ДО создания** — грепом по `Source/`, а не по письму лида. 07-28 письмо говорило `AM_MeleeSlash`, а код грузил `AM_Knife_Swing`; создай я по письму — фича молча умерла бы, а выглядело бы как «всё собралось». Расхождение доложить лиду и делать по коду, пока он не решит иначе. Мягкая ссылка на несуществующий ассет НЕ падает — она просто тихо не работает, поэтому такую ошибку не ловит ни сборка, ни тесты.
- **Отрицательный результат требует положительного контроля.** Прежде чем сказать «ссылок нет», в тот же прогон проверяю тем же методом заведомо ЖИВОЙ ассет и убеждаюсь, что список ссылок непустой. Иначе не отличить «ссылок нет» от «метод молча вернул пусто».
- **Поиск имени ловит имя-надстройку.** `MI_FogBand` как подстрока сидит внутри живого `MI_FogBandSoft`, `M_FogWall` — внутри `M_FogWall2`. Голый поиск даёт ложную «живую ссылку». Разводить: `grep -rao 'ИМЯ.\{0,4\}' | sort | uniq -c` — видно, чем продолжается каждое вхождение.
- **Grep-тул по ПАПКЕ с бинарями врёт.** ripgrep бросает бинарный файл на первом NUL-байте и отвечает «No files found» (BP_WorldBorder.uasset не нашёл сам себя). Бинарный поиск по папке — ТОЛЬКО `grep -rla` через Bash. По одному явному файлу Grep-тул ищет честно.
- **Реестр ассетов НЕ видит загрузку строкой-путём из C++** (`ConstructorHelpers`/`LoadObject`/`TSoftObjectPtr`): «0 референсеров» в реестре ≠ мёртвый ассет. Обязательно подтверждать грепом по `Source/` (искать и имя, и `/Game/...`-путь). Ловушка срабатывала на `SM_Pistol`.
- **`get_all_level_actors` НЕ ВИДИТ transient-акторы.** Полный список — `unreal.GameplayStatics.get_all_actors_of_class(world, unreal.Actor)` + фильтр + контрольный повтор листинга.
- **Созданный из Python подобъект (например уведомление в анимации) НАВСЕГДА блокирует удаление своего ассета в этом сеансе редактора.** Возвращённую обёртку держит питоновский учётчик ссылок движка: `obj refs name=<Ассет>` показывает цепочку `GCObjectReferencer → FPyReferenceCollector::AddReferencedObjects(<подобъект>) → Outer = <ассет>`. Не помогают ни `delete_asset`, ни `delete_loaded_asset`, ни `collect_garbage`, ни `gc.collect()`, ни закрытие редактора ассета, ни `TRANSACTION RESET` — все возвращают False при НУЛЕВЫХ внешних ссылках. Лечится перезапуском редактора либо удалением файла с диска. **Вывод: временные пробы делать в ОТДЕЛЬНОМ headless-процессе** — там та же уборка проходит с первого раза (проверено).
- **`delete_asset` в населённой папке исторически НЕнадёжен** (возвращал True, а файл оставался). Всегда проверять `os.path.exists` внутри того же скрипта и `ls`/`git status` снаружи.
- **Переименование ассета лучше заменять «удалить + создать заново» в свежем процессе** — не остаётся ни переходника-редиректора, ни старого имени внутри файла. Проверять: список редиректоров в папке пуст, листинг диска, и вытяжка строк из файла не содержит старого имени.
- **`get_referencers` СРАЗУ после операции в ТОМ ЖЕ процессе — устаревший.** Итоговую проверку делать в СВЕЖЕМ процессе после полного пересканирования.
- **In-memory ≠ диск.** Верификацию правки карты делать перезагрузкой (`load_map` заново после save), а не чтением того, что уже в памяти.
- **Движок ЧИНИТ флаги использования материала в памяти — чтение свойства врёт.** `M_VColor` отдавал `used_with_instanced_static_meshes=True` ещё ДО правки, хотя в файле флага не было: редактор при загрузке карты сам ставит флаг и пишет «needed to have new flag set» + MapCheck «если ассет не пересохранить, вне редактора будет рисоваться неверно». Значение свойства для таких флагов — НЕ доказательство. Доказательство — отсутствие этих строк в логе СВЕЖЕГО процесса после пересохранения (положительный контроль: раньше строки были в каждом логе).
- **`StaticMeshEditorSubsystem` в 5.5 НЕ умеет считать треугольники** — есть только `get_number_verts`, метода `get_number_triangles` нет (проверено `dir()`). Рабочий путь — тег реестра: `AssetData.get_tag_value("Triangles")`.
- **Проект можно делить с другим агентом.** 08-05 в дереве параллельно работал `cpp`: его незакоммиченные правки лежали рядом с моими, и пока я работал, он закоммитил их поверх. Перед коммитом — `git status`, добавлять ТОЛЬКО свои точные пути; полную сборку APK при чужих незакоммиченных правках не запускать (скомпилирует чужой непроверенный код). Для проверки упаковки контента хватает `BuildCookRun -cook -stage -pak -skipbuild -nocompileeditor` — он не трогает C++ и даёт нужный `Manifest_UFSFiles_Android.txt` за ~3 минуты.
- **`set_collision_enabled()` на компоненте НЕ сериализуется** — на reload откат в дефолт из профиля. Записывается только `set_collision_profile_name("NoCollision"/"BlockAll")`.
- **Headless trace/sweep без PIE НЕВАЛИДЕН** — Chaos-сцена не инициализирована, возвращает канонический мусор. Коллизию проверять только в живом PIE.
- **`connect_material_expressions`: имя входного пина сопоставляется ПОСЛЕ сокращения** — «Input»→пустая строка, «Exponent»→«Exp», «Coordinates»→«UVs», «TextureObject»→«Tex», «MipLevel»→«Level», «MipBias»→«Bias», сравнения «A>B»/«A=B»/«A<B»; ВСЁ остальное (A, B, Alpha, Base, Min, Max, Value, X, Y) — как есть. Полный список — `UMaterialGraphNode::GetShortenPinName` в `Engine/Source/Editor/UnrealEd/Private/MaterialGraphNode.cpp`. При несовпадении connect ТИХО возвращает False → недосоединённый граф. Всегда проверять bool результата.
- **Спавн с `transient=True` карту всё равно ПОМЕЧАЕТ изменённой.** `bTransient` добавляет только флаг `RF_Transient` — актор не сериализуется, но пакет карты становится dirty, и снять флаг из Python НЕЧЕМ (`Package.set_dirty_flag` не экспонирован). Содержимое на диске не меняется (проверять md5 `.umap`). После теста уничтожить акторов и честно предупредить лида.
- **Поле `result` у `AssetImportTask` в 5.5 устарело и защищено** — чтение роняет скрипт УЖЕ ПОСЛЕ успешного импорта, выглядит как «импорт упал». Читать `imported_object_paths` или `get_objects()`.
- **Сокет на скелетном меше исчезает при смене меша, на скелете — нет.** Движок ищет сокет сначала в самом меше, потом в его скелете (`SkeletalMesh.cpp`, `FindSocketAndIndex`). В нашем модульном персонаже броня подменяет ассет меша слота (`MasterHumanoidCharacter.cpp:349`), в том числе меша-ЛИДЕРА головы, поэтому сокет надо класть на СКЕЛЕТ. Регистрация скелетов как совместимых сокеты НЕ разделяет. Python умеет `SkeletalMesh.add_socket`, но функций для сокетов СКЕЛЕТА не даёт вовсе. И главное: сокет НЕЛЬЗЯ называть именем кости — при совпадении сокет перекрывает кость (`GetSocketTransform`: сокет применяется первым).
- **Структуру анимационного блюпринта Python НЕ читает:** `EdGraph.Nodes` и `Skeleton.Sockets` protected, `BakedStateMachines` вообще не экспонировано. Рабочий метод — вытяжка печатных строк из `.uasset` (таблица имён) с обязательным положительным контролем. Кости и сокеты — через временный `unreal.SkeletalMeshComponent()` + `set_skeletal_mesh_asset` + `get_num_bones`/`get_bone_name`/`get_parent_bone`/`get_all_socket_names` (сокеты = то, что осталось после вычета костей). Позы костей — `Skeleton.get_reference_pose()` + `unreal.AnimPoseExtensions.get_bone_pose(поза, кость, WORLD)`.
- **Совместимость скелета и анимации движок проверяет ПО КОСТЯМ, а не по ассету** (`USkeleton::IsCompatibleMesh` сверяет имена и иерархию). Два разных ассета-скелета с одинаковым деревом костей работают вместе — не поднимать ложную тревогу.
- **Материал-инстансы:** поле `InitialParent` у фабрики в Python не экспонировано — родителя задавать `MaterialEditingLibrary.set_material_instance_parent(mi, parent)` ПОСЛЕ `create_asset`. **Баг UE 5.5:** `set_material_instance_scalar_parameter_value` применяет значение, но ВСЕГДА возвращает False — bool игнорировать, проверять обратным чтением.
- **Перевод в редакторе намеренно НЕ применяется** (`ILocalizedTextSource.h`, `ShouldLoadNativeGameData` при `GIsEditor` отдаёт родной язык). Проверять локализацию только в режиме игры: `-game -culture=<язык> -nullrhi -unattended -ExecCmds="Localization.DumpLiveTable -Key=<ключ>, quit"`, обязательно два прогона плюс контрольная непереведённая строка.
- **Прочие квирки UE 5.5 Python:** у WidgetBlueprint `WidgetTree`/`RootWidget` protected (брать `unreal.find_object(None, "/Game/UI/WBP_X.WBP_X:WidgetTree.<Имя>")`, обход — `get_children_count()`/`get_child_at(i)`); `unreal.Rotator(roll, pitch, yaw)` — надёжнее задавать присваиванием полей; кириллица в ASCII-логе UE превращается в мохибейк, значение проверять обратным чтением; кириллица в `.ps1` ломает парсер Windows PowerShell — строки скриптов писать латиницей.
- **GUI-автоматизация: рабочий стол может быть занят живым Ринатом.** Обязательный предохранитель перед каждым вводом: `GetForegroundWindow` → процесс должен быть UnrealEditor, иначе прервать всё. Дважды спасло от клика в чужое окно. Случайные скрины чужих окон удалять.
- **Дробное число в Android-настройках читает СБОРЩИК МАНИФЕСТА (C#-код UnrealBuildTool) ПО ЛОКАЛИ КОМПЬЮТЕРА, не всегда движком.** `ConfigHierarchy.cs:601` — `Single.TryParse(Text, out Value)` без `InvariantCulture`. На русской локали разделитель дробей — запятая, поэтому `MaxAspectRatio=2.4` в `DefaultEngine.ini` молча НЕ разбирается (`TryParse` возвращает False), и код (`UEDeployAndroid.cs:2672-2675`) тихо подставляет запасное движковое значение — ни ошибки, ни предупреждения в логе, сборка зелёная, а настройка не действует. Проверено в лоб: разбор строки «2.4» → False/0, «2,4» → True/2.4. **Лечение — писать значением БЕЗ разделителя (целым числом: `3`, а не `3.0`/`2.4`)**, если свойство это допускает. Проверено: в `UEDeployAndroid.cs` (сборщик манифеста) это ЕДИНСТВЕННАЯ переменная типа `float` на весь файл — других дробных ловушек в манифесте нет. Дробные CVar'ы, которые читает САМ ДВИЖОК в игре (`r.MobileContentScaleFactor` и т.п.), этой ловушке НЕ подвержены — движковый разбор через `FCString::Atof` от локали не зависит, точка читается всегда правильно (это другой, C++, путь чтения, не C#-инструмент сборки).
- **CVar профиля устройства писать в тот профиль, который телефон РЕАЛЬНО выбирает, а не в корневой.** Движок сам задаёт `r.MobileContentScaleFactor=1.0` в корневом `[Android DeviceProfile]` (`Engine/Config/BaseDeviceProfiles.ini:856`). Наш override того же CVar в том же корневом профиле в `DefaultDeviceProfiles.ini` проекта — НЕ подействовал: телефон Рината выбирает более частный профиль `Android_Unknown_Vulkan` → наследует `Android_High` → наследует `Android`, и более БЛИЗКИЙ к листу профиль побеждает корневой (проверено живым журналом игры: `Pushing Device Profile CVar: [[r.MobileContentScaleFactor:1 -> 1.0]]` — вместо наших 0.8). Лечение — прописывать CVar в САМОМ частном профиле из цепочки, которую реально проходит устройство (для realme RMX3938 — `Android_High` и `Android_Unknown_Vulkan`), не полагаясь на то, что корневой override докатится вниз. Проверять реальную цепочку — журналом живой игры, не догадкой по имени телефона.

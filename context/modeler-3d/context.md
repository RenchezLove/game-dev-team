# context: modeler-3d

> Персональная память агента `modeler-3d`. Сюда агент дистиллирует решения, ограничения и текущее состояние (правило H). Новые сессии стартуют с этого файла.

## Решения
- **Blender = v5.1** (build blender-v5.1-release, exe `E:\Programs\Blender\5.1`). API экспорта FBX сверял с живым RNA оператора, не по памяти. FBX-формат на экспорте = 7400.
- **Материал бандита = `M_Bandit_Flat`** — один материал на все 3 слота. Principled BSDF, Base Color из vertex-color атрибута `Col` (нода ShaderNodeVertexColor, layer "Col"). Metallic 0, Roughness 0.65, Specular 0.15. Без normal-map, baked-friendly (плоские регионы цвета + rust-акцент).
- **Палитра (hex):** skin C9A084, jacket 2B2B2E, tee 6E6E70, joggers 303033, sneaker ECECEC, cap 28282B, rust C64B2C. Vertex-color в линейном пространстве, домен CORNER (BYTE_COLOR).
- **Подход к одежде:** новые части (кепка, оболочка бомбера, лампас) строятся как добавленная геометрия и ДЖОЙНЯТСЯ в исходный слот-меш → 1 скинённый меш на слот. Веса новых вершин переносятся с ближайшей исходной вершины (KDTree) → корректная деформация. Проверено позой — clipping/отрыва нет.

## Ограничения
- **Общий скелет `RootAnim` (ADR-018):** 21 кость, шарится со всеми гуманоидами. НЕ переименовывать кости, включая аномальную `Pelvis.009_R.002`.
- **Пайплайн экспорта (docx — источник истины):** копия blend1 → правка → Object mode: выделить меш + скелет → Export FBX → галка "limit to selected objects" (use_selection=True), снять "Bake Animation" (bake_anim=False). Имена осмысленные. UE-импорт против скелета HeadAndSkeletonFBX, import skeletal meshes, без Create Physics Asset.
- **Перед экспортом базового меша — сбросить позу в rest** (Pose mode → select all → Clear Transforms; удалить action). База в blend1 приходит в РАН-позе (action "ArmatureAction", frame 3) — это поза анимации, не bind. Одежду авторить на rest.
- **Сокет Blender одноклиентный** (localhost:9876) — не спавнить 2 modeler параллельно.
- **CLI background Blender НЕ настроен** (нет BLENDER_PATH) — `execute_blender_code_for_cli` не работает. Проверка round-trip FBX — реимпортом в живом инстансе в factory-startup сцену (read_homefile use_factory_startup=True), потом reopen рабочего .blend.
- Импорт FBX в сцену, где УЖЕ есть armature RootAnim → KeyError C_Root (коллизия имён арматур). Артефакт импорта, не дефект FBX. Проверять в чистой сцене.
- **Bash-шелл здесь без Python и капризен к PowerShell-кириллице.** Для распаковки docx/прочего Python использовать через Blender bpy (zipfile есть). find работает.

## Текущее состояние (2026-06-12)
- **Бандит-гопник готов и экспортирован.** Рабочая папка `E:\ForGameLead(Materials)\bandit-work\`.
  - `bandit_work.blend` — копия MasterHumanoidCharacterSeparatedMesh.blend1, rest-поза, action удалён.
  - `SK_Bandit_Head.fbx` (101 v, 156 tris) — голова skin + тёмная кепка-kepka.
  - `SK_Bandit_Torso.fbx` (202 v, 332 tris) — тёмный бомбер + серая футболка-воротник + rust-нашивка на левом плече + skin-кисти.
  - `SK_Bandit_Legs.fbx` (64 v, 102 tris) — тёмные спортивки + rust-лампас на левой ноге + белые кроссы.
  - **Суммарно 590 трисов** (бюджет был 1.5–2.5k, потолок 4k → большой запас).
  - Все 3 слота: 1 материал M_Bandit_Flat, vertex-color "Col", Armature-модификатор, чистые Smart-UV (0–1, без overlap), 21-костный RootAnim в каждом FBX. Round-trip реимпортом подтверждён.
- **Бэкап на Google Drive — НЕ делал** (нет конвенции/доступа в постановке; уточнить у game-lead).
- **Прежние SK_BanditTorsoArmor1/2/3.fbx** — старые эксперименты, не использовались.

## Раунд 3 (2026-06-12): диагностика «БАГ 2 ОРИЕНТАЦИЯ» — экспорт НЕ виноват
- Гипотеза баг-репорта (ось экспорта Blender→UE перевёрнута) НЕ подтвердилась. Сравнил мой `SK_Bandit_Torso.fbx` с эталоном `SK_BanditTorsoArmor3.fbx` (тот же пайплайн docx, импортится в UE верно), парсером `io_scene_fbx.parse_fbx`:
  - GlobalSettings ИДЕНТИЧНЫ: UpAxis=Y(1), FrontAxis=Z(2), CoordAxis=X(0), все знаки +1. FBX 7400.
  - Model-узлы ИДЕНТИЧНЫ: RootAnim(Null) Lcl Rot[-90,0,0] scale100; меш Lcl Rot[-90,0,0]; C_Root(LimbNode) Trans[0,-0.001,0.839] Rot[+90,0,0]. Это стандартная Blender Z-up→FBX Y-up конверсия — как у эталона.
  - Геометрия (bbox в Blender-local) совпадает по ориентации: torso Z[0.9,1.55], head Z[1.53,1.82], legs Z[0,0.9] — вертикальный гуманоид Z-up, ground=Z0.
- Round-trip: реимпорт моего FBX в factory-сцену (через temp_override с VIEW_3D-area, иначе mode_set падает «Context missing active object») → RootAnim rot[0,0,0] высота Z=1.569 вертикально; меш rot[0,0,0]; vertex-color «Col» (CORNER/BYTE_COLOR) + материал M_Bandit_Flat НА МЕСТЕ. Скрин — стоит вертикально, цвет виден.
- **⚠️ ДОПОЛНЕНО game-lead 2026-06-13 (конец дня) — НОВАЯ ЗАДАЧА РАУНД 5:** ориентация/ось действительно были НЕ виноваты (флип оказался размещением актора в UE). НО при нормальном свете (BugReport 6) вскрылась ДРУГАЯ проблема — **РЕГИОНЫ vertex-color в UE сдвинуты vs Blender:** низ ног (голени) БЕЛЫЙ выше щиколотки (в Blender тёмные джоггеры до щиколотки, белые только ступни-кроссы); оранжевая rust-зона оказалась на ПРАВОМ ПРЕДПЛЕЧЬЕ (в Blender — на плечах + полоса). Color-space (sRGB→linear) UE закрыл материалом Power(2.2) — торс стал правильно тёмным. Осталась именно ГЕОМЕТРИЯ ГРАНИЦ ЦВЕТА. Гипотеза: BYTE_COLOR домен CORNER → FBX → UE схлопывается в per-vertex/интерполируется → на low-poly без рёбер по границам зон цвет «расплывается»/сдвигается. **ЗАДАЧА завтра:** выбрать и сделать (а) разрез геометрии по границам цветовых зон, ЛИБО (б) **бейк vertex-color/зон в ТЕКСТУРУ** (UV уже чистые 0-1, модель готова под бейк — рекомендация game-lead, надёжнее на low-poly + снимает gamma), ЛИБО (в) проверить настройки экспорта/импорта color-атрибута. Прежний вывод «в Blender чинить нечего» относился ТОЛЬКО к ориентации — по ЦВЕТУ/ГРАНИЦАМ работа есть.
## Фаза 3 (2026-06-13): серия ассетов + dev-окружение, headless-пайплайн
- **MCP-инжект в субагенте НЕ работает** (`mcp__blender__*` / `execute_blender_code` недоступны в треде modeler). РАБОЧИЙ ПУТЬ — headless CLI через Bash: `E:/Programs/Blender/blender.exe -b --factory-startup --python script.py`. exe = `E:/Programs/Blender/blender.exe` (v5.1.2). FBX-экспортёр включается `addon_utils.enable('io_scene_fbx')`. Не нужен ни живой сокет, ни окно. Скрипты-билдеры лежат в `E:/ForGameLead(Materials)/phase3-assets/_build/` (common.py — хелперы: srgb→linear, vcol-материал, smart_uv, triangulate, export_fbx; verify.py/verify_anim.py — round-trip).
- **🔑 БЛОКЕР FBX-АНИМАЦИИ Blender 5.1 (новая система action «Baklava») + ОБХОД.** У Action в 5.1 НЕТ `.fcurves` (есть `.layers/.slots/.strips[].channelbag(slot).fcurves`). FBX-экспортёр детектит «анимирован ли объект» по старому API → loose Baklava-action НЕ виден → `export_scene.fbx(bake_anim=True, bake_anim_use_all_actions=False, bake_anim_use_nla_strips=False)` пишет **0 AnimationCurve** (пустой Takes). Проверено `io_scene_fbx.parse_fbx` (счёт AnimationCurve) + реимпортом. **ФИКС:** протолкнуть нужный action во временный NLA-трек (`ad.nla_tracks.new(); tr.strips.new(name,1,act)`, `ad.action=None`) и экспортировать с `bake_anim_use_nla_strips=True` → один take на файл, полный покадровый бейк. Альтернативы: `bake_anim_use_all_actions=True` (тянет ВСЕ action в файл), `bake_anim_force_startend_keying=True` (кеит только старт/энд — ТЕРЯЕТ промежуточные позы цикла, не годится). keyframe_insert на pose-bone работает штатно (slot 'OBA' создаётся авто); в памяти поза двигается — ломается именно экспорт.
- **Gotcha проверки анимации:** мерить движение по `pose_bone.tail`, НЕ по `matrix.to_translation()` (=голова кости): поворот кости вокруг своей головы её НЕ двигает → ложный 0.
- **Готовые ассеты (`E:/ForGameLead(Materials)/phase3-assets/`, вне git):** SM_Knife (154 tris, M_Knife), SM_Tree_01/02/03 (112/106/264, M_Tree), SM_Rock_01/02/03 (108/192/108, M_Rock), SM_Fence_Section (220, M_Fence_Wood, тайл по X 2.0м), SK_Wolf (616 tris, скелет WolfRig 22 кости, M_Wolf, скин программный nearest-bone-segment, 0 неназначенных) + Anim_Wolf_Idle/Run/Bite.fbx (NLA-бейк, движение подтверждено). Все: 1 материал, vertex-color «Col» BYTE_COLOR/CORNER, Smart-UV 0-1, faceted flat shade, FBX7400, round-trip реимпортом подтверждён. Пропы origin низ-центр (ground Z0); нож origin в основании рукояти. Стиль/бюджеты — GDD §2.

## Раунд (2026-06-13): ФИКС ВОЛКА по Play-фидбеку (BugReport 7)
- **Конвенция гуманоида (MasterHumanoidCharacterSeparatedMesh, источник истины):** forward = **−Y** (носки ступней до −0.243, нос до −0.143), up = +Z, подошвы на Z≈0, меши+RootAnim rot 0. Проверено headless-инспекцией исходника.
- **Проблема 1 (нормали):** меш волка имел непоследовательные/вывернутые нормали → тёмный «наизнанку» в UE. Фикс: `bmesh.ops.recalc_face_normals` перед to_mesh. Round-trip: signed-volume +0.086 → OUTWARD.
- **Проблема 2 (ориентация):** волк был построен носом в **+Y** = 180° наоборот к гуманоиду → в UE смотрел задом/лежал. Фикс в build_wolf.py: после скина+анимаций повернул меш (`me.transform`) и кости (`edit_bone.transform`, корректно правит roll) на 180° вокруг Z → нос теперь **−Y**, совпадает с гуманоидом. Pose-local анимации не пострадали. Доп.: поднял сборку так, что нижняя точка лап = Z0 (как подошвы гуманоида).
- **Итог волка в Blender: forward = −Y, up = +Z, лапы Z0, rot 0 — ИДЕНТИЧНО гуманоиду.** => В UE cpp/unreal должны дать SkeletalMesh-компоненту волка ТОТ ЖЕ относительный трансформ, что и базовому гуманоиду (убрать спец-квадрупед-трансформ). Тогда волк стоит и смотрит вперёд.
- Перезаписаны: SK_Wolf.fbx + Anim_Wolf_{Idle,Run,Bite}.fbx. Бэкап старых в `phase3-assets/_backup_wolf_pre180/`. Round-trip реимпортом в factory-сцену подтвердил нормали/ориентацию/движение костей. Гуманоидный RootAnim/бандита НЕ трогал.

## Раунд (2026-06-13): ФИКС ДЕФОРМАЦИИ ВОЛКА (BugReport 8) — откат 180° + реальный дефект скина
- **180°-поворот рест-позы был ОШИБКОЙ** (диагноз game-lead подтверждён): поворот меша+костей (`EditBone.transform`) после авторинга анимаций ломает связку анимация↔bind при ребайнде в UE → расплывание. **Поворот рест-позы УБРАН из build_wolf.py.** Фикс нормалей (`recalc_face_normals`, до to_mesh) СОХРАНЁН. Приземление (uniform Z-translate меша+костей) сохранено — чистый перенос, скиннинг не трогает.
- **Волк вернулся в ИСХОДНУЮ ориентацию: forward = +Y** (нос/морда на y=+0.86, хвост y=-0.92), up=+Z, лапы Z0. NB: гуманоид forward=−Y → волк на 180° иначе → cpp должен дать SkeletalMesh-компоненту волка yaw на 180° отличный от гуманоида (rigid-поворот компонента скиннинг НЕ ломает).
- **🔑 НАЙДЕН реальный дефект скина (был всё время, не от 180°):** вершины ПЕРЕДНЕЙ стопы (y≈0.02,z≈0.03) привязывались к `root`+`rearpaw_R` (ЗАДНЯЯ лапа!) → при вращении задней лапы вылетали (max_edge_stretch 7.96 = расплывание). Причина: кости передней ноги стояли на y≈0.30 константой, а МЕШ стопы на y0.02 — кости не следовали за мешем (задние кости трассируют станции меша и работали). **Фикс:** frontlower/frontpaw переставлены трассировать станции меша до стопы (frontlower (0.33,0.26)→(0.17,0.13), frontpaw (0.17,0.13)→(0.02,0.0)).
- **ПРУФ деформации в Blender (depsgraph-оценка деформ.меша по всем кадрам, новый verify-блок в build_wolf.py):** Idle/Run/Bite ВСЕ → OK. max_edge_stretch 1.675 (легитимный шов thigh_L↔thigh_R на крупе, чистые веса 1.0, не сирота), NaN=0, span sane, лапы движутся (Run: rearpaw travel 1.06–1.40, frontpaw 0.79–0.82). VERTS_WEIGHT_NOT_1 = 0/324.
- **Нормали наружу:** round-trip реимпорт SK_Wolf.fbx → signed volume +0.0861 = OUTWARD, mesh rot 0.
- Перезаписаны (13:33): `E:/ForGameLead(Materials)/phase3-assets/{SK_Wolf,Anim_Wolf_Idle,Anim_Wolf_Run,Anim_Wolf_Bite}.fbx`. Скелет WolfRig 22 кости (не менял имена). Гуманоидный RootAnim/бандита НЕ трогал. Диаг-скрипты: `_build/dbg_skin.py`, `_build/verify_normals.py`.

## Фаза 4 (2026-06-13): МОДУЛЬНАЯ БРОНЯ (placeholder, draft) — 5 скелетных мешей на RootAnim
- **Папка:** `E:/ForGameLead(Materials)/phase4-assets/` (вне git). Билдер `_build/build_armor.py`, verify `_build/verify_armor.py`. Headless CLI (`blender.exe -b --factory-startup --python ...`), как в Ф3.
- **Подход:** броня = НОВАЯ процедурная low-poly faceted геометрия (loft-кольца/боксы) в world-координатах поверх тела, скин через **KDTree-перенос весов с ближайшей вершины базового слот-меша** (Head/Torso/Legs из bandit_work.blend) → тот же общий **RootAnim (21 кость)**, синхронная деформация. Метод тот же, что для одежды бандита. Перед сборкой RootAnim сброшен в rest (`pose.transforms_clear`, action=None).
- **Меши (1 материал `M_Armor_Flat` vcol-нода Col, BYTE_COLOR/CORNER, Smart-UV 0-1, flat, нормали recalc OUTWARD):**
  - `SK_Armor_Head_01.fbx` — шлем/каска, 52 tris, слот Head, веса C_Head/C_Spine02.
  - `SK_Armor_Head_02.fbx` — капюшон (DRAFT), 56 tris, слот Head.
  - `SK_Armor_Torso_01.fbx` — бронежилет+плита+наплечники, 96 tris, слот Torso.
  - `SK_Armor_Torso_02.fbx` — куртка с пластинами (DRAFT), 192 tris, слот Torso.
  - `SK_Armor_Legs_01.fbx` — усиленные штаны+наколенники, 144 tris, слот Legs.
  - Полный сет (Head01+Torso01+Legs01)=292 tris брони, + база перса ~590 = ~880 → в бюджете 1.5-4k с запасом.
- **Round-trip реимпортом (verify_armor.py) — ВСЕ 5 OK:** RootAnim 21 кость (аномальная Pelvis.009_R.002 на месте), меш+арматура rot 0, vcol Col BYTE_COLOR/CORNER, M_Armor_Flat, UVMap, signed_volume>0 (OUTWARD), unweighted=0. Ориентация Z-полос совпадает с базовыми слотами, forward=−Y (визор/наколенники к −Y). Pose-тест (defcheck): NaN=0, все вершины двигаются с правильными костями, анатомия верная.
- **Палитра брони (sRGB):** STEEL #4A4E57 (плиты), DARK #292930 (ремни/окантовка), KHAKI #6B6345/OLIVE #4D5333 (ткань), RUST C64B2C (запас, как у бандита). Границы цвета по геом. кольцам/отдельным частям (минимизирует bleed на low-poly, см. Ф3-замечание про vcol-сдвиг в UE).
- **Цвет vcol в UE:** тот же риск регион-bleed/gamma, что и у бандита — placeholder, при необходимости game-lead закажет бейк в текстуру (UV чистые 0-1, готовы под бейк). Стиль/дизайн = DRAFT placeholder-грейд.
- Гуманоидный RootAnim/бандита/волка НЕ трогал (база только читалась для KDTree).

## Раунд (2026-06-13): ПЕРЕДЕЛКА БРОНИ по Play-фидбеку (BugReport 9) — body+armor merged
- **Проблема (скрин 1):** броня = ОТДЕЛЬНЫЕ куски → при экипе слот тела заменялся на кусок → «броня парит без тела» (скрин 4: тело появлялось при снятии брони).
- **Фикс (замысел Рината):** каждый меш брони `_01` = **БАЗОВЫЙ меш слота ГГ + броня поверх**, объединённый в ОДИН скинённый меш на RootAnim.
- **Источник базы:** `MasterHumanoidCharacterSeparatedMesh.blend1` (НЕ bandit_work — там клозы бандита вджойнены). Базовые слоты ГГ: Head 82v/130tri Z[1.532,1.801], Torso 126v/218tri X±0.999(руки враскид) Z[0.905,1.549], Legs 56v/96tri Z[0.001,0.905] Y до -0.243(носки), forward=−Y, rot 0, 21 vgroup, 0 unweighted, mat 'Material', UVMap, без vcol. RootAnim 21 кость, приходит в action ArmatureAction → сброс в rest (transforms_clear, action=None). Файл только читался, не перезаписан.
- **Пайплайн (build_armor.py, headless CLI):** (1) строю armor-геометрию как раньше, triangulate/flat/vcol(армор-палитра)/smart_uv, KDTree-скин весов с базового слота → RootAnim; (2) дублирую базовый слот ГГ, снимаю Auto Smooth (NODES-модиф.), ставлю общий mat M_Armor_Flat, крашу vcol телесными цветами (body_head=SKIN; body_torso: |x|>0.55→SKIN(руки/кисти) иначе TEE; body_legs: z<0.06→SNEAKER иначе JOGGER), smart_uv; (3) `parent_clear KEEP_TRANSFORM` на арморе → `object.join()` армора в дубль базы (active) → один меш, единый 'Col' BYTE_COLOR/CORNER, единый M_Armor_Flat, единый UVMap (мерж по имени/типу), Armature-модиф. базы (RootAnim) сохраняется. Веса базы НЕ трогаются (родная риг-привязка ГГ), армор = тот же KDTree-метод (уже pose-тестирован прошлым раундом).
- **Перезаписаны (15:58):** SK_Armor_{Head,Torso,Legs}_01.fbx. Round-trip (verify_armor.py) ВСЕ OK:
  - Head_01: 182 tris (130 тело+52 шлем), 115v, Z[1.532,1.860](низ=тело,верх 1.86=шлем), signed_vol +0.0675 OUTWARD.
  - Torso_01: 314 tris (218+96), 190v, X[-0.999,0.999]=РУКИ ТЕЛА присутствуют(жилет только ±0.27)→тело+броня, signed_vol +0.139 OUTWARD.
  - Legs_01: 240 tris (96+144), 152v, Z[0.001,0.905]+Y-0.243(носки)=тело, X±0.261(плиты), signed_vol +0.063 OUTWARD.
  - Все: RootAnim 21 кость (аномальная Pelvis.009_R.002 на месте), mesh+arm rot 0 (ориентация = базовые слоты, forward −Y, без зазоров), Col BYTE_COLOR/CORNER, M_Armor_Flat, UVMap, unweighted=0.
- Head_02/Torso_02 НЕ трогал (по задаче — по 1 на слот). Стиль/палитра брони и телесные цвета = DRAFT placeholder. RootAnim/бандит/волк не сломаны (мастер только читался).

## Фаза 5 (2026-06-13): NPC-СТАРОСТА (мирный житель, placeholder draft) — ОДИН меш на RootAnim
- **Папка:** `E:/ForGameLead(Materials)/phase5-assets/` (вне git). Билдер `_build/build_elder.py`, проверки `_build/{verify_elder,posecheck_elder}.py`. Headless CLI как в Ф3/Ф4.
- **Подход (единый меш, как просил game-lead — проще модульного):** дублирую ВСЕ 3 базовых слота ГГ (Head/Torso/Legs из `MasterHumanoidCharacterSeparatedMesh.blend1`), перекрашиваю в светлую палитру, строю процедурную одежду/волосы (халат/борода/волосы) поверх в world-координатах, скин через **ГЛОБАЛЬНЫЙ KDTree** по ВСЕМ базовым вершинам (Head+Torso+Legs) с переносом весов ближайшей → общий **RootAnim (21 кость)**. Всё join в ОДИН меш. Базовые слоты сохраняют родные веса; одежда/волосы — KDTree-перенос (метод как у брони Ф4/одежды бандита). RootAnim сброшен в rest (action ArmatureAction → None, transforms_clear). UV всех частей переименованы в 'UVMap' → чистый merge при join. НЕ крутил рест-позу (урок волка).
- **Итог: `SK_Elder.fbx` — 653 трисов, 395 верт, бюджет 1.5-4k → большой запас.** 1 материал `M_Elder_Flat` (Principled, vcol-нода 'Col', Metallic0/Rough0.7), vertex-color 'Col' BYTE_COLOR/CORNER, 1 UVMap (Smart-UV 0-1), faceted flat.
- **Палитра (sRGB, DRAFT placeholder — GDD конкретики по старосте НЕТ):** светлая/тёплая, чтобы читался мирным и ОТЛИЧАЛСЯ от тёмного бандита. SKIN_OLD #D6BCA3 (бледная кожа), ROBE #CFC29E (кремовый лён — длинная рубаха/халат = силуэт), ROBE_TRIM #AD9E78 (кайма подола), SASH #735433 (коричневый пояс), HAIR #DBDBD4 (бело-седые волосы+борода), TROUSER #8C806B (штанина ниже подола), SHOE #54402E. Границы цвета по геом.зонам/частям (минимизирует vcol-bleed в UE, см. Ф3-замечание; placeholder, при нужде game-lead закажет бейк — UV готовы 0-1).
- **Дизайн DRAFT-пометки:** длинный халат-туника (плечи z1.47→подол z0.42 мид-голень, флер), борода-острие под лицом (front −Y), седые волосы по затылку/бокам (бэк +Y, лицо открыто = намёк на лысину) — отличимый силуэт от бандита/игрока. **Посох НЕ добавлял:** рест-поза = руки враскид (X±0.999, T-pose), посох в кисти лёг бы горизонтально/криво; рекомендация — отдельным пропом в сокет кисти в UE, либо доб. позже. Эскалируемых дизайн-решений по старосте в GDD нет → весь облик помечен как разумный placeholder.
- **Round-trip реимпортом (verify_elder.py) OK:** RootAnim 21 кость (аномальная Pelvis.009_R.002 на месте), mesh+arm rot 0, Col BYTE_COLOR/CORNER, M_Elder_Flat, UVMap, signed_volume +0.335 OUTWARD, unweighted=0. Z[0.001,1.825] (feet Z0, макушка/волосы 1.825), X±0.999 (руки тела ЕСТЬ), Y[-0.245,0.235] = forward −Y (носки/нос/борода к −Y, драпировка волос к +Y) — ИДЕНТИЧНО базовому телу.
- **Pose-деформ (posecheck_elder.py, реимпорт + поворот всех костей 18°/12° + depsgraph):** NaN/inf=0, движутся 395/395 вершин, max_disp 1.13 (вменяемо) → RESULT OK. mesh_vgroups=20/21: единственная не-деформирующая — `C_Neck` (в базовом риге тоже без весов; шея/голова на C_Head/C_Spine02). Скелет полные 21 кость.
- Гуманоидный RootAnim/бандита/волка/броню НЕ трогал — мастер-блэнд только читался.

- ВЫВОД (старый, про ОРИЕНТАЦИЮ бандита): экспорт корректен и байт-в-байт по ориентации = рабочий пайплайн. Менять Forward/Up НЕЛЬЗЯ — сломает относительно меша игрока. Флип 180° и «нет текстур» — UE-сторона (трансформ компонента/сокета модульных мешей; vertex-color: import «Replace» + материал с VertexColor-нодой). FBX НЕ переэкспортировал (нечего чинить). Эскалировано game-lead → unreal/cpp.

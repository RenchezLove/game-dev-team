# context: modeler-3d

> Персональная память агента `modeler-3d` (правило H). Прошлое — в `context/modeler-3d/archive.md` (read-on-demand).

## 🧭 СЕЙЧАС (2026-07-28)
**🟡 АНИМАЦИИ ГУМАНОИДА (Build 1.1, п.2–3 плана Рината) — СДАНЫ НА ПРИЁМКУ game-lead.** Три FBX в `assets/anim_humanoid/`: `Anim_AimPistol_Humanoid` (поза прицеливания, 0.033 с), `Anim_FirePistol_Humanoid` (отдача, 0.267 с), `Anim_MeleeSlash_Humanoid` (размашистый удар, 0.667 с). Паспорт `anim_humanoid.asset.md`, рендеры в `renders/`. В UE не импортировал (не моя зона).
- **🔑 АНИМАЦИЮ НА ЭТОМ СКЕЛЕТЕ АВТОРИТЬ ТОЛЬКО МИРОВЫМИ НАПРАВЛЕНИЯМИ, НЕ ИМЕНАМИ/ЭЙЛЕРАМИ.** Имена правой руки сдвинуты на сустав: `R_Arm` = плечо (аналог `L_Shoulder`), предплечье = `Pelvis_009_R_002`. Развороты костей вокруг оси неизвестны. Хелперы в `_work/acommon.py`: `aim_world` (направь кость туда) и `rot_world` (поверни вокруг мировой оси через свою голову) + `Keyer`, который держит кватернионы знаково-непрерывными (иначе кость крутится длинным путём между ключами).
- **🔑 ПРЕДЕЛ УГЛА В ПЛЕЧЕ 110°** (угол ключица↔плечо). За ним дельтовидная область расплывается в плоский «щит» поперёк груди — у скелета нет скручивающих/вспомогательных костей. Поймал крупным рендером на 127° (проводка удара). Проверка зашита в билдер, FAIL при превышении. NB: БОЛЬШЕЕ выведение ключицы вперёд УМЕНЬШАЕТ этот угол при движении руки поперёк тела — интуиция обратная.
- **🔑 Экспорт анимации гуманоида = ТОЛЬКО АРМАТУРА, без мешей** (так устроен эталон проекта `Anim_Run_Humanoid.fbx`), `add_leaf_bones=False` → 21 кость + RootAnim = 22 в UE, `bake_anim_simplify_factor=0.0`. Действие обязательно пропихивать во временную NLA-полосу — экспортёр 5.x игнорирует «свободное» действие (урок волка).
- **🔑 ДЛИНУ ТАКТА ПРОВЕРЯТЬ В САМОМ КОНТЕЙНЕРЕ FBX** (`io_scene_fbx.parse_fbx`, узел `Takes/LocalTime`), а не по диапазону сцены после реимпорта — там вылезает заводское 1..250 и врёт. Скрипт `_work/14_take_length.py`. Оси моих файлов сверены с эталонами: 7400, Up=Y, Front=Z, Coord=X, все знаки +, UnitScale 1.0. Частота у меня 30 к/с, у старого бега 24 — на смешивание не влияет.
- **🔑 Верхнетелесная анимация: ключи ТОЛЬКО на 12 костях верха, ноги и корень не трогать** — тогда суммарное перемещение ног ровно 0.000000 м и ходьба не ломается (проверяется в билдере). Накладывать послойно от `C_Spine01`, полным телом не играть.
- Двуручный хват пистолета на этом скелете НЕВОЗМОЖЕН: размах плеч шире длины руки, левая кисть не достаёт до рукояти при вытянутой правой. Сделана согнутая поддержка у груди.
- Камеру «строго сверху» ставить с ЯВНЫМ вектором верха (у меня — «вперёд» персонажа), иначе `to_track_quat` вырождается и крен кадра случайный — не понять, куда персонаж смотрит.
- Отдача выстрела вертикальная → с игровой камеры под 60° читается слабо, сбоку отлично. Сказал лиду: добирать вспышкой и тряской камеры, либо заказать более заметный откат.

## 🧭 БЫЛО (2026-07-19)
**🟢 БЕРЁЗА `SM_Tree_Birch_01` (модель С НУЛЯ) — ПРИНЯТА ОКОНЧАТЕЛЬНО (владелец + лид, 07-19). Работа закрыта, заданий нет.** 226 трис (лид: «40 треугольников за читаемую бересту — нормальная цена, мы дешевле SM_Tree_03»). Паспорт `assets/birch/SM_Tree_Birch_01.asset.md`, рендеры в `renders/`.
- **Импорт делает лид/оператор, НЕ я:** берёза идёт ОТДЕЛЬНЫМ ассетом `SM_Tree_Birch_01`, существующее `SM_Tree_01` НЕ подменяется — чтобы в лесу можно было смешивать оба вида. Моё имя меша в FBX под этот план подходит. Напоминание передано: Vertex Color = Replace.
- Перекраску `SM_Tree_01` НЕ УДАЛЯТЬ — лежит запасным вариантом на случай, если отдельный ассет окажется неудобен при импорте.
- **История разворотов (важно для понимания папки):** сначала задача была «модель с нуля» → лид ОТМЕНИЛ, велел перекрасить `SM_Tree_01` → владелец выбрал ОБРАТНО модель с нуля («крона такая даже лучше»). Перекраска `SM_Tree_01` (112 трис, 3 варианта, `SM_Tree_01_birch*.fbx` + `renders/recolor/`) — ЗАПАСНОЙ вариант, НЕ развивать (так записано и у лида).
- Чёрточки: 5 поясков вместо 3, тоньше и вразнобой (5.0/4.5/4.0/3.5/3.0 см; тёмных граней то 2, то 1; номера граней 0-2, 4, 1-3, 0, 2-4). **Все ниже 1.03 м — выше начинается крона и ствол не виден, туда треугольники не тратим.** С игровой камеры (30 м) ствол остаётся БЕЛЫМ, в «серую кашу» не превращается — проверено рендером.
- **🔑 ЛОВУШКА НЕДЕТЕРМИНИРОВАННОЙ СБОРКИ (мой баг, поймал сам):** в `clump()` дрожание вершин применялось при обходе `set` bmesh-вершин. Порядок обхода множества зависит от адресов объектов ⇒ смещается при изменении числа вершин СТВОЛА ⇒ крона молча пересобиралась при каждой правке ствола (замерено 0.103 м на вершину — а лид явно сказал крону не трогать). ЛЕЧЕНИЕ: принятую крону снял из `_qc_birch.blend1` скриптом `24_extract_crown.py` в данные `_work/crown_approved.py`, билдер её больше НЕ генерирует. Сверка `25_verify_crown.py`: расхождение 0.00001 м = float32 в FBX. **ДВА ПРАВИЛА НА БУДУЩЕЕ:**
  1. Никогда не применять случайные смещения при обходе множества/словаря — только по отсортированному списку. Иначе сборка недетерминирована и ассет молча меняется от посторонних правок.
  2. **🔑 ПОДПИСЬ ГЕОМЕТРИИ ПРИМЕНЯТЬ К НЕПРИКОСНОВЕННОМУ, А НЕ К ИЗМЕНЁННОМУ.** Сверка позиций всех вершин ловит не твою правку (её ты и так видишь на рендере), а её ПОБОЧНЫЕ ПОСЛЕДСТВИЯ в тех частях, которые трогать было нельзя. Правишь ствол — сверяй крону. Лид принял эту формулировку как общее правило команды. Цена вопроса в этот раз: без сверки я бы сдал крону, уехавшую на 10 см, и никто бы не заметил — по рендеру такое списывается на другой ракурс, а владелец крону отдельно хвалил.
- Осторожно: повторный прогон билдера затирает `_qc_birch.blend` и сдвигает старый в `.blend1` — эталон принятой кроны теперь только в `crown_approved.py`, не в blend1.
- Геометрия при перекраске НЕ менялась — доказано сверкой подписи (позиции ВСЕХ вершин + UV + число граней), а не только числом треугольников: 112 трис / 60 верт до и после. Скрипт падает с ОСТАНОВом, если подпись разошлась.
- **🔑 ГЛАВНОЕ ОГРАНИЧЕНИЕ: у стволов ВСЕХ трёх лиственных деревьев ровно ДВА кольца вершин** (`SM_Tree_01` z=0.000 и 1.100; `_02` 0/0.700; `_03` 0/0.900) — лог `_work/20_inspect.log`. Цвет вершин живёт в углах граней ⇒ поперечные чёрточки берёзы положить НЕКУДА без добавления кольца рёбер. Доступен только градиент снизу вверх (тёмный комель → белая кора). Это ограничение геометрии, а не мой выбор.
- **Сверху берёза читается, оба признака работают** (рендеры с настоящими параметрами камеры): белый ствол ВИДЕН светлой полоской из-под кроны, потому что камера под наклоном 60°, а не строго вниз (у обычных деревьев там же коричневая полоска); подсветлённая крона `#5C7A3E` читается тем более и работает под любым углом.
- Ориентир/исходники: `E:/ForGameLead(Materials)/phase3-assets/` (все деревья/камни/забор), билдер `_build/build_props.py`, палитра в `context/concept-artist/tmp/cs-palette.html`.
- **🔑 ПЕРЕКРАСКА ЧУЖОГО FBX БЕЗ ДРЕЙФА ЦВЕТА:** в файле лежит `linear = s2l(sRGB)`; Blender на импорте кладёт это сырое число в `.color_srgb`, т.е. `.color` становится на шаг темнее. Поэтому нетронутые углы — `.color = сырое V`, перекрашенные — `.color_srgb = задуманный sRGB`, экспорт всегда `colors_type='LINEAR'`. Без этого цвет темнеет на КАЖДОМ круге импорт-экспорт.
- **🔑 ВЕТЕР У ДЕРЕВЬЕВ = МАСКА ПО ВЫСОТЕ, НЕ ПОКРАСКА ВЕРШИН.** `M_WindTrees`: `LocalPosition` → нормировка через `ObjectLocalBounds` (Min/Max) → `Saturate` → `WindWeight` узла `SimpleGrassWind`. Источники: ADR-039 п.2 + таблица имён `M_WindTrees.uasset`. От меша нужен только пивот в основании и `zmin=0`; высота меша роли не играет (нормировка по своим границам). Vertex color целиком идёт в БАЗОВЫЙ ЦВЕТ — текстур у материала нет вообще.
- **🔑 `M_WindTrees` = `BLEND_Opaque` без текстур** → полупрозрачная листва на плоскостях НЕВОЗМОЖНА, крону делать объёмом (у `SM_Tree_01` крона = сплошная икосфера 80 трис). Плоскости встали бы картонками.
- **🔑 Кодировка цвета старых пропсов СОВПАДАЕТ с моей текущей.** Старый `build_props.py` писал `.color = s2l(sRGB)` и экспортировал без `colors_type`; я пишу `.color_srgb = sRGB` и экспортирую `colors_type='LINEAR'`. В FBX оба кладут `linear = s2l(задуманный sRGB)` — сверено (`_work/12_color.log`). Новые ассеты не будут ярче/темнее старых соседей.
- **🔑 Реимпорт FBX в Blender: `.color_srgb` отдаёт СЫРОЕ linear-число из файла** (не задуманный sRGB). Чтобы отрендерить старый ассет в задуманном цвете — прочитать `.color_srgb` и положить это же значение в `.color`.
- **Игровая камера (факт из `Source/ContrarySurvivor/Characters/PlayerCharacter.h`):** перспектива, `CameraBoomRotation` pitch −60°, `CameraArmLength` 3000 uu = 30 м, `CameraFieldOfView` 40° → видимая ширина кадра ≈21.8 м. В Blender: `rot_x = 30°`, камера на `цель − 30 м · (0, cos60, −sin60)`, `sensor_fit='HORIZONTAL'`, `angle_x = 40°`.
- Скрипты: `_work/10_build_birch.py` (сборка+самопроверка+экспорт), `11_verify_birch.py` (round-trip), `12_color_compare.py` (сверка кодировки цвета), `13_render_birch.py` (13 рендеров).
- **Урок этой задачи (сам поймал на своих же рендерах, до сдачи):** первая версия вышла «фонарным столбом» — тонкий прямой ствол, тёмный комель сплошным кольцом читался «сапогом», чёрточка была видна ровно одна и выглядела наклейкой, крона мелкая с хвойным шпилем. Починил: ствол толще (r 0.105 у комля) + читаемый S-изгиб, комель тёмный на 3 грани из 5, три пояска-чёрточки, крона шире и округлая сверху. **Смотреть свои рендеры глазами и переделывать ДО отправки лиду.**
- Хвосты: лут-пикапы ПРИНЯТЫ 07-19 (коммит `7b13e26`). ТОРГОВЕЦ (`assets/npc_trader/`, 896 трис, `16e43cc`) — ждал приёмки game-lead; снимок в `archive.md`.

## Постоянные директивы (не трогать без причины)
- **Инструмент (ADR-021):** headless CLI `E:/Programs/Blender/blender.exe -b [файл.blend] --factory-startup --python <скрипт> -- <args>` (Blender 5.1.2). MCP `mcp__blender__*` в субагенте НЕ инжектится. Лог — в файл. Bash без Python; PowerShell-кириллица капризна.
- **Конвенции:** метры, FBX uniform scale 1 (UE ×100), FBX7400 MESH-only, vcol 'Col' CORNER/BYTE, 1 мат/ассет (минимум слотов — Android), flat shade, нормали наружу (recalc + signed_volume>0), Smart-UV 0-1. Пропсы: forward +Y, origin центр основания Z=0. Оружие: forward +X, origin рукоять. Гуманоиды: forward −Y.
- **Каталог (ADR-022):** `assets/<id>/` = паспорт `<id>.asset.md` (цифры ТОЛЬКО из round-trip) + превью + рендеры. Бленды пер-ассет НЕ копируем — источник правды = билдер + QC-бленд в `_work/`.
- **Правило сдачи:** приёмка визуально game-lead по PNG → потом финал-экспорт + round-trip + пруф файла на диске. «Экспортировано/готово» — только после проверки файла.
- **🔑 Пайплайн NPC = пайплайн экипировки:** `10_build_*.py` (детерминированный из `armor_wearables/_work/master.blend`) → `11_pose_check_*.py` → `12_export_verify_*.py`; хелперы `armor_wearables/wcommon.py`. Схема: dup базы (BaseHead/L1_Torso/L1_Legs) → weld 1e-4 → перекраска зон vcol → навесная геометрия bmesh → перенос весов KDTree → finalize → export LINEAR. Трюк «тело под одеждой красить цветом одежды» убирает просветы. У старосты `drop_front_boxes` удалял 4 коробки торса; у торговца они ОСТАВЛЕНЫ как подсумки — выбор зависит от костюма.
- **🔑 КОНВЕНЦИЯ ЦВЕТА FBX:** vcol экспорт ТОЛЬКО `colors_type='LINEAR'` (зашит в `wcommon.export_skeletal`); умолчание 'SRGB' → двойное кодирование в UE → тёмно-серо. vcol пишем `color_srgb` = sRGB-хекс палитры напрямую; в FBX ложится linear-пересчёт — это норма. Судить о цвете в UE — ТОЛЬКО по UE (PIE/Geometry Script), Blender-ре-импорт FBX — ложный слепок (урок 07-09). О тёплых/светлых тонах — по РЕНДЕРУ, не по хексу (урок седины: палитровый `#9AA08C` холодит, брали `#ADA492`).
- **🔑 УРОК ×100:** арматура из UE-FBX приходит ребёнком EMPTY (scale 0.01) базис 100; `transform_apply` печёт только базис → кости ×100, в покое невидимо, при позе меши взрываются. Печь ПОЛНУЮ matrix_world в edit-кости (`wcommon.apply_armature_transform`). Позовый прогон ОБЯЗАТЕЛЕН перед сдачей.
- **🔑 УРОК weld:** UE-меши развёрнуты по вершинам (verts≈3×tris) — перед островным анализом `remove_doubles` 1e-4 (веса/vcol переживают). Headless-скрипт может упасть Traceback при EXIT=0 — всегда grep Traceback в логе.
- **UE-памятка:** импорт Vertex Color = Replace; материал слота в FBX — заглушка, в UE вешать рабочий `M_VColor` (М_*_Flat не компилился, прецедент 07-09); тонкое (ткань/хвосты/лезвия) — Two-Sided.
- **Blender-сокет одноклиентный** — не спавнить двух modeler к Blender параллельно.
- Сданное ранее: староста (07-09, в UE на BP_Elder), экипировка T0-T3 (07-06, в UE), пропсы/оружие/машина/палатка — архив.

## ЧЕК-ЛИСТ перед QC-рендером (наказ Рината)
- [ ] Пропорции не «бумажные», силуэт читается с 3 ракурсов; узнаваемость формы.
- [ ] Все грани прокрашены (magenta=0); рендер view_transform='Standard', без контрового света.
- [ ] Оси/origin по конвенции части; rot 0, scale 1, zmin по конвенции.
- [ ] Головные уборы не закрывают лицо; шея/обувь объёмные (гуманоиды).
- [ ] Тонкая геометрия → пометка Two-Sided в паспорте.

# context: modeler-3d

> Персональная память агента `modeler-3d` (правило H). Прошлое — в `context/modeler-3d/archive.md` (read-on-demand).

## 🧭 СЕЙЧАС (2026-07-06)
**🟢 ЭКИПИРОВКА ЗАКРЫТА (07-06): обе партии (Т0/Т1 + Т2/Т3) сданы; game-lead переимпортировал все 12 FBX (переэкспорт 19:07, `colors_type='LINEAR'`) в UE — 12/12 прошли, цвета совпали с палитрой 1-в-1 (его проверка данными ассетов). Конвейер Blender→UE по скелетным мешам с vcol подтверждён рабочим. Активных задач НЕТ. Финальный вкус по Т2/Т3 — за Ринатом (возможны мелкие правки; билдеры воспроизводимы).**
- **🔑 КОНВЕНЦИЯ ЦВЕТА FBX (07-06, записана в паспорт; для ВСЕХ будущих экспортов):** vcol экспортировать ТОЛЬКО с `colors_type='LINEAR'` (зашит в `wcommon.export_skeletal`). Умолчание 'SRGB' кладёт байты color_srgb в файл как есть → UE кодирует второй раз → тёмно-серые меши (дефолт: исходник 5.1.2 `io_scene_fbx/__init__.py:432`, механика: `export_fbx_bin.py:1307`). Пруфы: `_dump_vcol.log` (в файлах линейные значения), `_export.log`+`_export_t2t3.log` (ROUNDTRIP ALL PASS). Диагностика: `07_cstest_colorspace.py`, `08_dump_vcol.py`; временный cstest-файл удалён.
- Выход: `assets/armor_wearables/SK_Armor_T2_{Head,Torso,Legs}.fbx` (110/422/328 трис) + `SK_Armor_T3_{Head,Torso,Legs}.fbx` (212/410/328). Замечание Рината «меньше лишней геометрии» учтено: комплект Т2=860 трис (у Т1 было 1260); всё нательное (камо/заплатки/сколы) — vcol по граням. Т-позу не трогал (замечание №2, чинится отдельной задачей).
- Паспорт (оба батча): `armor_wearables.asset.md`. Рендеры: `renders/` (lit/wire на предмет, сборки `T2_assembled_*`/`T3_assembled_*`, кросс-миксы `MIX_T2torsoT3legs_*`/`MIX_T3torsoT2legs_*` + все `_pose_*` — взрывов/клиппинга нет). Логи-пруфы: `_build04.log`, `_pose_t2t3.log`, `_export_t2t3.log` (ROUNDTRIP ALL PASS: 21 кость, имена = handoff, рест-дельта 2e-5, unweighted 0).
- Пайплайн партии 2: `04_build_t2t3.py` (из `_work/master.blend` → `_work/wearables_t2t3.blend`) → `05_pose_check_t2t3.py` → `06_export_verify_t2t3.py`; хелперы `wcommon.py`. Веса новой геометрии — KDTree с ближайшей вершины источника.
- **🔑 Уроки партии 2:** (1) headless-скрипт может упасть с Traceback при EXIT=0 — всегда grep Traceback в логе; (2) навесное НЕ сажать на захардкоженные центры/радиусы и НЕ брать max-радиус среза (срез ловит чужую геометрию: у голенища — ступню, у плеча — весь бок куртки) — либо радиус от фронтальной поверхности (cy−ymin), либо драпировка по max-z поверхности; (3) на свёрнутом (welded) меше узкие координатные полосы выборки пустеют — расширять с fallback.
- Следующее по ТЗ: Т0 назначение дефолтом в BP + импорт в UE — за game-lead/unreal; за мной остаётся 10-я позиция ТЗ только если она не закрыта партией 1 (Т0 сдан) и возможные правки по приёмке Т2/Т3.
- **🔑 УРОК (арматура из UE-FBX!):** арматура приходит ребёнком EMPTY (scale 0.01) с базисом 100; `ops.transform_apply` печёт только базис → после удаления EMPTY кости остаются ×100. В ПОКОЕ НЕВИДИМО (деформация тождественна), при любой позе меши взрываются. Лечение: печь ПОЛНУЮ matrix_world в edit-кости (`wcommon.apply_armature_transform`). Позовый прогон ОБЯЗАТЕЛЕН перед сдачей скелетного меша.
- **🔑 УРОК:** UE-выгруженные меши полностью развёрнуты по вершинам (verts≈3×tris) — перед островным анализом weld (`remove_doubles` 1e-4; веса/vcol переживают). Перед чтением `matrix_world` — `view_layer.update()` (несвежая матрица подсунула ×100 базис). При перепривязке ставить `matrix_basis`/`matrix_parent_inverse` = Identity явно.
- **🔑 vcol одежды персонажей:** байты = sRGB-хексы палитры НАПРЯМУЮ (`color_srgb` / bmesh raw hex/255) — конвенция существующего контента L1/бандита (дамп: кожа L1 = #C9A084 в байтах). lin() тут НЕ использовать.
- Прошлые серии (D8 машина+палатка — сданы, vcol-контракт M_CarPaint, хвосты переделок) — в archive.md.

## Постоянные директивы (не трогать без причины)
- **Инструмент (ADR-021):** headless CLI `E:/Programs/Blender/blender.exe -b [файл.blend] --factory-startup --python <скрипт> -- <args>` (Blender 5.1.2). MCP `mcp__blender__*` в субагенте НЕ инжектится. Лог — в файл. Bash без Python; PowerShell-кириллица капризна.
- **Конвенции:** метры, FBX uniform scale 1 (UE ×100), FBX7400 MESH-only, vcol 'Col' CORNER/BYTE, 1 мат/ассет (минимум слотов — Android), flat shade, нормали наружу (recalc + signed_volume>0), Smart-UV 0-1. Пропсы: forward +Y, origin центр основания Z=0. Оружие: forward +X, origin рукоять. Гуманоиды: forward −Y.
- **Каталог (ADR-022):** `assets/<id>/` = паспорт `<id>.asset.md` (цифры ТОЛЬКО из round-trip) + превью + рендеры. Бленды пер-ассет НЕ копируем — источник правды = билдер + QC-бленд в `_build/`.
- **Правило сдачи:** приёмка визуально game-lead по PNG → потом финал-экспорт + round-trip + пруф файла на диске. «Экспортировано/готово» — только после проверки файла.
- **🔑 Цвет (двойное затемнение):** lin() темнит палитру (BYTE_COLOR читает linear как sRGB). Для точного/светлого тона — писать СЫРОЙ sRGB как linear (машина/палатка так и сделаны) или attr.color напрямую. Судить о цвете FBX по РЕНДЕРУ, не по дампу.
- **UE-памятка:** импорт Vertex Color = Replace; материал VertexColor('Col')→BaseColor; тонкое (ткань/трава/лезвия) — Two-Sided.
- **Blender-сокет одноклиентный** — не спавнить двух modeler к Blender параллельно.

## ЧЕК-ЛИСТ перед QC-рендером (наказ Рината)
- [ ] Пропорции не «бумажные», силуэт читается с 3 ракурсов; узнаваемость формы.
- [ ] Все грани прокрашены (magenta=0); рендер view_transform='Standard', без контрового света.
- [ ] Оси/origin по конвенции части; rot 0, scale 1, zmin по конвенции.
- [ ] Головные уборы не закрывают лицо; шея/обувь объёмные (гуманоиды).
- [ ] Тонкая геометрия → пометка Two-Sided в паспорте.

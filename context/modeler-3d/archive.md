# archive: modeler-3d (read-on-demand, только ПРОШЛОЕ)

> Закрытые этапы/раунды. Единственный источник правды на «сейчас» — блок «🧭 СЕЙЧАС» в context.md.

## 🧭 СЕЙЧАС (2026-06-29) — АРХИВНЫЙ СНИМОК (этап закрыт)
**🟢 SM_GroundPlane (пол уровня под vertex-paint дороги) — ✅ ГОТОВ + ЭКСПОРТИРОВАН + round-trip OK (2026-06-29). Импорт в UE за game-lead.** `assets/ground_plane/SM_GroundPlane.fbx` (651436 б) + билдер `build_ground_plane.py` рядом. Квад-грид 181×181 = **32761 в / 32400 квадов** (НЕ триангулирован — UE сам), ячейка 2.0 м, **360×360 м Blender → 36000×36000 ед. UE** (конвенция: строю в метрах, import uniform_scale=1, UE ×100). Пивот в ЦЕНТРЕ на Z=0 (bbox −180..180 XY, origin мира 0,0,0). Нормали ВСЕ +Z (polys_not_up 0). vcol 'Col' CORNER/BYTE = ВСЕ чёрные (max_rgb 0.0) — маска «весь пол трава». UV планарная 0..1 (НЕ smart_project-острова). 1 мат M_GroundPlane (vcol→BaseColor; game-lead назначит свой). UE-импорт: Vertex Color = Replace. Допущения отданы game-lead: масштаб (если пол импортится иначе — перегоню), триангуляция (если пайплайн требует — добавлю).

**🟢 СЕССИЯ ПЕРЕДЕЛОК (4 ассета по новым рефам) — ВСЕ 4 ГОТОВЫ headless, ждут финального визуала Рината вживую. Коммит за game-lead.**
  - **SM_Pistol ✅ + РЕВИЗИЯ 06-28 вечер: ДОБАВЛЕН ЧЁРНЫЙ СТВОЛ/ДУЛО, СДЕЛАНО КАНОНОМ.** Ринат в живом Blender добавил у среза затвора ствол (гранёный цилиндр +X, 13 граней/38 трис) и сохранил в `assets/pistol/NewPistol/SM_Pistol.blend`. Я (headless): вычленил ствол однозначно (дифф с прошлой 204-трис версией + геометрия цилиндра — радиальные нормали, чистый разрыв по X между боксом затвора ≤0.10 и стволом ≥0.165), окрасил 13 граней в ЧЁРНЫЙ vcol 'Col' = точный цвет рамы (RGB 0.0745, ADR-032), затвор сине-серый/рукоять коричневую не трогал. Затем КАНОНИЗИРОВАЛ (перезапись прежнего 192-трис канона): нормали наружу + триангуляция (0 n-граней) → `assets/pistol/SM_Pistol.{blend,fbx}` + 6 рендеров (front/side/threeq/top + wire×2) + preview + паспорт. **Round-trip FBX: 240 трис/146в**, bbox 0.201×0.031×0.141, rot0/scale1, sv +0.00036 (нормали наружу), 1 мат M_Pistol, Col CORNER/BYTE, UVMap, чёрный ствол пережил экспорт. origin рукоять(0,0,0), forward +X. NewPistol/ (исходник Рината) НЕ тронут. Коммит за game-lead. ⚠️ .blend Рината был в EDIT MODE → force object mode (mode_set сработал в -b). Скрипты: scratchpad/{analyze_pistol,paint_barrel,canon_pistol,verify_fbx,render_canon}.py. Источник правды пистолета теперь = NewPistol blend (Ринат правил руками), НЕ build_weapons.py.
  - **SM_Woodpile ✅** — переделана по `assets/woodpile/NewRef/1.jpg`: торцы поленьев hex-упаковкой (3 ряда 6/5/6), ДВУХТОН (светлый распил CUT + коричневые торцы коры), 2 полена ПОПЕРЁК сверху (log_along_x — НОВЫЙ хелпер), тёмно-КОРИЧНЕВЫЙ задник (не чёрный PINE). **392 трис/236в** (было 352), bbox 1.178×0.55×0.638, sv +0.40. `build_villageprops.py` build_woodpile переписан.
  - **SM_Awning ✅** — жерди+перекладины с круглого(seg6) на КВАДРАТНЫЙ 4-гранный брус (D.box). **168→120 трис**, bbox 2.2×1.37×2.435, sv +2.07. Брезент не трогал (Two-Sided в UE).
  - **SM_Clothesline ✅** — столбы→квадратный брус, верёвка 8→4 сегмента. **160→112 трис**, bbox 2.58×0.12×1.8, sv +0.14. Ткань не трогал (Two-Sided).
  - **SM_ChoppingBlock ✅** — game-lead выбрал вариант (а): Ринатов `NewChoppingBlock/SM_ChoppingBlock.blend` сделан КАНОНОМ КАК ЕСТЬ. Уже 100 трис/64в, всё треуг. — резать без потери силуэта нечего (оптимизация неприменима, факт). Скрипт `scratchpad/canon_block.py`: открыл Ринатов blend, transforms уже identity, нормали пересчитал наружу (sv +0.053), на землю Z0, **save-as копией** в `assets/choppingblock/SM_ChoppingBlock.{blend,fbx}` (Ринатов исходник НЕ тронут), рендер props_render, verify_props OK. bbox 0.79×0.38×0.903, M_ChoppingBlock+Col+UVMap. Процедурный build_villageprops-вариант ЗАМЕНЁН. ⚠️ ГОТЧА: Ринат сохранил .blend в EDIT MODE → в headless `-b` to_mesh падает «in editmode» + операторы select_all/mode_set ловят «context incorrect»; фикс — force_object_mode() через temp_override с VIEW_3D-area из bpy.data.screens, селекция через data API (o.select_set), transform_apply пропустить если уже identity.
  - **ВСЕ паспорта .asset.md + превью обновлены реальными цифрами (round-trip из FBX). Коммит — за game-lead.**
  - Билдеры теперь принимают `--only <id[,id]>` (build_weapons.py, build_villageprops.py) — собирать один ассет, не трогая остальные FBX.

<details><summary>архив СЕЙЧАС (2026-06-26)</summary>

## 🧭 СЕЙЧАС (2026-06-26)
**🟢 ЭТАП C3 (4 ДЕРЕВЕНСКИХ ПРОПСА) — слеплены+экспортированы+проверены, ЖДУТ ОСМОТРА game-lead (2026-06-26).** Спек `docs/contrary-survivor/props-village-spec.draft.md`. Билдер `E:/ForGameLead(Materials)/demo-assets/_build/build_villageprops.py` (все 4, dcommon), verify `verify_props.py`, рендер `props_render.py` (lit+wire в один проход; угол **+Y = фронт пропса** → ракурсы pthreeq/pfront/side/top/bthreeq). Каталог `assets/<id>/` (.blend+.fbx+рендеры+превью+сайдкар). КОНВЕНЦИЯ ПРОПСОВ: forward **+Y**, up +Z, **origin центр основания на земле Z=0** через finalize(origin='ground'), геометрия центрирована X=0,Y=0. round-trip все 4: rot0/scale1/zmin0/sv>0(нормали наружу)/Col CORNER-BYTE/1 мат/1 UVMap.
  - **SM_Awning** 168 трис, bbox 2.2×1.37×2.43. M_Awning. Лин-ту: 4 жерди+2 перекладины+скатный брезент (провис, обвисшие края), открытая сторона +Y. ⚠️ брезент однослойный → **Two-Sided в UE**. `assets/awning/`.
  - **SM_Woodpile** 352 трис, bbox 1.32×0.57×0.815. M_Woodpile. 14 поленьев-торцов шахматкой + тёмный блок-заполнитель (щели) + 3 верхних. Контраст СВЕТЛЫЙ срез / тёмная кора; срезы по +Y. `assets/woodpile/`.
  - **SM_Clothesline** 160 трис, bbox 2.569×0.12×1.8. M_Clothesline. 2 столба+подпорки + провисшая верёвка-катенария + 3 драпированные вещи (низ ≥0.78м). ⚠️ ткань однослойная → **Two-Sided в UE**. `assets/clothesline/`.
  - **SM_ChoppingBlock** 100 трис, bbox 0.79×0.38×0.882. M_ChoppingBlock. Пень (срез+зарубки) + воткнутый топор (стальной клин+ржавая кромка, топорище вверх+Y) + 2 расколотых полена. Один меш. `assets/choppingblock/`.
  - **УРОК (CUT-цвет):** dcommon lin() темнит палитру → светлый срез #C9A86A читался как бурый. Фикс: CUT задан СЫРЫМ sRGB `(0.90,0.80,0.58)` БЕЗ lin() → реально светлый. Остальная палитра через H()=lin(hex) как обычно.
  - **УРОК (ракурс QC):** harness `_qc_render.py` зовёт камеру −Y «front» (под гуманоидов forward−Y). У пропсов forward **+Y** → стандартный front/threeq показывал ЗАД. Сделал `props_render.py` с углами от +Y (pthreeq/pfront) + bthreeq (зад). Для +Y-пропсов рендерить им.
**🟢 ЭТАП D (ОРУЖИЕ) — ✅ ОСМОТР game-lead ПРОЙДЕН (2026-06-26), ждёт приёмки Рината + интеграции в UE.** Низкополи по референсам `context/concept-artist/tmp/hud-icons/{pistol,knife}.png`. Билдер `build_weapons.py`, verify `verify_weapons.py`. Каталог .blend+.fbx+рендеры+сайдкар в `assets/{pistol,knife}/`. КОНТРАКТ: forward **+X** (ствол/клинок), up +Z, **origin в РУКОЯТИ (0,0,0)** (origin='offset'). NB game-lead (план D4): при креплении ножа в UE развернуть клинок плоскостью к изо-камере (не ребром).
  - **SM_Pistol** 132 трис, bbox 0.199×0.028×0.137 м. M_Pistol. `assets/pistol/`. (старый прототип в `demo-assets/SM_Pistol.fbx` игнор)
  - **SM_Knife** 80 трис, bbox 0.278×0.028×0.036 м. M_Knife. `assets/knife/`.
**Фаза (демка):** ContrarySurvivor, доводка Фазы B по фидбеку Рината (живой PIE). Планка стиля — концепт `art/village.png` + LDoE.
**🟢 НОВЫЙ АССЕТ `SM_Campfire` — ✅ ПРИНЯТ game-lead (осмотрел top/threeq/front + wireframe, round-trip OK) + ЭКСПОРТИРОВАН (2026-06-25 20:09). Реимпорт UE + сокет огня (0,0,0.1175) — за game-lead.** Решение game-lead: SM_Campfire ЗАМЕНЯЕТ костёр деревни (сейв-точка); `SM_CampfireLogs`/`SM_BanditCampfire` НЕ трогаем. Обобщённый лагерный костёр по реф. `Screenshots/6/1.png` (низкополи фасеточный). Билдер `demo-assets/_build/build_campfire.py` → `_qc_campfire.blend` → `demo-assets/SM_Campfire.fbx` (36812 б). **456 трис**, 276 в, bbox 1.076×1.05×0.516 м, signed_vol +0.103 (нормали наружу), Col CORNER/BYTE, 1 мат M_Campfire, UVMap, rot0/scale1/zmin0, magenta=0. Состав: 6 поленьев шалашиком (warm wood WOOD/WOOD2, верхние концы CHAR per-face z-градиент CHAR_LO0.33/HI0.47), кольцо 10 гранёных icosphere-камней (subdiv0 + jitter, shade по нормали ROCK_LIT/MID/DARK + per-камень тон), тёмная зола-octagon + 2 обугленных стуба + угли EMBER/EMBER2. **Сокет огня FireVFX_Socket = (0,0,0.1175)** (Empty в бленде, в MESH-only FBX НЕ экспортится — координаты для сокета в UE). Рендеры `_qc_renders/Campfire_v2_{front,threeq,side,top,top34}.png` + wireframe `Campfire_v2_wire_{threeq,top}.png`. Сайдкар `assets/SM_Campfire/` (ADR-022, числа из FBX). NB: уже есть `SM_CampfireLogs`(деревня 260т) и `SM_BanditCampfire`(база бандитов 248т) — этот — отдельный по 1.png, применение за game-lead. Реимпорт UE за game-lead.
**🟢 ФАЗА B ДОВОДКА — ПРИНЯТА game-lead + ВСЕ 5 FBX ЭКСПОРТИРОВАНЫ (2026-06-25 15:35-15:36, поверх существующих в `demo-assets/`). Реимпорт в UE — за game-lead.** round-trip всех 5 OK: rot0/scale1/zmin0/signed_vol>0(нормали наружу)/Col/1 мат/magenta0; трис House868/Shed352/Campfire260/Bush01-140/Bush02-200; тон/форма не поехали (verify `scratchpad/verify_roundtrip.py`; экспорт по имени `scratchpad/export_by_name.py`, кусты с `--recenter` т.к. в _qc_vegetation сдвинуты по X; дом — через `recolor_house_roof.py` сам экспортирует, тон скатов #5F6657). ⚠️ NB game-lead: если крыша под top-down в UE светловата — опустить тон #5F6657 одной строкой в `recolor_house_roof.py` и переэкспортировать дом.
**4 правки (детали):**
  - **Кусты `SM_Bush_01`(140т)/`_02`(200т)** — переделаны под LDoE: вместо гранёных icosphere-комков — кластер перекрывающихся округлых лоб (купол с холмистым верхом, читаемый top-down), вертикальный градиент per-loop тёмный низ #25431B → светлый верх #5F8A3C. `build_vegetation.py` (функция `bush()` переписана; ёлки/камни НЕ тронуты, билдер тот же). Рендеры `_qc_renders/_after/Bush0{1,2}_AFTER_*`.
  - **Крыша дома `SM_VillageHouse`** — была почти-белая #9AA08C + полосы (доска ROOF2/скат разной светлоты). ФИКС: единый спокойный выветренный серо-зелёный #5F6657 на ОБА ската (полосы убраны в `slope()`: все доски ROOF; финальный тон в `recolor_house_roof.py`). Стены/окна не тронуты.
  - **Трубы открыты** (`SM_VillageHouse` + `SM_BanditShed`): убрал сплошной cap, верх = полая труба (4 стенки-обод + тёмное углубление-нутро) — читается как труба, не заглушка. Хелпер `chimney()` в обоих билдерах. Дом 868т, сарай 352т.
  - **Костёр деревни `SM_CampfireLogs`(260т)** — был плоский крест брёвен («грустный»). Переделан в ШАЛАШ/домик (6 поленьев конусом, основания на земле, вершины сходятся вверху) + кольцо камней-очага (светлый серый) + тлеющие угли + ПАРА обугленных поленьев (тёплое дерево доминирует). Новый билдер `build_campfirelogs.py`. (`SM_BanditCampfire` НЕ трогал.)
  - Кости/кровь логова — НЕ делаю (game-lead собирает в BP_WolfDen в UE).
  - **Пайплайн:** headless CLI `E:/Programs/Blender/blender.exe -b --factory-startup`; MCP-сокет 9876 НЕ слушается (хоть `blender-mcp.exe` в процессах). Рендер-харнесс `_qc_renders/_qc_render.py` (+добавил ракурс `top` строго сверху). По приёмке — экспорт FBX поверх существующих в `demo-assets/`, round-trip, реимпорт за game-lead.
**Фаза (ранее):** Этап C (внешний вид). Планка стиля — концепт `art/village.png` + борд `docs/.../ui/house-material-board.draft` (выветренное дерево, vcol, mobile).
**🟢 МЕШ-СЕРИЯ ДЕМКИ ЗАКРЫТА (всё экспортировано в demo-assets/, принято game-lead):** дом, сарай-база, 4 пропа базы, логово-пещера, кости, 7 растений (2 ёлки/2 куста/3 камня). Реимпорт в UE за game-lead (vcol «Replace» + VertexColor('Col')→BaseColor, scale1; ТРАВА и тонкие лопасти — **Two-Sided**).
  - **C5 растительность ✅ ЭКСПОРТИРОВАНА** (`build_vegetation.py`): `SM_Tree_Pine_01`132т/`_02`104т (ёлки: ствол+ярусы-конусы), `SM_Bush_01`60т/`_02`80т (icosphere-клампы), `SM_Rock_01`20т/`_02`40т/`_03`60т (гранёные серые валуны + нормаль-шейдинг + мох + jitter). Все нормали наружу, 1 мат/ассет, round-trip OK.
  - **C5 трава-тафт ✅ ЭКСПОРТИРОВАНА.** `demo-assets/SM_GrassTuft_01.fbx`(18т)/`_02.fbx`(26т), `build_grasstuft.py`. V-сложенные лопасти веером, БЕЗ альфы, per-loop градиент Moss #3F5A31 база → Sun Grass #5C7A3E кончик (GDD §2). round-trip OK. ⚠️ В UE материал **Two-Sided** (открытые лопасти; signed_vol~0 ОЖИДАЕМО). NB: старый `SM_GrassTuft.fbx` (06-15) заменяется на _01/_02.
**🟢 STANDBY (2026-06-25): МЕШ-СЕРИЯ ДЕМКИ ПОЛНОСТЬЮ ЗАКРЫТА** (дом, сарай, 4 пропа, логово, кости, 7 растений, 2 травы — всё экспортировано+принято). Очередь далее — по пингу game-lead: **C4** облик NPC (модульная одежда старосты/торговца по `npc-humanoid-spec.draft.md`, ждёт 6 решений Рината по облику), **C2** материалы земли/дороги, либо правки по editor-расстановке.
**✅ B2 крыша дома (Фаза B) — ПРИНЯТА game-lead + реимпорт в UE сделан.** Вариант B (светлый холодный). Детали — запись «ДОМ C1» ниже.
  - ⚠️ **МАТЕРИАЛ ДОМА в UE (к сведению, от game-lead 2026-06-25):** `M_House` оказался БИТЫМ (нода Power без входа Base → не компилировался → шахматка). game-lead перевёл дом на рабочий `M_VColor` (как вся vcol-серия) — рендерится верно. Если позже нужен отдельный материал для домов — собрать НОРМАЛЬНЫЙ vcol-материал, не возвращаться к `M_House`.
**✅ РЕЕСТР assets/ (ADR-022) — НАПОЛНЕН демо-серией (17 ассетов), ПРИНЯТ game-lead 2026-06-25.** На каждый меш `E:/game-dev-team/assets/<id>/` = сайдкар `<id>.asset.md` (id/status=preview/material/tris/verts/bbox_m/color_attr/теги/описание) + превью `<id>_preview_v1.png`. ВСЕ числа сняты РЕАЛЬНО из экспортированных FBX (скрипт `scratchpad/dump_all_fbx.py`), не по памяти; превью — `scratchpad/preview_all.py` (3/4 lit, Standard). `source_model`/`builder` ссылаются на `demo-assets/_build/` — **бленды пер-ассет НЕ копируем** (game-lead подтвердил: дубли + раздувание git + рассинхрон; единый источник правды = QC-бленд+билдер; уточнение к ADR-022 он внесёт сам). Резать мультиобъектные бленды пер-ассет НЕ надо. `index.html` НЕ пересобирать вручную (генератор — JIT позже). Эталон структуры — `assets/iron_barrel/`. NB про двойное затемнение vcol (тон темнее hex-борда) отражён в сайдкарах где релевантно.
**✅ ДОМ C1 — ПРИНЯТ + ЭКСПОРТИРОВАН (game-lead визуально):** геометрия (крыша Вар.1 — конёк вдоль Y, фронтоны на ±Y; стены-щели закрыты сплошной подложкой) + реколор vcol под борд. `demo-assets/SM_VillageHouse.fbx` 820 трис, нормали наружу, Col CORNER/BYTE, M_VillageHouse. Билдер `demo-assets/_build/build_house.py`. Детали/палитра/маппинг — запись «C1 дом» ниже. Реимпорт в UE — за game-lead.
**✅ B2 (Фаза B, 2026-06-25) КРЫША → СВЕТЛАЯ ХОЛОДНАЯ — ЭКСПОРТИРОВАНА (рендеры из FBX отданы, ждёт реимпорт UE за game-lead).** game-lead выбрал Вариант B: скаты #9AA08C (Faded Sage, light&cool, доминанта) + теневой борд подсвечен #828A78 (чтобы сверху не проваливался), конёк/труба Cold Steel. Геометрия НЕ тронута (820 трис, round-trip OK). Способ — постшаг `demo-assets/_build/recolor_house_roof.py` (открыть _qc_house.blend → перекрасить ТОЛЬКО скаты → save → export FBX тем же осям, что `export_br12_demo.py`).
  - ⚠️ **УРОК (gotcha по цвету, важный для всех vcol-ассетов):** `dcommon.finalize` создаёт слой 'Col' как float-linear, а ложится он BYTE_COLOR → linear-значение из билдера читается как уже-sRGB → ДВОЙНОЕ затемнение (#9AA08C в палитре → на меше ~ (82,90,67), а не (154,160,140)!). Т.е. реальный тон ВСЕХ ассетов темнее hex борда. Чтобы получить ТОЧНЫЙ sRGB на меше — писать `attr.color = linear(target)` напрямую в BYTE_COLOR (round-trip даёт ровно тот sRGB байт). Менять hex в `H()`-палитре билдера для точного тона НЕ работает (съедается finalize).
**✅ База бандитов сарай (C6, юг) — ПРИНЯТА + ЭКСПОРТИРОВАНА.** `demo-assets/SM_BanditShed.fbx` (24364 б), `build_banditshed.py`, **304 трис**, СПЛОШНЫЕ стены, палитра борда + рыжий #C64B2C. (двускатка конёк X, торцы ±X, дверь −Y).
**✅ ПРОПЫ базы бандитов (C6, спек §2) — ПРИНЯТЫ + ЭКСПОРТИРОВАНЫ.** Билдер `build_banditprops.py` → `_qc_banditprops.blend` (4 объекта, в блоке разнесены по X для группового рендера; при экспорте каждый ре-центрирован по bbox X/Y→0, ground). 1 мат на пропс, нормали наружу, magenta-guard (setdefault), round-trip OK. `demo-assets/SM_{BanditBarrel,BanditCrate,BrokenFence,BanditCampfire}.fbx`. Палитра борда выветренная + скупой рыжий/янтарь:
  - `SM_BanditBarrel` 132 трис — низкополи ржавый барабан (НЕ тяжёлый `assets/iron_barrel` 8444!): cyl seg12 Cold Steel + 2 хупа Concrete + рыжие #C64B2C потёки.
  - `SM_BanditCrate` 84 трис — ящик Timber + тёмные угловые батенсы + ободы Dry Earth.
  - `SM_BrokenFence` 96 трис — выветренный сломанный сегмент (наклонный столб ry14, щель-пропуск, короткая/наклонная штакетина).
  - `SM_BanditCampfire` 248 трис — каменное кольцо (6 тёмн.камней) + крест брёвен (обугленные концы CHAR) + угли Amber #E0A32E/#C64B2C + 2 бревна-сиденья. ⚠️ ЕСТЬ старые `SM_CampfireLogs`(204)+`SM_CampfireFlame`(68) — этот bandit-вариант с сиденьями/кольцом/Amber-Rust; game-lead выберет.
  - Бюджет GDD ч.2 (мелочь<1500) ✓. Пруфы группой `_qc_renders/BProps_{front,threeq,topdown}_{lit,orient}`. Финал-экспорт после приёмки.
**ЛОГОВО ВОЛКОВ (C6/B, север) — ✅ ОБА ЭКСПОРТИРОВАНЫ.** Спек §1. Форма не дана концептом → минимальный силуэт. (старые удалены `678e703` → заново)
**ЛОГОВО ВОЛКОВ (C6/B, север).** Спек `enemy-locations-spec.draft.md` §1. Форма скалы не дана концептом → минимальный честный силуэт. Старые SM_WolfDenCave/SM_BonePile УДАЛЕНЫ как редиректоры `678e703` → лепил заново:
  - **✅ SM_BonePile — ПРИНЯТ + ЭКСПОРТИРОВАН.** `demo-assets/SM_BonePile.fbx` (24316 б), `build_bonepile3.py`, **204 трис**: череп (свод+морда+тёмные глазницы) + длинные кости с набалдашниками-эпифизами. Bone Ink #E7E4D8 приглуш. КРОВЬ ПРОПУЩЕНА (вариант, не дан). round-trip OK, нормали наружу.
  - **✅ SM_WolfDenCave — ПРИНЯТ (ревизия 1) + ЭКСПОРТИРОВАН.** `demo-assets/SM_WolfDenCave.fbx` (26204 б), `build_wolfden.py`, **208 трис**. Перв.проход был «кубический» → фикс: блоки-рамку устья НАКЛОНИЛ (rx/ry/rz off-axis), тело = гранёные icosphere-валуны, **покраска ПО НОРМАЛИ грани** (n.z>0.35→Faded Sage лит / средн→Concrete / n.z<−0.18→Cold Steel тень) = фасеты читаются как камень. Тёмное устье Pine Shade. Холодный камень + мох. round-trip OK, нормали наружу. Земля-проплешина = материал/декаль грунта (C2), не в меше.
**⚠️ УРОК (gotcha):** `D.box(... bevel=...)` в dcommon ОСТАВЛЯЕТ основные грани НЕтегированными в cm → magenta в finalize (bevel-ветка не ловит сжатые исходные грани). Фикс: НЕ использовать bevel у D.box ЛИБО `for f in bm.faces: cm.setdefault(f, COLOR)` перед finalize. Резкие боксы + vertex-jitter дают грубый камень и без bevel.
**Правило сдачи:** приёмка визуально у game-lead по PNG → ТОЛЬКО потом финал-экспорт FBX + round-trip + пруф файла на диске.

</details>

## Решения (скелетная серия, июнь)
- **Blender = v5.1** (exe `E:\Programs\Blender`). FBX-формат на экспорте = 7400.
- **Материал бандита = `M_Bandit_Flat`** — один материал на все 3 слота. Principled BSDF, Base Color из vertex-color атрибута `Col`. Без normal-map.
- **Палитра бандита (hex):** skin C9A084, jacket 2B2B2E, tee 6E6E70, joggers 303033, sneaker ECECEC, cap 28282B, rust C64B2C. Vertex-color линейно, домен CORNER (BYTE_COLOR).
- **Подход к одежде:** новые части строятся как добавленная геометрия и джойнятся в исходный слот-меш → 1 скинённый меш на слот. Веса новых вершин — KDTree-перенос с ближайшей исходной вершины.

## Ограничения (скелетная серия)
- **Общий скелет `RootAnim` (ADR-018):** 21 кость, шарится со всеми гуманоидами. НЕ переименовывать кости, включая аномальную `Pelvis.009_R.002`.
- **Пайплайн экспорта (docx):** копия blend1 → правка → выделить меш+скелет → Export FBX use_selection=True, bake_anim=False. UE-импорт против скелета HeadAndSkeletonFBX, без Create Physics Asset.
- **Перед экспортом базового меша — сбросить позу в rest** (база в blend1 приходит в РАН-позе, action "ArmatureAction"). Одежду авторить на rest.
- Импорт FBX в сцену, где УЖЕ есть armature RootAnim → KeyError C_Root (коллизия имён). Проверять в чистой сцене.
- **Bash-шелл без Python и капризен к PowerShell-кириллице.** Python — через Blender bpy.

## Текущее состояние (2026-06-12)
- **Бандит-гопник готов и экспортирован.** Рабочая папка `E:\ForGameLead(Materials)\bandit-work\`: SK_Bandit_Head.fbx (156 tris), SK_Bandit_Torso.fbx (332), SK_Bandit_Legs.fbx (102). Суммарно 590 трисов. Все: M_Bandit_Flat, vcol Col, RootAnim 21 кость, round-trip OK.
- **Бэкап на Google Drive — НЕ делал** (нет конвенции/доступа; уточнить у game-lead).

## Раунд 3 (2026-06-12): диагностика «БАГ 2 ОРИЕНТАЦИЯ» — экспорт НЕ виноват
- Сравнил мой FBX с эталоном парсером `io_scene_fbx.parse_fbx`: GlobalSettings/Model-узлы ИДЕНТИЧНЫ (стандартная Blender Z-up→FBX Y-up конверсия). Round-trip вертикально, vcol на месте.
- **⚠️ ДОПОЛНЕНО game-lead 2026-06-13:** флип оказался размещением актора в UE. НО вскрылась проблема сдвига РЕГИОНОВ vertex-color в UE (низ ног белый выше щиколотки; rust на предплечье). Гипотеза: CORNER→per-vertex интерполяция. Варианты: (а) разрез геометрии по границам зон, (б) бейк в текстуру (UV чистые), (в) настройки экспорта. Прежний вывод «чинить нечего» — только про ориентацию.
- ВЫВОД (ориентация): экспорт корректен; менять Forward/Up НЕЛЬЗЯ. Флип и «нет текстур» — UE-сторона.

## Фаза 3 (2026-06-13): серия ассетов + headless-пайплайн
- **MCP-инжект в субагенте НЕ работает** → headless CLI: `E:/Programs/Blender/blender.exe -b --factory-startup --python script.py` (v5.1.2). FBX-экспортёр: `addon_utils.enable('io_scene_fbx')`. Билдеры в `E:/ForGameLead(Materials)/phase3-assets/_build/`.
- **🔑 БЛОКЕР FBX-АНИМАЦИИ Blender 5.1 (Baklava) + ОБХОД:** у Action нет `.fcurves` → экспортёр не видит loose action → 0 AnimationCurve. ФИКС: протолкнуть action во временный NLA-трек и экспорт с `bake_anim_use_nla_strips=True`. `bake_anim_force_startend_keying=True` ТЕРЯЕТ промежуточные позы — не годится.
- **Gotcha:** движение анимации мерить по `pose_bone.tail`, не по `matrix.to_translation()`.
- **Готовые ассеты (`phase3-assets/`):** SM_Knife 154т, SM_Tree_01/02/03, SM_Rock_01/02/03, SM_Fence_Section 220т (тайл X 2.0м), SK_Wolf 616т (WolfRig 22 кости) + Anim_Wolf_Idle/Run/Bite. Round-trip OK.

## Раунды волка (2026-06-13): ориентация + скин
- **Конвенция гуманоида (MasterHumanoidCharacterSeparatedMesh, источник истины):** forward = **−Y**, up = +Z, подошвы Z≈0, rot 0.
- **180°-поворот рест-позы волка был ОШИБКОЙ** (ломает bind при ребайнде в UE → расплывание). Откатил. **Волк forward=+Y** (на 180° иначе гуманоида) → cpp даёт компоненту yaw +180°. Rigid-поворот компонента скиннинг не ломает.
- **Реальный дефект скина:** вершины передней стопы привязывались к rearpaw_R (кости передней ноги не трассировали меш). Фикс: frontlower/frontpaw переставлены по станциям меша. Пруф depsgraph-оценкой по всем кадрам: OK, NaN=0.
- Нормали: `recalc_face_normals` до to_mesh, signed volume +0.086 OUTWARD.

## Фаза 4-5 + одежда (2026-06-13/15): броня→одежда, староста
- **Броня `_01` = базовый меш слота ГГ + броня поверх, JOIN в один скинённый меш** (фикс «брони без тела»). KDTree-перенос весов. SK_Armor_{Head,Torso,Legs}_01.fbx (182/314/240т) round-trip OK.
- **SK_Elder.fbx** — 653 трис, один меш (3 слота + халат/борода/волосы), M_Elder_Flat, светлая палитра (ROBE #CFC29E...). Посох НЕ добавлял (T-pose). Pose-деформ OK.
- **ОДЕЖДА BugReport 12 (НЕ латы!):** L1 гражданка (шапка 227т/куртка 386т/штаны 224т), L2 (каска 212т/кожанка 362т/карго 248т). Рукава = перекрас рук базы; кисти GLOVE. **🔑 УРОК:** головной убор НЕ закрывает лицо — низ убора выше ~z1.65.
- **🔑 УРОК (баг скина):** после `bmesh.ops.delete(FACES)` верты переиндексируются → добирать веса по «вершинам без групп», не по range.

## Дрессинг деревни + демо-серии (2026-06-14/15)
- SM_VillageFence 108т (тайл X 2.0м), SM_VillageWell 196т — приняты+экспортированы.
- Кепка бандита v2 (тулья полного объёма, лицо открыто) — принята.
- Демо: SM_Pistol 208т (origin рукоять), SM_CampfireLogs 204т, SM_Shed 368т, SM_GrassTuft 28т (Two-Sided!), SM_Bush 100т, SM_BonePile, SM_WolfDenCave, SM_CampfireFlame 68т (градиент vcol per-LOOP по высоте — свой блок финализации вместо per-face cm).
- **BugReport 12:** кости-передел 210т (эпифизы-набалдашники; кровь пятнами, не заливкой), дом-изба 748→820т (венцы, наличники; сквозные пазы закрыты подложкой), трава V-лезвия 96т. **ПАМЯТКА UE:** импорт vcol «Replace», материал VertexColor→BaseColor, тонкое — Two-Sided.
- **C1 дом (2026-06-24):** конёк вдоль Y, фронтоны ±Y (по концепту); реколор под борд. **🔑 ИНСТРУМЕНТ ДИАГНОСТИКИ:** scratchpad `diag_house.py` (дубли/non-manifold/пер-остров signed_volume), `render_full.py` (face-orientation overlay Backfacing→синий/красный). ⚠️ round-trip color-DUMP даёт тёмные значения = артефакт readback, судить по РЕНДЕРУ.

## Этап D8 — abandoned_car + tent (2026-07-04, СДАНО; вынесено из «СЕЙЧАС» 07-06)
- **abandoned_car (модульная «Лада», 11 FBX):** `demo-assets/SM_AbandonedCar_{Body,Glass,Door_FL/FR/RL/RR,Hood,Trunk,Wheel,Block,Scratches}.fbx`, 1314 трис в сборе (бюджет ≤1500). Пивот навесных = ось петли; attach-оффсеты в `assets/abandoned_car/abandoned_car.asset.md` (в UE-координатах!). Билдер `demo-assets/_build/build_abandonedcar.py` → `_qc_abandonedcar.blend` + `_qc_abandonedcar_pose.blend` (перед на блоках, дверь FR rz+50, реф 3.png). Рефы `concept-art/CarReference/{2,3}.png`.
- **vcol-КОНТРАКТ M_CarPaint (RGBA):** RGB = готовый вид, A = маска «не-краски» (0 = перекрашивается параметром). UE: `BaseColor = lerp(BodyColorParam, VertexColor.RGB, VertexColor.A)`. Альфа vcol переживает FBX (verify_car.py). Слоты: M_CarPaint+M_CarProps, двери 1 слот, M_CarGlass (Two-Sided translucent).
- **tent:** `demo-assets/SM_Tent.fbx`, 116 трис (≤200), 1 мат M_Tent Two-Sided. Билдер `_build/build_tent.py`.
- Фикс «леса в окнах» = светлые рамки дверей → тёмный SEAL. NB: посадка Hood/Trunk Z в UE = 0.880/0.893 (живая сцена главнее расчёта из бленда, дельта +0.05).
- Инструменты: `_build/render_car.py` (рендер сцены из .blend), `_build/verify_car.py` (round-trip пачки FBX + alpha).
- Хвосты: SM_GroundPlane экспортирован (импорт за game-lead); 4 переделки (пистолет-канон/дрова/навес/верёвка) ждут финального визуала Рината.

## 🧭 СЕЙЧАС (2026-07-09) — АРХИВНЫЙ СНИМОК (этап закрыт: староста сдан)
**🟢 СТАРОСТА (NPC, комплект «Б») СДАН ПОЛНОСТЬЮ (07-09): принят game-lead + импортирован в UE (`/Game/Characters/Elder/SK_Elder_*`, общий скелет, `M_VColor`), надет на BP_Elder, Ринат смотрел — хорошо.** Коммит `26047dd`. 3 модульных меша на RootAnim (21 кость) в `assets/npc_elder/`: 284/594/260 трис = 1138. Round-trip ALL PASS (rest_maxdelta 2e-5), позовый чек чист. Паспорт `npc_elder.asset.md`.
- ✅ «Шахматка» брони разобрана (07-09): материал `M_Clothes_Flat` не компилился в UE → серая заглушка; game-lead заменил на `M_VColor` на 12 мешах брони, Ринат принял в PIE. Мой vcol был на месте, переокраска не потребовалась.
- ✅ 77-80% «чёрных» вершин в UE-дампе = невидимое тело под одеждой (живой PIE подтвердил верную окраску видимой поверхности всех 3 мешей). Урок в живой памяти: судить по PIE, не по доле чёрных в дампе.
- Диагностические скрипты того раунда: `_diag_black*.py`, `13_probe_colorspace.py`, `_dump_facts.py` (в assets/npc_elder/).

## 🧭 СЕЙЧАС (2026-07-12) — АРХИВНЫЙ СНИМОК (передан на приёмку, ветка docs/phase0-gdd)
**🟡 ТОРГОВЕЦ (NPC, комплект «А»: бандана + жилет-разгрузка) — СОБРАН+ЭКСПОРТИРОВАН, статус preview: ждал визуальной приёмки game-lead по рендерам; импорт в UE — отдельный шаг (не мой).** Коммит `16e43cc`. Референс `concept-art/TraiderReference/1.png` + палитра `npc-elder-trader-prompts.md`.
- 3 модуля на общем RootAnim (21 кость) в `assets/npc_trader/`: `SK_Trader_{Head,Torso,Legs}.fbx` = 210/414/272 = 896 трис (потолок = факт старосты 1138, −21%). Round-trip ALL PASS (rest_maxdelta 2e-5), позовый чек чист, unweighted 0.
- Экономия: жилет+свитер+щетина+волосы+ботинки = только vcol-покраска; 4 коробки-кармана базового L1_Torso НЕ удалены (как у старосты), а перекрашены в подсумки. Геометрия только: купол банданы (N=12) + узел + 2 хвоста, глаза, задний карман, 2 карго-кармана бёдер, подошвы. Янтарный клапан AmberLoot на ЛЕВОЙ груди (bisect верха кармана), пруф `_qc_trader/Trader_set_top.png`.
- Пруф цвета данными: `13_color_census.py` / `_census.log`.
- Итерации визуала после лично просмотренных рендеров: v1 щетина = «маска-балаклава» → сузил до подбородка/челюсти; v2 «волосы» красили уши → затылок только y>0.085.

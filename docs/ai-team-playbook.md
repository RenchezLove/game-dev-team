# AI-Team Playbook — переиспользуемый опыт для создания новых ИИ-команд

> **Назначение.** Project-agnostic копилка проверенного опыта, паттернов и уникальных находок, полученных при работе команды (game-lead + специалисты + QA-пилот «Сборщик»). Ведут СОВМЕСТНО: **game-lead** (оркестрация/разработка) и **«Сборщик»** (QA/тестирование на Computer Use). Цель — из этого позже собрать **промпт-инструкцию + файлы контекста + ТЗ для НОВОЙ ИИ-команды** под другую задачу.
> **Правило ведения:** сюда — только ОБОБЩАЕМОЕ (паттерны, что сработало/провалилось, почему). Проектную специфику — в `context/*` и `docs/decisions.md` (ADR). Дополнять по мере находок.
> Версия 0.1 · начат 2026-06-13 (проект ContrarySurvivor, UE5.5).

---

## 1. АРХИТЕКТУРА КОМАНДЫ
- **Плоское дерево, глубина 1:** оркестратор (`game-lead`) → специалисты-терминалы. **Субагент НЕ порождает субагентов** (ограничение платформы) → если нужен другой спец, специалист возвращает результат лиду, лид делегирует дальше.
- **Потолок параллелизма ≤3** одновременно (чтобы не раздувать контекст/ресурсы).
- **Роли тул-скоупятся** (frontmatter `tools:`) — спец видит только нужные инструменты (напр. модельер не пушит в master). Это фича, не «урезанность».
- **Имена напарников = по роли** (cpp/unreal/modeler/qa/concept) — читаемость трейса.
- **Project-agnostic ядро + проект как загружаемый контекст** (диздок/ТЗ грузится в лида, не висит постоянно).
- **frontmatter (`tools:`, `mcpServers:`) кешируется на старте сессии** → правки определений агентов вступают в силу ТОЛЬКО после рестарта сессии. Закладывать заранее.

## 2. ПРОЦЕСС РАЗРАБОТКИ
- **Источник истины — диздок/ТЗ.** Чего нет — не выдумывать, эскалировать владельцу.
- **Фазы = играбельные инкременты с пруфом.** Роудмап утверждается ОДИН раз, дальше — рабочий ориентир. Вертикальный срез сначала.
- **Цикл фазы:** строим в фича-ветке → мерж-гейт (qa: реальная сборка + артефакт) → merge `--no-ff` → отчёт. Каждый шаг — с доказательством.
- **Мерж-гейт технический:** маркер-файл (`QA_OK`)/хуки, создаётся лидом ТОЛЬКО после «добро» qa с артефактом, снимается сразу после push. Не коммитится.

## 3. 🔑 АВТОНОМНАЯ QA-ПЕТЛЯ С Computer-Use ТЕСТЕРОМ («Сборщик»)
**Находка дня:** тестера-человека в цикле можно заменить ИИ-агентом с Computer Use (реально играет/кликает/смотрит) — до оговорённой вехи; владелец ревьюит на вехе.
- **Петля:** лид собирает (ресурс ЗАКРЫТ) → сигнал «🟢 ГОТОВО К ТЕСТУ + чеклист + состояние ресурса» → тестер прогоняет → отчёт + «🔵 свободно» → лид чинит → повтор до приёмки.
- **Полномочия тестера** настраиваются (у нас: решает до MVP, эскалирует крупное; на вехе — владелец).
- **Тестер честен:** PASS только по факту; чего не смог — пишет честно; «ощущение»/тонкое — оставляет владельцу.

## 4. 🔑 QA-ХАРНЕСС (ЭНЕЙБЛЕР для Computer-Use тестирования)
**Computer Use НЕ МОЖЕТ надёжно:** открыть игровую консоль (раскладка/фокус), кликать UI (захват мыши в play-режиме), рассмотреть мелкую 3D-деталь (камера сверху). Медленный, слаб в реакционном геймплее.
**Решение (строить ЗАРАНЕЕ под механики каждой фазы):**
- тест-действия на ОБЫЧНЫЕ КЛАВИШИ (не консоль): выдать предметы, экип, сделка, телепорт к NPC, зачесть прогресс и т.п.;
- структурированные ЛОГ-строки (отдельная категория, префикс напр. `QA:`) на ключевые события → тестер верифицирует ФУНКЦИЮ по логу, даже не видя UI;
- debug-камера/телепорт-клавиши для осмотра;
- подавить экранный спам.
→ Тестер закрывает ~80% (функция/визуал-статика), «фид»/мышь/баланс — финальный проход владельца. **Играем по сильным сторонам каждого.**

## 5. 🔑 MCP-НЕЗАВИСИМОСТЬ / HEADLESS-КОНВЕЙЕРЫ
- **Живые MCP-каналы к тяжёлым приложениям ФЛАККИ:** падают (троттлинг/тайминг), теряют инжект тулзов в свежие субагенты, требуют ручного reconnect владельцем (вне инструментов агента).
- **Надёжный обход — headless-CLI с исполнением скрипта:** напр. UE `UnrealEditor-Cmd "<proj>" -ExecutePythonScript="<abs.py>" -unattended -nopause -nosplash` (вывод в лог-файлы, НЕ stdout!); Blender `blender -b --factory-startup --python <скрипт>`. Без окна/сокета/MCP — только CLI+скрипт.
- **Приоритет инструментов:** (1) нативный коннектор → (2) встроенное → (3) MCP (если канал жив) → (4) **headless-коммандлет (когда MCP флакки)** → (5) Computer Use (крайний случай).
- **Граница headless:** годен для детерминированных операций (импорт/правки ассетов/материалы/сборка). НЕ годен для редактирования визуальных графов (UI/anim-blueprint) и для «поиграть» — это Computer Use / редактор.

## 6. КОММУНИКАЦИЯ / АСИНХРОН / ОЧЕРЁДНОСТЬ РЕСУРСА
- **Каналы = файлы в git** (владелец читает/пишет с телефона через веб-GitHub): лид→все (STATUS), тестер→лид (TEST_REPORT), владелец→лид (INBOX). Раздельные файлы → нет конфликтов; перед своим push — `pull`.
- **Фоновые watchers** (цикл `git fetch` + сравнение хеша/блоба, выход при изменении → платформа будит агента) — асинхронное «входящее». ⚠️ после рестарта сессии — поднять заново.
- **Очерёдность общего ресурса (редактор/устройство) — строго по сигналам + lock-файл** (тестер кладёт `*_TESTING.lock` на время; никогда одновременно). Не закрывать тяжёлое приложение без нужды (экономит минуты на перезапуск) — указывать состояние в сигнале.

## 7. 🔑 АНТИ-ГАЛЛЮЦИНАЦИЯ — СВОДНЫЕ ЛУЧШИЕ ПРАКТИКИ (обязательно для всех ролей)
> Самый ценный капитал команды. Ниже — и базовый протокол, и конкретные приёмы, что мы РЕАЛЬНО применяли (с примерами). Переносится в любую новую команду как есть.

### 7.1 Базовые принципы (протокол A–I)
- **A. Факты — из первоисточника.** API/синтаксис/возможности — из официальной документации КОНКРЕТНОЙ версии (UE 5.5 / Blender / SDK), не «по памяти». Перед использованием класса/функции/узла подтвердить, что он есть именно в этой версии.
- **B. Доказательство, а не утверждение.** «Компилируется/тест прошёл» — только после реального запуска, с логом и exit-кодом. «Закоммичено/запушено» — только после реального `git status`/`git log`/вывода push. Фабриковать результаты запрещено.
- **C. Состояние — из реальности.** Перед правкой — прочитать реальный файл/дерево/ассет, а не действовать по памяти. Не выдумывать пути/классы/ассеты/плагины — сперва проверить, что объект существует.
- **D. Отрицательный результат = граница проверки.** Прежде чем сказать «такого нет/так не работает» — проверить ≥2 способами (расширить поиск + сменить метод) и формулировать как «в диапазоне X не нашёл», а не «нет».
- **E. Допущения и эскалация.** Допущение озвучивать явно («предполагаю, что…»). Дизайнерские/продуктовые решения НЕ гадать — эскалировать (лиду → владельцу). При блокировке/неуверенности — остановиться и спросить, а не копить ошибку.
- **F. Единый источник истины по продукту** (диздок/ТЗ). Лор/механики/баланс/стиль — только оттуда; чего нет — запросить, не изобретать. При передаче задачи ЦИТИРОВАТЬ формулировку из источника, не пересказ по памяти.
- **G. Калибровка уверенности.** Явно разделять: **проверено** (с источником/артефактом) / **выведено** / **предположение**. Технические утверждения — с указанием источника.
- **H. Борьба с дрейфом контекста.** Периодически дистиллировать состояние/решения/ограничения в `context/<agent>.md` (новые сессии стартуют отсюда). Крупное/сырое — по запросу, не держать в контексте постоянно.
- **I. Самопроверка перед «готово».** Чек-лист (см. 7.4). Хоть один «нет» — задача не готова.

### 7.2 Конкретные практики, что мы применяли (с примерами)
- **Независимая аттестация сборки (qa-роль).** Отдельный агент `qa` делает ЧИСТУЮ сборку (Clean+Build) и даёт PASS ТОЛЬКО при `exit 0` + факте линковки + свежем артефакте (имя/размер/таймстамп DLL) + пути к логу. «Build succeeded» движок может не печатать — критерий формулировать по реальным маркерам (линковка + WriteMetadata + exit-код).
- **git-верификация.** Хеши коммитов, ветка/HEAD, `origin == local`, LFS-указатель (`git show HEAD:file` = pointer, не блоб) — приводить факты, не «я закоммитил».
- **Читать реальные заголовки/доки для API.** Пример: подтвердили `SetSkeletalMeshAsset`/`SetLeaderPoseComponent` в заголовках UE 5.5; нашли, что `SetMasterPoseComponent`/`SetSkeletalMesh(…)` DEPRECATED с 5.1 — НЕ применять по памяти.
- **НЕ передавать агентам guessed-API.** Урок: лид вписал в задание несуществующий `is_in_pie()` и интроспекцию материала — агент честно упал, потом нашёл верный путь по докам (`get_editor_world().get_name()` без `UEDPIE_`; `hasattr(unreal,'Class')`). Вывод: класть в инструкции только сверенные вызовы; агенту — право находить API и требование сверять.
- **Визуальный пруф — корректный.** Скрин для проверки ЦВЕТА/вида — только с ФРОНТАЛЬНЫМ/нейтральным светом (свет за камерой). Контровой кадр даёт силуэт → дважды приняли чёрный силуэт за «материал тёмный/исправлен» = ЛОЖНЫЙ пруф. При «равномерно тёмный» — насторожиться, переснять.
- **Верификация в источнике до экспорта.** Деформацию анимации/скиннинга проверять В Blender (depsgraph-оценка по кадрам) ДО экспорта — поймали мисвейт «передняя лапа на кости задней» (растяжение) и развеяли ложную гипотезу.
- **Различать классы ошибок.** `LogPython: Error/AttributeError` от движка = КАНАЛ РАБОТАЕТ, баг в КОДЕ/API; «No command channel open» = канал мёртв. Не путать при диагностике.
- **Отрицательный результат — двумя методами.** Пример: гипотезу «ось экспорта Blender→UE перевёрнута» опровергли байт-в-байт сравнением FBX с рабочим эталоном (парсер) + round-trip — оказалось, причина в другом.
- **Тестер честен про границы.** PASS только по факту; «не смог проверить — причина (инструментальный лимит Computer Use / слишком быстро / не тот мир)» пишет прямо; отделяет «сломанная функция» от «плейсхолдер-визуал/не баг». Не self-аппрувит веху, если так оговорено.
- **Самотест перед сдачей владельцу.** Прогнать силами команды (тестер/скрины), починить найденное, ПОТОМ владельцу — его время = подтвердить, не искать баги.
- **Время — системное.** Не выдумывать таймстампы — брать `date`. (Ловили себя на фабрикации времени — это та же галлюцинация.)

### 7.3 Техники/приёмы
- **Конфаунды фиксировать.** Примеры: чтение в PIE возвращает play-инстанс/не тот мир (перед операциями — убедиться, что НЕ в PIE и активен нужный мир); долгоживущий редактор копит «протухшее превью» (рефреш/перезапуск); авто-экспозиция маскирует яркость альбедо. Записывать конфаунд, не выдавать за результат.
- **Независимый путь верификации.** qa пересобирает независимо; Computer-Use тестер проверяет независимо от того, кто строил. Лог-категория `QA:` + клавиши (см. QA-харнесс) — чтобы тестер верифицировал ФУНКЦИЮ фактом, а не доверием.
- **Маркеры в логах под поиск** (`QA:`, `P3IMPORT:` и т.п.) + вывод headless-коммандлета в файл-лог (stdout пуст) — пруф брать из лога.
- **Round-trip как пруф ассета** (экспорт→реимпорт в чистую сцену→замер: трисы, нормали наружу через signed volume, скелет/кости, vertex-color, ориентация).

### 7.4 Чек-лист «перед готово» (self-check, правило I)
1. Каждый факт сверен с реальным артефактом (лог/скрин/git/round-trip)?
2. Сборка/тест РЕАЛЬНО запускались (exit-код, лог)?
3. git-операции подтверждены выводом (хеш/origin)?
4. API сверен с доком/заголовком версии (не по памяти)?
5. Допущения озвучены явно; дизайн-дыры эскалированы, не выдуманы?
6. Калибровка проставлена (проверено/выведено/предположение)?
7. Визуальные пруфы корректны (свет фронтальный, не силуэт; не тот-мир/не PIE)?
Хоть один «нет» → НЕ готово.

## 8. ОБЩИЕ ИНЖЕНЕРНЫЕ ГРАБЛИ (обобщаемые)
- Классификатор автономии блокирует само-модификацию конфига (расширение прав/правка startup) без ЯВНОЙ авторизации владельца в сообщении — закладывать прямую фразу-разрешение.
- Тяжёлый редактор держит СТАРЫЙ бинарь в памяти → после пересборки кода нужен перезапуск; сборка кода требует ЗАКРЫТОГО редактора (иначе линковка-лок).
- Перед сдачей фазы — САМОТЕСТ силами команды (тестер/скрины), починить, ПОТОМ владельцу: его время = подтвердить, не искать баги.
- Большой непроверенный стек копить нельзя — не строить фазу N+1 поверх непринятой фазы N сверх разумного.

## 9. ДЛЯ БУДУЩЕЙ ЗАДАЧИ «СОЗДАТЬ НОВУЮ КОМАНДУ»
Артефакт (делаем СОВМЕСТНО с «Сборщиком»): **промпт-инструкция оркестратора + промпты ролей + файлы контекста (`context/*`, ADR, этот плейбук) + ТЗ под задачу.** game-lead даёт оркестрацию/процесс/анти-галлюцинацию/tooling; «Сборщик» — QA-петлю/харнесс/Computer-Use практику. Адаптировать роли под домен новой задачи (не обязательно геймдев), но каркас (разделы 1-8) переносится.

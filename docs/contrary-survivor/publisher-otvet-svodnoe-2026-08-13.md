# Ответ на сводное ТЗ от 13.08.2026 (+ дополнение про политику)

**От:** game-lead. **Дата:** 13.08.2026, вечер.
**Код:** ветка `feature/main-menu-0808`, голова `fae0eb0`, всё на сервере. Сборка зелёная, полный прогон автотестов: **156 прошло, 0 провалено** (`Saved/tests-sensors-0813-final.log`).
**Общая оговорка:** Ринат на эту сессию прямо запретил живые проверки работоспособности (реклама, аналитика, ссылки) — «вставить и не проверять». Всё, что упирается в этот запрет, помечено «при Ринате».

---

## Задача 1 — боевые ключи и адреса

**Строки конфига как есть** (`Config/DefaultEngine.ini`, приёмка 1.1):

```
AdUnitIdDeathBackpack=R-M-19754980-1
AdUnitIdShopSellBonus=R-M-19754980-2
AdUnitIdDailyDouble=R-M-19754980-3
AdUnitIdSupportAuthor=R-M-19754980-4
```

и `Config/DefaultGame.ini`: `SupportPostUrl="https://t.me/marevogame/8"` (кавычки обязательны, без них движок режет адрес).

Общий идентификатор остаётся демонстрационным намеренно: код берёт его только при пустом точечном поле (`YandexAdService.cpp`, `GetAdUnitIdForPlacement` — проверено чтением кода), а все четыре точки заполнены. Отдельного поля «номер приложения» у SDK Яндекса нет — номер зашит в каждом идентификаторе (`R-M-19754980-N`), это уже было проверено по байт-коду SDK и записано в конфиге.

**Приёмка 1.2 (журнал ролика с телефона) и 1.3 (ответ про тестовый статус кабинета) — при Ринате:** живой показ в эту сессию запрещён им дословно. Вопрос 1.4 остаётся открытым до первого живого запуска с ключами.

---

## Задача 2 — групповой обыск тел

Сделано и собрано, коммиты `68a69a4` + `fae0eb0`.

- Одно нажатие открывает одно окно с содержимым всех тел группы, «Забрать всё» опустошает группу разом. Второй кнопки нет, окно не убрано.
- **Радиус: 600 см (6 метров)** от тела, которое подсветила подсказка, без цепочек «от тела к телу». Почему: стая падает вокруг игрока в пределах пары метров — шесть метров накрывают такую кучу с запасом, а тела в других углах локации не попадают. Число правится в редакторе без пересборки.
- Подсказка: «Обыскать (4) — E», при одном теле — «Обыскать — E».
- Обысканное тело через 1 с начинает уходить в землю, за 2 с опускается на 150 см и исчезает (все три числа правятся в редакторе). Ящики и прочие контейнеры не затронуты — они в реестр тел не входят по коду.
- Сводка одной строкой существующим всплывающим сообщением: «Получено: Шкура волка x4, 12 монет».
- Переполнение рюкзака: ветка честно написана (тела с незабранным остаются полными и не уходят в землю, игроку говорится словами), но **проверить её нечем — вместимости у рюкзака в игре не существует**, отказ сегодня невозможен в принципе. Заводить вместимость ради проверки не стали — это отдельное игровое решение.

**Приёмка:**
1. Кадр «Обыскать (4)» — `Saved/Screenshots/group-search-0813/01-prompt.png` (просмотрен лидом: подпись с числом 4 над кучей из четырёх волков).
2. Кадр после обыска — `02-after.png` (та же точка: тел не осталось, деньги 75 → 155). Бонус — кадр окна группы (`debug.png`: общее содержимое, кнопка «Забрать всё»).
3. Кадр сводки не снят — решение Рината остановить съёмку (сводка висит секунды, ловить её сочли не стоящим времени). Сводку подтверждает код и тест.
4. Строки журнала (`Saved/Screenshots/group-search-0813/gameframes-20260813-210409.log`): «QA: CORPSE loot window OPEN (тел в группе: 4)», затем четыре «sink started» и «sink finished -> removed» за одно нажатие. Есть и одиночный случай «тел в группе: 1» — подсказка без числа.
5. Радиус и обоснование — выше.

---

## Задача 3 — аналитика GameAnalytics

**Живую доставку событий до веб-панели в эту сессию не проверяли — прямой запрет Рината** (ровно та ловушка бесконечной проверки, о которой он предупредил). Кадра панели поэтому нет; всё ниже — по коду, с точными местами.

**1. Ключи.** Вшиваются в сборку на этапе компиляции из файлов вне репозитория (`AnalyticsSubsystem.cpp:50-65`); файлы ключей лежат с 11.07 и непустые. Без ключей события молча не уходят. До ответа игрока на экране согласия SDK не поднимается вовсе.

**2. Полный список событий, которые шлёт игра** (единственная точка отправки — `SendDesignEvent`, прямых вызовов SDK мимо неё нет):

| Семейство | События | Где в коде |
|---|---|---|
| Запуск | `app:first_launch` | AnalyticsSubsystem.cpp:307 |
| Обучение | `tutorial:step:<5 шагов>`, `tutorial:completed` | OnboardingComponent.cpp:67, 74 |
| Смерть/бой | `player:death`, `combat:kill:<Wolf/Bandit>` | PlayerCharacter.cpp:2478, 2490 |
| Экономика | `shop:buy:<предмет>`, `shop:sell_completed` | PlayerCharacter.cpp:1384, 1545, 1582 |
| Квесты | `quest:offered:<id>` **(новое)**, `quest:accept:<id>`, `quest:turnin:<id>` | QuestComponent.cpp |
| Ежедневка | `retention:daily_login`, `retention:daily_reward_claimed` | DailyRewardComponent.cpp:170, 173 |
| Реклама, 3 точки | `ad:<точка>:{button_shown, not_shown:<причина>, button_clicked, started, completed, dismissed, failed}` для backpack / shop / daily | DeathScreenWidget, ShopScreenWidget, DailyRewardComponent, YandexAdService |
| Окно поддержки | `support:window_opened:{main_menu/pause}`, `support:ad_offered` **(новое)**, `support:ad_not_shown:no_ad` **(новое)**, `support:ad_started`, `support:ad_completed`, `support:ad_dismissed` **(новое)**, `support:link_opened`, плюс `ad:support:failed` на технический сбой | ContrarySurvivorPlayerController.cpp, YandexAdService.cpp |

**3. Чего не хватало и что сделано:** «первый квест выдан» — заведено `quest:offered:<id>` (первый квест читается по имени `KillWolves`); для окна поддержки заведены «ролик предложен / недоступен» и «отказ игрока». Начало и конец сессии игра не шлёт — ими управляет сам SDK автоматически (настройка ручных сессий выключена в плагине); живым запуском не проверено.

**4. Задвоения.** Отправок из тика нет (проверено по каждому вызову); одноразовые события защищены записью в отдельный файл сохранения (первый запуск, шаги и завершение обучения), рекламные кнопки — сторожами «раз на сделку», новое `quest:offered` — сторожем на уровне компонента (место вызова дёргается каждый кадр диалога — без сторожа испортило бы все проценты).

**5. AppMetrica.** Нашего кода, ключа и инициализации НЕТ нигде. Найдено и устранено расхождение: текст экрана согласия называл AppMetrica сборщиком данных — убрано, теперь текст слово в слово согласован с политикой (и в коде, и в файле окна; тест перевёрнут и требует отсутствия слова). **Честная оговорка:** сама библиотека AppMetrica физически едет в пакете как обязательная зависимость рекламного набора Яндекса (`YandexAds_UPL.xml:58`) — убрать её нельзя, без неё не работает реклама.

**6. Ответ на вопрос 3.4.4 — что можно посчитать текущим набором:** удержание первого дня — да (сессии SDK + `app:first_launch`); длину сессии — да (сессии SDK; живьём не проверено); долю прошедших обучение — да (`tutorial:completed` к `app:first_launch`); долю согласившихся смотреть ролик — да, по каждой из четырёх точек отдельно (`started` к `button_shown`, у окна поддержки теперь есть свой знаменатель `ad_offered`).

**Две замерные тонкости, решать издателю (в коде не меняли):** (а) при техническом сбое показа игра по прежнему требованию издателя выдаёт награду и шлёт `completed` — досмотры чуть завышены; (б) в отказ окна поддержки попадает и редкий случай «показ заказали, а ролик уже истёк».

---

## Дополнение — политика конфиденциальности

- Адрес `https://renchezlove.github.io/marevo/` вставлен в конфиг (`PrivacyPolicyUrl`, в кавычках, коммит `7d21b4e`). Отдельно встраивать ничего не пришлось: ссылка на экране согласия, строка внизу главного меню и строка в паузе уже были в коде и молчали только из-за пустого адреса.
- Расхождение с политикой было одно — AppMetrica в тексте согласия, устранено (см. задачу 3, п.5). Остальные утверждения политики сверены с кодом: регистрации и учётных записей нет, сохранение только на устройстве, покупок нет, ролики только по нажатию, точек показа четыре — всё совпадает.
- Кадры экрана согласия и пункта меню, нажатие ссылки с телефона — **при Ринате** (живые проверки в эту сессию запрещены им).

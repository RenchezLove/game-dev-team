# Схемы раскладки UMG-виджетов (ADR-048)

Этот файл — инструкция для Рината: как собрать мышкой экраны интерфейса в редакторе.
Логика каждого экрана уже написана в C++; тебе нужно только разложить «кубики»
(кнопки, тексты, списки) так, как нравится визуально. Файл пополняется по мере
готовности C++-классов: сейчас здесь этап 1 — магазин.

## Как это устроено (один раз прочитать)

- Для каждого экрана есть C++-класс с логикой. Ты создаёшь на его основе **Widget
  Blueprint** (визуальный ассет, дальше «WBP») и раскладываешь в нём кубики.
- Код находит кубики **по точным именам**. Имя задаётся в редакторе виджета: выбери
  элемент в дереве слева (панель Hierarchy) и впиши имя в самом верху панели Details.
  Если имя не совпадёт с таблицей — кубик просто не заработает (в лог упадёт
  предупреждение, игра НЕ сломается).
- Класть можно НЕ все кубики сразу: чего нет — то не работает, остальное живёт.
  Можно собирать экран постепенно и сразу проверять в игре.
- Каждый WBP потом назначается в свой слот на `BP_ContrarySurvivorHUD` (категория
  **HUD | UMG Widgets** в Class Defaults). Пока слот пуст — работает старый экран,
  нарисованный кодом; назначил — включается твой WBP.

### Как создать WBP на нужном C++-классе

1. В Content Browser: правая кнопка → **User Interface → Widget Blueprint**.
2. В появившемся окне выбора родителя (Pick Parent Class) найди через поиск нужный
   C++-класс из таблицы ниже (например `ShopScreenWidget`) и выбери его.
   Если окно не появилось и виджет создался сразу — открой его, вверху меню
   **File → Reparent Blueprint** и выбери нужный класс там.
3. Назови ассет как в схеме (например `WBP_Shop`) и положи в `Content/UI/`.

---

## Этап 1 — Магазин

Нужно два ассета: `WBP_Shop` (сам экран) и `WBP_ShopRow` (одна строка списка —
товар или предмет рюкзака; код сам наплодит таких строк сколько нужно).

### WBP_Shop — экран магазина

- **Родительский класс:** `ShopScreenWidget`
- **Куда назначить готовый:** `BP_ContrarySurvivorHUD` → Class Defaults →
  категория **HUD | UMG Widgets** → поле **Shop Widget Class**.
- **Обязательная настройка внутри WBP_Shop:** открой Class Defaults самого WBP_Shop
  (кнопка Class Defaults на панели сверху), найди категорию **Shop** и в поле
  **Row Widget Class** выбери `WBP_ShopRow` (без этого списки будут пустыми).

Кубики (тип → точное имя → что делает):

| Тип кубика | Точное имя | Что делает и где ему место |
|---|---|---|
| Text | `MoneyText` | Деньги игрока, например «Монеты 150». Код обновляет сам каждый кадр. Обычно — левый верх панели. |
| Scroll Box | `BuyList` | Список товаров торговца («FOR SALE»). Код сам наполняет его строками WBP_ShopRow. Прокрутка пальцем и колесом работает сама. Левая половина панели. |
| Scroll Box | `SellList` | Список предметов рюкзака на продажу. Тоже наполняется кодом. Правая половина панели. |
| Button | `CloseButton` | Закрыть магазин. Текст на кнопке (например «Close») напиши прямо в редакторе — положи внутрь кнопки обычный Text. Правый верхний угол. |

Панель количества (появляется при покупке/продаже стака; в остальное время код сам
её прячет). Собери эти кубики в отдельный контейнер — например Border или Vertical
Box поверх списков по центру:

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Любой контейнер (Border / Canvas Panel / Vertical Box) | `SliderPanel` | Корень панели количества. Код показывает/прячет ИМЕННО его — все кубики ниже клади ВНУТРЬ него. |
| Text | `SliderTitleText` | Заголовок «КУПИТЬ: Аптечка» / «ПРОДАТЬ: Патроны 9мм». Ставит код. |
| Text | `SliderQtyText` | Строка «Кол-во: 3 / 10». Ставит код. |
| Slider | `QtySlider` | Ползунок количества. Диапазон и шаг код настраивает сам — в редакторе ничего в нём не меняй, только внешний вид. |
| Button | `QtyMinusButton` | Минус один. Подпись «−» положи внутрь кнопки сам. |
| Button | `QtyPlusButton` | Плюс один. Подпись «+» положи сам. |
| Text | `SliderTotalText` | Живая сумма: «Итого: 120» при покупке, «Выручка: +40» при продаже. Ставит код. |
| Button | `SliderConfirmButton` | Подтвердить сделку. Подпись (например «Confirm») положи сам. |
| Button | `SliderCancelButton` | Отменить. Подпись положи сам. |

Статичные надписи, которые код НЕ трогает (заголовки «TRADER», «FOR SALE (Купить)»,
«SELL FROM BACKPACK (Продать)») — добавляй обычными Text с любым именем и пиши в них
что хочешь: они целиком твои.

Настраиваемые строки кода (если захочешь поменять «Монеты», «Кол-во:», «Итого:»,
«Выручка: +», подписи Buy/Sell в строках, «КУПИТЬ»/«ПРОДАТЬ») — лежат в Class
Defaults WBP_Shop, категории **Shop | Texts**.

### WBP_ShopRow — одна строка списка

- **Родительский класс:** `ShopRowWidget`
- **Куда назначить:** в поле **Row Widget Class** в Class Defaults `WBP_Shop`
  (см. выше). На HUD этот ассет назначать НЕ нужно.
- Это одна горизонтальная строка: имя, цена, кнопка. Удобная основа — Horizontal
  Box на всю ширину. Высоту строки задаёшь ты (для пальца комфортно 40–60 px).

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `NameText` | Имя товара/предмета (у брони код допишет «(+15% защиты)»). Ставит код. |
| Text | `PriceText` | Цена: «120» в каталоге, «+40» в рюкзаке. Ставит код. Если этот кубик не положить — код допишет цену к имени, информация не потеряется. |
| Button | `ActionButton` | Кнопка действия строки. У недоступных к покупке товаров (не хватает денег) код сам гасит кнопку. |
| Text | `ActionText` | Подпись на кнопке («Buy»/«Sell» — ставит код). Положи этот Text ВНУТРЬ ActionButton. |

### Как проверить магазин

1. Назначь `WBP_Shop` в слот Shop Widget Class на `BP_ContrarySurvivorHUD`.
2. Запусти игру, подойди к торговцу, нажми E (или кнопку ДЕЙСТВИЕ на телефоне).
3. Должны появиться твоя панель со списками. Купи что-нибудь со слайдером, продай
   предмет, закрой крестом и клавишей E. Если какой-то кубик не заработал — проверь
   его имя по таблице (лог пишет предупреждение с именем недостающего кубика).
4. Чтобы вернуть старый магазин — просто очисти слот Shop Widget Class.

---

## Этап 2 — Диалог старосты и инвентарь

### WBP_Dialog — окно разговора со старостой

- **Родительский класс:** `DialogScreenWidget`
- **Куда назначить:** `BP_ContrarySurvivorHUD` → **HUD | UMG Widgets** →
  поле **Dialog Widget Class**.
- Классическая нижняя панель «как в визуальных новеллах», но раскладка целиком твоя.
  Тексты реплик, как и раньше, настраиваются на самом старосте (BP_Elder, категория
  Dialog) и в его квестах — окно их только показывает.

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `NPCNameText` | Имя собеседника («СТАРОСТА» — берётся с BP_Elder). Ставит код. |
| Text | `ReplicaText` | Сама реплика. ОБЯЗАТЕЛЬНО включи у него галочку **Auto Wrap Text** (панель Details → Wrapping) — длинные реплики переносятся на строки. Ставит код. |
| Button | `AcceptButton` | «Принять квест». Код сам показывает её только когда квест предлагается. Подпись (например «[ Принять ]») напиши внутри кнопки сам. |
| Button | `DeclineButton` | «Отказаться». Видна вместе с AcceptButton. Подпись напиши сам. |
| Button | `TurnInButton` | «Сдать квест» — видна, когда квест выполнен. Подпись ставит КОД (в ней сумма награды) — положи внутрь кнопки Text с именем `TurnInText`. |
| Text | `TurnInText` | Подпись кнопки сдачи «[ Сдать (+150) ]». Лежит ВНУТРИ TurnInButton. Ставит код. |
| Button | `CloseButton` | «Закрыть» — видна при взятом и при сданном квесте. Подпись напиши сам. |

Кнопки, не подходящие текущему состоянию квеста, код прячет сам — клади все четыре
рядом (например в Horizontal Box внизу панели), лишние в каждый момент исчезнут.

Настраиваемые строки кода (префикс/суффикс подписи сдачи) — Class Defaults
WBP_Dialog, категория **Dialog | Texts**.

### WBP_Inventory — экран инвентаря

- **Родительский класс:** `InventoryScreenWidget`
- **Куда назначить:** `BP_ContrarySurvivorHUD` → **HUD | UMG Widgets** →
  поле **Inventory Widget Class**.
- **Обязательная настройка внутри WBP_Inventory:** Class Defaults → категория
  **Inventory** → поле **Row Widget Class** = `WBP_InventoryRow` (иначе рюкзак пуст).

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `StatsText` | Строка «Монеты 150      Голод 80 / 100      Жажда 60 / 100». Код обновляет каждый кадр. Верх панели. |
| Button | `HeadSlotButton` | Слот шлема: клик по занятому — снять броню. Подпись слота («Шлем») напиши рядом сам. |
| Text | `HeadSlotText` | Что надето в слоте шлема («(пусто)» или имя брони). Ставит код. Удобно положить внутрь кнопки. |
| Image | `HeadSlotIcon` | Иконка надетого шлема. Когда слот пуст, код её прячет — можешь подложить под неё свою картинку пустого слота. Необязательный кубик. |
| Button | `TorsoSlotButton` | То же для торса. |
| Text | `TorsoSlotText` | То же для торса. |
| Image | `TorsoSlotIcon` | То же для торса (необязательный). |
| Button | `LegsSlotButton` | То же для штанов. |
| Text | `LegsSlotText` | То же для штанов. |
| Image | `LegsSlotIcon` | То же для штанов (необязательный). |
| Text | `ProtectionText` | «Защита: 45%» — реальное снижение урона, пересчитывает код. |
| Text | `WeaponText` | «Оружие: Пистолет». Ставит код. |
| Scroll Box | `BackpackList` | Список рюкзака, код наполняет строками WBP_InventoryRow. Прокрутка штатная. |
| Button | `CloseButton` | Закрыть инвентарь (то же, что клавиша Tab). Необязательный — можно закрывать и как раньше. Подпись напиши сам. |

Статичные надписи («ИНВЕНТАРЬ», «СНАРЯЖЕНИЕ», «РЮКЗАК», подсказка про снятие брони) —
обычные Text с любыми именами, целиком твои.

Настраиваемые строки кода («Монеты/Голод/Жажда», «(пусто)», «Защита: », «Оружие: »,
«использовать»/«надеть») — Class Defaults WBP_Inventory, категория **Inventory | Texts**.

### WBP_InventoryRow — одна строка рюкзака

- **Родительский класс:** `InventoryRowWidget`
- **Куда назначить:** поле **Row Widget Class** в Class Defaults `WBP_Inventory`.

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `NameText` | Имя предмета. Ставит код. |
| Button | `UseButton` | Применить предмет (съесть/надеть). У предметов, которые нельзя применить, код прячет кнопку целиком. |
| Text | `UseText` | Подпись кнопки применения («использовать»/«надеть» — ставит код). Положи ВНУТРЬ UseButton. |
| Button | `DropButton` | Выбросить предмет. Подпись (например «X») напиши внутри сам. |

### Как проверить диалог и инвентарь

1. Диалог: назначь WBP_Dialog в слот, подойди к старосте, нажми E. Пройди цепочку:
   принять квест → закрыть → выполнить → сдать (кнопка с суммой награды).
2. Инвентарь: назначь WBP_Inventory в слот, открой Tab (кнопка СУМКА на телефоне).
   Надень броню из рюкзака, проверь «Защита: %», сними кликом по слоту, выбрось
   что-нибудь, закрой.
3. Вернуть старые экраны — очисти соответствующий слот на BP_ContrarySurvivorHUD.

---

## Этап 3 — Экран смерти и постоянные панели

### WBP_Death — экран смерти

- **Родительский класс:** `DeathScreenWidget`
- **Куда назначить:** `BP_ContrarySurvivorHUD` → **HUD | UMG Widgets** → **Death Widget Class**.
- Статичные строки пиши сам: заголовок «ВЫ ПОГИБЛИ», подсказка «Enter / Пробел —
  возродиться», строки штрафа «Возрождение у костра в деревне.», «Расходники обронены
  мешком на месте гибели.» (ВАЖНО: эту формулировку не менять — решение ADR-044, без
  «можно вернуться и забрать»), «Снаряжение, оружие и важные предметы сохранены.».
  Затемнение фона — твой Border на весь экран.

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `LifetimeText` | «Прожито:  02:31». Ставит код. |
| Text | `KillerText` | «Убийца:  Волк». Ставит код. |
| Text | `MoneyText` | «Монеты:  120». Ставит код. |
| Text | `QuestsText` | «Квестов выполнено:  2». Ставит код. |
| Text | `KillsText` | «Врагов убито:  7». Ставит код. |
| Text | `MoneyLossText` | «−40% монет — часть монет утрачена при гибели.» — процент живой, из настройки игрока. Ставит код. |
| Button | `RespawnButton` | Возродиться. Подпись («ВОЗРОДИТЬСЯ») напиши внутри сам. Клавиши Enter/Пробел работают как раньше независимо от кнопки. |

Настраиваемые префиксы строк — Class Defaults WBP_Death, категория **Death | Texts**.

### WBP_PlayerStats — постоянная панель статов (верх-лево)

- **Родительский класс:** `PlayerStatsWidget`
- **Куда назначить:** **Player Stats Widget Class**. Панель висит на экране всю игру.

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Progress Bar | `HealthBar` | Полоска здоровья, заполнение ставит код. Цвета полоски — твои (Details → Appearance). |
| Text | `HealthText` | «HP 80/100». Ставит код. Удобно положить ПОВЕРХ полоски (Overlay). |
| Progress Bar | `HungerBar` | Полоска голода. |
| Text | `HungerText` | «Hunger 64». |
| Progress Bar | `ThirstBar` | Полоска жажды. |
| Text | `ThirstText` | «Thirst 71». |
| Text | `AmmoText` | «Ammo 7 / 21  (bag 30)» — код сам прячет её, когда в руках не огнестрел. |
| Text | `MoneyText` | «Монеты 150». |

Префиксы строк — Class Defaults WBP_PlayerStats, категория **PlayerStats | Texts**.

### WBP_QuestTracker — строка квеста (верх-право)

- **Родительский класс:** `QuestTrackerWidget`
- **Куда назначить:** **Quest Tracker Widget Class**. Сам прячется без квеста и при
  открытых окнах.

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `TrackerText` | «Квест: Шкуры волков — Шкура волка 1/3»; у выполненного — «Квест выполнен: … - вернись к старосте». Текст и цвет (золотой/зелёный) ставит код. Подложку-плашку под текстом сделай сам (Border). |

Тексты и оба цвета — Class Defaults WBP_QuestTracker.

### WBP_InteractPrompt — подсказка «E — …» (низ-центр)

- **Родительский класс:** `InteractPromptWidget`
- **Куда назначить:** **Interact Prompt Widget Class**. Сам прячется, когда рядом
  нет пикапа/торговца/старосты или открыто окно.

| Тип кубика | Точное имя | Что делает |
|---|---|---|
| Text | `PromptText` | «E — подобрать» / «E — торговать» / «E — поговорить». Ставит код; сами тексты настраиваются на контроллере (категория Interact). Плашку под текстом сделай сам. |

### Как проверить этап 3

1. Смерть: назначь WBP_Death, дай волку себя убить (или QA-клавиша P) — проверь
   статистику, процент штрафа и кнопку возрождения.
2. Статы: назначь WBP_PlayerStats — полоски живут, патроны появляются только с
   пистолетом в руках.
3. Трекер: назначь WBP_QuestTracker, возьми квест у старосты — строка в углу; открой
   инвентарь — строка прячется.
4. Подсказка: назначь WBP_InteractPrompt, подойди к предмету/торговцу — «E — …» внизу.

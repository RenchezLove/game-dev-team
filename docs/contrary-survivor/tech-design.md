# Contrary Survivor — Тех-дизайн (сверка иерархии классов под Фазы 1-2)

> **Версия:** 1.0 · **Дата:** 2026-06-12 · Сверено с реальным C++ проекта (Фаза 0, живой осмотр) и с `ИерархияКлассов(...).svg`.
> Источник правды по геймплею — `GDD.md`. Этот документ — инженерный план классов на ближайшие фазы, не замена GDD.
> **Метки:** `[ЕСТЬ]` — в проекте есть. `[НОВЫЙ]` — создать. `[ПРАВКА]` — доработать существующий.

---

## 1. Текущая иерархия (сверена живьём, Фаза 0)
```
ACharacter → AMasterHumanoidCharacter → APlayerCharacter           [ЕСТЬ]
APlayerController → AContrarySurvivorPlayerController               [ЕСТЬ]
UActorComponent → UInventoryComponent                              [ЕСТЬ, данные]
AActor → AMasterInventoryItem ┬→ AMasterWeapon → ARangedWeapon → APistol   [ЕСТЬ]
                              └→ AArmor → {AHeadArmor, ATorsoArmor, APantsArmor}  [ЕСТЬ, заглушки]
GameMode = BP_ContrarySurviorGameMode на движковом AGameModeBase    [ЕСТЬ, BP]
HUD = движковый дефолт                                              [ЕСТЬ, дефолт]
```
**GAS НЕ используется** — статы голыми float в персонаже. Решено: кастомный `UStatsComponent` (ADR-015).

## 2. Плановые классы под Фазы 1-2

### Фаза 1 — первый вертикальный срез (бандит + AI + смерть)
| Класс | Тип | База | Назначение |
|---|---|---|---|
| `UStatsComponent` | `[НОВЫЙ]` | `UActorComponent` | Health/MaxHealth (+ задел Hunger/Thirst/Money/MoveSpeed, расширяемо под модификаторы). В Фазе 1 — **только Health + смерть**. Делегаты `OnHealthChanged`, `OnDeath`. |
| `AEnemyCharacter` | `[НОВЫЙ]` | `AMasterHumanoidCharacter` | Базовый враг-человек (бандит). **Модульный** (Head/Torso/Legs из мастер-базы — ADR-018). На Android стоимость держим runtime mesh merge при спавне (Фаза 3+); в Фазе 1 — живые компоненты. Содержит `UStatsComponent`. Бандит — через этот класс (data/BP-вариант) или дочерний `ABanditCharacter`. |

> **Находка из живого чтения `MasterHumanoidCharacter.h` (2026-06-12):** модульные меши `HeadMesh/TorsoMesh/LegsMesh` + `UpdateCharacterAppearance()` зашиты в **самой мастер-базе** `AMasterHumanoidCharacter`. База также несёт: инлайн-статы `Health/MaxHealth`, `TakeDamage()`/`RestoreHealth()`, оружие (`CurrentWeapon`, Equip/Unequip/Fire/Reload), `UInventoryComponent`, спринт.
> **Решение (ADR-018, Ринат 2026-06-12 — РАЗВОРОТ):** модульность — у ВСЕХ гуманоидов (игрок + враги-люди + NPC). Поэтому **`AEnemyCharacter` наследует `AMasterHumanoidCharacter` КАК ЕСТЬ** (модульные меши остаются в базе) — рефактор «опустить модульность в игрока» НЕ нужен. Спаунер собирает врага из модулей. Стоимость на Android — **runtime mesh merge при спавне** (`USkeletalMergingLibrary`/`FSkeletalMeshMerge` — наличие в 5.5 **на живой проверке cpp-dev**; фолбэк — живые компоненты + LOD + лимит врагов). Merge — оптимизация при росте числа врагов (~Фаза 3+), не в Фазе 1. Health мигрируем в `UStatsComponent` (новый враг — сразу на компоненте; игрок — поэтапно). Всё — через qa-гейт.
| `AEnemyAIController` | `[НОВЫЙ]` | `AAIController` | Примитивный цикл: **заметил → подошёл → атакует**. На MVP — без Behavior Tree (прямой state-machine в коде) либо минимальный BT. Решить на старте Фазы 1. |
| Хелсбар врага | `[НОВЫЙ]` | `UUserWidget` + `UWidgetComponent` | Полоска HP над врагом (worldspace) / либо вверху экрана при наведении (GDD ч.8). Первый UMG-виджет в проекте. |
| Слой наведения | `[ПРАВКА]` | в `AContrarySurvivorPlayerController` | Абстракция «точка экрана → выбор цели» (ADR-017). ПК: курсор/клик-захват. |

\* **Открытый инженерный вопрос (решить на старте Фазы 1, не блокер GDD):** наследовать `AEnemyCharacter` от `AMasterHumanoidCharacter` (переиспользовать общий код движения/меша) или вынести общую базу. Рекомендация — наследовать от `AMasterHumanoidCharacter`, т.к. он уже несёт скелетный меш и базовую логику; врагу отключить модульность (один меш). Финал — за `cpp-dev` по факту чтения класса.

### Фаза 2 — выживание, смерть/респаун, сейв
| Класс | Тип | База | Назначение |
|---|---|---|---|
| `UStatsComponent` | `[ПРАВКА]` | — | Включить Hunger/Thirst/Money + деградацию по таймеру + урон при истощении (числа — GDD ч.7.3). |
| `UContrarySaveGame` | `[НОВЫЙ]` | `USaveGame` | Снапшот: статы, инвентарь, позиция/точка сейва. |
| `ASavePoint` / `ACampfire` | `[НОВЫЙ]` | `AActor` | Точка сейва/респауна (дом/лагерь/костёр). Триггер-зона → автосейв + регистрация точки респауна. |
| GameMode (C++) | `[ПРАВКА/НОВЫЙ]` | `AGameModeBase` | Обработка смерти/респауна на последней точке + потеря % рюкзака. Сейчас GameMode = BP; решить, переносить ли в C++ (рекомендация — да, для логики смерти/респауна). |
| HUD (C++/UMG) | `[НОВЫЙ]` | `AHUD` + `UUserWidget` | Базовый HUD: HP, иконки голода/жажды при критич., деньги. |

### Последующие фазы (эскиз, детализируется перед фазой)
- **Ближний бой:** `AMeleeWeapon` `[НОВЫЙ]` (`AMasterWeapon`) — нож. Волк — через `AEnemyCharacter`.
- **Броня:** подключить `ArmorProtection` к расчёту урона; модульная экипировка игрока через **Leader/Master Pose Component** (GDD ч.7.4).
- **Инвентарь UI:** UMG-экран (paper-doll слоты + рюкзак).
- **Экономика/торговец:** NPC-торговец, цены (GDD ч.7.6).
- **Квесты:** `UQuestComponent` `[НОВЫЙ]` + структура квеста (Kill/Collect/Deliver; MVP — Kill), диалоги, NPC-староста.
- **Android-ввод:** виртуальный тач-UI поверх абстракции наведения.

## 3. Принципы реализации (из ADR/GDD)
- **Без GAS** — `UStatsComponent` на float (ADR-015).
- **World Partition отложен** — одна малая карта (ADR-016).
- **Наведение через абстракцию точки экрана** (ADR-017), ПК-клик-захват в Фазе 1.
- **ВСЕ гуманоиды модульные** (ADR-018: игрок + враги-люди + NPC, Head/Torso/Legs); стоимость на Android — runtime mesh merge при спавне (Skeletal Merging — на проверке в 5.5; фолбэк — живые компоненты + LOD). Звери (волки) — единый меш. Merge — оптимизация ~Фаза 3+.
- Числа баланса — в `UPROPERTY`/конфиге, крутятся без перекомпиляции логики.
- Каркасы (квесты, модификаторы статов) — расширяемые, но в MVP реализуется минимум.

## 4. Тех-долги к учёту при касании соответствующих систем (из GDD Прил. C)
Перезарядка без таймера; трасса не из MuzzleSocket; броня не влияет на урон; смерть = только лог; орфан-BlendSpace (нет сглаживания анимации по скорости); дубль `BP_PlayerCharacter` на карте; рассинхрон InputAction (нет `IA_Interact`/`IA_Inventory`); именование/`GetMesh()`-затенение/пустой Tick. Не чинить «заодно» вне фазы — заводить как задачу при работе над системой.
